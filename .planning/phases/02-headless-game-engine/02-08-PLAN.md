---
phase: 02-headless-game-engine
plan: 08
type: tdd
wave: 6
depends_on: ["02-01", "02-05", "02-06"]
files_modified:
  - aamatch/generator.py
  - tests/test_generator.py
  - tests/test_purity.py
autonomous: true

must_haves:
  truths:
    - "generate(seed, setup, candidates, ligand_data, difficulty_levels) emits a complete level-spec payload: difficulty mapped (grid_n 3..9, types 1..7 clamped, size class), required set per approved mode semantics, solvable-by-construction slots, deterministic grid poses, placement offsets"
    - "Same seed -> byte-identical payload; every RNG touch goes through sorted/fixed-order lists"
    - "Every required item has a dedicated role='required' slot whose can_form is truthful against capability; distractors drawn from all 20 AAs"
    - "Infeasible configurations raise GenerationError naming the cause (OQ-1 semantics)"
  artifacts:
    - path: "aamatch/generator.py"
      provides: "GRID_SPACING/GAP_MARGIN/INTER_GRID_MARGIN constants, difficulty_params, slot_position, derive_required, allocate_slots, generate, GenerationError"
      min_lines: 250
    - path: "tests/test_generator.py"
      provides: "Difficulty interpolation, mode semantics, allocation, determinism, guards"
      min_lines: 200
  key_links:
    - from: "aamatch/generator.py"
      to: "aamatch/capability.py"
      via: "aa_capable/ligand_support for pools + feasibility (DETECT-04 single home)"
      pattern: "from .capability import"
    - from: "aamatch/generator.py"
      to: "aamatch/level_spec.py"
      via: "DETECTOR_VERSION + LEVEL_SPEC_VERSION stamped into the payload"
      pattern: "DETECTOR_VERSION"
    - from: "tests/test_generator.py"
      to: "aamatch/detector.py"
      via: "can_form claims cross-checked against detector typing on synthetic atoms (DETECT-04)"
      pattern: "from aamatch import detector"
---

<objective>
Build the pure seeded generator: difficulty expression (GEN-05), grid geometry (GEN-03), required-set derivation with the approved mode semantics (OQ-1), solvability-by-construction slot allocation (GEN-04), and payload assembly with version stamps + deterministic RNG (GEN-01).

Purpose: This is the game brain's producer side — its payload is what materialization (02-13), E2E (02-14) and Reset replay forever after.
Output: generator.py + tests incl. the generator<->detector agreement cross-check.
</objective>

<execution_context>
@~/.config/opencode/get-shit-done/workflows/execute-plan.md
@~/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/02-headless-game-engine/02-RESEARCH-generation.md   # §3 API, §4 algorithm + edge rulings, §5 grid geometry, §6 difficulty, §7 RNG/serialization, §9 payload example
@docs/DETECTION_THRESHOLDS.md                                          # OQ-1 mode semantics + OQ-5 hydrophobic exclusion — binding
@aamatch/capability.py                                                 # capability tables (consumed)
@aamatch/level_spec.py                                                 # DETECTOR_VERSION / LEVEL_SPEC_VERSION / container helpers
@aamatch/setup_state.py                                                # INTERACTION_TYPES / INTERACTION_MODES / defaults
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Difficulty mapping + grid geometry + RNG skeleton (RED -> GREEN)</name>
  <files>aamatch/generator.py, tests/test_generator.py</files>
  <action>
RED:
1. difficulty_params(D, L) -> {'tier': L, 'grid_n': 3 + frac*6, 'n_required_types': 1 + frac*6, 'molecule_size_class': bucket} with INTEGER HALF-UP interpolation frac = (L*6 + (D-1)//2) // (D-1) for D > 1, else 0. Tests: NO round() (banker's trap); for every D in 1..10, mapping yields exactly D levels, tiers 0..D-1, grid_n within 3..9 non-decreasing, n_required_types within 1..7 non-decreasing (pre-clamp), size-class rank non-decreasing; D=1 -> single easiest level; spot-check the research §6 reference table rows for D=3 (L0 -> 3/1/small, L1 -> 6/4/medium, L2 -> 9/7/large) — assert via computed values, and if the formula disagrees with the hand table, THE FORMULA WINS (record deviation).
2. slot_position(row, col, n, centroid, R) -> (x, y, z) exactly per generation research §5.2 formula (spacing 8.0, gap R + 5.0, plane +z, centered on centroid xy). Tests: n=3 centroid (0,0,0) R=4 -> slot(0,0) == (-8.0, -8.0, 9.0) within 1e-9; all pairwise slot distances >= GRID_SPACING (n up to 9); every slot-to-centroid distance >= R + GAP_MARGIN; inter-molecule offset m*(grid_width + INTER_GRID_MARGIN) applied additively along +x — two molecules' slots never coincide.
3. Guards: ligand_data with NaN/inf centroid or R <= 0 raises GenerationError (allow_nan=False would refuse at save — fail early with a clear message).

GREEN: constants GRID_SPACING=8.0, GAP_MARGIN=5.0, INTER_GRID_MARGIN=6.0, GRID_SPAN=6, SIZE_S1=25, SIZE_S2=60; functions as above; sub-seed derivation helper `_sub_seeds(master_rng, n_units)` drawing per-unit seeds in fixed order (generation research §7.1). NO itertools (explicit loops/comprehensions).

Commit: `feat(02-08): difficulty + grid geometry (TDD)`.
  </action>
  <verify>python3.6 -m unittest tests.test_generator -v</verify>
  <done>Interpolation monotonic over all 10 legal D; geometry invariants hold; degenerate ligand data refused</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: derive_required + allocate_slots — solvability by construction (RED -> GREEN)</name>
  <files>aamatch/generator.py, tests/test_generator.py</files>
  <action>
RED:
1. derive_required(setup, ligand_profile, rng, n_required_types) per the approved OQ-1 semantics (docs/DETECTION_THRESHOLDS.md): exclusive + non-empty allowed -> {'mode':'any','items':[]}; exclusive + empty allowed -> GenerationError naming the setup field; block_exclusive -> mode 'list' with items EXACTLY the checked set in canonical INTERACTION_TYPES order, count 1 each; block_exclusive with an unsupported type -> GenerationError naming the type(s) (never silently degrade); unset -> sample k = min(n_required_types, len(supported ∩ allowed)) from the SUPPORTED∩ALLOWED list (canonical order, rng.sample over a sorted list); unset + empty allowed -> draw from all 7 types; OQ-5: hydrophobic EXCLUDED from unset sampling (policy from the gate doc) — a ligand supporting only hydrophobic + h_bond samples from the remainder, and hydrophobic can appear ONLY via block_exclusive explicit check.
2. allocate_slots(required_items, n, ligand_profile, rng): one global pass; every required item gets a dedicated slot with role='required', can_form=[type], aa drawn via rng.choice from the capable pool (sorted list); distractors = all remaining slots, role='distractor', can_form=[], aa = rng.choice over the SORTED 20-AA list; grid too small (n*n < needed + 1) -> GenerationError; no capable AA -> GenerationError naming the type. Tests: required slots are unique; can_form truthfulness (aa_capable true for every can_form entry); distractor AAs come from the full 20.
3. Detector cross-check (DETECT-04, real dependency on 02-06): build a synthetic scene where a required slot's AA (e.g. SER) is placed within h_bond geometry of a synthetic ligand donor/acceptor and assert detector.detect returns an h_bond record whose aa.side resn matches the slot — proving "capable" and "detectable" agree through the SAME capability tables.

GREEN: implement per generation research §4 (edge-case rulings table verbatim in docstrings). rng consumed ONLY via randint/choice/sample/shuffle over sorted/fixed-order lists — never sets/dicts.

Commit: `feat(02-08): required-set derivation + slot allocation (TDD)`.
  </action>
  <verify>python3.6 -m unittest tests.test_generator -v</verify>
  <done>All mode semantics + refusals tested; solvability invariant holds; capability<->detector agreement proven on synthetic geometry</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: generate() assembly + purity registration (RED -> GREEN)</name>
  <files>aamatch/generator.py, tests/test_generator.py, tests/test_purity.py</files>
  <action>
RED:
1. generate(seed, setup, candidates, ligand_data, difficulty_levels) -> payload dict matching the reserved shape + additive 'placement': per generation research §7.2/§9: seed (real int — reject bool with GenerationError explicitly for a clearer message than the parse gate), detector_version == DETECTOR_VERSION, format_version == LEVEL_SPEC_VERSION, per level: level_index/tier/difficulty dict, per molecule: molecule_id 'mol-NNN', ligand {source/set_id/entry_id/file/sha256/protonation/provenance} (file package-relative; protonation selected via rng.choice(sorted(states)) when a candidate offers multiple, else the recorded default), required dict, placement.offset, grid {n, slots}. Molecule selection: size-class bucket filter then rng.sample from the deterministically sorted candidate list; molecules_per_level from setup.
2. Determinism: generate twice with the same seed -> payloads equal AND json.dumps(sort_keys=True) byte-identical; different seeds -> differ; sub-seed isolation: adding a molecule does not change level 1 of a 1-molecule game's payload bytes for its existing molecule (fixed-order stream).
3. Round-trip: payload passes parse_level_spec_dict(make_level_spec_container(payload)) unchanged (the standing container discipline).
4. Candidates must be pre-sorted by the caller; generate re-sorts defensively (sorted by (set_id, entry_id)) and documents it.

GREEN: assemble; append 'generator' to PURE_MODULES (imports: random, math, from .capability, from .level_spec, from .setup_state — all pure).

Commit: `feat(02-08): generate() payload assembly (TDD)`.
  </action>
  <verify>python3.6 -m unittest discover -s tests -v  (full suite — purity gates now scan generator)</verify>
  <done>Payload complete + stamped + byte-deterministic; round-trips through the Phase-1 gates; PURE_MODULES = 12</done>
</task>

</tasks>

<verification>
python3.6 -m py_compile aamatch/*.py && python3.6 -m unittest discover -s tests -v
</verification>

<success_criteria>
GEN-01/03/04/05 logic exists pure and green: seeded deterministic solvable levels with approved mode semantics, version stamps, deterministic grids. (The >= 100-seed sweep is plan 02-12.)
</success_criteria>

<output>
After completion, create `.planning/phases/02-headless-game-engine/02-08-SUMMARY.md`
</output>
