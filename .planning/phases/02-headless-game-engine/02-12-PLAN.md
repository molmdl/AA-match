---
phase: 02-headless-game-engine
plan: 12
type: execute
wave: 7
depends_on: ["02-08"]
files_modified:
  - tests/test_generator_invariants.py
autonomous: true

must_haves:
  truths:
    - "Across >= 100 seeds x D in {1,3,10} x all 3 modes x molecules in {1,2}: every generated level is solvable by construction (each required item has a dedicated capable slot), grids are NxN with globally disjoint beyond-gap slots, and payloads round-trip through the Phase-1 gates"
    - "Difficulty escalates monotonically (grid_n, type count, size class) with tiers 0..D-1 and clamping honesty"
    - "Corpus statistics stay healthy: >= 95/100 distinct payloads, all 20 AAs appear as distractors somewhere, every type gets required somewhere, required slots not concentrated on fixed positions"
  artifacts:
    - path: "tests/test_generator_invariants.py"
      provides: "The >= 100-seed invariant suite (ROADMAP Phase-2 criterion 3)"
      min_lines: 200
  key_links:
    - from: "tests/test_generator_invariants.py"
      to: "aamatch/generator.py"
      via: "generate() swept over the seed corpus"
      pattern: "generate"
    - from: "tests/test_generator_invariants.py"
      to: "aamatch/level_spec.py"
      via: "every payload round-trips parse_level_spec_dict(make_level_spec_container(payload))"
      pattern: "parse_level_spec_dict"
---

<objective>
Build the >= 100-seed generator invariant suite (ROADMAP Phase-2 success criterion 3): the permanent proof that GEN-01/03/04/05 hold across the seed corpus, not just on hand-picked cases.

Purpose: PITFALL 11 (uniqueness/solvability/determinism) is closed by invariant assertion, not by inspecting sample outputs.
Output: tests/test_generator_invariants.py, all green under python3.6.
</objective>

<execution_context>
@~/.config/opencode/get-shit-done/workflows/execute-plan.md
@~/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/02-headless-game-engine/02-RESEARCH-generation.md   # §8 invariant list items 1-19 (items 20-22 are headless, plan 02-13/14), §9 payload shape
@aamatch/generator.py                                                 # the surface under test
@aamatch/capability.py                                                # ligand_profile fixtures
</context>

<tasks>

<task type="auto">
  <name>Task 1: Solvability + grid + format invariants over the seed corpus</name>
  <files>tests/test_generator_invariants.py</files>
  <action>
Fixture: a multi-feature ligand profile (donor+acceptor+ring+charges+hydrophobe; hand-built dict like the 02-08 tests — WSL-pure, NO manifest/file reads) and a small sorted candidate list. Sweep seeds range(100) x D in {1, 3, 10} x mode in {'exclusive','block_exclusive','unset'} x molecules in {1, 2} (allowed_interactions: a realistic checked set for block mode; empty-allowed exclusive refusals are covered in 02-08 — here sweep legal configs). Per generation research §8 assert:
A. determinism (generate twice -> json.dumps(sort_keys=True) byte-equal), round-trip through parse_level_spec_dict(make_level_spec_container(payload)), version stamps (detector_version == DETECTOR_VERSION, format_version == LEVEL_SPEC_VERSION, seed real int — bool impossible), cross-seed distinctness >= 95/100 per config.
B. required coverage: for every level/molecule/item, count of role='required' slots with item['type'] in slot['can_form'] >= item['count']; can_form truthfulness via capability.aa_capable; mode semantics ('any' vs 'list' shapes; unset items ⊆ allowed and == min(n_required_types, len(supported∩allowed)) with hydrophobic excluded per OQ-5).
C. grid: grid.n == difficulty.grid_n; len(slots) == n*n; (row,col) cover the full range exactly once; slot_id unique per molecule; pairwise slot distances >= GRID_SPACING (epsilon 1e-9); every slot beyond R + GAP_MARGIN from the ligand centroid; across molecules in a level no two slots share a position; every position finite (math.isfinite).
Keep total runtime acceptable: 100 seeds x ~12 configs of a pure generator is fine on 3.6; if a single sweep is slow, sub-parametrize (seeds sweep at D=3 all modes; D in {1,10} over 100 seeds with one mode) but NEVER below 100 seeds per swept dimension pair (research §8 preamble).
  </action>
  <verify>python3.6 -m unittest tests.test_generator_invariants -v</verify>
  <done>Groups A/B/C green across the corpus</done>
</task>

<task type="auto">
  <name>Task 2: Difficulty invariants + corpus statistics</name>
  <files>tests/test_generator_invariants.py</files>
  <action>
D. monotonic escalation: across a game's levels grid_n non-decreasing, n_required_types non-decreasing (pre-clamp intent recorded in difficulty dict), size-class rank non-decreasing, level_index == tier, tiers 0..D-1 strictly ascending; clamping honesty: emitted required.items count <= len(supported ∩ allowed) per molecule; cap compliance: for EVERY D in 1..10 the mapping produces exactly D levels (parametrized over all 10 legal values).
E. soft corpus statistics (single 100-seed sweep, D=3, unset mode, 2 molecules): every one of the 20 AAs appears in at least one distractor slot across the corpus; no seed produces an all-identical grid; each interaction type is required at least once across the corpus (multi-type fixture); a fixed slot (e.g. r0c0) is required in < 50% of seeds.
  </action>
  <verify>python3.6 -m unittest discover -s tests -v  (full suite green)</verify>
  <done>Groups D/E green; the suite is a permanent >= 100-seed regression gate</done>
</task>

</tasks>

<verification>
python3.6 -m py_compile aamatch/*.py && python3.6 -m unittest discover -s tests -v
</verification>

<success_criteria>
ROADMAP Phase-2 criterion 3 ([WSL] generator invariant tests over >= 100 seeds) is satisfied and permanently enforced.
</success_criteria>

<output>
After completion, create `.planning/phases/02-headless-game-engine/02-12-SUMMARY.md`
</output>
