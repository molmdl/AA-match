---
phase: 07-checkpoint-game-file-persistence
plan: 11
type: execute
wave: 7
depends_on: [07-10]
files_modified: []
autonomous: true

must_haves:
  truths:
    - "The FULL WSL suite (python3.6, incl. purity gates) is green on the merged Phase-7 tree"
    - "EVERY smoke 01-20 passes: the four Phase-7 smokes (17 two-run, 18, 19, 20 two-run) plus all prior-phase smokes re-run green on the final tree"
    - "The matrix round-trip verdict is recorded in the phase record: object matrices survive .pse bit-exactly; the sidecar needs NO matrices"
    - "Every ROADMAP Phase-7 criterion has its mechanical coverage identified and passing (criterion 1 HEADLESS half, criterion 2 refusal battery, criterion 3 gate mechanics)"
  artifacts:
    - path: ".planning/phases/07-checkpoint-game-file-persistence/07-11-SUMMARY.md"
      provides: "The battery verdict table + the matrix-round-trip verdict recording + criterion coverage map"
      contains: "SMOKE-20"
  key_links:
    - from: "SMOKE-17 (two-run)"
      to: "ROADMAP criterion 1 matrix verdict + criterion 3 wizard pickling"
      via: "the recorded PROVEN verdicts (coords/view/matrices exact; wizard strict restore)"
      pattern: "SMOKE-17 VERDICT"
    - from: "SMOKE-20 (two-run)"
      to: "ROADMAP criterion 1 HEADLESS half + criterion 3 sentinel-first reconcile"
      via: "the save->quit->relaunch->load reconstruction proof"
      pattern: "SMOKE-20 VERDICT"
---

<objective>
Run the full Phase-7 regression battery on the merged tree and record the verdicts: all WSL tests, all 20 smokes (with the two-run contracts), and the criterion coverage map.

Purpose: Phase 7 touches wizard.py, engine.py, gamestart.py, placement.py, game_window.py, persistence.py, status_text.py + adds checkpoint.py — the battery proves zero regressions across all nine phases of accumulated behavior and produces the recorded verdicts the phase verifier and the [HUMAN] checkpoint (07-12) consume.
Output: green battery + recorded verdicts in the SUMMARY (no production code changes expected; Rule-1 fixes if a regression surfaces).
</objective>

<execution_context>
@~/.config/opencode/get-shit-done/workflows/execute-plan.md
@~/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/phases/07-checkpoint-game-file-persistence/07-01-SUMMARY.md
@.planning/phases/07-checkpoint-game-file-persistence/07-09-SUMMARY.md
@smoke/run_smoke.sh
</context>

<tasks>

<task type="auto">
  <name>Task 1: Full WSL suite + py_compile</name>
  <files></files>
  <action>
Run from the repo root: `python3.6 -m py_compile aamatch/*.py` then `python3.6 -m unittest discover -s tests -v`. Expect the full suite green (826+ prior tests + the new test_checkpoint.py battery + the evolved pins in test_status_text/test_persistence). If ANY test fails: fix it (Rule 1 — bugs are auto-fixed), re-run, and record the fix in the SUMMARY deviations. Do NOT weaken any gate (purity, PROSE_PIN, source-scan, refusal pins).
  </action>
  <verify>python3.6 -m py_compile aamatch/*.py && python3.6 -m unittest discover -s tests -v (exit 0, zero failures/errors)</verify>
  <done>Full WSL suite green; any fix recorded</done>
</task>

<task type="auto">
  <name>Task 2: Full smoke battery (01-20, two-run contracts honored)</name>
  <files></files>
  <action>
Run EVERY smoke in number order, recording PASS/check-counts:
- Single-run: smoke_01..16, 18, 19 (`bash smoke/run_smoke.sh smoke/smoke_NN_*.py [240]`).
- TWO-RUN contracts: smoke_17 (run 1 SAVE then run 2 VERIFY — delete `tmp/smoke fixtures/aam_pse17/` first so run 1 executes) and smoke_20 (delete `tmp/smoke fixtures/aam_pse20/` first; run 1 SAVE then run 2 VERIFY).
Expected all green: 04 25/25, 07 (incl. the 07-08 books part), 08 (incl. the 07-05 payload part), 11 (incl. the evolved 5-key PART-H pin), 14, 15 75/75, 16 (incl. the 07-06 save + 07-10 resume parts), 17 strict-restore, 18, 19, 20.
If a regression surfaces: fix (Rule 1), re-run the affected smoke AND its neighbors, record.
Then record in the SUMMARY: (a) the matrix round-trip verdict line — `object matrices survive .pse bit-exactly (SMOKE-17 max_delta 0.0); the sidecar carries NO matrices (fallback documented, additive .get(None)-skip)`; (b) the criterion coverage map: criterion 1 HEADLESS half = SMOKE-17+18+20; criterion 2 mechanical = SMOKE-19 PART D refusals; criterion 3 mechanics = SMOKE-17 strict restore + SMOKE-20 reconcile/adopt asserts + purity gates.
  </action>
  <verify>Every runner invocation exits 0 with its `=== SMOKE-NN PASS ===` marker; the two-run smokes pass BOTH runs</verify>
  <done>All 20 smokes green (two-run contracts honored); verdicts + coverage map recorded in 07-11-SUMMARY.md</done>
</task>

</tasks>

<verification>
1. `python3.6 -m unittest discover -s tests -v` — zero failures.
2. All 20 smokes green with their markers.
3. 07-11-SUMMARY.md contains the battery table, the matrix verdict, and the criterion coverage map.
</verification>

<success_criteria>
- The merged Phase-7 tree is regression-free across the whole accumulated suite.
- The recorded verdicts are ready for the phase verifier and the [HUMAN] checkpoint.
</success_criteria>

<output>
After completion, create `.planning/phases/07-checkpoint-game-file-persistence/07-11-SUMMARY.md`
</output>
