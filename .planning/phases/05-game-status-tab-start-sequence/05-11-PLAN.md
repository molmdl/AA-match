---
phase: 05-game-status-tab-start-sequence
plan: 11
type: execute-checkpoint-record
wave: 5
depends_on: ["05-08", "05-09", "05-10"]
files_modified: []
autonomous: false

must_haves:
  truths:
    - "[HUMAN] Start stores the initial state, generates representations per setup, switches to the Game status tab, plays a 3-2-1 countdown, and the timer starts from zero (SETUP-11, ROADMAP criterion 1)"
    - "[HUMAN] The Game status tab shows a rolling info box, the elapsed timer outside the info box, and the required interaction types + counts (`any` or from the allowed list) (SCORE-04, ROADMAP criterion 2)"
    - "[HUMAN] Hint recolors carbon atoms of amino acids that could form one of the required interactions — recolor only, never lines/dots/geometry (PLAY-05, ROADMAP criterion 3)"
    - "[HUMAN] The timer pauses (freezes, <=1 s granularity) while any modal child is open — the one behavior NOT headlessly provable (PITFALL P5); the hint color 'orange' is human-confirmed or adjusted"
  artifacts:
    - path: ".planning/phases/05-game-status-tab-start-sequence/05-11-SUMMARY.md"
      provides: "the recorded human verdict table for SETUP-11 + SCORE-04 + PLAY-05 (all three ROADMAP Phase-5 criteria) with pass/fail + notes"
  key_links:
    - from: "human operator"
      to: "the window-driven start sequence -> Game status tab -> Hint"
      via: "real GUI session on Windows PyMOL 2.5.0 (the only tier that can prove countdown feel, modal timer-freeze, and recolor legibility)"
      pattern: "SETUP-11 / SCORE-04 / PLAY-05"
---

<objective>
[HUMAN] consolidated GUI checkpoint for ALL THREE ROADMAP Phase-5 criteria (SETUP-11 start sequence, SCORE-04 status surface, PLAY-05 hint) — one sitting, one PyMOL session, numbered steps with expected outcomes. The final gate of Phase 5.

Purpose: countdown cadence/feel, the modal timer-freeze (P-5: timers fire inside a modal child's nested loop — not headlessly provable), tab visual layout, required-display readability across modes, and hint color legibility are the human-verify tier by design; everything machine-provable was already proven (T0/T1a/T1b: SMOKE-08/11/13/14 + the WSL batteries).
Output: recorded verdicts; Phase 5 ready for the phase verifier.
</objective>

<execution_context>
@~/.config/opencode/get-shit-done/workflows/execute-plan.md
@~/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/05-game-status-tab-start-sequence/05-RESEARCH-window-start-timer.md
@.planning/phases/05-game-status-tab-start-sequence/05-RESEARCH-status-surface.md
@.planning/phases/05-game-status-tab-start-sequence/05-RESEARCH-hint.md
@.planning/phases/04-qt-setup-window/04-14-SUMMARY.md
@.planning/STATE.md
@.planning/ROADMAP.md

Session conventions (04-14/04-15 pattern):
- Windows PyMOL 2.5.0 via the setenv.bat conda env; the repo root is on the PyMOL plugin path (01-09 PLUGIN-PATH install — repo edits live, no copy-install; NEVER also copy-install, AGENTS.md gate 5).
- The Plugins -> AA-match menu item opens the 'AA-match Setup' window (title KEPT per the adopted decision; rename is a Phase-9 polish candidate).
- Console triage baseline: stock-PyMOL plugin-init warnings (findseq, aKMT_Lys_pred, colorblindr/colorblindfriendly, wfmesh, bnitools, mtsslPlotter/mtsslTrilaterate, SuperSymPlugin, phase9_ssl_probe) are pre-existing and UNRELATED to AA-match (the recorded 04-14 baseline) — only NEW AA-match tracebacks/prints are defects.
- Human may adjust the hint color at step 5 (research C OQ-1: 'orange' recommended; alternatives magenta/salmon/pink verified present) — an adjustment is a one-constant edit + re-verify, recorded in the verdict table.
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>
The complete Phase-5 surface: the two-tab window (Setup + Game status), the window-driven deferred start sequence (pop prior wizard -> prepare -> tab switch -> cancellable 3-2-1 countdown -> GO activation + timer from zero), the live status surface (event-log info box, M:SS timer label with modal-freeze pause, required-interactions label in both modes), and the capability-driven Hint (carbon-only orange recolor with full restore) — all headlessly proven beneath this tier (SMOKE-08/11/13/14 + the WSL batteries incl. GEN-04 parity).
  </what-built>
  <how-to-verify>
One session, numbered steps. Record PASS/FAIL + notes per step in the summary table.

1. SETUP-11 (start sequence): open the window (defaults). Click 'Start'. EXPECT: the window auto-switches to the 'Game status' tab; the info box shows 'Get ready...' then 3, 2, 1 (one per second) then 'GO!'; DURING the countdown no wizard panel is open — click an amino acid: nothing is picked (the game has NOT started); AT 'GO!' the wizard panel appears; the timer label (outside the info box) starts at 0:00 and advances ~1/s in M:SS; the scene shows the materialized game composed per the Phase-3 framing (ligand above grid, active molecule framed).
2. SETUP-11 (mid-countdown robustness — P-2): Start again IMMEDIATELY during a countdown (double-Start): EXPECT the pending countdown is cancelled and a fresh one runs; no double-GO, no orphaned wizard. Then: Start, and during the 3-2-1 switch to Setup and click 'Cleanup model': EXPECT no stale GO afterwards (no wizard appears), objects removed, console clean.
3. SCORE-04 (info box + timer + required label): with the game running, EXPECT the info box to carry the event narrative: the start lines, 'Level 1, molecule 1 of N.' at GO; click an amino acid -> 'Selected: slot <id> (<object>).' appears in the box (the wizard panel STILL shows its own imperative prompt — two surfaces, two roles, no verbatim mirroring); press a movement key with nothing selected (fresh moment) -> 'ERROR: ...' appears ONCE in the box (not repeated every second). EXPECT the timer OUTSIDE the box and the required label showing the requirement (defaults = unset mode -> 'Required: N interaction(s): h_bond x1, ...' matching the panel's Required line).
4. SCORE-04 (timer pause under a modal — P-5): mid-game, switch to Setup and click 'Save Setup' (opens the real file dialog; cancel it). EXPECT the timer label FREEZES while the dialog is open (allow <=1 s over-count at the open edge) and resumes on close. Repeat once with a _guard warning box if convenient (e.g. trigger a Start refusal) — same freeze.
5. PLAY-05 (hint): on the live game press 'Hint'. EXPECT: the CARBONS of every amino acid that could form a required interaction turn orange; the ligand and incapable amino acids are unchanged; no lines/dots/geometry ever appear. Press Hint again -> no visible change (idempotent). Select (green) one candidate AA, then Hint -> that slot shows orange carbons + green everything else (coexistence). Press Done -> ALL colors restored (including hinted-never-selected slots). COLOR VERDICT: is 'orange' legible and distinct from green + default element colors? (If not, name the preferred alternative — magenta/salmon/pink — for a one-constant fix.)
6. PLAY-05 (hint-before-selection trap): fresh game -> press Hint BEFORE ever selecting -> Done -> EXPECT full color restore (the trap class SMOKE-11 asserts mechanically; confirm on-screen).
7. SCORE-04 (both required modes): Configure an EXCLUSIVE game (mode radio 'exclusive') -> Start -> EXPECT 'Required: any 1 interaction' in the label; Hint on this game highlights AAs capable of ANY of the 7 types. Cleanup, then an UNSET or block_exclusive game -> EXPECT the 'Required: N interaction(s): ...' list form.
8. Restart feel (feeds Phase 6): mid-game, click Start again (Setup tab) -> EXPECT: info box cleared + fresh countdown, prior wizard popped cleanly, no color/msm residue, timer restarts from 0:00.
9. Console: zero AA-match tracebacks across the whole session; plugin-init warnings match the recorded 04-14 baseline.
  </how-to-verify>
  <resume-signal>Type "approved" or list the failing step numbers + notes (fix-batch follows the 04-15 pattern)</resume-signal>
</task>

</tasks>

<verification>
After the human verdicts are recorded:
- All three ROADMAP Phase-5 criteria carry [HUMAN] verdict coverage in 05-11-SUMMARY.md.
- Any fix-batch items are applied + re-verified (the 04-15 pattern: fix commit + re-green of the affected smokes + visual re-confirm note).
</verification>

<success_criteria>
- 05-11-SUMMARY.md records the per-step verdict table (steps 1-9) with the human's pass/fail + notes, including the hint-color verdict.
- SETUP-11, SCORE-04, PLAY-05 are all human-verified or a fix-batch is scoped from the failures.
- Phase 5 is ready for /gsd-verify-phase.
</success_criteria>

<output>
After completion, create `.planning/phases/05-game-status-tab-start-sequence/05-11-SUMMARY.md`
</output>
