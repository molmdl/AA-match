---
phase: 02-headless-game-engine
plan: 06
type: tdd
wave: 4
depends_on: ["02-01", "02-02", "02-05"]
files_modified:
  - aamatch/thresholds.py
  - aamatch/detector.py
  - tests/test_thresholds.py
  - tests/test_detector.py
  - tests/test_purity.py
autonomous: true

must_haves:
  truths:
    - "Every approved threshold constant carries its source citation and approval date, transcribed from docs/DETECTION_THRESHOLDS.md"
    - "Detector features precompute once (donor-H pairs, acceptors, charge-group centers, rings with center/normal/radius, hydrophobes, halogens, metals) — no per-pair recomputation"
    - "Candidate pairs come ONLY from spatial.cross_pairs + an AA bounding-sphere prefilter — no full atom-set double loop"
    - "h_bond, salt_bridge, hydrophobic detect with explicit partner sides and D1 side-chain-only restriction on the AA side; records carry {type, aa{...}, lig{...}, metrics, formed}"
  artifacts:
    - path: "aamatch/thresholds.py"
      provides: "Approved table as named constants (single import point for the detector)"
      min_lines: 80
    - path: "aamatch/detector.py"
      provides: "extract_features + prefilter + candidates + 3 of 7 type tests + canonical record shape"
      min_lines: 200
  key_links:
    - from: "aamatch/detector.py"
      to: "aamatch/thresholds.py"
      via: "all cutoffs/angles imported, never inlined"
      pattern: "from .thresholds import"
    - from: "aamatch/detector.py"
      to: "aamatch/capability.py"
      via: "typing tables + side_chain sets (DETECT-04 single home)"
      pattern: "from .capability import"
    - from: "aamatch/detector.py"
      to: "aamatch/spatial.py"
      via: "cross_pairs is the ONLY candidate source"
      pattern: "cross_pairs"
---

<objective>
Build the detector foundation: thresholds.py (the approved table as constants) plus detector.py's pipeline through features, spatial candidates, and the three contact types (h_bond, salt_bridge, hydrophobic) with explicit partner sides.

Purpose: DETECT-01/02/04 start here; the two-level pruning (AA bounding sphere, then cell list) is the correctness+performance core of DETECT-05.
Output: thresholds.py + detector core with 3 of 7 types detecting on scripted geometries.
</objective>

<execution_context>
@~/.config/opencode/get-shit-done/workflows/execute-plan.md
@~/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@docs/DETECTION_THRESHOLDS.md                                        # rows 1-5, 8-10 + D1 policy — the frozen source
@.planning/phases/02-headless-game-engine/02-RESEARCH-detection.md   # §5.1 contract + §5.2 pipeline + §7 chemistry policy (docstring content)
@aamatch/capability.py                                               # typing tables + side_chain sets + ligand typing
@aamatch/spatial.py, @aamatch/vec3.py                                # candidates + math
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: thresholds.py — approved table as constants (RED -> GREEN)</name>
  <files>aamatch/thresholds.py, tests/test_thresholds.py</files>
  <action>
RED: test that each constant equals the approved value AND that a policy tuple set is complete: HBOND_D_MAX=4.0, HBOND_ANGLE_MIN_DEG=140.0, SALT_CENTER_D_MAX=5.5, PISTACK_CENTER_D_MAX=5.5, PISTACK_ANGLE_TOL_DEG=30.0, PISTACK_OFFSET_MAX=2.0, CATIONPI_D_MAX=6.0, CATIONPI_OFFSET_MAX=2.0, HYDRO_D_MAX=4.0, HALOGEN_D_MAX=4.0, HALOGEN_DONOR_ANGLE_DEG=(135.0, 195.0), HALOGEN_ACC_ANGLE_DEG=(90.0, 150.0), METAL_D_MAX=3.0, METAL_ELEMENTS=frozenset({'MG','ZN','FE','CA','MN','CU','NI','CO','CD'}), MIN_DIST=0.5, AROMATIC_PLANARITY_FALLBACK_DEG=15.0, AA_PREFILTER_MARGIN=7.5 (max cutoff headroom per detection research §5.2.2), CELL-CUTOFF note. Also: MAX_CUTOFF = max of all distance constants (computed, used by the prefilter).

GREEN: aamatch/thresholds.py — pure constants ONLY, one `source:` comment per constant (row # from docs/DETECTION_THRESHOLDS.md + published source + approval date transcribed from the doc's approval record). Module docstring: bump policy (any change here => DETECTOR_VERSION bump in level_spec.py).

Commit: `feat(02-06): approved threshold constants`.
  </action>
  <verify>python3.6 -m unittest tests.test_thresholds -v</verify>
  <done>Constants match the approved document exactly; every constant cites source + approval date</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: detector core — features, prefilter, candidates, h_bond/salt_bridge/hydrophobic (RED -> GREEN)</name>
  <files>aamatch/detector.py, tests/test_detector.py</files>
  <action>
RED: tests first with fully scripted geometries (hand-placed coordinates; NO PyMOL):
1. extract_features(atom_records, ligand_bonds): given a benzamide-like ligand + 2 AA records, returns features with: ligand donor-H pairs (N-H from bonds), acceptors, ring (center/normal/radius via vec3), hydrophobes (C with all neighbors in {C,H}); AA side features built ONLY from side-chain atoms (D1) using capability.AA_RESIDUES[resn]['side_chain']; unclassified AA atom names are counted and returned (features['unclassified_aa_atoms']) so SMOKE-03 can assert zero.
2. Prefilter: an AA whose bounding sphere (centroid+max dist of its atoms) does not intersect the ligand sphere inflated by thresholds.AA_PREFILTER_MARGIN yields ZERO candidates (test: far-away AA produces no records).
3. h_bond: geometry D···A = 3.0 A, angle at H = 160 deg -> record formed with aa role 'donor' (SER OG+HG vs ligand carbonyl O); angle 100 deg -> NOT formed (boundary sensitivity: 141 deg forms, 139 deg does not); dist 4.1 -> not; 3.9 -> yes. Fail-closed: donor O/N/S without attached H produces nothing.
4. salt_bridge: ASP (carboxylate center = midpoint of OD1/OD2) vs ligand ammonium N+ at 4.5 A -> record; same-charge pair -> no record; polarity from ligand charge sign (D3); center-vs-atom trap: a carboxylate whose first O is far from the midpoint still uses the GROUP center (detection research §9.12 — test it).
5. hydrophobic: ligand qualifying C within 4.0 A of an ALA side-chain carbon -> record; TYR (not in pedagogical set) -> NO hydrophobic record even at 3.0 A (residue-name-based AA side — coordination decision #2); ligand C bonded to N -> not a qualifying hydrophobe.
6. Record shape: {"type", "aa": {"object","atom_ids","resn","resi","role"}, "lig": {"object","atom_ids","role"}, "metrics": {...}, "formed": True}; results sorted canonically by (type, aa object, aa atom_ids, lig atom_ids); same input twice -> identical list (determinism).

GREEN: implement detector.py: module docstring carries the FULL chemistry policy (detection research §7: waters excluded, alt-conf ''/'A' only, fail-closed donors, state-1 only, metal gating preview, covalent exclusions, MIN_DIST guard, D1 side-chain-only). Functions: extract_features, _aa_bounding_spheres, detect_part1 (h_bond/salt_bridge/hydrophobic) sharing the pipeline the 7-type version will finish. Candidate enumeration: spatial.cross_pairs over (near-AA side-chain atoms) x (ligand typed atoms); charge/ring FEATURE pairs enumerated directly (tiny counts). All cutoffs from thresholds — never inline a number. NO numpy, NO itertools.

Commit: `feat(02-06): detector core + 3 contact types (TDD)`.
  </action>
  <verify>python3.6 -m unittest tests.test_detector -v</verify>
  <done>3 types detect/not-detect at scripted boundaries with explicit partner sides; pruning provably excludes far AAs; records canonical + deterministic</done>
</task>

<task type="auto">
  <name>Task 3: Register thresholds + detector in PURE_MODULES + full suite</name>
  <files>tests/test_purity.py</files>
  <action>
Append 'thresholds' and 'detector' to PURE_MODULES. detector imports: from .vec3, from .spatial, from .capability, from .thresholds, from .setup_state import INTERACTION_TYPES (canonical type order). No itertools, no numpy.
  </action>
  <verify>python3.6 -m unittest discover -s tests -v</verify>
  <done>PURE_MODULES = 11; full suite green</done>
</task>

</tasks>

<verification>
python3.6 -m py_compile aamatch/*.py && python3.6 -m unittest discover -s tests -v
</verification>

<success_criteria>
Threshold constants frozen to the approved doc; detector core classifies 3 types on scripted geometry with D1 restriction, explicit sides, spatial pruning; purity gates green.
</success_criteria>

<output>
After completion, create `.planning/phases/02-headless-game-engine/02-06-SUMMARY.md`
</output>
