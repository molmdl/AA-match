---
phase: 04-qt-setup-window
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: [smoke/smoke_09_qt_probe.py]
autonomous: true

must_haves:
  truths:
    - "The offscreen-Qt verdict for this exact Windows conda env is recorded: either 'PASS (platform=offscreen)' or 'PASS (platform=default)' or 'FAIL' — no TBD left"
    - "The probe opens zero modals (a modal in a headless probe hangs until the run_smoke.sh timeout backstop)"
    - "Later Phase-4 plans' T1b verify steps can reference a definite probe verdict"
  artifacts:
    - path: "smoke/smoke_09_qt_probe.py"
      provides: "Headless widget-construction probe (QApplication + QDialog under QT_QPA_PLATFORM=offscreen, default-platform fallback)"
      min_lines: 40
  key_links:
    - from: "smoke/smoke_09_qt_probe.py"
      to: "pymol.Qt"
      via: "from pymol.Qt import QtWidgets, QtCore (the wrapper shim, never raw PyQt5)"
      pattern: "from pymol\\.Qt import"
---

<objective>
Settle research open question Q1 (headless offscreen-Qt widget construction) with a cheap one-script probe, so every later Phase-4 plan knows whether its `<verify>` may use the T1b tier (construct dialogs, drive collect/apply round-trips, click buttons headlessly) or must degrade to T1a (import-only) + [HUMAN].

Purpose: `import pymol.Qt` headless is PROVEN (v1 smokes executed module-level Qt imports under `-cq`, PA-__init__:156-157), but widget CONSTRUCTION needs a QApplication that PyMOL `-cq` never builds. Whether `QT_QPA_PLATFORM=offscreen` works in this conda PyQt5 5.12.3 / Qt 5.12.9 env is unrecorded anywhere in the repo. This probe is the cheapest possible plan and its verdict rewrites the verify columns of plans 04-05 through 04-13.
Output: `smoke/smoke_09_qt_probe.py` + a recorded verdict (SUMMARY) naming the working platform (or the definitive failure).
</objective>

<execution_context>
@~/.config/opencode/get-shit-done/workflows/execute-plan.md
@~/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/04-qt-setup-window/04-RESEARCH-qt-window-architecture.md
@.planning/STATE.md
@smoke/run_smoke.sh
@smoke/smoke_01_bootstrap.py
</context>

<decisions>
- Verdict markers: the script prints BOTH `=== PROBE PASS (platform=offscreen) ===` (or `platform=default` / `=== PROBE FAIL ===`) — the research verdict marker — AND `=== SMOKE-09 PASS ===`, because `smoke/run_smoke.sh` derives NN=09 from the basename and greps exactly `=== SMOKE-09 PASS ===` (exit codes cannot carry verdicts through cmd.exe).
- Probe hygiene (PITFALL P5): NO modal anywhere — no `.exec_()`, no QFileDialog, no QMessageBox. Only construct → show → processEvents → assert.
- Tries offscreen FIRST (env var set BEFORE the Qt import), then the default platform; records whichever works.
</decisions>

<tasks>

<task type="auto">
  <name>Task 1: Write the offscreen-Qt probe smoke script</name>
  <files>smoke/smoke_09_qt_probe.py</files>
  <action>
Write `smoke/smoke_09_qt_probe.py` following the repo smoke header conventions (see `smoke/smoke_01_bootstrap.py`: anchor repo root via `sys.argv[0]`/cwd fallback — `__file__` is unusable in `-cq` scripts per 01-07).

Structure (exact order matters — the platform env var MUST be set before any Qt import):

1. Header docstring: what the probe settles (research Q1, 04-RESEARCH-qt-window-architecture.md §verification_tiers), how to run it (`bash smoke/run_smoke.sh smoke/smoke_09_qt_probe.py 120`), and the no-modals rule.
2. PART A (offscreen): `os.environ['QT_QPA_PLATFORM'] = 'offscreen'` set at the very top, THEN `from pymol.Qt import QtWidgets, QtCore` (the wrapper shim — NEVER `from PyQt5 import ...`, banned repo-wide). Print `QT_QPA_PLATFORM=offscreen` attempt banner.
3. `app = QtWidgets.QApplication.instance() or QtWidgets.QApplication(['aamatch-qt-probe'])` — reuse-or-create, exactly the shape research §verification_tiers Q1 specifies.
4. Construct a throwaway `QtWidgets.QDialog()`, `setMinimumWidth(420)`, a `QtWidgets.QVBoxLayout` with one `QtWidgets.QLabel('probe')` and one `QtWidgets.QPushButton('probe')`, `dlg.show()`, `app.processEvents()`, then assert the dialog `isVisible()`... NOTE: under offscreen, `isVisible()` should be True after show(); do NOT hard-fail the whole probe if visibility semantics differ — wrap the visibility assert so a failure prints `offscreen: visible-assert failed (<detail>)` and continues to the round-trip check below. The probe's verdict rests on CONSTRUCTION succeeding, not on pixel visibility.
5. Mini round-trip: a `QtWidgets.QSpinBox` with `setRange(1, 10)`, `setValue(7)`, read back `value() == 7`; a `QCheckBox` toggle + `isChecked()`. This proves signal/slot objects work, not just construction.
6. `app.processEvents()` once more, then print `=== PROBE PASS (platform=offscreen) ===` and stop (skip PART B).
7. PART B (fallback, reached only if PART A raises): wrap PART A in try/except; on ANY exception, print the exception summary, then delete the QApplication instance reference (`QtWidgets.QApplication.instance()` may persist — if an app object was created in PART A, do not recreate; note the limitation in a comment), retry the identical construct sequence WITHOUT setting the env var (default windows platform — cmd.exe inherits the interactive desktop session). On success print `=== PROBE PASS (platform=default) ===`.
8. If BOTH parts fail: print the two exception summaries and `=== PROBE FAIL ===` (no SMOKE-09 marker — run_smoke.sh then fails, which is the correct verdict).
9. On any success, ALSO print `=== SMOKE-09 PASS ===` as the final line (run_smoke.sh grep contract).
10. House rules: python3.6-compatible syntax only (no f-strings — use `%`-formatting; Gate D discipline), no logging, no `print` except the banner/marker lines, comments citing the research file sections used.
  </action>
  <verify>T0: `python3.6 -m py_compile smoke/smoke_09_qt_probe.py` exits 0 (the script must be 3.6-clean even though it runs under the Windows conda Python 3.9). Static read-through: no `.exec_(`, no `QFileDialog`, no `QMessageBox` anywhere in the file (P5 modal ban).</verify>
  <done>smoke_09_qt_probe.py exists, compiles under python3.6, contains the offscreen→default probe ladder, the two marker families, and zero modal calls.</done>
</task>

<task type="auto">
  <name>Task 2: Run the probe via run_smoke.sh and record the verdict</name>
  <files>smoke/smoke_09_qt_probe.py</files>
  <action>
Run from repo root: `bash smoke/run_smoke.sh smoke/smoke_09_qt_probe.py 120`.

- If the output contains `=== PROBE PASS (platform=offscreen) ===` AND `=== SMOKE-09 PASS ===`: the T1b tier is AVAILABLE for all later Phase-4 plans; record platform=offscreen.
- If it contains `=== PROBE PASS (platform=default) ===`: T1b is available with the default platform (invisible windows are acceptable for logic asserts); record platform=default.
- If it contains `=== PROBE FAIL ===` (run_smoke.sh exits nonzero): T1b is UNAVAILABLE; later plans degrade to T1a + [HUMAN] and handlers must be factored construct-free. Record the failure verbatim.

Do not "fix" a FAIL by editing PyMOL/conda — a FAIL is a legitimate verdict. The only legitimate script fix is a bug in the probe itself (e.g. an import typo), after which re-run.
  </action>
  <verify>`bash smoke/run_smoke.sh smoke/smoke_09_qt_probe.py 120` prints a definite verdict line: one of `=== PROBE PASS (platform=offscreen) ===`, `=== PROBE PASS (platform=default) ===`, or `=== PROBE FAIL ===` — and, in the PASS cases, the trailing `=== SMOKE-09 PASS ===`.</verify>
  <done>The probe ran in real headless Windows PyMOL and produced one definite verdict; the verdict text (including the exact marker line and any exception summaries from a FAIL) is captured for the SUMMARY.</done>
</task>

</tasks>

<verification>
- `python3.6 -m py_compile smoke/smoke_09_qt_probe.py` green (T0).
- `bash smoke/run_smoke.sh smoke/smoke_09_qt_probe.py 120` prints a definite verdict (T1a/T1b probe run in real PyMOL).
- Zero modal calls in the probe source (grep: no `exec_`, `QFileDialog`, `QMessageBox`).
</verification>

<success_criteria>
Research Q1 is no longer UNVERIFIED: the recorded verdict names the platform (or definitive failure) that later Phase-4 plans' T1b verify steps will cite. The probe itself leaves no scene objects, opens no modals, and is rerunnable.
</success_criteria>

<output>
After completion, create `.planning/phases/04-qt-setup-window/04-01-SUMMARY.md` — the SUMMARY MUST contain a line `Probe verdict: <PASS (platform=offscreen) | PASS (platform=default) | FAIL> <one-line evidence>` because plans 04-05..04-13 cite this exact line.
</output>
