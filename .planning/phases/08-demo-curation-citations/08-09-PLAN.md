---
phase: 08-demo-curation-citations
plan: 09
type: execute
wave: 6
depends_on:
  - "08-07"
files_modified:
  - docs/DATA_SOURCES.md
autonomous: true
must_haves:
  truths:
    - "docs/DATA_SOURCES.md covers EVERY bundled ligand file: download source + exact fetch URL, CID/CCD, PDB complex ID + entry DOI + primary paper DOI, PLIP interaction provenance, protonation record with why, license permitting bundling/redistribution, sha256, fetch date"
    - "Set sections appear in dropdown (tier) order and match MANIFEST.json set-level license/provenance fields exactly"
    - "The sources-and-licenses section carries the verbatim policy quotes (wwPDB CC0, NCBI/PubChem public domain) with the URLs they were fetched from, plus database citations"
    - "The not-bundled acknowledgements section records DrugBank (CC BY-NC — informational only, never our license) and PDBsum's unavailability"
  artifacts:
    - path: "docs/DATA_SOURCES.md"
      provides: "The HELP-02 dedicated sources document (gate-doc format like docs/DETECTION_THRESHOLDS.md)"
      min_lines: 100
      contains: "DATA_SOURCES"
  key_links:
    - from: "docs/DATA_SOURCES.md rows"
      to: "aamatch/data/MANIFEST.json"
      via: "per-entry sha256/counts cross-referenced to the manifest pins (regenerate via scripts/build_demos.py, never hand-edit)"
      pattern: "MANIFEST\\.json"
    - from: "docs/DATA_SOURCES.md"
      to: "08-PROPOSALS.md"
      via: "verification-protocol header naming the proposal + recorded human approval"
      pattern: "08-PROPOSALS"
    - from: "docs/DATA_SOURCES.md"
      to: "scripts/build_demos.py --report-only"
      via: "rows generated from the committed specs (machine-reproducible, regenerable)"
      pattern: "report-only|--report"
---

<objective>
Write `docs/DATA_SOURCES.md` — the HELP-02 dedicated sources document — in the gate-doc format of `docs/DETECTION_THRESHOLDS.md`, generated from the committed specs (builder `--report-only`), the manifest, and the approved proposal.

Purpose: ROADMAP criterion 3 [HUMAN]: every bundled file carries download source, PDB ID + DOI, protonation/interaction provenance from known binding databases, and a verified license permitting bundling/redistribution — human sign-off lands in 08-11's checkpoint.
Output: docs/DATA_SOURCES.md committed.
</objective>

<execution_context>
@~/.config/opencode/get-shit-done/workflows/execute-plan.md
@~/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/08-demo-curation-citations/08-PROPOSALS.md
@.planning/phases/08-demo-curation-citations/08-RESEARCH-sourcing.md
@aamatch/data/MANIFEST.json
@scripts/demo_specs/demo-easy-1.json
@docs/DETECTION_THRESHOLDS.md

RQ9 skeleton (sourcing research): header with last-verified date + verification protocol; §1 sources & licenses (verbatim quotes); §2 demo sets in tier-grouped order (one section per set, one row per entry); §3 multi-state & ions policy (the ratified RQ7 text); §4 not-bundled acknowledgements.
</context>

<tasks>

<task type="auto">
  <name>Task 1: generate docs/DATA_SOURCES.md per the RQ9 skeleton</name>
  <files>docs/DATA_SOURCES.md</files>
  <action>
Run `python3.6 scripts/build_demos.py --report-only` and write `docs/DATA_SOURCES.md` from its rows plus the proposal/research content:
- Header: `# Demo Data Sources (AA-match)`, 'Last verified: <today>', and the verification protocol line: every claim carries a fetched URL + date; human approval recorded in `.planning/phases/08-demo-curation-citations/08-PROPOSALS.md` (APPROVED <date>) before any bundled file was committed.
- §1 Sources & licenses: table with PubChem (public domain / no restrictions — quote the NCBI policy + FTP Fair Use lines recorded in the sourcing research, with both URLs + fetch dates) and wwPDB/RCSB (CC0 1.0 — quote the wwPDB usage-policy line + URL). Add the database citations: Berman 2000 (doi:10.1093/nar/28.1.235), wwPDB 2018 (doi:10.1093/nar/gky949), PLIP 2025 (doi:10.1093/nar/gkaf361), ChEBI Malik 2025 (doi:10.1093/nar/gkaf1271) — and the PubChem canonical citation ONLY if 08-04 recorded it VERIFIED (otherwise omit and keep it flagged in the proposal).
- §2 Demo sets: one `### <set_id> — "<title>" (tier: <tier>)` section per set in TIER_ORDER (dev set last, clearly marked as the development fixture, license '' noted as placeholder-by-design); per set: Rationale line + License line + the per-entry table from the --report-only rows: entry_id | file | molecule | source (URL) | ID (CID/CCD) | PDB complex (ID + entry DOI + paper DOI) | interaction provenance (PLIP on <PDB ID>: types) | protonation ('as-recorded' + why) | halogen/metal | sha256 | fetched.
- Cross-ref line per set: "counts/sha256 pinned in aamatch/data/MANIFEST.json; regenerate via `python3.6 scripts/build_demos.py --spec scripts/demo_specs/<set_id>.json --fetch` — never hand-edit an SDF or the manifest (02-04 law)."
- §3 Multi-state & ions policy: the ratified RQ7 text from 08-PROPOSALS (one record per file, states_expected 1, proposal-time protonation choice, ions are scene context never ligand content, lone-ion ligands rejected).
- §4 Not-bundled acknowledgements: DrugBank data (CC BY-NC 4.0) surfaces on RCSB ligand pages — informational only, NOT our license, NOT bundled; PDBsum currently unavailable (its own EBI page says so) — PDBe/PLIP used instead; no context complexes shipped (provenance-only), 1OXR cited as the cleanup-field-test's real-world analog.
Match the DETECTION_THRESHOLDS.md gate-doc register: every factual claim carries its source URL + date; no claim without evidence.
  </action>
  <verify>test -f docs/DATA_SOURCES.md && grep -c 'demo-' docs/DATA_SOURCES.md (≥ 10 set sections) && grep -q 'CC0' docs/DATA_SOURCES.md && grep -q 'pubchem\\|PubChem' docs/DATA_SOURCES.md && grep -q '10.2210/pdb' docs/DATA_SOURCES.md</verify>
  <done>DATA_SOURCES.md written with all four sections, tier-ordered set sections, per-entry rows from --report-only.</done>
</task>

<task type="auto">
  <name>Task 2: coverage audit + DOI spot-checks</name>
  <files>docs/DATA_SOURCES.md</files>
  <action>
Audit the doc mechanically:
1. File coverage: for every `.sdf` under `aamatch/data/ligands/`, grep the filename in docs/DATA_SOURCES.md — every file must appear (16 curated + 2 dev = 18 total). A miss = a missing row: fix the doc.
2. Row completeness: every curated set section's table rows have non-empty source URL, PDB complex ID, entry DOI, PLIP reference, protonation, license, sha256, fetched date.
3. Manifest consistency: every set-level license string in MANIFEST.json appears verbatim in the doc's corresponding section.
4. DOI spot-checks (webfetch, 3 samples across different sets — must include one `10.2210/pdbXXXX/pdb` entry DOI): each resolves (HTTP 200 at doi.org). Record the spot-check date in the doc header's verification-protocol line.
5. `python3.6 -m unittest discover -s tests -v` still green (docs-only change, but the standing gate runs).
Commit `docs(08-09): add DATA_SOURCES.md (per-file provenance, licenses, policy quotes)`.
  </action>
  <verify>for f in aamatch/data/ligands/*.sdf; do base=$(basename $f); grep -q "$base" docs/DATA_SOURCES.md || echo "MISSING: $base"; done (no MISSING output) && python3.6 -m unittest discover -s tests -v (green) && webfetch DOI spot-checks return 200</verify>
  <done>Every bundled file covered; DOIs spot-checked; doc committed.</done>
</task>

</tasks>

<verification>
- Coverage loop: zero MISSING filenames.
- grep checks: CC0 + PubChem policy quotes + 10.2210/pdb DOIs + PLIP citations present.
- MANIFEST.json licenses ↔ doc sections consistent.
- Full WSL suite green.
</verification>

<success_criteria>
- HELP-02's document exists in gate-doc format and covers every bundled file with license + provenance.
- The document is regenerable (specs + --report-only), not a hand-maintained orphan.
- Human sign-off deferred to 08-11's [HUMAN] checkpoint (by design — criterion 3's human tier).
</success_criteria>

<output>
After completion, create `.planning/phases/08-demo-curation-citations/08-09-SUMMARY.md`
</output>
