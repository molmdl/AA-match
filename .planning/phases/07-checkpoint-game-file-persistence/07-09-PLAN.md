---
phase: 07-checkpoint-game-file-persistence
plan: 09
type: execute
wave: 5
depends_on: [07-02, 07-05, 07-08]
files_modified: [aamatch/gamestart.py, smoke/smoke_20_checkpoint_e2e.py]
autonomous: true

must_haves:
  truths:
    - "load_checkpoint(path) runs ALL parse gates BEFORE any scene mutation — a foreign/corrupt/newer/incomplete checkpoint never touches the session"
    - "The load order is: read zip + gates -> pop any-identity GameWizard (cleanup runs) -> cmd.load (full session replace) -> sentinel sweep + reconcile (never-ghost + current-level completeness) -> engine adopt + timer rebase -> msm repair + wizard adopt-or-rebuild -> _last_start rebuild"
    - "The resumed game equals the saved game: positions/orientations from the .pse bit-exact, scores/counters/level position from the sidecar, timer rebased to continue from the save moment (elapsed_at_save)"
    - "The wizard is adopted when the pickle restored it (with a registry-equality verify) and REBUILT from the sidecar block otherwise (resume_from + msm repair + activate)"
    - "The restored engine remains playable: detect/score/confirm continue on the restored scene"
  artifacts:
    - path: "aamatch/gamestart.py"
      provides: "load_checkpoint(path) — the full sentinel-first reconstruction orchestration"
      contains: "def load_checkpoint"
    - path: "smoke/smoke_20_checkpoint_e2e.py"
      provides: "SMOKE-20: TWO-RUN checkpoint E2E — run 1 saves mid-game, run 2 (fresh process) loads and verifies full reconstruction"
      contains: "SMOKE-20"
  key_links:
    - from: "aamatch/gamestart.py load_checkpoint"
      to: "checkpoint.read_checkpoint_zip / reconcile_registry"
      via: "gates + sentinel-first reconcile BEFORE engine adopt; observed = per-object sorted ids over the loaded atoms"
      pattern: "reconcile_registry"
    - from: "aamatch/gamestart.py load_checkpoint"
      to: "engine.adopt_game"
      via: "payload + reconciled registry + sidecar game_state + ligand_texts + elapsed_at_save (rebase inside)"
      pattern: "adopt_game"
    - from: "aamatch/gamestart.py load_checkpoint"
      to: "GameWizard resume_from / activate"
      via: "adopt path verifies pickled _registry == reconciled registry (mismatch -> fail-safe rebuild); rebuild path = GameWizard(...) + resume_from(books) + msm repair + activate(replace=0) — NOT activate_game (no timer re-anchor; rebase owns the clock)"
      pattern: "resume_from"
---

<objective>
Implement `gamestart.load_checkpoint(path)` — the sentinel-first reconstruction orchestration (07-RESEARCH-state.md §4's end-to-end load order) — and SMOKE-20, the two-process checkpoint E2E (save -> quit -> relaunch -> load).

Purpose: PERSIST-03's "checkpoint save/restore fully reconstructs the game" and criterion 1's HEADLESS half + criterion 3's [GATE] mechanics. The reconstruction model is EXACT-SCENE RESTORE (decision recorded below): the loaded .pse atoms are the truth; the sidecar repairs Python-side metadata only.
Output: the load seam + the crown two-run smoke.

**RECORDED DECISIONS (yolo):**
1. **Reconstruction model = exact-scene restore** (07-RESEARCH-state.md §3 recommendation (a)/(c)): no regeneration at load, zero pose data in the sidecar (coords proven bit-exact), engine rehydrated from JSON (survives plugin reload), gates re-run on every load.
2. **Timer = rebase** (freeze at the save moment): restore runs `rebase_timer(now, elapsed_at_save)` — a checkpoint resumes where the player left off (07-RESEARCH-pse.md item 5 recommendation).
3. **NO countdown on resume:** the scene and books come back intact; the wizard is activated directly (`activate(replace=0)`), never `activate_game` (which anchors the timer from zero). The tab re-arm lands in 07-10.
4. **Degraded sidecar-less adopt path is NOT built.** With the kind-dispatch load affordance (07-10), a bare .pse at Import refuses at the JSON parse ("could not parse AA-match JSON") — the degraded "playable-but-without-metadata" state is unreachable through the button; File->Open of a bare .pse runs no plugin code (the restored wizard's own 'engine: no live game' guard is the recovery, documented in the docstring). Never partially reconstruct in silence.
5. **Adopt-with-verify:** if the pickled wizard restored, verify its `_registry` equals the reconciled registry (plain-dict equality); on mismatch, pop it and rebuild from the sidecar (fail-safe to the repair path).
</objective>

<execution_context>
@~/.config/opencode/get-shit-done/workflows/execute-plan.md
@~/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/phases/07-checkpoint-game-file-persistence/07-RESEARCH-state.md
@.planning/phases/07-checkpoint-game-file-persistence/07-RESEARCH-pse.md
@aamatch/gamestart.py
@aamatch/checkpoint.py
@aamatch/engine.py
@aamatch/wizard.py
@aamatch/placement.py
@aamatch/geometry.py
@aamatch/paths.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: gamestart.load_checkpoint(path)</name>
  <files>aamatch/gamestart.py</files>
  <action>
Add the module-level orchestration (lazy imports per file style). The EXACT order (07-RESEARCH-state.md §4):

1. **Gates first:** `pse_path, data = checkpoint.read_checkpoint_zip(paths.to_windows_path(path))` — the container header, checkpoint version, embedded-game five-gate chain, GameState validator ALL run before any scene mutation. Keep `pse_path` + the temp dir for cleanup.
2. **Pop any-identity GameWizard:** `prior = cmd.get_wizard()`; `if wizard_mod.is_game_wizard_any_identity(prior): cmd.set_wizard()` — the popped wizard's own cleanup runs (msm/colors/pk1 restore); cmd.load would otherwise replace the stack WITHOUT running the outgoing wizard's cleanup.
3. **`cmd.load(pse_path)`** — full session replace (partial=0 default): objects + view + settings + the (07-01-fixed) pickled wizard stack.
4. **Sentinel sweep:** `observed = {}` — for each name in `geometry.game_object_names()`: sorted atom ids via ONE `cmd.iterate` pass (the `_sorted_ids` pattern: uppercase `ID`, explicit `space=` dict) + sentinel verification `cmd.count_atoms('%s and segi AAM and b < 0' % name) == cmd.count_atoms(name)` (the b<0 selector discipline, placement.py:50); a sentinel-mismatch object is reported as dropped by reconcile (fail-closed via the completeness gate).
5. **Reconcile:** `registry, report = checkpoint.reconcile_registry(data['registry'], observed, parsed_payload, gs['current_level_index'])` — raises FormatError (completeness) naming the first missing piece; a ghost entry is dropped, never registered.
6. **Engine adopt:** `game = engine.adopt_game(parsed_payload, registry, data['game_state'], data['ligand_texts'], elapsed_at_save=data['elapsed_at_save'])` — sets all four globals; rebases the timer iff not game_over and elapsed is not None (07-05's op).
7. **Wizard adopt-or-rebuild:**
   - `restored = cmd.get_wizard()`; adopt iff `wizard_mod.is_game_wizard_any_identity(restored)` AND `restored._registry == registry` (plain-dict equality — both derive from the same materialize output; mismatch -> fail-safe). On adopt: nothing more (books intact, scene shows the saved colors).
   - Rebuild otherwise: `wiz = GameWizard(parsed_payload, registry, gs['current_level_index'], gs['current_molecule_index'])` + `wiz.resume_from(data['wizard'])` + msm repair `cmd.set('mouse_selection_mode', data['wizard']['saved_msm'])` when saved_msm is not None (the session restored 0 — the save-time defensive value — not the user's pre-game value) + `wiz.activate(replace=0)` (push; NOT activate_game). `restored` may be None (restore failed) or a user wizard (never touched — push with replace=0 preserves it? NO — a user wizard under a game wizard is the stack-native pattern; activate(replace=0) pushes on top, correct).
8. **_last_start rebuild:** `gamestart._last_start = {'setup': parsed_setup, 'seed': parsed_payload['seed'], 'candidates': data['candidates'], 'ligand_content': data['ligand_texts'] or None, 'payload': parsed_payload}` — the 5-key tuple (Restart-after-resume replays payload-direct).
9. **Cleanup + return:** best-effort `shutil.rmtree(temp_dir, ignore_errors=True)`; return a plain summary dict: `{'path': path, 'adopted': bool, 'level_pos': gs['current_level_index'] + 1, 'molecule_pos': gs['current_molecule_index'] + 1, 'game_over': game.game_over, 'elapsed_at_save': data['elapsed_at_save']}` — the 07-10 tab re-arm consumes it.
Refusals (ValueError family) propagate to the caller's _guard; unexpected exceptions propagate (bug surfacing).
  </action>
  <verify>python3.6 -m py_compile aamatch/*.py && python3.6 -m unittest discover -s tests -v</verify>
  <done>load_checkpoint implements the 9-step order with gates-first, never-ghost reconcile, adopt-with-verify, and the 5-key _last_start rebuild</done>
</task>

<task type="auto">
  <name>Task 2: SMOKE-20 — two-process checkpoint E2E</name>
  <files>smoke/smoke_20_checkpoint_e2e.py</files>
  <action>
New TWO-RUN smoke (the SMOKE-17 shape: run 1 when the fixture is absent, run 2 when present; both grep `=== SMOKE-20 PASS ===`; runner contract `bash smoke/run_smoke.sh smoke/smoke_20_checkpoint_e2e.py 240` run TWICE):

- **Run 1 (SAVE):** start_game(seed 42) -> scripted pick -> `wiz.move_to(...)` + `wiz.rotate_axis(...)` (baked) -> `wiz.hint()` (non-trivial color store) -> `wiz.skip_molecule()` (score 0.00 recorded + skip_count 1 + molecule advance) -> `data = gamestart.capture_checkpoint_snapshot(elapsed=123.0)` -> `gamestart.save_checkpoint('tmp/smoke fixtures/aam_pse20/game.aamz', data)` -> snapshot the expected post-load facts into the check set: `engine.game_status()` (to_dict minus timer_anchor), per-object centroids of a few moved slots, `len(engine.detect())` record count on the moved scene, the wizard books. Print `SMOKE-20 RUN1 saved ...` lines. Teardown (cleanup + _last_start None).
- **Run 2 (VERIFY — fresh process = the quit->relaunch half):** assert the dead start state (`engine._game is None` via a safe probe — engine.game_status() raises EngineError; `cmd.get_wizard()` None) -> `summary = gamestart.load_checkpoint(zip_path)` -> assert:
  - `summary['adopted']` True (same-identity pickle restore through the 07-01 fix) and the wizard on stack isinstance-GameWizard with books equal to run 1's (_current_slot None post-skip, _color_store non-empty, _event_seq equal).
  - `engine.game_status()` equals the saved to_dict EXCEPT timer_anchor: `abs((time.time() - anchor) - 123.0) <= 5.0` (the rebase — clock continues from the save moment).
  - Registry equality: reconciled registry rows == the saved sidecar registry (modulo tuple/list).
  - Positions: the moved slots' centroids match run 1's within the float32-realizable tolerance (1e-6 floor + ulp slack).
  - `len(engine.detect())` equals run 1's record count (the restored scene is detector-equivalent).
  - Post-load identity invariant: `_assert_identity`-equivalent on every recolored slot object (mirroring the 07-01 SMOKE-17 addition).
  - Continue-play proof: script a pick on molecule 2's first slot -> `wiz.confirm_molecule()` returns a dict with `game_over` False and the score recorded (the game LIVES on).
  - Print `SMOKE-20 VERDICT resume: ... -> PROVEN` lines. rmtree fixtures + final marker.
  </action>
  <verify>bash smoke/run_smoke.sh smoke/smoke_20_checkpoint_e2e.py 240 run TWICE (run 1 SAVE, run 2 VERIFY) — both grep `=== SMOKE-20 PASS ===`</verify>
  <done>SMOKE-20 green on both runs: mid-game save; fresh-process load reconstructs positions/scores/counters/level/timer bit-faithfully; the wizard adopts with equal books; the game continues to be playable</done>
</task>

</tasks>

<verification>
1. `python3.6 -m py_compile aamatch/*.py` + full WSL suite green.
2. SMOKE-20 both runs PASS.
3. SMOKE-18 regression (gamestart.py touched): `bash smoke/run_smoke.sh smoke/smoke_18_save_checkpoint.py 240` PASS.
</verification>

<success_criteria>
- The full sentinel-first reconstruction order is implemented with refusal-first gating and the never-ghost + completeness laws enforced at the reconcile boundary.
- The two-process round-trip proves criterion 1's HEADLESS half: positions/orientations (bit-exact), scores, counters, timer (rebased) all survive save -> quit -> relaunch -> load.
- The wizard adopt/rebuild decision is verified (registry equality), and the restored game remains playable.
</success_criteria>

<output>
After completion, create `.planning/phases/07-checkpoint-game-file-persistence/07-09-SUMMARY.md`
</output>
