---
phase: 06-scoring-lifecycle-endgame
plan: 08
type: execute
wave: 6
depends_on: ["06-07"]
files_modified:
  - aamatch/game_window.py
  - smoke/smoke_16_tab.py
autonomous: true
must_haves:
  truths:
    - "Restart replays the stored _last_start tuple verbatim through the deferred start sequence: pop -> start_game(activate=False) -> countdown -> 'Game restarted.' line -> fresh game at GO"
    - "Restart with nothing ever started refuses with a clear message (house fail-closed)"
    - "Reset routes through the wizard's public reset_grid() behind the isinstance gate — NEVER engine.reset_to_grid() from the tab (PITFALL 6 at the widget layer)"
    - "Reset keeps the timer running and touches NO GameState (positions only; rotations persist)"
  artifacts:
    - path: "aamatch/game_window.py"
      provides: "btn_restart + btn_reset_grid + _on_restart/_restart_now + _on_reset_grid/_reset_grid_now handlers"
      contains: "btn_restart"
    - path: "smoke/smoke_16_tab.py"
      provides: "PART B: restart/reset tab-drive E2E incl. the None refusal"
      contains: "PART B"
  key_links:
    - from: "game_window._restart_now"
      to: "gamestart._last_start + gamestart.start_game(activate=False)"
      via: "the 05-05 binding replay (PITFALL 9 spec-replay mechanism; backup.py has NO role)"
      pattern: "_last_start"
    - from: "game_window._reset_grid_now"
      to: "GameWizard.reset_grid()"
      via: "isinstance-gated public op (identity asserts + _result clearing live in the wizard)"
      pattern: "reset_grid"
    - from: "_restart_now"
      to: "status_text.game_restarted_line()"
      via: "handler-logged AFTER start_countdown arms (the countdown clears the box)"
      pattern: "game_restarted_line"
---

<objective>
Game tab lifecycle controls, part 2 (SCORE-09/10): the Restart button replaying `gamestart._last_start` through the 05-09 deferred sequence, and the Reset button dispatching to the wizard's grid replay — with the pinned event lines and refusal semantics.

Purpose: "restart/reset recover" (ROADMAP goal); the PITFALL-9 reconciliation (spec replay IS the mechanism; backup.py stays Phase-7-only) is recorded in the handler docstrings, never wired as code.
Output: two more tab handlers + SMOKE-16 PART B.
</objective>

<execution_context>
@~/.config/opencode/get-shit-done/workflows/execute-plan.md
@~/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-scoring-lifecycle-endgame/06-06-SUMMARY.md
@.planning/phases/06-scoring-lifecycle-endgame/06-07-SUMMARY.md
@.planning/phases/06-scoring-lifecycle-endgame/06-RESEARCH-restart-reset.md
@aamatch/game_window.py
@aamatch/gamestart.py
@aamatch/wizard.py
@smoke/smoke_16_tab.py

Anchors (verified):
- `gamestart._last_start` gamestart.py:178 (module-level, deep-copied 4-key tuple {'setup','seed','candidates','ligand_content'}, captured :413-416; NEVER cleared in production; replay = `start_game(setup=ls['setup'], seed=ls['seed'], candidates=ls['candidates'], ligand_content=ls['ligand_content'], activate=False)`).
- The 05-09 deferred sequence: `_pop_game_wizard()` (P-3) -> start_game(activate=False) -> tab switch -> `game_tab.start_countdown(wiz)` (setup_window.py:1020-1026); `start_countdown` cancels any pending countdown FIRST (self-healing, game_window.py:172) and CLEARS the info box (:174).
- 06-07 gave GameTab its own `_pop_game_wizard()` helper + `_endgame_sequence`; Restart reuses both.
- Reset owner law (restart-reset research §4): the tab calls the wizard's PUBLIC `reset_grid()` (wizard.py:586-610 — engine.reset_to_grid + identity asserts + _result=None + refresh) behind the isinstance gate; `reset_grid()` returns None always -> the 'game_reset' line flows via the marker + the handler's synchronous `_refresh_status()` (06-06 added the marker; 06-02 pinned the wording).
- _last_start is None refusal (restart-reset D1): ValueError('Restart: no game has been started yet.') — the op is NOT gated behind an active wizard (works from the post-endgame popped state too).
- D7 line placement: log 'Game restarted.' AFTER `start_countdown` arms (the countdown's box clear would wipe a pre-chain line).
- No restart confirmation warning (D9): spec requires warnings for Skip/Give Up only (spec.md:43-45).
- fresh GameState on Restart proven by construction (engine.py:295); fresh timer anchor at GO via activate_game (gamestart.py:339) — correct for a NEW game.
- Grep laws + SCANNED_MODULES unchanged; PROSE_PIN: docstrings mention backup.py's NO-ROLE in prose only ("spec replay IS the PITFALL-9 mechanism") — no banned tokens.
</context>

<tasks>

<task type="auto">
  <name>Task 1: Restart button + handler</name>
  <files>aamatch/game_window.py</files>
  <action>
    1. In __init__ after btn_skip_menu: `self.btn_restart = QtWidgets.QPushButton('Restart', self)`; tooltip 'Replay the stored initial state into a fresh game (countdown, timer from zero, scores cleared).'; `btn_row.insertWidget(btn_row.count() - 1, self.btn_restart)`; `self.btn_restart.clicked.connect(self._on_restart)`.
    2. `_on_restart(self)`: `self._guard(self._restart_now)` (NO warning dialog — D9; the countdown log is the feedback, the _on_start precedent).
    3. `_restart_now(self)` (non-modal, headless-driveable):
       a. lazy `from . import gamestart, status_text`; `ls = gamestart._last_start`; `if ls is None: raise ValueError('Restart: no game has been started yet.')` (surfaces via the wrapper's _guard box in GUI; the smoke asserts the message).
       b. `self.cancel_pending_start()` (P-2 belt-and-braces, mirroring _on_cleanup's handler-top cancel).
       c. `self._pop_game_wizard()` (P-3 — the live GameWizard pops; its cleanup restores msm/colors/pk1; a user wizard on top is never popped).
       d. `wiz = gamestart.start_game(setup=ls['setup'], seed=ls['seed'], candidates=ls['candidates'], ligand_content=ls['ligand_content'], activate=False)` (NO build_state pre-checks — the tuple was validated at first start; deterministic replay; the _guard still catches OSError/ValueError e.g. a deleted fixture — accepted residue per the 04-13 clean-then-refuse law).
       e. `self.start_countdown(wiz)` (self-healing cancel + clear + 'Get ready...' + member timer).
       f. `self._log(status_text.game_restarted_line())` — AFTER the countdown arms (D7; the line survives the box clear).
       g. return True.
    4. Docstring records the PITFALL-9 reconciliation verbatim-class: "Restart = spec replay of the _last_start INPUT tuple (05-05) — the PITFALL-9 mechanism; backup.py has NO Phase-6 role (it remains the Phase-7 persistence policy home)". Also note the pre-existing dormant-user-wizard stack-hole is documented-only (restart-reset D6 — NOT a Phase-6 fix).
    - At GO the existing `_begin_play` path re-anchors the timer from zero and re-seeds _last_status — no changes needed there.
  </action>
  <verify>py_compile + WSL suite green; grep `grep -n "btn_restart\|no game has been started" aamatch/game_window.py` hits; grep law: zero engine._game/_payload hits.</verify>
  <done>Restart replays _last_start through the deferred sequence with the pinned refusal and the 'Game restarted.' line placement.</done>
</task>

<task type="auto">
  <name>Task 2: Reset button + handler</name>
  <files>aamatch/game_window.py</files>
  <action>
    1. In __init__ after btn_restart: `self.btn_reset_grid = QtWidgets.QPushButton('Reset', self)` (attribute name btn_reset_grid — DISTINCT from SetupWindow's game-setup btn_reset, restart-reset D7 naming law); tooltip 'Place all amino acids back to their grid positions; orientations are kept.'; `btn_row.insertWidget(btn_row.count() - 1, self.btn_reset_grid)`; `self.btn_reset_grid.clicked.connect(self._on_reset_grid)`.
    2. `_on_reset_grid(self)`: `self._guard(self._reset_grid_now)` (no box in the wrapper — the reset is non-destructive).
    3. `_reset_grid_now(self)`: lazy `from pymol import cmd` + `from . import wizard as wizard_mod`; `prior = cmd.get_wizard()`; `if not isinstance(prior, wizard_mod.GameWizard): return None` (silent no-op pre-GO/no-game — the AAs are ALREADY at grid poses in a fresh game; D8 no countdown-state check needed); `prior.reset_grid()` (returns None; house refusals land on the PANEL via the wizard's own _guard); `self._refresh_status()` (the game_reset marker flows -> the pinned 'Amino acids reset to grid positions (orientations kept).' line — the poll CANNOT emit it: no pose keys in _state_dict, 'result' never fingerprinted); return True.
    4. What the handler must NOT do (docstring): touch engine._game/_payload, reach wizard privates (_color_store/_result are instance-internal), add pose data to the poll.
  </action>
  <verify>py_compile + WSL suite green; grep `grep -n "btn_reset_grid\|reset_grid()" aamatch/game_window.py` hits the dispatch (and does NOT hit engine.reset_to_grid from the tab).</verify>
  <done>Reset dispatches through the wizard's public op with the marker-driven line; the tab never touches the engine's reset directly.</done>
</task>

<task type="auto">
  <name>Task 3: SMOKE-16 PART B (restart/reset drive)</name>
  <files>smoke/smoke_16_tab.py</files>
  <action>
    Append PART B (T1b, impl-driven, ZERO modals):
    1. Live game via the countdown drive (PART A shape); move an AA (`wiz.move_to` after a scripted select) + `wiz.confirm_molecule()` (a score exists) + stamp the generation marker band on the `_aam_*` instances (the SMOKE-08 :146-162 pattern — instance identity, NEVER name sets).
    2. `dlg.game_tab._restart_now()` -> assert: pending wizard armed (`_pending_wizard` set) and NOT on the stack; info box == ['Game restarted.', 'Get ready...'] in that order (the line logged after the clear); drive the 4 `_countdown_tick()` calls -> GO: float anchor (fresh), `engine.game_status()` == fresh zeros (molecule_scores [], skip_count 0, giveup_count 0, game_over False), marker-band survivors == 0 (old generation dead) with exactly one fresh generation (count), pose of the previously-moved slot == effective_position target (1e-6 + ulp slack).
    3. Refusal: with `gamestart._last_start = None` (fresh module state), `dlg.game_tab._restart_now()` raises ValueError with the EXACT message 'Restart: no game has been started yet.' — assert message + scene untouched + no wizard. Restore `_last_start = None` after.
    4. Reset drive: fresh game; scripted move + rotate on one AA (SMOKE-06 recipe) + record anchor + game_status to_dict; `dlg.game_tab._reset_grid_now()` -> assert: the moved AA centroid == effective_position target (float32 slack class), detect() on-grid -> 0 records (rotation-persist evidence), `get_status()['last_event']['kind'] == 'game_reset'`, the info box carries the pinned reset line, `engine.game_status()` to_dict UNCHANGED (scores/counters/anchor untouched — Q3b), anchor UNCHANGED float (Q7), selection key unchanged in get_status().
    5. Wrapper-refusal path: post-cleanup (no wizard), `_reset_grid_now()` returns None silently (H-8 contract).
    6. Restore per PART A.
  </action>
  <verify>`bash smoke/run_smoke.sh smoke/smoke_16_tab.py` -> `=== SMOKE-16 PASS ===` (A+B).</verify>
  <done>Restart (replay + refusal + line placement + fresh state) and Reset (mechanism + invariants + marker) proven headlessly through the tab impls.</done>
</task>

</tasks>

<verification>
- `python3.6 -m py_compile aamatch/*.py` && `python3.6 -m unittest discover -s tests -v` ALL green.
- `bash smoke/run_smoke.sh smoke/smoke_16_tab.py` -> PASS (A+B); SMOKE-08 regression re-run PASS (the replay path exercises start_game again).
- PROSE_PIN untouched.
</verification>

<success_criteria>
- SCORE-09: Restart = verbatim _last_start replay -> fresh game (fresh GameState, fresh timer, instance-identity proven).
- SCORE-10: Reset = wizard-owned spec replay via the tab, marker-driven line, timer/GameState untouched.
- SMOKE-16 A+B green.
</success_criteria>

<output>
After completion, create `.planning/phases/06-scoring-lifecycle-endgame/06-08-SUMMARY.md` (frontmatter: subsystem ui, provides the restart/reset handler contracts for 06-09/06-10).
</output>
