---
phase: 02-headless-game-engine
plan: 11
type: execute
wave: 6
depends_on: ["02-07"]
files_modified:
  - tests/test_detector_invariance.py
autonomous: true

must_haves:
  truths:
    - "Detector results are invariant under >= 100 random rigid transforms of the whole scene (rotation + translation) — no camera/origin dependence"
    - "Results are invariant under atom-list permutation and bit-identical across repeated runs (canonical ordering + fixed iteration)"
    - "Every type has boundary sensitivity controls: 0.2 A beyond cutoff or past the angle window kills the interaction, inside restores it"
    - "A synthetic worst-case scene (81 AAs + 200-atom ligand, all crammed within cutoff) detects in < 2.0 s under python3.6 — the loose O(N^2) regression guard"
  artifacts:
    - path: "tests/test_detector_invariance.py"
      provides: "Transform/permutation/determinism property tests + per-type sensitivity controls + WSL perf guard"
      min_lines: 150
  key_links:
    - from: "tests/test_detector_invariance.py"
      to: "aamatch/detector.py"
      via: "detect() called on transformed/permuted scenes"
      pattern: "detect"
---

<objective>
Add the detector's property-test battery (detection research §6): rigid-transform invariance, permutation invariance, determinism, per-type boundary sensitivity, and the loose WSL perf guard.

Purpose: Rotation/translation invariance holds by construction only if proven; the sensitivity controls double as executable documentation of every criterion boundary; the perf guard catches accidental O(N^2) cheaply in WSL (the real budget is asserted headlessly in plan 02-15).
Output: tests/test_detector_invariance.py, all green.
</objective>

<execution_context>
@~/.config/opencode/get-shit-done/workflows/execute-plan.md
@~/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/02-headless-game-engine/02-RESEARCH-detection.md   # §6 tests 1-5, §8.4 WSL perf guard, §9 pitfalls 7/10
@aamatch/detector.py                                                  # 7-type surface under test
@aamatch/capability.py                                                # residue tables for building scripted AAs
</context>

<tasks>

<task type="auto">
  <name>Task 1: Invariance property tests (transform / permutation / determinism)</name>
  <files>tests/test_detector_invariance.py</files>
  <action>
Build a canonical scripted scene helper in the test file: ligand (benzene ring + amide donor/acceptor + ammonium N + C-Cl, ~15 atoms with bonds) + 3 AAs (PHE stacked for pi_stacking, SER H-bonded, LYS salt-bridge) forming >= 3 interaction types. Then:
1. Rigid-transform invariance: for 100 seeds, random rotation (axis-angle via a local pure rotation-matrix helper; normalize the axis; random angle in [0, 2pi)) + random translation applied to EVERY atom of the scene -> detect() -> assert the result list equals the untransformed run: same (type, aa.object, aa.atom_ids, lig.atom_ids) records, metrics equal within 1e-9. NO numpy — the rotation helper is plain list math in the test.
2. Permutation invariance: shuffle the atom-record list order (seeded shuffle) -> identical canonical results.
3. Determinism: two consecutive detect() calls on the same input -> identical lists (==, not just set equality).
Keep runtime sane: 100 transform runs of a ~50-atom scene is fine on 3.6; if slow, drop to 100 seeds with 30-atom scene but NEVER below 100 seeds for the invariance property (research §6.1).
  </action>
  <verify>python3.6 -m unittest tests.test_detector_invariance -v</verify>
  <done>100-seed transform invariance + permutation + determinism all green</done>
</task>

<task type="auto">
  <name>Task 2: Sensitivity controls + WSL loose perf guard</name>
  <files>tests/test_detector_invariance.py</files>
  <action>
1. Sensitivity controls per type (7 parametrized cases): start each type at a FORMING geometry (from the 02-06/02-07 test scenes), then produce a NON-forming variant by moving 0.2 A beyond the distance cutoff or rotating past the angle window, and assert formed-set changes exactly by that type's record disappearing; and the reverse (0.2 A inside restores it). Cover all 7 types (h_bond, salt_bridge, pi_stacking, cation_pi, hydrophobic, halogen, metal — metal's variant toggles the ligand's metal presence per the gating rule).
2. WSL loose perf guard (detection research §8.4): synthetic worst case — 81 AAs (simple resn mix from the table, ~12 atoms each) + a 200-atom ligand, ALL positions within the max cutoff of each other (crammed sphere) -> time detect() with time.time(); assert wall < 2.0 s. Docstring: this is a REGRESSION guard against accidental O(N^2), NOT the budget (the < 100 ms budget is asserted only in the headless smoke, plan 02-15; WSL 3.6 is slower and CI-noisy — never assert sub-100 ms here).
  </action>
  <verify>python3.6 -m unittest discover -s tests -v  (full suite green including the new battery)</verify>
  <done>7/7 types have sensitivity controls; worst-case WSL detect < 2 s; full suite green</done>
</task>

</tasks>

<verification>
python3.6 -m py_compile aamatch/*.py && python3.6 -m unittest discover -s tests -v
</verification>

<success_criteria>
Invariance (100 seeds), permutation, determinism, 7-type sensitivity, and the loose perf guard are permanent suite members — the correctness half of DETECT-05 is closed in WSL.
</success_criteria>

<output>
After completion, create `.planning/phases/02-headless-game-engine/02-11-SUMMARY.md`
</output>
