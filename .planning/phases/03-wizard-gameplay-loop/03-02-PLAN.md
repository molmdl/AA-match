---
phase: 03-wizard-gameplay-loop
plan: 02
type: tdd
wave: 2
depends_on: ["03-01"]
files_modified:
  - aamatch/wizard_text.py
  - tests/test_wizard_text.py
  - tests/test_purity.py
autonomous: true

must_haves:
  truths:
    - "Every panel entry the builder produces is a 3-element [kind, text, code] list with kind in {0,1,2} and text <= 255 ASCII chars (the C parser renders 2-element entries BLANK — Wizard.cpp:239-246)"
    - "Button codes reference only cmd.get_wizard().method() or cmd.set_wizard() — no module name appears anywhere (module-identity trap dodged by construction)"
    - "Detection results render as text lines only: score fraction, formed types, missing types — never geometry descriptors (PLAY-04 no-helper-visuals)"
    - "Required interactions render as 'pi_stacking x1' style summaries for list mode and 'any interaction' for any mode"
  artifacts:
    - path: "aamatch/wizard_text.py"
      provides: "required_summary, panel_entries, prompt_lines, result_lines (pure text builders over plain-data state)"
      min_lines: 100
    - path: "tests/test_wizard_text.py"
      provides: "RED-first battery: panel contract (3-element, kinds, 255-char cap, ASCII, cmd.get_wizard codes), prompt composition, result rendering"
      min_lines: 120
  key_links:
    - from: "aamatch/wizard_text.py"
      to: "aamatch/wizard_core.py"
      via: "movement constants imported from the single home (NUDGE_STEP, ROTATE_BUTTON_STEP_DEG)"
      pattern: "from \\.wizard_core import|wizard_core\\."
    - from: "aamatch/wizard_text.py"
      to: "engine.confirm payload"
      via: "result_lines consumes (score, formed_types, required) — the plain data engine.confirm returns"
      pattern: "required\\['items'\\]|mode"
---

<objective>
Create `aamatch/wizard_text.py` — the PURE text half of the Phase-3 wizard: panel entry lists, prompt lines, and detection-result rendering, all as pure functions of plain-data wizard state — via a RED→GREEN→REGISTER TDD cycle.

Purpose: the wizard panel is the game's entire pre-Qt UI (PLAY-03: "result visible in the wizard panel"). Its 3-element entry contract, 255-char cap, module-identity-safe button codes, and no-helper-visuals text-only results are all mechanical properties that must be pinned by tests before the cmd-tier wizard (03-03) wires them.
Output: aamatch/wizard_text.py (PURE_MODULES gated) + tests/test_wizard_text.py green.
</objective>

<execution_context>
@~/.config/opencode/get-shit-done/workflows/execute-plan.md
@~/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@AGENTS.md                                        # standing gates — binding
@.planning/STATE.md                               # Phase-2 decisions (SCORE-01 semantics; INTERACTION_TYPES canonical order)
@.planning/phases/03-wizard-gameplay-loop/03-RESEARCH-wizard-interaction.md   # §3 panel mechanics (kinds, 255 cap, PParse-at-click), §3.3 result rendering design
@aamatch/wizard_core.py                           # movement constants (created by 03-01)
@aamatch/game_state.py                            # SCORE-01: 'any' binary / 'list' fraction; formed types canonical order
@aamatch/setup_state.py                           # INTERACTION_TYPES canonical order for rendering
@tests/test_purity.py                             # PURE_MODULES mechanics
</context>

<tasks>

<task type="auto">
  <name>Task 1: RED — failing test battery for the wizard text builders</name>
  <files>tests/test_wizard_text.py</files>
  <action>
Create tests/test_wizard_text.py (unittest, python3.6-safe, imports aamatch.wizard_text at top — ImportError is the RED proof; no stubs). State dicts are PLAIN DATA shaped exactly like what 03-03's GameWizard will hold: {'molecule_id': 'mol-001', 'molecule_pos': 1, 'molecule_total': 2, 'selected': {'slot_id': 'r0c2', 'object': '_aam_aa003'} | None, 'result': {'score': 1.0, 'formed': ['pi_stacking'], 'required': {...}} | None, 'error': None | str, 'required': {...}}.

Test cases:

1. `required_summary(required)`:
   - {'mode': 'list', 'items': [{'type': 'pi_stacking', 'count': 1}]} → 'pi_stacking x1'
   - two items → 'h_bond x1, pi_stacking x2' (items rendered in the order GIVEN — the payload already carries canonical order)
   - {'mode': 'any', 'items': []} → 'any interaction'
   - unknown mode → ValueError; list mode with missing/empty items → ValueError (mirrors game_state.score's fail-closed contract)
2. `panel_entries(state)` — THE panel contract battery:
   - Every entry is a list of EXACTLY 3 elements; kind (entry[0]) in {0, 1, 2}; entry[1] is a str.
   - ASCII-only text (each entry[1].encode('ascii') succeeds — no degree signs, use 'deg').
   - No text exceeds 255 chars (a state with a pathologically long error string is clipped, not truncated to a crash).
   - Contains exactly one 'Done' button whose code == 'cmd.set_wizard()' (the canonical exit primitive).
   - Every OTHER button code matches r'^cmd\.get_wizard\(\)\.[a-z_]+\(' — no module name, ever (module-identity trap; panel commands are PParse'd at click time against the live top-of-stack instance).
   - Button inventory (exact set, order flexible): Left/Right/Up/Down/In/Out nudges → codes cmd.get_wizard().nudge_cam(±1/0, ...); 'Toward ligand' → step_to_ligand(); 'Rotate 90 deg' → rotate_view(90); 'Confirm' → confirm_molecule(); 'Reset to Grid' → reset_grid().
   - Header line contains the molecule_id and position 'mol-001' and '(1/2)'; a required line contains required_summary(required).
   - When state['result'] is set, panel contains the result lines; when None, it contains none of them.
   - Buttons/labels appear exactly once each (no duplicated Confirm).
3. `prompt_lines(state)`:
   - No selection, no result, no error → starts with the click instruction line ('Click an amino acid to select it.'-shaped; assert substring 'Click an amino acid').
   - Selected → contains 'Move/rotate' guidance and the slot label with slot_id AND object name ('r0c2' and '_aam_aa003' substrings).
   - result present → the result lines are included verbatim as prompt lines.
   - error set → an 'ERROR'-prefixed line appears LAST (errors appended after status, measurement.py:262-263 shape).
   - Returns a list of plain str (never None) — get_prompt returns list-of-strings.
4. `result_lines(score, formed, required)`:
   - list mode, 1 of 2 formed → one line contains '1/2' AND the score '0.50'; a 'Formed:' line naming pi_stacking; a 'Missing:' line naming the absent type. Numbers only — no coordinates, no atom names beyond type names, no geometry words (assert none of ('x:', 'y:', 'z:', 'Angstrom', 'A ') appear — PLAY-04 text-only results).
   - all formed → no 'Missing:' line; score '1.00'.
   - none formed (score 0.0) → 'Formed:' line present with '(none)' or the line omitted — pick one shape and PIN it in the test.
   - 'any' mode, formed non-empty → 'Formed:' line lists types; no 'Missing:' line; any mode with no records → a 'nothing formed (score 0.00)'-shaped line (pin the exact shape).
   - score renders with exactly 2 decimals.
5. 255-char clip: `_clip`-style behavior — a 400-char error string surfaces clipped (len <= 255) in prompt_lines AND panel_entries.
Run: `python3.6 -m unittest tests.test_wizard_text -v` — MUST FAIL (ImportError). Commit: `test(03-02): add failing tests for wizard text builders`.
  </action>
  <verify>python3.6 -m unittest tests.test_wizard_text -v 2>&1 | tail -5  (must show failure)</verify>
  <done>RED battery committed covering the full panel contract, prompt composition, result rendering, and the 255-char cap</done>
</task>

<task type="auto">
  <name>Task 2: GREEN — implement aamatch/wizard_text.py + register in PURE_MODULES</name>
  <files>aamatch/wizard_text.py, tests/test_purity.py</files>
  <action>
Implement aamatch/wizard_text.py to make the battery green — PURE module (module-level imports: `from .wizard_core import NUDGE_STEP, ROTATE_BUTTON_STEP_DEG` ONLY — the single home for movement constants, never re-transcribed; no pymol/Qt anywhere; Gate A scans every scope). House style: % formatting, fail-closed ValueError naming the cause, module docstring stating layer PURE + the binding facts it encodes:

- Panel entries are ALWAYS 3-element lists [kind, text, code]; kinds 0=blank/1=text/2=button (C parser requires PyList_Size > 2 — 2-element entries render BLANK; Wizard.cpp:239-246). Text cap 255 chars/line (WordType[256]) — clip, never crash. Button codes are PParse'd at CLICK time: `cmd.get_wizard().method()` resolves the live top-of-stack instance so commands dodge the aamatch vs pmg_tk.startup.aamatch module-identity trap (AGENTS.md gate 5); Done is the canonical `cmd.set_wizard()`.
- ASCII-only panel/prompt text (PyMOL overlay font safety; 'deg' not '°').
- Result rendering is TEXT ONLY (PLAY-04): score fraction + formed/missing type names — never geometry, never coordinates. SCORE-01 semantics mirror game_state.score: 'any' = binary (formed = all produced types or nothing), 'list' = formed items / items.
- Movement labels embed NUDGE_STEP from wizard_core (e.g. the movement header mentions '1 A per press' derived from the constant — %g formatting; if NUDGE_STEP changes, text follows).

Functions: `required_summary(required)`, `panel_entries(state)`, `prompt_lines(state)`, `result_lines(score, formed, required)`, plus a private `_clip(text, limit=255)`. Build panel in the documented order (header, required, blank, result lines, blank, movement header, 6 nudges + Toward ligand + Rotate 90 deg, blank, Confirm, Reset to Grid, Done). Keep builders dumb: they never import cmd, never look at PyMOL state — plain-data in, plain-data out (the GameWizard assembles the state dict).

Then register: tests/test_purity.py PURE_MODULES += 'wizard_core', 'wizard_text' style edit (add 'wizard_text' next to the 03-01 comment, one line comment "# Phase 3 (03-02): wizard_text (pure panel/prompt/result text builders)"). REFACTOR only if obvious; tests stay green.

Commits: `feat(03-02): implement pure wizard text builders` then `test(03-02): register wizard_text in PURE_MODULES`.
  </action>
  <verify>python3.6 -m unittest tests.test_wizard_text -v && python3.6 -m py_compile aamatch/wizard_text.py && python3.6 -m unittest discover -s tests -v 2>&1 | tail -3</verify>
  <done>Full WSL suite green; wizard_text gated as PURE; panel contract mechanically pinned</done>
</task>

</tasks>

<verification>
python3.6 -m py_compile aamatch/*.py && python3.6 -m unittest discover -s tests -v 2>&1 | tail -3
</verification>

<success_criteria>
- aamatch/wizard_text.py is a PURE module (imports only .wizard_core + nothing else), registered in PURE_MODULES.
- Panel contract (3-element entries, kinds, 255-cap, ASCII, cmd.get_wizard codes, canonical Done) is enforced by tests.
- Result rendering is text-only with SCORE-01 semantics, pinned by tests.
</success_criteria>

<output>
After completion, create `.planning/phases/03-wizard-gameplay-loop/03-02-SUMMARY.md`
</output>
