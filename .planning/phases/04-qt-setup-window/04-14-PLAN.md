---
phase: 04-qt-setup-window
plan: 14
type: execute
wave: 10
depends_on: ["04-13"]
files_modified: []
autonomous: false

must_haves:
  truths:
    - "Plugins -> AA-match opens the setup window and the 3D viewer stays fully interactive while it is open (rotate/zoom the scene with the mouse); the window survives minimize and re-opens as the SAME window (raised, not duplicated) (SETUP-01, ROADMAP criterion 1)"
    - "All form fields work by hand: demo dropdown, upload Browse, molecules/difficulty spinboxes, 3-way mode with context label, 7 checkboxes; Reset restores defaults; Randomize produces a valid configuration; Save Setup / Load Setup round-trip a real file (SETUP-02..07, ROADMAP criteria 2-3)"
    - "Tooltips are present and helpful; no helper visuals ever appear; the window never blocks the viewer"
  artifacts:
    - path: ".planning/phases/04-qt-setup-window/04-14-SUMMARY.md"
      provides: "the recorded human verdict table for SETUP-01..07 (ROADMAP criteria 1-3) with pass/fail + notes"
  key_links:
    - from: "human operator"
      to: "PyMOL Plugins menu"
      via: "real GUI session on Windows PyMOL 2.5.0 (the only tier that can prove modeless interactivity)"
      pattern: "SETUP-01"
---

<objective>
[HUMAN] consolidated GUI checkpoint for ROADMAP Phase-4 criteria 1-3 (SETUP-01..07): window modelessness, form configuration, and setup-file round-trip. One sitting, one PyMOL session, numbered steps with expected outcomes. Automation-first: everything machine-provable was already proven (T0/T1a/T1b); this checkpoint covers only what a human must feel and see.

Purpose: Qt/GUI behaviors are human-verify checkpoints BY DESIGN (PITFALL 1); ROADMAP tags all four Phase-4 criteria [HUMAN].
Output: recorded verdicts; blocking gate before checkpoint B (04-15).
</objective>

<execution_context>
@~/.config/opencode/get-shit-done/workflows/execute-plan.md
@~/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/04-qt-setup-window/04-RESEARCH-qt-window-architecture.md
@.planning/phases/04-qt-setup-window/04-13-SUMMARY.md
@.planning/STATE.md
@.planning/ROADMAP.md
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>
The complete Phase-4 setup window minus the game-flow buttons: modeless window lifecycle (04-05), full 7-field form with collect/apply (04-07), Reset/Randomize/Save/Load (04-09), upload ingestion widgets (04-10). Start/Generate/Cleanup are verified in checkpoint B.
  </what-built>
  <how-to-verify>
Prerequisites: Windows PyMOL 2.5.0 via the recorded plugin-path method (01-09: repo root on the PyMOL plugin path -- repo edits live; never ALSO copy-install). Launch PyMOL from the setenv.bat environment.

1. SETUP-01 (modeless open): Plugins -> AA-match. EXPECT: the 'AA-match Setup' window opens; the 3D viewer stays fully interactive -- rotate/zoom the default scene with the mouse WHILE the window is open. The window must not block anything.
2. SETUP-01 (survives minimize/re-open): minimize the window, then Plugins -> AA-match again. EXPECT: the SAME window returns raised to front (not a second window).
3. SETUP-01 (double menu fire): click Plugins -> AA-match twice quickly. EXPECT: one window, raised -- never two instances.
4. SETUP-02 (dropdown): EXPECT the dropdown lists 'Phase-2 development set' (title; tier suffix optional). No crash, no console error.
5. SETUP-04/05 (spinboxes): set molecules-per-level to 4 and difficulty to 2 by typing 10 then spinning down (tests the 1..10 bounds). EXPECT: values clamp at the edges (1..10), never silently change after the fact.
6. SETUP-06 (mode): select 'Block-exclusive (checked set)'. EXPECT the context label changes to the EXACT-checked-set wording. Check 'Hydrogen bond' + 'Pi-stacking'. Switch to 'Unset (random)' -- EXPECT the checkboxes stay ENABLED and the label changes to the random wording (the checked set is the sampling vocabulary).
7. SETUP-07 (Reset): click Reset. EXPECT: molecules 2, difficulty 3, mode Unset, all checkboxes cleared, demo page active.
8. SETUP-07 (Randomize): click Randomize 3 times. EXPECT: each press changes mode/allowed/numbers but the demo dropdown selection STAYS; the configuration always looks valid (mode radio + checkboxes consistent).
9. SETUP-07 (Save/Load): set molecules=4, difficulty=2, mode=Block-exclusive, check h_bond; Save Setup -> choose a location (try omitting the extension). EXPECT a '.aam.setup.json' file lands (extension auto-appended). Change the form (Reset), then Load Setup from that file. EXPECT the form returns exactly to the saved values. Console clean throughout.
10. Upload widget: switch to 'Upload my molecules...' and click Browse -- EXPECT a native file dialog filtered to SDF/MOL2; Cancel it (full ingestion is checkpoint B step 5).
11. Tooltips: hover 5+ widgets. EXPECT clear one-line explanations (mode radios, spinboxes, checkboxes).
12. Console: throughout, the PyMOL console shows no tracebacks and no stray prints.
Record a PASS/FAIL + notes per step. Any FAIL is a fix-batch; re-run the affected steps after fixes.
  </how-to-verify>
  <resume-signal>Type "approved" (all steps PASS) or list the failing step numbers + what you saw.</resume-signal>
</task>

</tasks>

<verification>
All ROADMAP Phase-4 criteria 1-3 verified by a human on real PyMOL: modeless interactivity + minimize/re-open (SETUP-01), form configuration incl. dropdown/upload-widget/spinboxes/mode (SETUP-02..06), Reset/Randomize/Save/Load round-trip (SETUP-07).
</verification>

<success_criteria>
Human verdict recorded as APPROVED for criteria 1-3 (or a fix-batch list captured and later re-approved). The verdicts land in 04-14-SUMMARY.md and STATE.md is updated by the workflow.
</success_criteria>

<output>
After completion, create `.planning/phases/04-qt-setup-window/04-14-SUMMARY.md` with the per-step verdict table (SETUP-01..07 mapped), the PyMOL/env used, and any fix-batch items.
</output>
