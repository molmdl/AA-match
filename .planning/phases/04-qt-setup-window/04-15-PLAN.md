---
phase: 04-qt-setup-window
plan: 15
type: execute
wave: 10
depends_on: ["04-14"]
files_modified: []
autonomous: false

must_haves:
  truths:
    - "Generate and export writes a shareable .aamatch.json game file with the seed shown in the success message and the viewer scene untouched (SETUP-08)"
    - "Cleanup removes only game-generated objects (a user object placed before Start survives) and restores the original scene, including mid-game with a live wizard (SETUP-09)"
    - "Start generates the configured game and drops the player into playable wizard gameplay (click AA, move, confirm scores) while the setup window stays open and the viewer interactive (SETUP-10)"
    - "Upload end-to-end by hand: one SDF/MOL2 file ingests, reflects in the form, and Start builds a game from it (SETUP-03 full path)"
    - "The two cosmetic file extensions are human-confirmed and recorded: '.aam.setup.json' (setup files) and '.aamatch.json' (game exports) -- the bundled one-line confirm (planning directive)"
  artifacts:
    - path: ".planning/phases/04-qt-setup-window/04-15-SUMMARY.md"
      provides: "the recorded human verdict table for SETUP-08/09/10 + upload E2E + extension confirms (ROADMAP criterion 4)"
  key_links:
    - from: "human operator"
      to: "gamestart.start_game via the Start button"
      via: "real GUI gameplay loop (the only tier that can prove 'drops the player into playable gameplay')"
      pattern: "SETUP-10"
---

<objective>
[HUMAN] consolidated GUI checkpoint for ROADMAP Phase-4 criterion 4 (SETUP-08/09/10) plus the upload end-to-end path and the two cosmetic extension confirms. One sitting, one PyMOL session; the final gate of Phase 4.

Purpose: game-flow behavior (export/cleanup/start) is the last machine-unprovable tier; the upload seam chain (04-04 -> 04-06 -> 04-08 -> 04-10) lands its [HUMAN] deliverable here per the sequencing requirement.
Output: recorded verdicts; Phase 4 ready for the phase verifier.
</objective>

<execution_context>
@~/.config/opencode/get-shit-done/workflows/execute-plan.md
@~/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/04-qt-setup-window/04-RESEARCH-export-upload.md
@.planning/phases/04-qt-setup-window/04-14-SUMMARY.md
@.planning/STATE.md
@.planning/ROADMAP.md
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>
The complete 7-button game flow: Generate and export (04-12), Cleanup model (04-11), Start (04-13), and the upload ingestion path (04-08/04-10) -- all proven headlessly beneath the GUI tier; this checkpoint proves the human-facing behavior.
  </what-built>
  <how-to-verify>
Same session prerequisites as checkpoint A (Windows PyMOL 2.5.0, plugin-path install).

1. SETUP-08 (generate + export, demo): with the window open and defaults, note the current objects (e.g. type `get_names` mindset: load a dummy object first -- Objects menu or `cmd` line: create a small user object, e.g. fetch nothing; simplest: draw a quick indeterminate object via Built-in 'Objects' panel OR skip if awkward). Click 'Generate and export' -> save as 'mygame' in a folder. EXPECT: a file 'mygame.aamatch.json' lands; the success message shows a SEED number and level/molecule counts; the 3D viewer is UNCHANGED (no new objects, no wizard opened). Open the JSON in a text editor: EXPECT kind 'game', header version 1, an embedded level_spec with detector_version 'det-1'.
2. SETUP-09 (cleanup, no game): click 'Cleanup model'. EXPECT: message 'Removed 0 game object(s)...' (nothing to clean), user objects untouched.
3. SETUP-10 (start + gameplay): click 'Start'. EXPECT: the wizard game starts in the viewer (AA grid + molecule, zoomed and composed) while the setup window REMAINS OPEN and the viewer stays interactive. Play the loop by hand: click an amino acid (color change), move it (arrows / rotate keys), press Confirm. EXPECT a scored result in the wizard panel. (This is the Phase-3 proven loop reached through the window.)
4. SETUP-09 (cleanup mid-game): with the wizard still active, click 'Cleanup model'. EXPECT: the game objects vanish, the GameWizard is popped (picking returns to normal), the message reports the deleted count, and any prior user object is still there. Console clean.
5. SETUP-03 (upload E2E): in the setup window choose 'Upload my molecules...', Browse -> pick the bundled 'aamatch/data/ligands/benzamide.sdf'. EXPECT: the path label shows the file with '(1 molecule record(s))'; set molecules=1, difficulty=1; click 'Start'. EXPECT a game builds from the uploaded molecule (the ligand looks like benzamide) and gameplay works. Then Cleanup.
6. SETUP-10 + Decision 4 (start-after-generate): Generate and export again with defaults (note the seed in the message), then immediately click 'Start'. EXPECT the status print shows the SAME seed as the export (the game you play IS the game you shared).
7. Cosmetic confirm (one line each): (a) setup files use '.aam.setup.json' -- approved? (b) game exports use '.aamatch.json' with default name 'game.aamatch.json' -- approved? These two confirmations close Decisions 2/3.
8. Console + robustness: throughout, no tracebacks; clicking Start twice mid-game restarts cleanly (Phase-3 restart hygiene through the window); the setup window never freezes the viewer.
Record PASS/FAIL + notes per step. Any FAIL is a fix-batch; re-run affected steps after fixes.
  </how-to-verify>
  <resume-signal>Type "approved" (all steps PASS, including both extension confirms) or list failing step numbers + what you saw.</resume-signal>
</task>

</tasks>

<verification>
ROADMAP Phase-4 criterion 4 verified by a human: export writes the shareable versioned game file (SETUP-08), Cleanup removes only game objects and restores the scene incl. mid-game (SETUP-09), Start drops into playable wizard gameplay (SETUP-10), upload works end-to-end (SETUP-03), and the cosmetic extensions are confirmed.
</verification>

<success_criteria>
Human verdict APPROVED for criterion 4 + upload E2E + both extension confirms. Phase 4's four ROADMAP criteria then have complete verdict coverage (A: 1-3; B: 4) and the phase verifier can run.
</success_criteria>

<output>
After completion, create `.planning/phases/04-qt-setup-window/04-15-SUMMARY.md` with the per-step verdict table (SETUP-03/08/09/10 + confirms), the PyMOL/env used, and any fix-batch items.
</output>
