---
phase: 05-game-status-tab-start-sequence
plan: 04
type: tdd
wave: 1
depends_on: []
files_modified: [aamatch/game_state.py, tests/test_game_state.py]
autonomous: true

must_haves:
  truths:
    - "GameState.rebase_timer(now, elapsed) re-anchors the ONE timer so the shown elapsed FREEZES at `elapsed`: timer_anchor becomes float(now) - float(elapsed) — the 1 Hz tick's pause mechanism rebases the single anchor, never a GUI-side clock copy (05-RESEARCH-window-start-timer.md Pattern 3 + pitfall P-4)"
    - "Fail-closed: a negative elapsed raises ValueError naming the cause; float coercion on both args keeps the anchor a real float"
  artifacts:
    - path: "aamatch/game_state.py"
      provides: "rebase_timer(now, elapsed) pure method beside start_timer (game_state.py:187-191); game_state stays PURE (time already whitelisted)"
      contains: "rebase_timer"
    - path: "tests/test_game_state.py"
      provides: "the rebase battery: anchor math, float coercion, negative-elapsed refusal, round-trip neutrality"
  key_links:
    - from: "aamatch/game_window.py _on_tick (plan 05-06)"
      to: "aamatch/game_state.GameState.rebase_timer"
      via: "the modal-open pause branch calls the named pure op"
      pattern: "rebase_timer"
---

<objective>
The named pure op behind the timer-fairness pause: when any modal child is open, the 1 Hz tick rebases `GameState.timer_anchor` so the elapsed clock freezes at the last shown second instead of silently advancing under the dialog.

Purpose: A routed the pause through a named op (05-RESEARCH-window-start-timer.md Pattern 3 + module-placement table "rebase_timer(now, elapsed) — pure op ... no PURE_MODULES change; time already whitelisted") so the freeze semantics are WSL-pinnable and the Qt tick stays a dumb caller. This kills the v1 bug class (P-4: two clocks drifted — v1 kept a GUI-side copy and needed a patch, PA-gui_game:279-284): ONE anchor, mutated in place.
Output: `GameState.rebase_timer` + its battery.
</objective>

<execution_context>
@~/.config/opencode/get-shit-done/workflows/execute-plan.md
@~/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/05-game-status-tab-start-sequence/05-RESEARCH-window-start-timer.md
@.planning/STATE.md
@aamatch/game_state.py
@tests/test_game_state.py

Key facts (verified, transcribe exactly):
- GameState.timer_anchor (game_state.py:174, currently None until start_timer) + start_timer(now) (game_state.py:187-191: stores float(time.time() if now is None else now)) — start_timer is NEVER called in production yet (grep-verified; plan 05-05 wires it at GO). to_dict/from_dict already carry timer_anchor losslessly (game_state.py:225, 240).
- The consumer shape (plan 05-06's tick): `gs.rebase_timer(time.time(), self._last_shown_elapsed)` — after the call, time.time() - timer_anchor == the frozen elapsed (<= 1 s over-count at the modal-open edge, documented).
- Purity: game_state.py is a PURE_MODULES member importing `time` (already in ALLOWED_STDLIB) — rebase_timer needs NO new imports.
- python3.6 floor; fail-closed house style (compare game_state.score's ValueError family, game_state.py:77-85).
</context>

<feature>
  <name>GameState.rebase_timer — the pause-freeze anchor op</name>
  <files>aamatch/game_state.py, tests/test_game_state.py</files>
  <behavior>
All cases expect-before-fn (python3.6, unittest, zero stubs):
- gs = GameState(); gs.start_timer(1000.0); gs.rebase_timer(1100.0, 42.0) -> gs.timer_anchor == 1058.0 (float(now) - float(elapsed)); re-derived elapsed at t=1100.0 is exactly 42.0.
- float coercion: rebase_timer('1100', 42) behaves as above — but the STORED anchor is a float (isinstance assert), matching start_timer's float() coercion contract.
- works from a never-started game too: gs2 = GameState(); gs2.rebase_timer(500.0, 0.0) -> timer_anchor == 500.0 (the tick only calls it after GO, but the op itself is total over valid inputs).
- negative elapsed -> ValueError naming the cause ('elapsed must be non-negative' family wording; a negative elapsed would push the anchor into the future and REWIND the clock — fail-closed, never silent).
- to_dict() after a rebase carries the rebased anchor (the lossless round-trip inherits it — one assert).
- docstring documents: the pause contract (freeze at the last shown second), the <= 1 s granularity note at the modal-open edge, and that the CALLER (the 1 Hz tick) owns modal detection via QApplication.activeModalWidget().
  </behavior>
  <implementation>
- game_state.py: add `rebase_timer(self, now, elapsed)` directly after start_timer (game_state.py:187-191). Body: coerce both to float; refuse negative elapsed with a ValueError naming the values; `self.timer_anchor = now_f - elapsed_f`. Docstring carries the pause contract + granularity note + caller-owns-modal-detection note. Zero new imports.
- tests/test_game_state.py: new TestCase class TestRebaseTimer appended (anchor math; coercion; negative refusal; to_dict inheritance).
  </implementation>
</feature>

<tasks>

<task type="tdd-red">
  <name>Task 1 (RED): the rebase battery</name>
  <files>tests/test_game_state.py</files>
  <action>Append TestRebaseTimer with the <behavior> cases. Run — MUST fail (AttributeError: rebase_timer). Commit: `test(05-04): add failing rebase_timer battery`.
  </action>
  <verify>python3.6 -m unittest tests.test_game_state -v exits nonzero naming rebase_timer (RED proven)</verify>
  <done>The battery exists and fails for the RIGHT reason</done>
</task>

<task type="tdd-green">
  <name>Task 2 (GREEN): implement rebase_timer</name>
  <files>aamatch/game_state.py</files>
  <action>Implement per <implementation> — a ~10-line method beside start_timer. Run the battery — MUST pass. Commit: `feat(05-04): implement GameState.rebase_timer`.
  </action>
  <verify>python3.6 -m unittest tests.test_game_state -v green; python3.6 -m py_compile aamatch/*.py clean</verify>
  <done>Anchor math + coercion + negative refusal pinned; game_state stays pure with zero new imports</done>
</task>

</tasks>

<verification>
- `python3.6 -m py_compile aamatch/*.py` — floor.
- `python3.6 -m unittest discover -s tests -v` — full suite green (purity gates untouched: no new imports in game_state.py).
</verification>

<success_criteria>
- rebase_timer exists as the named pure op the 1 Hz tick will call; its freeze semantics are WSL-pinned.
- timer_anchor remains the SINGLE clock home (no GUI-side copy anywhere in the design).
</success_criteria>

<output>
After completion, create `.planning/phases/05-game-status-tab-start-sequence/05-04-SUMMARY.md`
</output>
