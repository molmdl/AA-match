---
phase: 08-demo-curation-citations
plan: 11
type: execute
wave: 7
depends_on:
  - "08-08"
  - "08-09"
  - "08-10"
files_modified: []
autonomous: false
must_haves:
  truths:
    - "Full regression battery green on the final Phase-8 tree: complete WSL suite (incl. purity gates + demo data + supply tests) AND all smokes 01-21 PASS headless"
    - "Detector-coverage check run headless over the first real halogen/metal ligands: halogen records provable on chloramphenicol/thyroxine C–Cl/C–I geometry and metal records on HEM's Fe — verdict RECORDED; any threshold surprise becomes a DETECTOR_VERSION bump PROPOSAL (never a silent edit)"
    - "Human verifies in the REAL viewer: the setup dropdown lists all sets GROUPED BY TIER (Easy/Hard/Challenge/Very challenging with separators), a curated set starts and plays, Cleanup behaves"
    - "Human signs off docs/DATA_SOURCES.md (HELP-02 [HUMAN] tier) — criterion 3's human verification recorded"
  artifacts:
    - path: ".planning/phases/08-demo-curation-citations/08-11-SUMMARY.md"
      provides: "Criterion coverage map + detector-coverage verdict + regression results"
      contains: "DETECTOR"
  key_links:
    - from: "detector-coverage check"
      to: "aamatch.data ligands (chloramphenicol/thyroxine/heme)"
      via: "headless scripted scenes through engine/placement/detect — the 02-01 provisional-threshold caveat's check"
      pattern: "detect"
    - from: "08-11-SUMMARY verdict"
      to: "DETECTOR_VERSION bump policy (docs/DETECTION_THRESHOLDS.md §4.7)"
      via: "surprise → bump PROPOSAL in a new plan; no threshold edit inside this plan"
      pattern: "DETECTOR_VERSION"
---

<objective>
Close Phase 8: full regression battery (WSL + smokes 01-21), the headless detector-coverage check over the first real metal/halogen ligands with the verdict recorded, and the [HUMAN] GUI checkpoint — grouped dropdown in the real viewer + DATA_SOURCES.md sign-off.

Purpose: ROADMAP criteria 2 and 3 have [HUMAN] tiers; criterion 1's protocol and criterion 4's [HEADLESS] proof are already done — this plan verifies the whole phase holds together and records the DETECT-03 revisit verdict.
Output: regression + verdict records + human approval (SUMMARY + STATE).
</objective>

<execution_context>
@~/.config/opencode/get-shit-done/workflows/execute-plan.md
@~/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/08-demo-curation-citations/08-PROPOSALS.md
@docs/DATA_SOURCES.md
@docs/DETECTION_THRESHOLDS.md
@aamatch/data/MANIFEST.json

DETECT-03 caveat (STATE.md 02-01, binding): threshold approval was PROVISIONAL pending the Phase-8 dataset revisit; any revisit that changes a value is a DETECTOR_VERSION bump event, never a silent edit. 08-PROPOSALS decision 8 recorded the revisit as TRIGGERED-FOR-CHECK (first real metal = HEM Fe; first real halogens = chloramphenicol C–Cl, thyroxine C–I).
</context>

<tasks>

<task type="auto">
  <name>Task 1: full regression battery (WSL suite + smokes 01-21)</name>
  <files></files>
  <action>
Run the complete battery on the final tree:
1. `python3.6 -m py_compile aamatch/*.py scripts/build_demos.py` — syntax floor.
2. `python3.6 -m unittest discover -s tests -v` — ALL WSL tests (purity gates, demo-data battery, supply test, everything).
3. All smokes via `bash smoke/run_smoke.sh smoke/<script>.py` (TIMEOUT 180 for smoke_02, smoke_05, smoke_11, smoke_21 and any other big battery; default 120 otherwise): smoke_01 through smoke_21 — every script in smoke/ must print its `=== SMOKE-NN PASS ===` marker.
Record the tally (N/N smokes, M/M tests) for the SUMMARY. Any failure = Rule-1 fix in the OWNING file + re-run before proceeding (do not silently widen tolerances or skip tests).
  </action>
  <verify>python3.6 -m unittest discover -s tests -v (green) && for s in smoke/smoke_*.py; do bash smoke/run_smoke.sh $s 180 | grep -qE "=== SMOKE-[0-9]+ PASS ===" || echo "FAIL: $s"; done (no FAIL lines; per-script markers verified individually)</verify>
  <done>Full battery green; tally recorded.</done>
</task>

<task type="auto">
  <name>Task 2: detector-coverage check over real halogen/metal ligands + verdict</name>
  <files></files>
  <action>
Headless scripted check (a tmp/ script run via `bash smoke/run_smoke.sh`-style invocation pattern is NOT required — this is a one-off probe; write `tmp/detector_coverage_phase8.py` and run it headless like a smoke, printing a clear verdict line it must NOT name SMOKE-21): for each of chloramphenicol, thyroxine, and heme: load the bundled SDF via the standard path helpers into a private `_aam_tmp*` object, materialize ONE capable AA fragment near the target geometry using the SMOKE-04 scripted-placement recipe (halogen: an acceptor-capable AA atom within the halogen window of a C–X axis; metal: a chelator-capable AA carboxylate O within 3.0 Å of HEM's Fe), run `engine`-level detect (geometry.extract_game_atoms + bond remap + detector.detect), and assert the expected record types appear (halogen records for the C–Cl/C–I scenes, metal record for the Fe scene) with sane metrics. THE VERDICT RULE: if every expected record appears with sane metrics → verdict "NO threshold surprise — DETECT-03 provisional values hold on real data; no bump proposal needed". If any expected record is MISSING or metrics look pathological (e.g. halogen angle outside the approved window on an obviously-correct geometry) → verdict "DETECTOR_VERSION bump PROPOSAL required" + record the exact geometry/metric in the SUMMARY; do NOT edit thresholds.py or detector.py in this plan. Clean up temp objects (delete-in-finally) and re-assert zero leakage. Record the verdict verbatim in the SUMMARY (and STATE.md's blockers/concerns if a bump proposal is needed).
  </action>
  <verify>bash-style headless run of tmp/detector_coverage_phase8.py completes with the expected record-type asserts green (halogen ≥1 record on each halogen scene, metal ≥1 on the heme scene) and the verdict line printed; `cmd.get_names('objects')` leakage check green</verify>
  <done>Detector-coverage verdict RECORDED (expected: no surprise → no bump proposal; alternative: bump proposal documented with evidence).</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>
The complete Phase-8 deliverable: 10 bundled sets (dev + 9 curated) in a grouped dropdown, DATA_SOURCES.md, the bundling pipeline, and green headless proofs (battery + detector-coverage verdict from Tasks 1-2).
  </what-built>
  <how-to-verify>
  1. Launch PyMOL in the Windows conda env (setenv.bat flow) and install/load the plugin per the standing PLUGIN-PATH method (repo root on the plugin path — repo edits live).
  2. Plugins → AA-match → open the setup window: the demo dropdown shows all sets GROUPED BY TIER — Easy (…4 sets incl. the dev set…), then a separator, Hard, separator, Challenge, separator, Very challenging; labels read "<title> (<tier>)". Selecting each group's sets works; a stale set id (Load Setup with a bogus id) falls back to a real row with the visible note.
  3. Pick demo-easy-1, Start: the game generates and plays on the curated aspirin/benzoic-acid ligands; Confirm/advance behaves; Cleanup (after adding a stray water/object to the scene) removes ONLY game objects.
  4. Open docs/DATA_SOURCES.md and spot-check 2-3 entries against their live sources (PubChem CID page, RCSB ligand page, one doi.org link) — sign off HELP-02's [HUMAN] verification.
  5. Confirm the detector-coverage verdict from Task 2 (and any bump proposal it recorded) is acceptable as recorded.
  </how-to-verify>
  <resume-signal>Type "approved" to close Phase 8 — or describe the issues (they become gap-closure plans before phase verification).</resume-signal>
</task>

<task type="auto">
  <name>Task 3: record the checkpoint verdicts</name>
  <files></files>
  <action>
After the human responds: record the GUI-checkpoint verdict (approved / issues list) in 08-11-SUMMARY.md together with the battery tally, the detector-coverage verdict, and the criterion coverage map: c1 [GATE] = 08-04 approval record; c2 [HUMAN] = this checkpoint's grouped-dropdown verification (HEADLESS supply halves = SMOKE-02 coverage + data battery); c3 [HUMAN] = this checkpoint's DATA_SOURCES sign-off; c4 [HEADLESS] = SMOKE-02 (every-manifest-id) + SMOKE-21 (messy-scene cleanup). If issues were reported, list them as candidate gap-closure work — do not fix silently beyond Rule-1 bugs. This plan commits no code; the SUMMARY + STATE metadata are committed per the standard flow.
  </action>
  <verify>08-11-SUMMARY.md exists with the verdict sections; STATE.md Current Position updated by the standard flow</verify>
  <done>Phase-8 verification evidence assembled; human verdicts recorded.</done>
</task>

</tasks>

<verification>
- Full WSL suite green; all 21 smokes print PASS markers.
- Detector-coverage verdict recorded in 08-11-SUMMARY.md (with STATE.md concern line if a bump proposal is required).
- Human checkpoint verdicts recorded (dropdown grouping + DATA_SOURCES sign-off).
</verification>

<success_criteria>
- All four ROADMAP Phase-8 criteria have their verification evidence mapped and recorded.
- The DETECT-03 revisit question is answered with data (expected: provisional thresholds hold; otherwise a formal bump proposal exists).
- Phase 8 is ready for /gsd-verify-phase.
</success_criteria>

<output>
After completion, create `.planning/phases/08-demo-curation-citations/08-11-SUMMARY.md`
</output>
