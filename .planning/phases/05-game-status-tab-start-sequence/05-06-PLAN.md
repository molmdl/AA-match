---
phase: 05-game-status-tab-start-sequence
plan: 06
type: execute
wave: 2
depends_on: ["05-04", "05-05"]
files_modified: [aamatch/game_window.py, aamatch/setup_window.py, tests/test_wizard_source.py, smoke/smoke_11_window.py]
autonomous: true

must_haves:
  truths:
    - "The SetupWindow is a two-tab QTabWidget: the 'Setup' tab wraps the EXISTING form_area + stretch + 7-button row (every attribute and label survives — SMOKE-11 PART B stays green), and a 'Game status' tab hosts the GameTab (spec.md:33 'the Game status tab of the SAME window'; 7 buttons STAY on Setup per spec.md:21)"
    - "The GameTab ships the status-surface shell: read-only rolling info box with _log, elapsed timer label OUTSIDE the info box (initial '0:00', M:SS), required-interactions label (initial 'Required: -'), and the Hint button created but NOT connected (the hint plan connects it — Phase-4 shell law, 04-05)"
    - "The countdown is a CANCELLABLE reusable member QTimer stepping 3->2->1->GO (never a singleShot chain — v1's chain was uncancellable and leaks a stale GO after Cleanup/double-Start, P-2); every new start_countdown cancels any pending countdown first (self-healing); _on_cleanup (plan 05-09) also cancels"
    - "The 1 Hz elapsed tick reads the LIVE GameState.timer_anchor each tick (never a GUI-side copy — P-4) and renders M:SS; when QApplication.activeModalWidget() is not None it REBASES the anchor via game_state.rebase_timer so the clock freezes at the last shown second (<=1 s granularity, documented)"
    - "The countdown runs with NO wizard on the stack: start_countdown stores _pending_wizard; only _begin_play (GO) calls gamestart.activate_game, then restarts the 1 Hz timer (defensive stop + start)"
  artifacts:
    - path: "aamatch/game_window.py"
      provides: "NEW Qt-tier module: GameTab(QtWidgets.QWidget) — info box/_log, timer label + 1 Hz QTimer + _on_tick + modal-pause rebase, required label, unconnected btn_hint, countdown member QTimer + start_countdown/_countdown_tick/_begin_play + cancel_pending_start; module-level pymol.Qt imports (legal+required), lazy sibling/cmd imports inside methods"
      min_lines: 120
    - path: "aamatch/setup_window.py"
      provides: "QTabWidget restructure: setup_page wraps form_area+stretch+button row; self.tabs; self.game_tab = GameTab(self); 'Setup'/'Game status' tab titles"
      contains: "QTabWidget"
    - path: "tests/test_wizard_source.py"
      provides: "SCANNED_MODULES += 'game_window.py' (growth protocol, 03-05/04-05 precedent — both scans now cover the new Qt module)"
    - path: "smoke/smoke_11_window.py"
      provides: "new T1b PART before the Gate-A2 echo: tab asserts (count==2, titles, 7 buttons re-green, tab widgets), countdown step drive, cancel drive, timer-label drive ('1:15' via anchor manipulation)"
  key_links:
    - from: "aamatch/setup_window.py __init__"
      to: "aamatch/game_window.GameTab"
      via: "lazy `from . import game_window` inside __init__ (module-identity-safe) + self.game_tab = game_window.GameTab(self)"
      pattern: "game_window.GameTab"
    - from: "aamatch/game_window.py _on_tick"
      to: "aamatch/game_state.GameState.rebase_timer"
      via: "the modal-pause branch rebases the single anchor (plan 05-04's op)"
      pattern: "rebase_timer"
    - from: "aamatch/game_window.py _begin_play"
      to: "aamatch/gamestart.activate_game"
      via: "GO-time activation (plan 05-05's seam) — the countdown window itself is wizard-free"
      pattern: "activate_game"
---

<objective>
The window surface of Phase 5: restructure the modeless SetupWindow into a two-tab QTabWidget (Setup + Game status), create the NEW Qt-tier module `aamatch/game_window.py` holding the GameTab (info box, timer + 1 Hz tick with modal-pause, required label, cancellable countdown), and prove it all headlessly in SMOKE-11.

Purpose: this is the tab restructure + GameTab shell the three research files converge on (aligned decision #1: minimal churn — every existing SMOKE-11 PART assert survives; #2: game_window.py as the shared Qt home; #6: cancellable member QTimer; #7: single live-read anchor with the 1 Hz rebase pause). The status POLL and the hint handler land in later plans on this exact skeleton.
Output: game_window.py (new) + setup_window.py restructure + SCANNED_MODULES growth + SMOKE-11 T1b parts.
</objective>

<execution_context>
@~/.config/opencode/get-shit-done/workflows/execute-plan.md
@~/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/05-game-status-tab-start-sequence/05-RESEARCH-window-start-timer.md
@.planning/phases/05-game-status-tab-start-sequence/05-RESEARCH-status-surface.md
@.planning/phases/04-qt-setup-window/04-05-SUMMARY.md
@.planning/phases/04-qt-setup-window/04-07-SUMMARY.md
@.planning/STATE.md
@aamatch/setup_window.py
@aamatch/game_state.py
@aamatch/gamestart.py
@smoke/smoke_11_window.py
@tests/test_wizard_source.py

Key facts (verified, transcribe exactly):
- Current dialog structure (setup_window.py:137-216): top QVBoxLayout -> (a) self.form_area (QWidget + QVBoxLayout; the four form groups append via self.form_area.layout().addWidget) -> self._uploaded/_last_export/_loading slots -> _build_* calls -> (b) top.addStretch(1) -> (c) the 7-button QHBoxLayout added via top.addLayout(row). Every attribute (form_area, btn_reset..btn_start) must SURVIVE the restructure — PART B asserts attributes + labels + collect/apply, not parentage (smoke_11_window.py:189-260).
- The restructure (research Q1): create `setup_page = QtWidgets.QWidget(self)` with its own QVBoxLayout; MOVE (a)+(b)+(c) into it (addWidget re-parents automatically); `self.tabs = QtWidgets.QTabWidget(self)`; tabs.addTab(setup_page, 'Setup'); tabs.addTab(self.game_tab, 'Game status'); `top.addWidget(self.tabs)` replacing the three old top-level adds.
- Qt-tier module pattern (04-05 established): module-level `from pymol.Qt import QtWidgets, QtCore` legal+REQUIRED; never PURE_MODULES; never WSL-imported; `from PyQt5` stays banned repo-wide. Lazy relative sibling imports INSIDE methods (module-identity law, AGENTS.md gate 5).
- GameTab attribute names (pin them — the smokes assert them): _info_log (QTextEdit), _timer_label (QLabel), _required_label (QLabel), btn_hint (QPushButton), _countdown_timer, _countdown_n, _pending_wizard, _timer, _last_shown_elapsed.
- Countdown mechanics (research Q4 + Pattern 1): start_countdown(wizard): cancel_pending_start() FIRST (self-healing), store _pending_wizard, _info_log.clear(), _log('Get ready...'), _countdown_n = 3, _countdown_timer.start(1000). _countdown_tick: n > 0 -> _log('%d' % n), n -= 1; else stop + _log('GO!') + _begin_play(). _begin_play: lazy `from . import gamestart`; gamestart.activate_game(self._pending_wizard); self._pending_wizard = None; self._timer.stop(); self._timer.start(1000). (The level line + required label + _last_status content is plan 05-10's — mechanics only here.)
- cancel_pending_start: if self._countdown_timer.isActive(): self._countdown_timer.stop(); self._pending_wizard = None.
- Tick mechanics (research Pattern 3): _on_tick: if QtWidgets.QApplication.activeModalWidget() is not None -> lazy `from . import engine`; try: engine._current_game().rebase_timer(time.time(), self._last_shown_elapsed) except engine.EngineError: return; then return (no label update, no status poll — the poll plan skips during pause). Else: self._last_shown_elapsed = self._compute_elapsed(); self._timer_label.setText(self._format_mss(self._last_shown_elapsed)). _compute_elapsed: lazy engine; gs = engine._current_game(); anchor None -> 0.0; return max(0.0, time.time() - gs.timer_anchor). _format_mss: '%d:%02d' % (int(s) // 60, int(s) % 60) — v1's exact shipped format (PA-gui_game:228-232; minutes unbounded: 90 min renders '90:12').
- P-6: NO QMessageBox anywhere in _on_tick/_countdown_tick (a modal box inside a timer callback re-enters a modal loop and hangs offscreen smokes — the 04-09 smoke-99 receipt). Failures surface via handler _guard wrappers only (later plans).
- The 1 Hz timer: constructed in __init__ with timeout.connect(self._on_tick) (PA-gui_game:108-115 shape); NOT started until _begin_play.
- The countdown timer: constructed + timeout.connect(self._countdown_tick) in __init__.
- `import time` at module level (stdlib, Qt tier).
- Hint button: created with spec tooltip in __init__, NOT connected (the hint handler plan connects its own button — the 04-05 shell law verbatim).
- Gate edit: tests/test_wizard_source.py SCANNED_MODULES grows 'game_window.py' (growth protocol, tests/test_wizard_source.py:21-22,51-53) — commit WITH the module landing (registration-as-commit, 02-02 pattern).
- SMOKE-13 (smoke/smoke_13_qt_timer_probe.py) already PASSED (research A, 2026-09-18): repeating QTimer, stop() cancellation, singleShot(0), delayed singleShot, QTabWidget round-trip, activeModalWidget predicate — re-run as regression in this plan's verification.
</context>

<tasks>

<task type="auto">
  <name>Task 1: aamatch/game_window.py — the GameTab shell</name>
  <files>aamatch/game_window.py</files>
  <action>Create the NEW Qt-tier module exactly per the context facts:
- Module docstring: layer declaration (Qt tier — never PURE_MODULES, never WSL-imported; AST source gates via tests/test_wizard_source.py), the Game-status-tab role (SCORE-04/SETUP-11), and the ownership note (status poll + hint handler land in later plans).
- Module-level: `import time` + `from pymol.Qt import QtWidgets, QtCore`. NO `from PyQt5`. NO module-level pymol/cmd or sibling imports (lazy inside methods).
- class GameTab(QtWidgets.QWidget) with `def __init__(self, parent=None)`: QVBoxLayout; `self._info_log` = QTextEdit, setReadOnly(True) (the generic rolling info box, PA-gui_game:24-27); `self._timer_label` = QLabel('0:00'); `self._required_label` = QLabel('Required: -') (prior-art 'Remaining: -' label-row shape, PA-gui_game:31/39-43); `self.btn_hint` = QPushButton('Hint', self) with a spec-meaning tooltip, NOT connected; layout: a top row with timer label + required label, the info box with stretch, the Hint button row (reserve the slot with a stretch so later phases' button rows don't reflow the timer row — research OQ-1 recommendation).
- `self._countdown_timer` = QtCore.QTimer(self), timeout.connect(self._countdown_tick); `self._countdown_n = 0`; `self._pending_wizard = None`; `self._timer` = QtCore.QTimer(self), timeout.connect(self._on_tick); `self._last_shown_elapsed = 0.0`.
- Methods (exact shapes from the context facts): _log(line); start_countdown(wizard); _countdown_tick(); _begin_play(); cancel_pending_start(); _compute_elapsed(); _format_mss(seconds); _on_tick().
- Docstrings cite the pitfalls they avoid (P-1 wizard-free countdown window, P-2 cancellation, P-4 single anchor, P-6 no boxes in ticks, P-8 timers fire through nested loops).
- python3.6 floor; %-formatting; no f-strings.
Commit: `feat(05-06): GameTab shell — info box, timer tick + pause, cancellable countdown`.
  </action>
  <verify>python3.6 -m py_compile aamatch/game_window.py aamatch/*.py clean; grep -c 'from PyQt5' aamatch/game_window.py == 0; grep -c 'QMessageBox' aamatch/game_window.py == 0</verify>
  <done>The GameTab exists with the pinned attribute names, cancellable countdown, and live-anchor tick with modal-pause rebase</done>
</task>

<task type="auto">
  <name>Task 2: SetupWindow tab restructure + SCANNED_MODULES growth</name>
  <files>aamatch/setup_window.py, tests/test_wizard_source.py</files>
  <action>
1. In SetupWindow.__init__: lazy `from . import game_window` at the TOP of __init__ (module-identity-safe; no new module-level imports); build `setup_page` and MOVE the form_area add, the stretch, and the button-row add into it (the existing code lines stay, only their layout parent changes — minimal churn); create `self.tabs = QtWidgets.QTabWidget(self)`; addTab(setup_page, 'Setup'); `self.game_tab = game_window.GameTab(self)`; addTab(self.game_tab, 'Game status'); `top.addWidget(self.tabs)`. Comment cites spec.md:21 (buttons stay on Setup) + spec.md:33 (same window).
2. Update the module + class docstrings: the window is now two-tab; the Game tab hosts GameTab (game_window.py).
3. tests/test_wizard_source.py: SCANNED_MODULES += 'game_window.py' with the growth-protocol comment (03-05/04-05 precedent). Commit both together: `feat(05-06): two-tab SetupWindow + SCANNED_MODULES growth`.
  </action>
  <verify>python3.6 -m py_compile aamatch/*.py clean; python3.6 -m unittest discover -s tests -v green (the wizard-source scans now cover game_window.py: zero banned visual calls, zero banned-token mentions — write the docstrings accordingly); grep 'form_area' aamatch/setup_window.py still resolves (attribute preserved)</verify>
  <done>The window is two-tab with every pre-existing attribute intact; the new module is gate-covered</done>
</task>

<task type="auto">
  <name>Task 3: SMOKE-11 T1b parts — tabs, countdown, cancel, timer label</name>
  <files>smoke/smoke_11_window.py</files>
  <action>Insert ONE new T1b PART (probe-gated header like the other T1b parts) immediately BEFORE the Gate-A2 echo part; re-letter the echo. The part (lazy imports per the file's style):
1. Tab asserts: dlg.tabs.count() == 2; tabText(0) == 'Setup'; tabText(1) == 'Game status'; the 7 button attributes still resolve with exact labels (PART B unchanged — it re-greens); dlg.game_tab is the current GameTab instance.
2. Game-tab shell asserts: game_tab._info_log is a QTextEdit with isReadOnly() True; _timer_label.text() == '0:00'; _required_label.text() == 'Required: -'; btn_hint exists (text 'Hint').
3. Countdown drive (deterministic — drive steps AS METHODS per the research anti-flakiness strategy): `wiz = gamestart.start_game(activate=False)`; `dlg.game_tab.start_countdown(wiz)`; check: 'Get ready...' in _info_log.toPlainText(); check: _countdown_timer.isActive() and _pending_wizard is wiz; drive `_countdown_tick()` 4 times; check: the log contains '3', '2', '1', 'GO!' (each on its own line); check: cmd.get_wizard() IS wiz (GO activated) and engine._current_game().timer_anchor is a float; check: _timer.isActive() (the 1 Hz render loop started).
4. Timer-label drive (deterministic): set engine._current_game().timer_anchor = time.time() - 75; call dlg.game_tab._on_tick(); check: _timer_label.text() == '1:15'. (One real pumped ~1.1 s wait re-check is OPTIONAL — SMOKE-13 already proves the mechanism.)
5. Cancel drive: pop (cmd.set_wizard()) + cleanup; `wiz2 = gamestart.start_game(activate=False)`; start_countdown(wiz2); `dlg.game_tab.cancel_pending_start()`; check: _countdown_timer.isActive() is False and _pending_wizard is None; drive _countdown_tick() once; check: no 'GO!' appeared after the cancel and cmd.get_wizard() is NOT wiz2 (no stale activation — P-2).
6. Restore: cancel_pending_start + cmd.set_wizard() + placement.cleanup_game_objects() -> baseline scene EXACT; dlg.close(). Update the header part list. Commit: `test(05-06): SMOKE-11 tab + countdown + timer parts`.
  </action>
  <verify>bash smoke/run_smoke.sh smoke/smoke_11_window.py 120 — === SMOKE-11 PASS === with the new part green and ALL prior parts still green</verify>
  <done>Tab structure, GameTab shell, countdown cadence (as methods), cancellation, GO activation, and the M:SS label are headlessly proven</done>
</task>

</tasks>

<verification>
- `python3.6 -m py_compile aamatch/*.py` — floor.
- `python3.6 -m unittest discover -s tests -v` — full WSL suite (incl. the wizard-source scans over game_window.py).
- `bash smoke/run_smoke.sh smoke/smoke_11_window.py 120` — new parts + all existing parts green.
- `bash smoke/run_smoke.sh smoke/smoke_13_qt_timer_probe.py 120` — === SMOKE-13 PASS === (regression: the Qt timer mechanics probe stays green).
- `bash smoke/run_smoke.sh smoke/smoke_08_starter.py 120` — === SMOKE-08 PASS === (the seam's default path untouched by the window work).
</verification>

<success_criteria>
- The SetupWindow carries two tabs; the Setup page is a byte-preserving re-wrap (PART B green); the Game status page hosts the GameTab.
- game_window.py exists as the Qt-tier home with the pinned attribute names; countdown is cancellable; the tick reads the live anchor and rebases under modals.
- SCANNED_MODULES covers game_window.py; SMOKE-11 T1b parts prove the shell headlessly.
</success_criteria>

<output>
After completion, create `.planning/phases/05-game-status-tab-start-sequence/05-06-SUMMARY.md`
</output>
