---
phase: 06-scoring-lifecycle-endgame
plan: 07
type: execute
wave: 5
depends_on: ["06-02", "06-05", "06-06"]
files_modified:
  - aamatch/game_window.py
  - smoke/smoke_16_tab.py
autonomous: true
must_haves:
  truths:
    - "The Game tab has a Confirm button (spec.md:41) and a Skip/Give-Up dropdown button (spec.md:42) whose actions ask the spec-required confirmation warnings"
    - "Tab-triggered lifecycle ops log ZERO-latency: the handler runs the poll-diff synchronously right after the op, so event lines land instantly (no 1 Hz loss)"
    - "The required-interactions label refreshes on LEVEL change too — the stale-label bug (molecule_id repeats across levels) is fixed"
    - "The impls are non-modal and headlessly driveable; ONLY the wrappers own boxes (smoke-99 law)"
  artifacts:
    - path: "aamatch/game_window.py"
      provides: "btn_confirm + btn_skip_menu (QToolButton+QMenu) + _on_confirm/_on_skip/_on_giveup wrappers + _confirm_now/_skip_now/_giveup_now impls + _endgame_sequence helper + level_pos label fix + sync _refresh_status"
      contains: "btn_skip_menu"
    - path: "smoke/smoke_16_tab.py"
      provides: "SMOKE-16 PART A: T1b GameTab construction + impl-driven confirm/skip/give-up"
      contains: "SMOKE-16"
  key_links:
    - from: "game_window._confirm_now"
      to: "GameWizard.confirm_molecule()"
      via: "isinstance-gated dispatch + synchronous _refresh_status()"
      pattern: "confirm_molecule"
    - from: "game_window._on_skip/_on_giveup"
      to: "status_text.SKIP_WARNING_TITLE/TEXT (GIVEUP_*)"
      via: "QMessageBox.question Yes|No with the pinned strings; gate BEFORE any box"
      pattern: "QMessageBox.question"
    - from: "_refresh_status label condition"
      to: "level_pos"
      via: "the fix: molecule_id OR level_pos change refreshes the label"
      pattern: "level_pos"
---

<objective>
Game tab lifecycle controls, part 1 (SCORE-01/05/06 UI): the Confirm button, the Skip/Give-Up QToolButton+QMenu dropdown with spec-required confirmation warnings, the thin-wrapper/non-modal-impl factoring, synchronous status refresh after ops, and the latent stale-required-label fix.

Purpose: spec.md:41-44 puts these controls on the Game status tab; the tab stays a dumb renderer (all wording from pure status_text).
Output: game_window.py growth + smoke_16_tab.py (SMOKE-16) PART A.
</objective>

<execution_context>
@~/.config/opencode/get-shit-done/workflows/execute-plan.md
@~/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-scoring-lifecycle-endgame/06-02-SUMMARY.md
@.planning/phases/06-scoring-lifecycle-endgame/06-05-SUMMARY.md
@.planning/phases/06-scoring-lifecycle-endgame/06-06-SUMMARY.md
@.planning/phases/06-scoring-lifecycle-endgame/06-RESEARCH-endgame-ui.md
@aamatch/game_window.py
@aamatch/status_text.py
@aamatch/setup_window.py
@smoke/smoke_11_window.py

Anchors (verified):
- Button row game_window.py:118-126 (btn_hint + addStretch(1) — the reserved slot); _guard :332-343; _on_hint/_hint_now :345-371 (the handler-family precedent); _refresh_status :291-328 (the :323-327 label condition = THE BUG: keys on molecule_id only, which repeats across levels — generator.py:739-41); _begin_play :195-221; tick :257-287.
- 06-02 constants/builders: SKIP_WARNING_TITLE/'Skip this molecule?', SKIP_WARNING_TEXT, GIVEUP_WARNING_TITLE/'Give up?', GIVEUP_WARNING_TEXT, game_restarted_line().
- 06-05/06-06 wizard ops: confirm_molecule() / skip_molecule() / give_up() — all return plain data or None (None = the wizard _guard already surfaced the refusal on the PANEL; the tab stays silent on None, the hint precedent :366-368).
- Warnings are QMessageBox.question Yes|No, parent self.window(), modal CHILD (legal, PITFALL 4); during them the 1 Hz tick's existing activeModalWidget->rebase_timer branch freezes the clock automatically — wrappers must NOT call rebase_timer (caller-owns-modal-detection law).
- Pre-GO gating: the isinstance(cmd.get_wizard(), GameWizard) check fires BEFORE any box — a Skip/Give-Up press during the countdown is a SILENT no-op (no modal over a wizard-free game; endgame-ui §5.4).
- Row-order law: insert new buttons BEFORE the stretch with btn_row.insertWidget(btn_row.count() - 1, w) so the stretch stays last and the timer row never reflows (05-06 OQ-1 design).
- 04-09 _X_impl law: clicked -> thin MODAL wrapper -> NON-MODAL impl; a static QMessageBox under offscreen BLOCKS forever (smoke-99 receipt).
- Grep law: zero engine._game/_payload attribute hits in game_window.py; engine._current_game()/engine.endgame_summary() CALLS are the sanctioned accessor class.
- SCANNED_MODULES already covers game_window.py (no registry growth).
</context>

<tasks>

<task type="auto">
  <name>Task 1: Confirm button + impl + required-label fix + poll game_over block</name>
  <files>aamatch/game_window.py</files>
  <action>
    1. In __init__ after btn_hint's row insertion: `self.btn_confirm = QtWidgets.QPushButton('Confirm', self)` with tooltip 'Finish this molecule: run detection, score it, and advance.'; `btn_row.insertWidget(btn_row.count() - 1, self.btn_confirm)`; `self.btn_confirm.clicked.connect(self._on_confirm)`. Update the class docstring's button inventory.
    2. `_on_confirm(self)`: `self._guard(self._confirm_now)` (no warning needed for Confirm).
    3. `_confirm_now(self)`: lazy `from pymol import cmd` + `from . import wizard as wizard_mod`; `prior = cmd.get_wizard()`; `if not isinstance(prior, wizard_mod.GameWizard): return None`; `result = prior.confirm_molecule()`; `if result is None: return None` (panel surfaced); `self._refresh_status()` (SYNCHRONOUS poll — the marker flows instantly: scored line + debrief + position line, and the game_over transition block if the game completed); `if result.get('game_over'): self._endgame_sequence(result['summary'])`; return result.
    4. `_endgame_sequence(self, summary)`: `self._timer.stop()` (v1 _on_win precedent — the tick cannot stop the clock, engine._game stays live); `self._timer_label.setText(status_text.format_mss(summary['final_time']))` (EXACT final elapsed, not the <=1 s-stale tick value); `self._pop_game_wizard()`; return. (`_pop_game_wizard` = NEW GameTab helper mirroring setup_window.py:826-845: lazy cmd+wizard imports, isinstance gate, canonical `cmd.set_wizard()` None-pop, returns popped-bool — the pop's color-restore burst precedes 06-09's refresh+singleShot modal.)
    5. REQUIRED-LABEL FIX in _refresh_status (:323-327): the refresh condition becomes `if prev is None or prev.get('molecule_id') != state.get('molecule_id') or prev.get('level_pos') != state.get('level_pos'):` — molecule_id repeats across levels ('mol-001' again), level_pos does not; document the bug in a comment.
    6. Poll game_over-transition block in _refresh_status (AFTER the status_events lines): `prev_go = bool(prev.get('game_over')) if prev else False`; `if state.get('game_over') and not prev_go:` lazy `from . import engine`; `for line in status_text.endgame_lines(engine.endgame_summary()): self._log(line)` — ONE block-logging home for BOTH tab-triggered (via the sync refresh) and panel-triggered (via the 1 Hz tick) game ends; no double-log (the transition fires once).
    - Docstrings updated (module ROLE paragraph + handler docstrings); python3.6/%-formatting; no new module-level imports.
  </action>
  <verify>py_compile + full WSL suite green (test_wizard_source scans cover game_window.py — zero new banned hits); grep `grep -n "btn_confirm\|level_pos' != state.get" aamatch/game_window.py` hits both; grep law holds: `grep -n "engine\._game\|engine\._payload" aamatch/game_window.py` returns ZERO.</verify>
  <done>Confirm button wired end-to-end (op -> sync refresh -> endgame sequence when finished); the label fix ships; the game_over block has one logging home.</done>
</task>

<task type="auto">
  <name>Task 2: Skip/Give-Up dropdown + warning wrappers + impls</name>
  <files>aamatch/game_window.py</files>
  <action>
    1. In __init__ after btn_confirm: `self.btn_skip_menu = QtWidgets.QToolButton(self)`; `self.btn_skip_menu.setText('Skip / Give Up')`; tooltip 'Skip the current molecule or give up the game (both ask for confirmation).'; `self.btn_skip_menu.setPopupMode(QtWidgets.QToolButton.InstantPopup)`; `menu = QtWidgets.QMenu(self.btn_skip_menu)`; `self.act_skip = menu.addAction('Skip Molecule')` with tooltip 'Store the partial score and move to the next molecule (asks for confirmation).'; `self.act_giveup = menu.addAction('Give Up...')` with tooltip 'Ends the game at the current stage — asks for confirmation.'; `self.btn_skip_menu.setMenu(menu)`; `btn_row.insertWidget(btn_row.count() - 1, self.btn_skip_menu)`; `self.act_skip.triggered.connect(self._on_skip)`; `self.act_giveup.triggered.connect(self._on_giveup)`.
    2. `_on_skip(self)`: GATE FIRST (silent no-op pre-GO — BEFORE any box): lazy cmd+wizard imports; `if not isinstance(cmd.get_wizard(), wizard_mod.GameWizard): return`; then `btns = QtWidgets.QMessageBox.Yes | QtWidgets.QMessageBox.No`; `if QtWidgets.QMessageBox.question(self.window(), status_text.SKIP_WARNING_TITLE, status_text.SKIP_WARNING_TEXT, btns) == QtWidgets.QMessageBox.Yes: self._guard(self._skip_now)` (the v1 _confirm helper shape, gui_game.py:132-137; Escape/No = safe no-op).
    3. `_on_giveup(self)`: identical shape with GIVEUP_WARNING_TITLE/TEXT -> `self._guard(self._giveup_now)`.
    4. `_skip_now(self)`: isinstance gate (defensive) -> `result = prior.skip_molecule()` -> None: return None -> `self._refresh_status()` -> `if result.get('game_over'): self._endgame_sequence(result['summary'])` (skip of the LAST molecule completes the game — 06-06) -> return result.
    5. `_giveup_now(self)`: same shape with prior.give_up() (always game_over True on success) -> sync refresh (gave_up line + block via the transition) -> `self._endgame_sequence(result['summary'])` -> return result.
    6. NO rebase_timer anywhere in the wrappers (the tick owns modal detection); NO boxes in the impls.
    - The 06-09 plan adds ONLY the modal-scheduling tail to the wrappers — keep the wrappers' structure stable for that edit.
  </action>
  <verify>py_compile + WSL suite green; grep `grep -n "QMessageBox.question\|InstantPopup" aamatch/game_window.py` hits; the smoke-99 law holds structurally (impls box-free).</verify>
  <done>The spec-literal dropdown offers Skip Molecule / Give Up with the pinned warning texts; Yes-branches drive the wizard ops through _guard; impls are headless-driveable.</done>
</task>

<task type="auto">
  <name>Task 3: SMOKE-16 creation + PART A (tab drive)</name>
  <files>smoke/smoke_16_tab.py</files>
  <action>
    Create smoke/smoke_16_tab.py — the T1b house recipe (QT_QPA_PLATFORM='offscreen' at script top BEFORE any pymol.Qt import; repo-root anchor; QApplication reuse-or-create; ZERO modals — drive impls as METHODS, never wrappers; verdict `=== SMOKE-16 %s ===`). PART A:
    1. Build the window: `dlg = setup_window.SetupWindow()` (or the SMOKE-11 construction shape) — assert GameTab inventory: btn_hint, btn_confirm ('Confirm'), btn_skip_menu ('Skip / Give Up' QToolButton) with a 2-action menu ('Skip Molecule', 'Give Up...'), tooltips non-empty on all; the stretch stays LAST in the row layout (timer-row no-reflow); fresh-construction placeholder labels '0:00' / 'Required: -' (the PART I2 pin class).
    2. Live game: `wiz = gamestart.start_game(activate=False)`; `dlg.game_tab.start_countdown(wiz)`; drive 4 `_countdown_tick()` calls -> GO (anchor float, level line logged).
    3. Confirm impl: `dlg.game_tab._confirm_now()` -> info box contains the pinned scored line ('Molecule 1 of 2 scored 0.00 (total 0.00).') + the debrief lines (result_lines shapes) + the new position line; `_last_status` updated; molecule advanced (get_status molecule_pos 2).
    4. Sync-refresh proof: the lines landed WITHOUT any event-loop pump (assert immediately after the call — no processEvents between).
    5. Skip impl: `dlg.game_tab._skip_now()` -> 'Skipped molecule 2 of 2 (partial score 0.00, total 0.00).' + level 2 line; skip_count 1.
    6. Give-up impl: `dlg.game_tab._giveup_now()` -> 'Game ended at level 2, molecule 1 of 2 (total 0.00).' + the endgame block lines (headline 'Game over -- gave up at level 2, molecule 1 of 2.' + per-level + total + time + molecules + skips lines); `dlg.game_tab._timer.isActive()` is False (STOPPED); `_timer_label` text == status_text.format_mss(final_time); wizard popped (cmd.get_wizard() is None); required label reset to 'Required: -' by the post-pop poll? NO — the timer is stopped; assert the label holds the LAST required value (stable).
    7. Label-fix proof: fresh game via the countdown drive; confirm through level 1's molecules -> after the level advance, `_required_label` text reflects LEVEL 2's requirement (differs from level 1's when the payload's levels differ — with defaults both levels require 1 type; assert the label text equals required_display(level-2 required) fetched from get_status — identity of content, not difference).
    8. Wrapper gate proof: with NO wizard (post-cleanup), calling `_on_skip` would open a box — DO NOT call it; instead assert the gate logic exists by driving `_skip_now`/`_giveup_now` directly (isinstance gate -> None) — the box path is [HUMAN]-only (06-10).
    9. Restore block: cancel_pending_start + `_timer.stop()` + done pop + prefix cleanup + `gamestart._last_start = None` (05-06 Rule-2 + PART H shape) -> baseline scene EXACTLY.
  </action>
  <verify>`bash smoke/run_smoke.sh smoke/smoke_16_tab.py` greps `=== SMOKE-16 PASS ===` with the PART A count and zero FAIL lines.</verify>
  <done>The tab's lifecycle controls are constructed, wired, and proven headlessly impl-driven; the label fix and sync-refresh behavior are pinned.</done>
</task>

</tasks>

<verification>
- `python3.6 -m py_compile aamatch/*.py` && `python3.6 -m unittest discover -s tests -v` ALL green.
- `bash smoke/run_smoke.sh smoke/smoke_16_tab.py` -> PASS; `bash smoke/run_smoke.sh smoke/smoke_11_window.py` regression PASS (the two-tab structure + placeholder pins intact).
- SMOKE-14 re-run PASS (wizard _state_dict growth is additive; the poll consumes it).
</verification>

<success_criteria>
- Confirm + Skip/Give-Up dropdown live on the tab with spec wording; warnings are modal children with pinned text; impls are box-free.
- Zero-latency event logging via sync refresh; the stale-label bug is fixed; the game_over block logs exactly once per game end.
- SMOKE-16 PART A + SMOKE-11 regression green.
</success_criteria>

<output>
After completion, create `.planning/phases/06-scoring-lifecycle-endgame/06-07-SUMMARY.md` (frontmatter: subsystem ui, provides the wrapper/impl structure + _endgame_sequence/_pop_game_wizard seams for 06-08/06-09).
</output>
