---
phase: 04-qt-setup-window
plan: 10
type: execute
wave: 6
depends_on: ["04-08", "04-09"]
files_modified: [aamatch/setup_window.py, smoke/smoke_11_window.py]
autonomous: true

must_haves:
  truths:
    - "Browse picks ONE SDF/MOL2 file (single-select -- the frozen upload field shape {'path','sha256'} wins; Decision 9); ingesting splits it, caps it, extracts rows+content via upload.prepare_uploaded_set, and fills the session slot self._uploaded"
    - "After ingest the form reflects upload mode: source radio switches to the upload page, the path label shows the file, and collect_state returns upload={'path','sha256'} (the FILE hash) while source_mode stays coexistent (demo_set_id kept underneath)"
    - "Start/Export with upload mode but no session-ready content refuses via build_state's upload_ready pre-check BEFORE any scene-touching call"
    - "A second Browse REPLACES the slot (single slot v1); re-ingesting is idempotent and leaves the scene clean (temps deleted in finally)"
  artifacts:
    - path: "aamatch/setup_window.py"
      provides: "_on_browse_upload + _ingest_upload(path); collect_state upload reflection + upload_ready computation"
      min_lines: 40
    - path: "smoke/smoke_11_window.py"
      provides: "SMOKE-11 PART D: _ingest_upload drive on the bundled benzamide SDF (T1b, probe-gated)"
  key_links:
    - from: "aamatch/setup_window.py _ingest_upload"
      to: "aamatch/upload.prepare_uploaded_set"
      via: "the cmd-tier extraction pipeline (rows + ligand_content)"
      pattern: "prepare_uploaded_set"
    - from: "aamatch/setup_window.py _ingest_upload"
      to: "aamatch/game_file.read_upload_source"
      via: "file read + sha256 + format detect + split + cap -- all pure calls"
      pattern: "read_upload_source|split_sdf_records|split_mol2_segments|check_upload_supply"
    - from: "aamatch/setup_window.py collect_state"
      to: "aamatch/setup_form.build_state"
      via: "upload_ready=True iff self._uploaded matches the form's upload path (handlers pass it to build_state)"
      pattern: "upload_ready"
---

<objective>
Wire the upload button (SETUP-03 form side): Browse -> pure read/split/cap -> cmd-tier extraction -> session slot + widget reflection. This closes the last GAP the research flagged for the window: uploads must be ingestable INTO the engine's existing candidates/ligand_content pipe, not fabricated ad hoc.

Purpose: ROADMAP criterion 2 includes "upload an SDF/MOL2 molecule set" as a Phase-4 [HUMAN] deliverable; the seam plans (04-04/04-06/04-08) must land before this handler and before the final checkpoint (sequencing requirement).
Output: working upload ingestion + SMOKE-11 PART D.
</objective>

<execution_context>
@~/.config/opencode/get-shit-done/workflows/execute-plan.md
@~/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/04-qt-setup-window/04-RESEARCH-export-upload.md
@.planning/phases/04-qt-setup-window/04-08-SUMMARY.md
@aamatch/setup_window.py
@aamatch/upload.py
@aamatch/game_file.py
</context>

<decisions>
- Single-select upload (Decision 9): `QFileDialog.getOpenFileName` (NOT getOpenFileNames) -- the frozen setup field is `upload = None | {'path','sha256'}`; multi-file support would be a schema change (version-bump event). One multi-record SDF = one set of molecules (spec.md:14); a new upload REPLACES the slot.
- The ingested rows/content live ONLY in the window session (self._uploaded); setup files persist path+sha256 labels (Load restores labels, not molecules).
- upload_ready for build_state: True iff `self._uploaded is not None and self._uploaded['path'] == <form upload path>` -- handlers (04-12/04-13) compute it when calling build_state; collect_state itself stays a pure widget snapshot.
</decisions>

<tasks>

<task type="auto">
  <name>Task 1: _on_browse_upload + _ingest_upload</name>
  <files>aamatch/setup_window.py</files>
  <action>
1. `_on_browse_upload(self)`: `path, _ = QtWidgets.QFileDialog.getOpenFileName(self, 'Upload molecule file', '', 'Molecule files (*.sdf *.mol2);;All Files (*)')` (modal CHILD); `if path: self._guard(lambda: self._ingest_upload(path))`. Connect self.btn_browse.clicked -> self._on_browse_upload (the widget exists since 04-07).
2. `_ingest_upload(self, path) -> int` (non-modal; returns the record count):
   - lazy `from . import game_file, paths, upload` (siblings inside the method).
   - `text, sha256_hex, fmt = game_file.read_upload_source(paths.to_windows_path(path))` (extension whitelist + utf-8 + FILE sha256 -- Decision 19).
   - records = `game_file.split_sdf_records(text)` if fmt == 'sdf' else `game_file.split_mol2_segments(text)`.
   - `game_file.check_upload_supply(records, fmt, os.path.basename(path))` (cap 50 + degenerate refusals; import os lazily or at module top -- os is stdlib, module-level os import in the Qt tier is fine but keep consistent: lazy `import os` at top of method).
   - `rows, content = upload.prepare_uploaded_set(records, fmt)`.
   - `self._uploaded = {'rows': rows, 'content': content, 'path': path, 'sha256': sha256_hex, 'fmt': fmt}`.
   - Reflect into widgets (respect _loading? no -- set directly): `self.src_upload.setChecked(True)` (switches the stack); `self.upload_path_label.setText('%s (%d molecule record(s))' % (path, len(rows)))`; label tooltip = full path.
   - Return len(rows).
3. Docstring notes: replace-on-new-upload; scene stays clean (prepare_uploaded_set deletes its temps); EXT-04 depth NOT built (fail-closed minimum only -- research upload_pipeline boundary).
Commit: `feat(04-10): upload ingestion handler (single-select, session slot, widget reflection)`.
  </action>
  <verify>T0: py_compile + full WSL suite green. Static: single getOpenFileName; to_windows_path routing; the three pure helpers + prepare_uploaded_set called in order; no candidate-row fabrication in the window.</verify>
  <done>Upload ingestion is wired end-to-end through proven pure + cmd-tier helpers.</done>
</task>

<task type="auto">
  <name>Task 2: collect_state/upload_ready integration</name>
  <files>aamatch/setup_window.py</files>
  <action>
1. collect_state already returns `upload` from self._uploaded (04-07). Refine ONLY the label/tooltip read path if needed so the form's upload page label reflects the stored path after ingest (Task 1 already sets it).
2. Add `def upload_ready_for(self, form_values) -> bool`: returns True iff source_mode != 'upload', OR `self._uploaded is not None and self._uploaded['path'] == (form_values.get('upload') or {}).get('path') and self._uploaded['sha256'] == (form_values.get('upload') or {}).get('sha256')`. Docstring: the pure build_state receives `upload_ready=self.upload_ready_for(collect_state())` from handlers (04-12/04-13).
3. Apply-side completeness: `apply_state` already shows state['upload'] path labels; add the tooltip with the sha256 when present (Load UX: the file-moved warning lives in the pure layer's future -- for now the label + tooltip carry the info; note in docstring).
Commit: `feat(04-10): upload_ready plumbing for build_state pre-checks`.
  </action>
  <verify>T0: py_compile + full WSL suite green; grep: upload_ready_for used nowhere yet (handlers come later) but defined + documented.</verify>
  <done>The window can answer "is this upload configuration actually startable right now?" for the fatal pre-check.</done>
</task>

<task type="auto">
  <name>Task 3: SMOKE-11 PART D -- ingest drive on the bundled SDF</name>
  <files>smoke/smoke_11_window.py</files>
  <action>
Extend SMOKE-11 (probe-gated T1b, success paths only):
- PART D: path = repo-anchored `aamatch/data/ligands/benzamide.sdf`; `n = dlg._ingest_upload(path)`; assert n == 1; assert dlg._uploaded carries rows (1 row, entry_id 'mol-001', file key 'uploads/mol-001.sdf'), content dict with that key, sha256 == hashlib of the file bytes; assert the source radio switched (src_upload.isChecked()); assert collect_state()['upload']['path'] == path and upload['sha256'] matches; assert `dlg.upload_ready_for(dlg.collect_state()) is True`; then switch back to demo radio and assert upload_ready_for with source_mode 'demo' is True regardless. Cleanup: set self._uploaded = None; switch back to demo page. NO modals.
Commit: `test(04-10): SMOKE-11 PART D upload-ingest drive`.
  </action>
  <verify>T1b (probe-PASS): `bash smoke/run_smoke.sh smoke/smoke_11_window.py 120` prints `=== SMOKE-11 PASS ===` with PART D green. T1a (probe-FAIL): skipped + marked [HUMAN] (the pure and cmd tiers beneath are already smoke-proven by SMOKE-12). T0: full WSL suite green.</verify>
  <done>Upload ingestion proven headlessly to the probe's limit; SETUP-03's window side complete.</done>
</task>

</tasks>

<verification>
- T0: py_compile; full WSL suite green.
- T1b (probe-gated): SMOKE-11 PART D PASS.
- Sequencing check (ROADMAP criterion 2): the upload seam chain 04-04 -> 04-06 -> 04-08 -> THIS plan is complete before the checkpoints.
</verification>

<success_criteria>
The user can upload one SDF/MOL2 file; its records become engine-ready candidates + embedded content in the session; the form reflects the upload state; and any attempt to Start/Export an un-ingested upload configuration will refuse before touching the scene (pre-check in place, exercised by 04-12/04-13).
</success_criteria>

<output>
After completion, create `.planning/phases/04-qt-setup-window/04-10-SUMMARY.md` (frontmatter: affects [04-12 export, 04-13 start, 04-15 checkpoint B], subsystem qt-window handlers).
</output>
