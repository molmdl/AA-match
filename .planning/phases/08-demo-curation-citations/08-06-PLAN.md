---
phase: 08-demo-curation-citations
plan: 06
type: execute
wave: 4
depends_on:
  - "08-05"
files_modified:
  - aamatch/data/MANIFEST.json
  - aamatch/data/ligands/demo-hard-1-benzamidine.sdf
  - aamatch/data/ligands/demo-hard-1-guanidinium.sdf
  - aamatch/data/ligands/demo-hard-2-glutamic-acid.sdf
  - aamatch/data/ligands/demo-hard-2-acetylcholine.sdf
  - aamatch/data/ligands/demo-hard-3-atp.sdf
  - aamatch/data/ligands/demo-hard-3-nad.sdf
  - scripts/demo_specs/demo-hard-1.json
  - scripts/demo_specs/demo-hard-2.json
  - scripts/demo_specs/demo-hard-3.json
autonomous: true
must_haves:
  truths:
    - "demo-hard-1/2/3 ship pre-downloaded single-record SDFs whose derived counts/sha256 match MANIFEST.json exactly (SMOKE-02 real loads prove it)"
    - "The cation-pi showcases are real: benzamidine/guanidinium/acetylcholine carry their recorded charges per the approved proposal (guanidinium +1, acetylcholine +1 quaternary N)"
    - "First medium size-class entries land (ATP 31 heavy, NAD 44 heavy) — the generator's medium bucket becomes non-empty"
    - "Each set committed atomically (SDFs + MANIFEST.json + spec in one commit per set); demo-dev-1 and the easy sets byte-identical"
  artifacts:
    - path: "aamatch/data/MANIFEST.json"
      provides: "7 sets (dev + 3 easy + 3 hard); hard sets with real license/provenance"
      contains: "demo-hard-1"
    - path: "aamatch/data/ligands/demo-hard-3-nad.sdf"
      provides: "First 44-heavy (medium) bundled ligand"
    - path: "scripts/demo_specs/demo-hard-1.json"
      provides: "Approved curation input for demo-hard-1"
      contains: "demo-hard-1"
    - path: "scripts/demo_specs/demo-hard-2.json"
      provides: "Approved curation input for demo-hard-2"
      contains: "demo-hard-2"
    - path: "scripts/demo_specs/demo-hard-3.json"
      provides: "Approved curation input for demo-hard-3"
      contains: "demo-hard-3"
  key_links:
    - from: "scripts/demo_specs/demo-hard-*.json"
      to: "08-PROPOSALS.md approved rows"
      via: "transcription of approved rows only (CIDs 2332/32838/33032/187/5957/5892)"
      pattern: "demo-hard-"
    - from: "aamatch/data/MANIFEST.json"
      to: "SMOKE-02"
      via: "every new entry load-verified in real PyMOL BEFORE its commit"
      pattern: "SMOKE-02"
---

<objective>
Bundle the three Hard demo sets (GEN-06 tier slots: Hard ×3) through the committed pipeline — the cation-pi/charged-species tier and the first medium size-class supply.

Purpose: fills the generator's medium bucket (NEAREST-BUCKET fallback for large-tier games lands here) and proves the protocol on charged species where the capability rules (ammonium veto, guanidino centroid, COOH guard) meet real chemistry.
Output: 6 SDFs + 3 specs + regenerated MANIFEST.json (7 sets), committed atomically per set.
</objective>

<execution_context>
@~/.config/opencode/get-shit-done/workflows/execute-plan.md
@~/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/08-demo-curation-citations/08-PROPOSALS.md
@.planning/phases/08-demo-curation-citations/08-RESEARCH-sourcing.md
@scripts/build_demos.py
@aamatch/data/MANIFEST.json

**GATE precondition:** 08-PROPOSALS.md approval recorded. STOP otherwise.

Sets (tier 'hard'):
- demo-hard-1 'Trypsin classics': benzamidine (CID 2332) + guanidinium (CID 32838 — 3D status was resolved during 08-04; if it fetched 404/multi-record there, bundle the APPROVED alternate recorded in the proposal instead and note the substitution)
- demo-hard-2 'Charged messengers': glutamic-acid (CID 33032) + acetylcholine (CID 187, +1 — the DEFAULT approved pick; imidazolium was the alternate)
- demo-hard-3 'Cofactors': atp (CID 5957, ~31 heavy → medium) + nad (CID 5892, ~44 heavy → medium)
Fetch URL: `https://pubchem.ncbi.nlm.nih.gov/rest/pug/compound/cid/<CID>/SDF?record_type=3d`.

**BINDING LAWS (repeat per task):** NEVER hand-edit an SDF or MANIFEST.json — regenerate via the builder. NEVER substitute beyond the approved proposal's listed alternates; any approved-alternate substitution is recorded in the spec notes + SUMMARY. Fetch failure (non-200 / multi-record / counts mismatch vs proposal evidence) = STOP that task and surface it.
Chemistry guardrails: guanidinium's manifest entry must derive formal_charge_sum +1 and the protonation notes must cite the 02-06 law (guanidino center = centroid of 3 bonded Ns) as the reason this ligand exercises it; acetylcholine must derive +1 (quaternary N — the classic cation_pi donor); ATP stays neutral (its phosphates are NOT salt-bridge-typed per DETECT-04 parity — the proposal's interaction column must say h_bond/pi_stacking only).
</context>

<tasks>

<task type="auto">
  <name>Task 1: bundle demo-hard-1 (benzamidine + guanidinium)</name>
  <files>scripts/demo_specs/demo-hard-1.json, aamatch/data/ligands/demo-hard-1-benzamidine.sdf, aamatch/data/ligands/demo-hard-1-guanidinium.sdf, aamatch/data/MANIFEST.json</files>
  <action>
Spec per schema: set_id 'demo-hard-1', tier 'hard', approved title, license 'Public domain (NCBI PubChem)', set provenance; entries benzamidine (CID 2332, PDB complex 1S0R chain from the proposal) + guanidinium (CID 32838 or its approved alternate — record which in notes). expect_atom_count guards from the proposal's counts-line evidence. Run `python3.6 scripts/build_demos.py --spec scripts/demo_specs/demo-hard-1.json --fetch`; verify the derived formal_charge_sum matches the proposal (+1 for guanidinium); `bash smoke/run_smoke.sh smoke/smoke_02_manifest.py 180`; `python3.6 -m unittest tests.test_demo_data -v`; atomic commit `feat(08-06): bundle demo-hard-1 (benzamidine + guanidinium)`.
  </action>
  <verify>bash smoke/run_smoke.sh smoke/smoke_02_manifest.py 180 (PASS) && python3.6 -m unittest tests.test_demo_data -v (green) && python3.6 -c "import json; c=json.load(open('aamatch/data/MANIFEST.json')); s=[x for x in c['data']['sets'] if x['set_id']=='demo-hard-1'][0]; print([(e['entry_id'], e['formal_charge_sum'], e['size_class']) for e in s['entries']])"</verify>
  <done>demo-hard-1 committed atomically; charges manifest-verified; SMOKE-02 green.</done>
</task>

<task type="auto">
  <name>Task 2: bundle demo-hard-2 (glutamic acid + acetylcholine)</name>
  <files>scripts/demo_specs/demo-hard-2.json, aamatch/data/ligands/demo-hard-2-glutamic-acid.sdf, aamatch/data/ligands/demo-hard-2-acetylcholine.sdf, aamatch/data/MANIFEST.json</files>
  <action>
Same procedure: glutamic-acid (CID 33032, PDB complex from the proposal) + acetylcholine (CID 187, +1 quaternary N — cation_pi showcase both directions per capability D4). Derived formal_charge_sum must match the proposal evidence (+1 acetylcholine; glutamic acid as-recorded per its approved protonation record). SMOKE-02 → data battery → atomic commit `feat(08-06): bundle demo-hard-2 (glutamic acid + acetylcholine)`.
  </action>
  <verify>bash smoke/run_smoke.sh smoke/smoke_02_manifest.py 180 (PASS) && python3.6 -m unittest tests.test_demo_data -v (green)</verify>
  <done>demo-hard-2 committed atomically; cation_pi carrier species manifest-verified.</done>
</task>

<task type="auto">
  <name>Task 3: bundle demo-hard-3 (ATP + NAD) + full-suite regression</name>
  <files>scripts/demo_specs/demo-hard-3.json, aamatch/data/ligands/demo-hard-3-atp.sdf, aamatch/data/ligands/demo-hard-3-nad.sdf, aamatch/data/MANIFEST.json</files>
  <action>
Same procedure: atp (CID 5957) + nad (CID 5892) — the first medium entries (heavy counts ≥ 25 per the generator buckets; the builder's size_class derivation must yield 'medium' for both; if a fetched record's heavy count lands in a different bucket than the proposal predicted, the derivation wins — record the actual class in the spec notes and flag the delta in the SUMMARY, since 08-10 measures real distribution). SMOKE-02 → data battery → atomic commit `feat(08-06): bundle demo-hard-3 (ATP + NAD)`. Then FULL WSL suite + `python3.6 scripts/build_demos.py --report-only` (exit 0) + dev-set dry-run still exact.
  </action>
  <verify>bash smoke/run_smoke.sh smoke/smoke_02_manifest.py 180 (PASS) && python3.6 -m unittest discover -s tests -v (ALL green) && python3.6 scripts/build_demos.py --report-only (exit 0) && python3.6 scripts/build_demos.py --spec scripts/demo_specs/demo-dev-1.json --dry-run (derived == committed)</verify>
  <done>Hard tier complete (3 sets, 6 ligands incl. 2 medium) committed atomically; full suite green; other sets byte-identical.</done>
</task>

</tasks>

<verification>
- `python3.6 -m py_compile aamatch/*.py scripts/build_demos.py` — syntax floor.
- `python3.6 -m unittest discover -s tests -v` — full suite incl. purity gates.
- `bash smoke/run_smoke.sh smoke/smoke_02_manifest.py 180` — all 7 sets load-verify.
- MANIFEST.json: 7 sets, container version 1, manifest_version 1 (frozen schema untouched).
</verification>

<success_criteria>
- Hard ×3 tier slots filled with charge-verified cation-pi/salt-bridge carriers and the first medium bucket supply.
- One atomic commit per set, each gated by SMOKE-02 before commit.
- All substitutions (if any) limited to approved alternates and recorded.
</success_criteria>

<output>
After completion, create `.planning/phases/08-demo-curation-citations/08-06-SUMMARY.md`
</output>
