---
phase: 01-bootstrap-pure-foundation
plan: 09
type: execute
wave: 3
depends_on: ["01-01", "01-07"]
files_modified:
  - tmp/aamatch-0.1.0.zip   # git-ignored build artifact (tmp/ excluded by .gitignore)
autonomous: false
must_haves:
  truths:
    - "An install zip exists whose root contains the aamatch/ package directory (never a flat __init__.py), with no __pycache__ members"
    - "A human installs via Plugin Manager and, after restarting PyMOL, sees 'AA-match' under the Plugins menu and the placeholder line in the console (INSTALL-01)"
    - "The human explicitly confirms (or amends) the adopted setup defaults: molecules_per_level 2/min 1/cap 10, difficulty_levels 3/min 1/cap 9, interaction_mode 'unset'"
  artifacts:
    - path: "tmp/aamatch-0.1.0.zip"
      provides: "install artifact: single-package zip, aamatch/ at root, metadata-first __init__.py inside"
  key_links:
    - from: "Plugin Manager install flow"
      to: "aamatch/__init__.py::__init_plugin__"
      via: "installation.py:339-345 loads immediately in-session; restart re-runs GUI autoload"
      pattern: "AA-match"
---

<objective>
Build the install zip (automated) and run the [HUMAN] install checkpoint (Phase-1 success criterion 4 / INSTALL-01): Plugin-Manager install → restart → "AA-match" menu entry → placeholder prints. The same checkpoint records the human's confirmation of the adopted setup defaults.

Purpose: Menu registration is NOT assertable headless (HAVE_QT=False → `addmenuitemqt` raises, loader catches cleanly — by design). Only a human in a real GUI session can prove INSTALL-01. This is a by-design verification step, not a gap.

Output: verified zip + recorded human verdict (menu present, click feedback, console clean, defaults confirmed).
</objective>

<execution_context>
@~/.config/opencode/get-shit-done/workflows/execute-plan.md
@~/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/01-bootstrap-pure-foundation/01-RESEARCH-plugin-install.md   # §1.1 install contract, §2.4 exact human steps, §4 pitfalls E/F/G/H/I
@.planning/phases/01-bootstrap-pure-foundation/01-05-SUMMARY.md                 # the adopted defaults being confirmed
@.planning/phases/01-bootstrap-pure-foundation/01-07-SUMMARY.md                 # headless proof already passed
</context>

<tasks>

<task type="auto">
  <name>Task 1: Build + verify the install zip (WSL, automated)</name>
  <files>tmp/aamatch-0.1.0.zip</files>
  <action>
From the repo root:

1. `python3.6 -c "import shutil; shutil.rmtree('aamatch/__pycache__', ignore_errors=True)"` — REQUIRED: `python3.6 -m zipfile -c` sweeps in `__pycache__/` (verified 2026-09-05), and shipping bytecode in the install artifact is unclean. NOTE: `rm` is denied in opencode.json — use the shutil one-liner exactly as written.
2. `mkdir -p tmp && python3.6 -m zipfile -c tmp/aamatch-0.1.0.zip aamatch` (tmp/ is git-ignored).
3. Verify the contract (`installation.py:118-136` — zip must contain exactly one package dir at root):
   `python3.6 -c "import zipfile; names=zipfile.ZipFile('tmp/aamatch-0.1.0.zip').namelist(); print('\n'.join(names))"` and assert: contains `aamatch/` and `aamatch/__init__.py`; NO member contains `__pycache__`; the only top-level entry is `aamatch/` (a flat `__init__.py` at zip root would fail install with `BadInstallationFile('Missing __init__.py')`).
4. Record the Windows-visible absolute path for the human: `C:\Users\nglok\Desktop\WORKDIR\molmdl\AA-match\tmp\aamatch-0.1.0.zip`.
  </action>
  <verify>zip listing shows `aamatch/` + `aamatch/__init__.py` at root, zero `__pycache__` members</verify>
  <done>Install artifact built and contract-verified</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>
The installable AA-match plugin skeleton (Phase 1): `aamatch` package with metadata-first `__init__.py`, `__init_plugin__` registering the "AA-match" Plugins-menu item via `addmenuitemqt`, and a Qt-free print placeholder. The headless smoke (01-07) already proved the package loads clean under real Windows PyMOL; this checkpoint proves the GUI install + menu wiring that headless mode cannot assert.
  </what-built>
  <how-to-verify>
1. Launch PyMOL 2.5.0 (GUI, Windows conda env) as you normally would.
2. Open **Plugins → Plugin Manager**.
3. Click **"Install from local file"** and pick `C:\Users\nglok\Desktop\WORKDIR\molmdl\AA-match\tmp\aamatch-0.1.0.zip`.
4. If a **"Select plugin directory"** dialog appears (only when more than one startup path exists): pick `%APPDATA%\pymol\startup` if offered, else accept the default. If the chosen directory is not writable, PyMOL itself offers to create `%APPDATA%\pymol\startup` — accept. (Avoid letting it install into the conda env's own startup dir — that copy is lost on env changes.)
5. Expect the dialog **"Success: Plugin 'aamatch' has been installed."** The menu item is already live in this session.
6. **Restart PyMOL** (required by INSTALL-01's restart leg). After restart, GUI startup autoloads plugins.
7. Verify: the **Plugins menu shows "AA-match"** (directly under the top-level Plugins menu, below Plugin Manager). Click **AA-match** → the PyMOL console prints: `AA-match 0.1.0: plugin skeleton OK — game UI arrives in Phase 4.`
8. If the menu item is MISSING after restart: check the PyMOL console for `Unable to initialize plugin 'aamatch'` — load errors never crash PyMOL, they only print. Report the console text.
9. **Defaults confirmation** (research proposals adopted in 01-05 — confirm or amend):
   - `molecules_per_level`: default **2**, min **1**, cap **10**
   - `difficulty_levels`: default **3**, min **1**, cap **9**
   - `interaction_mode` (when the user has not chosen): **"unset"** → the generator picks a random required set
   These become frozen schema constants for Phases 2+; changing them later is a version-bump event.
  </how-to-verify>
  <resume-signal>Type "approved" (menu present + placeholder printed + defaults confirmed) — or list what failed / which defaults to change.</resume-signal>
</task>

</tasks>

<verification>
- Zip contract verified programmatically (single package at root, no pycache)
- Human verdict recorded in the phase UAT/summary: menu present Y/N, click feedback Y/N, console clean Y/N
- Defaults confirmed or amendment list captured
</verification>

<success_criteria>
- INSTALL-01 proven: Plugin-Manager install works, restart re-registers "AA-match" under the Plugins menu, clicking it prints the placeholder line
- Adopted defaults explicitly human-approved (or amendments recorded as follow-up todos)
</success_criteria>

<output>
After completion, create `.planning/phases/01-bootstrap-pure-foundation/01-09-SUMMARY.md` (include the human verdict + defaults decision verbatim)
</output>
