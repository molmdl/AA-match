---
phase: 06-scoring-lifecycle-endgame
plan: 06
type: execute
wave: 4
depends_on: ["06-05"]
files_modified:
  - aamatch/wizard.py
  - smoke/smoke_15_lifecycle.py
autonomous: true
must_haves:
  truths:
    - "Skip stores the partial score (detection at skip time) and advances exactly like Confirm — including completing the game when the LAST molecule of the LAST level is skipped"
    - "Give Up ends the game at the current stage: counters, end state, frozen time, and the summary return through the _guard seam"
    - "After game over, EVERY gameplay handler refuses visibly ('The game is over.') — movement, selection, confirm, skip, hint, reset"
    - "Reset emits the game_reset marker so the poll can render the grid-reset line (positions are invisible to the diff otherwise)"
  artifacts:
    - path: "aamatch/wizard.py"
      provides: "skip_molecule/give_up wizard ops + _require_playing gate across all gameplay impls + game_reset marker in _reset_grid_impl"
      contains: "def give_up"
    - path: "smoke/smoke_15_lifecycle.py"
      provides: "PART C: skip/give-up/gating/marker E2E"
      contains: "PART C"
  key_links:
    - from: "wizard.skip_molecule"
      to: "engine.skip_molecule + _advance_after_record"
      via: "same advancement path as Confirm (one shared decision site)"
      pattern: "_advance_after_record"
    - from: "wizard.give_up"
      to: "engine.give_up"
      via: "summary returned plain through _guard; wizard does NOT pop itself"
      pattern: "give_up"
    - from: "wizard._require_playing"
      to: "engine.is_over"
      via: "the single gate home for every gameplay handler"
      pattern: "_require_playing"
---

<objective>
Wizard lifecycle termination + protection (SCORE-05/06): the skip and give-up wizard ops reusing 06-05's shared advancement site, game-over gating across every gameplay handler, and the game_reset marker on reset_grid.

Purpose: "skip/give-up protect the player" (ROADMAP goal) — warnings are the tab's job (06-07); the wizard owns the semantics and the game-over lock.
Output: skip/give_up ops + gating + SMOKE-15 PART C.
</objective>

<execution_context>
@~/.config/opencode/get-shit-done/workflows/execute-plan.md
@~/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-scoring-lifecycle-endgame/06-03-SUMMARY.md
@.planning/phases/06-scoring-lifecycle-endgame/06-05-SUMMARY.md
@.planning/phases/06-scoring-lifecycle-endgame/06-RESEARCH.md
@aamatch/wizard.py
@aamatch/engine.py
@smoke/smoke_15_lifecycle.py

Anchors (verified):
- do_pick wizard.py:275-299 (soft gate site — NOT _guard-wrapped; an exception here would propagate into the C layer); _select_slot :301-325; movement impls :460-542 (_nudge_cam_impl/_rotate_axis_impl/_step_to_ligand_impl/_move_to_impl); _hint_impl :634-672; _reset_grid_impl :604-610.
- 06-05 seams available: _advance_after_record() -> (advanced, summary), _sync_end_state(), _set_event(kind, **payload), _rebind_molecule/_rebind_level/_compose_active_molecule.
- 06-03 ops: engine.skip_molecule(level, molecule, required) -> (value, formed) (record + skip_count += 1); engine.give_up(now=None) -> summary (giveup_count += 1, end_state 'gave_up', stop_timer); engine.is_over(); engine.total_score().
- Refusal surfacing (endgame-ui D7, hint-style): lifecycle ops self-guard via the wizard's own _guard — refusals land on the PANEL error line; the tab treats a None return as already-surfaced. The ops raise WizardError only for genuinely impossible states, which the gates make unreachable from the tab.
- give_up does NOT pop the wizard: the TAB's endgame sequence pops (06-07/06-09 binding design — "AFTER the wizard pop's color-restore burst"); between give-up-Yes and the pop the gates already hold; for a PANEL-era edge (no panel Give Up button exists — panel inventory unchanged, wizard_text.py:53-59) the gates keep the dead wizard inert.
- Skip on the LAST molecule of the LAST level completes the game (every molecule then has a record) — end_state 'completed' via the shared _advance_after_record path (its else-branch).
- PROSE_PIN + module-identity laws as in 06-05.
</context>

<tasks>

<task type="auto">
  <name>Task 1: skip_molecule wizard op (shared advancement)</name>
  <files>aamatch/wizard.py</files>
  <action>
    1. `skip_molecule(self)` public -> `return self._guard(self._skip_molecule_impl)`; docstring: spec.md:44 semantics + the 02-10 sanctioned partial-score reading (detection AT SKIP TIME via the same record path) + warning ownership note ("the confirmation warning is the tab wrapper's — 06-07; this op is the Yes-branch").
    2. `_skip_molecule_impl(self)`: lazy engine import; `self._require_playing()`; capture scored_pos/m_total exactly as confirm does; `score, formed = engine.skip_molecule(self._level_index, self._molecule_index, self._required())`; `total = engine.total_score()`; `self._error = None`; `advanced, summary = self._advance_after_record()`; `self._sync_end_state()`; `self._set_event('molecule_skipped', score=float(score), total=float(total), molecule_pos=scored_pos, molecule_total=m_total)`; `cmd.refresh_wizard()`; return `{'score': float(score), 'total': float(total), 'advanced': advanced, 'game_over': self._game_over, 'summary': summary, 'level_pos': scored_level, 'molecule_pos': scored_pos}` (same shape as confirm's return — 06-07's tab code is uniform).
    - The molecule/level/complete advancement + rebinds + compose are EXACTLY _advance_after_record's — zero duplication.
  </action>
  <verify>py_compile + WSL suite green; grep `grep -n "def skip_molecule" aamatch/wizard.py` hits.</verify>
  <done>Skip records the partial score, counts, and advances through the shared site; last-molecule-of-game skip completes the game.</done>
</task>

<task type="auto">
  <name>Task 2: give_up wizard op + game-over gating + game_reset marker</name>
  <files>aamatch/wizard.py</files>
  <action>
    1. `_require_playing(self)`: lazy engine import; `if engine.is_over(): raise WizardError('The game is over.')` (pinned refusal wording — visible on the panel via _guard). Call it at the TOP of: `_nudge_cam_impl`, `_rotate_axis_impl`, `_step_to_ligand_impl`, `_move_to_impl`, `_hint_impl`, `_reset_grid_impl`, `_confirm_molecule_impl` (replace 06-05's inline gate with the helper), `_skip_molecule_impl`.
    2. do_pick (:275-299) SOFT gate: after the rows check, before `self._select_slot(slot_id)`: lazy engine import; `if engine.is_over(): self._error = 'The game is over.'; cmd.refresh_wizard(); return` (fail-SOFT — never raise into the C-layer pick dispatch; document why).
    3. `give_up(self)` public -> `return self._guard(self._give_up_impl)`; docstring: spec.md:44-45 (ends the game at the stage; the current molecule is NOT scored), the no-self-pop note, the warning ownership note.
    4. `_give_up_impl(self)`: lazy engine import; `self._require_playing()`; capture `level_pos = self._level_index + 1`, `molecule_pos = self._molecule_index + 1`, `m_total = len(self._registry['molecules'])`; `summary = engine.give_up()`; `self._sync_end_state()`; `self._set_event('gave_up', total=float(summary['total']), molecule_pos=molecule_pos, molecule_total=m_total, level_pos=level_pos)`; `cmd.refresh_wizard()`; return `{'game_over': True, 'summary': summary, 'level_pos': level_pos, 'molecule_pos': molecule_pos}` (the tab's endgame sequence consumes this).
    5. `_reset_grid_impl` (:604-610) gains ONE line before cmd.refresh_wizard(): `self._set_event('game_reset')` (the marker is the ONLY way the poll can see a reset — no pose keys in _state_dict, 'result' never fingerprinted; the game_reset EVENT_KINDS wording is 06-02's pinned builder).
  </action>
  <verify>py_compile + WSL suite green; grep `grep -n "_require_playing\|def give_up\|game_reset" aamatch/wizard.py` shows all sites.</verify>
  <done>give_up freezes the game + returns the summary; every gameplay handler refuses after game over (movement/selection hard-or-soft as appropriate); reset emits its marker.</done>
</task>

<task type="auto">
  <name>Task 3: SMOKE-15 PART C (skip/give-up/gating E2E)</name>
  <files>smoke/smoke_15_lifecycle.py</files>
  <action>
    Append PART C (ALWAYS tier):
    1. Fresh `start_game(activate=True)`; `wiz.confirm_molecule()` (molecule 1 recorded) -> then `wiz.skip_molecule()` on molecule 2 -> assert: returned 'advanced' == 'level', `engine.game_status()['skip_count'] == 1`, molecule_scores len 2, marker kind 'molecule_skipped' with molecule_pos 2, level_pos advanced to 2.
    2. Skip-completes-game: drive to the LAST molecule of the LAST level (confirm/skip through), then `wiz.skip_molecule()` -> returned 'advanced' is None + 'game_over' True + summary end_state 'completed' (skip on the final molecule finishes the game).
    3. Gating: fresh game; `engine._current_game().game_over = True`? NO — fabricate game-over ONLY through the op: run `wiz.give_up()` on a fresh game -> then assert EVERY gate: `wiz.nudge_cam(1, 0, 0)` leaves `get_status()['error'] == 'The game is over.'` and the selected pose UNCHANGED (no movement); `wiz.rotate_view(10.0)` same; `wiz.hint()` returns None with the same error; `wiz.reset_grid()` same error; a scripted pick (`cmd.select('sele', ...)` + `do_select('sele')`) does NOT change `get_status()['selected']` (soft gate) and sets the same error; `wiz.skip_molecule()`/`wiz.confirm_molecule()` return None with the error set.
    4. give_up data: the give_up return carries game_over True + summary keys; `engine.game_status()` shows giveup_count 1, end_state 'gave_up', final_time float; `get_status()['end_state'] == 'gave_up'`.
    5. Reset marker: ANOTHER fresh game; move an AA (`wiz.move_to` after a scripted select), then `wiz.reset_grid()` -> marker kind 'game_reset' in get_status()['last_event']; pose back at the effective_position target (1e-6 + ulp slack); game_status to_dict UNCHANGED except nothing (reset touches NO GameState — capture before/after equality).
    6. Restore per PART A.
  </action>
  <verify>`bash smoke/run_smoke.sh smoke/smoke_15_lifecycle.py` -> `=== SMOKE-15 PASS ===` (A+B+C).</verify>
  <done>Skip/give-up semantics + full game-over lockdown + reset marker proven headlessly.</done>
</task>

</tasks>

<verification>
- `python3.6 -m py_compile aamatch/*.py` && `python3.6 -m unittest discover -s tests -v` ALL green.
- `bash smoke/run_smoke.sh smoke/smoke_15_lifecycle.py` -> PASS (A+B+C).
- SMOKE-07 regression re-run PASS (movement invariants intact; the new gate is a no-op pre-game-over).
- PROSE_PIN untouched.
</verification>

<success_criteria>
- Skip and Confirm share ONE advancement site; partial-score semantics are the 02-10 sanctioned reading.
- Game over = every gameplay handler refuses with the pinned wording; the wizard stays inert-but-present until the tab's endgame sequence pops it.
- SMOKE-15 A+B+C green.
</success_criteria>

<output>
After completion, create `.planning/phases/06-scoring-lifecycle-endgame/06-06-SUMMARY.md` (frontmatter: subsystem wizard, provides skip_molecule/give_up return contracts + the gate wording for 06-07).
</output>
