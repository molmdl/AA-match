---
phase: 02-headless-game-engine
plan: 10
type: tdd
wave: 7
depends_on: ["02-07"]
files_modified:
  - aamatch/game_state.py
  - tests/test_game_state.py
  - tests/test_purity.py
autonomous: true

must_haves:
  truths:
    - "score(required, results) returns the fraction of required interactions formed, binary per interaction: 'any' mode is binary (>=1 formed -> 1.0), 'list' mode is len(formed items)/len(items)"
    - "An item counts as formed iff >= 1 detector record of that type exists — counts of records never inflate the score"
    - "GameState tracks level/molecule position, per-molecule formed types, running score, skip/give-up counters, and a timer anchor as plain data"
  artifacts:
    - path: "aamatch/game_state.py"
      provides: "score(required, results), GameState container"
      min_lines: 80
  key_links:
    - from: "aamatch/game_state.py"
      to: "detector record contract (02-07)"
      via: "score consumes r['type'] from canonical records"
      pattern: "results"
    - from: "aamatch/game_state.py"
      to: "aamatch/setup_state.py"
      via: "INTERACTION_TYPES canonical order for formed-type bookkeeping"
      pattern: "INTERACTION_TYPES"
---

<objective>
Build the pure scoring + runtime-state module: the SCORE-01 fraction semantics ('any' binary / 'list' fraction, binary per item) and the minimal in-memory GameState the engine ops mutate.

Purpose: Scoring is the game's truth function; it must consume the SAME detector records the debrief will show (SCORE-02 later) — one source, no drift.
Output: game_state.py (pure) + tests + PURE_MODULES registration.
</objective>

<execution_context>
@~/.config/opencode/get-shit-done/workflows/execute-plan.md
@~/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/02-headless-game-engine/02-RESEARCH-materialization.md  # §7.2 engine ops (score/results surface)
@docs/DETECTION_THRESHOLDS.md                                              # OQ-1: 'any' is scoped by allowed_interactions
@aamatch/detector.py                                                       # canonical record shape (consumed)
@aamatch/setup_state.py                                                    # enums
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: score() — fraction semantics (RED -> GREEN)</name>
  <files>aamatch/game_state.py, tests/test_game_state.py</files>
  <action>
RED:
1. mode 'any': zero records -> 0.0; >= 1 record of ANY type -> 1.0 (binary; two h_bond records still 1.0 — counts never inflate).
2. mode 'list', items [{type h_bond}, {type pi_stacking}]: neither formed -> 0.0; only h_bond -> 0.5; both -> 1.0; duplicate records of one type do not double-count (dedupe by type).
3. Empty items list with mode 'list' -> ValueError (a required set must be non-empty in list mode; 'any' with empty items is the exclusive representation and is legal).
4. Records missing 'type' or with type outside INTERACTION_TYPES -> ValueError (defensive contract check).
5. OQ-1 scoping: in 'any' mode the formed-type check is over the records the detector produced (which only enumerate allowed/cross-side interactions) — document that scoping lives in the detector's input, not in score.
GREEN: implement score(required, results) -> float in aamatch/game_state.py. Imports: from .setup_state import INTERACTION_TYPES only. Docstring records the SCORE-01 semantics + the 'any'-mode scoping note.
Commit: `feat(02-10): score fraction semantics (TDD)`.
  </action>
  <verify>python3.6 -m unittest tests.test_game_state -v</verify>
  <done>All fraction/binary/dedupe/contract cases green</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: GameState container + purity registration (RED -> GREEN)</name>
  <files>aamatch/game_state.py, tests/test_game_state.py, tests/test_purity.py</files>
  <action>
RED: GameState with plain-data fields + no PyMOL/Qt/file I/O: current_level_index, current_molecule_index, molecule_scores (list of floats), total_score property (sum), skip_count, giveup_count, timer_anchor (float, set via start_timer(now) — time.time() anchor stored; rendering is a later phase's QTimer), formed_types_per_molecule (dict keyed 'L{level}M{molecule}' -> sorted list of formed type names, updated via record_molecule_result(level, molecule, required, results) which stores score + formed types together). Transitions: advance_molecule/advance_level move indexes forward; to_dict/from_dict round-trip ALL fields (plain JSON-able dict; formal sidecar container format is Phase 7 — to_dict is for engine/debug use and must already be lossless).
GREEN: implement; append 'game_state' to PURE_MODULES (imports: time + from .setup_state import INTERACTION_TYPES).
Commit: `feat(02-10): GameState runtime container (TDD)`.
  </action>
  <verify>python3.6 -m unittest discover -s tests -v  (full suite; purity gates scan game_state)</verify>
  <done>Container round-trips losslessly; PURE_MODULES = 13; full suite green</done>
</task>

</tasks>

<verification>
python3.6 -m py_compile aamatch/*.py && python3.6 -m unittest discover -s tests -v
</verification>

<success_criteria>
SCORE-01 semantics implemented pure (fraction, binary per item, 'any' binary); GameState carries engine-mutable runtime state with no I/O; purity gates green.
</success_criteria>

<output>
After completion, create `.planning/phases/02-headless-game-engine/02-10-SUMMARY.md`
</output>
