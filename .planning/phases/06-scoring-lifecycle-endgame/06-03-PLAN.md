---
phase: 06-scoring-lifecycle-endgame
plan: 03
type: execute
wave: 2
depends_on: ["06-01"]
files_modified:
  - aamatch/engine.py
  - smoke/smoke_15_lifecycle.py
autonomous: true
must_haves:
  truths:
    - "A second Confirm/Skip record on the same molecule is REFUSED with a message naming the cause — the Phase-3 double-append caveat is closed at the engine record site"
    - "Skip stores the detection-at-skip-time partial score via the same record path + increments skip_count (02-10 sanctioned semantics)"
    - "Give Up ends the game: game_over, end_state 'gave_up', incremented giveup_count, frozen final_time, and the endgame summary returned"
    - "Level advance rebuilds the scene (cleanup + materialize L+1) and advances GameState atomically, WITHOUT touching the timer anchor"
    - "The endgame summary read op returns the 06-01 contract dict from the live game"
  artifacts:
    - path: "aamatch/engine.py"
      provides: "record_scored (guarded), skip_molecule, advance_molecule, advance_level, give_up, complete_game, total_score, is_over, endgame_summary + module _ligand_content store"
      contains: "def record_scored"
    - path: "smoke/smoke_15_lifecycle.py"
      provides: "SMOKE-15 PART A: cmd-tier lifecycle E2E (guard, skip, advance, give-up, complete, summary)"
      contains: "SMOKE-15"
  key_links:
    - from: "engine.record_scored"
      to: "GameState.has_record"
      via: "the ONE guard site for Confirm AND Skip"
      pattern: "has_record"
    - from: "engine.advance_level"
      to: "placement.cleanup_game_objects + engine.materialize"
      via: "cleanup BEFORE materialize; GameState.advance_level AFTER (pure data last)"
      pattern: "cleanup_game_objects"
    - from: "engine.give_up/complete_game"
      to: "game_state.endgame_summary"
      via: "summary returned as plain data through the op"
      pattern: "endgame_summary"
---

<objective>
Engine lifecycle ops (SCORE-01/03/05/06 mechanics): the one-record-guarded scoring op, skip, molecule/level advance, give-up/complete end-state ops, read accessors, and the endgame summary read op — proven headlessly by a NEW SMOKE-15 PART A.

Purpose: the engine is the lifecycle's brain (ARCHITECTURE Pattern 2); 06-05/06-06 wrap these ops in the wizard, 06-07..06-09 surface them on the tab.
Output: grown engine.py + smoke_15_lifecycle.py PART A.
</objective>

<execution_context>
@~/.config/opencode/get-shit-done/workflows/execute-plan.md
@~/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-scoring-lifecycle-endgame/06-01-SUMMARY.md
@.planning/phases/06-scoring-lifecycle-endgame/06-RESEARCH.md
@aamatch/engine.py
@aamatch/game_state.py
@aamatch/placement.py
@smoke/smoke_04_e2e.py

Anchors (verified):
- engine.confirm engine.py:462-469 stays BYTE-IDENTICAL (smoke_04/06/07 legitimately re-score molecule (0,0) as detection probes — smoke_04_e2e.py:265/372/434/471, smoke_06:500/548 — so the one-record guard must NOT live inside score_current/confirm; it lives in the NEW op the lifecycle uses).
- score_current engine.py:443-459 (record site); detect_molecule :370-401; new_game :239-300 (`_game = game_state.GameState()` at :295 — fresh state on restart); materialize :303-320 (delegates, keeps module `_payload`/`_registry`); reset_to_grid :343-352; game_status :472-478.
- engine does NOT retain ligand_content module-side today (only threaded as args — 04-04). engine.advance_level needs it for uploaded-game re-materialization -> store it.
- game_state.py (post-06-01): has_record(level, molecule), stop_timer(now=None) (sets game_over + final_time, anchor untouched), score_per_molecule keyed store, endgame_summary(molecule_counts) method, advance_molecule/advance_level (:226-233).
- placement.cleanup_game_objects placement.py:442-455 (prefix-only deletion); materialize builds every molecule of ONE level (placement.py:299-411).
- Cleanup-order hazard (documented residue, research pitfall register): cleanup deletes objects the live wizard's maps reference; a mid-op failure leaves a cleaned scene with a live wizard — clicks no-op via the empty-pk1 path; Restart is the recovery. Document in the advance_level docstring.
- PROSE_PIN: no docstring may add/remove banned-token mentions (get_model/matrix_reset/get_object_ttt) — tests/test_code_audit.py counts stay exact.
</context>

<tasks>

<task type="auto">
  <name>Task 1: guarded record op + skip + data reads</name>
  <files>aamatch/engine.py</files>
  <action>
    1. Add module global `_ligand_content = None` beside `_payload/_registry/_game` (engine.py:102-104 area); in new_game (after :293's global assignment) also `global _ligand_content; _ligand_content = ligand_content` (additive; demo games store None — byte-identical). Document: the retained ligand_content is advance_level's re-materialization input for uploaded games.
    2. `record_scored(level_index, molecule_index, required, records=None)`: the ONE lifecycle record site. Guard FIRST: `if _current_game().has_record(level_index, molecule_index): raise EngineError('This molecule already has a recorded result (scored or skipped) -- use Restart to replay the game.')` (Q10 refusal wording, D4 — pinned verbatim, covers Confirm AND Skip with one check). Then `if records is None: records = detect_molecule(...)`; then delegate to score_current(..., records=records) -> (value, formed). Docstring: the Phase-3 re-Confirm caveat (wizard.py:550-553) closes HERE.
    3. `skip_molecule(level_index, molecule_index, required)`: `value, formed = record_scored(...)` then `_current_game().skip_count += 1`; return (value, formed). Docstring cites the 02-10 sanctioned semantics (detection-at-skip-time partial score via the same record path; spec.md:44).
    4. `advance_molecule()`: `_current_game().advance_molecule()` — the data-only transition (the wizard rebinds its own books; research Q1 ownership split).
    5. Reads: `total_score()` -> `_current_game().total_score`; `is_over()` -> `_current_game().game_over` (cheap bool gates for the wizard; both EngineError via _current_game before new_game).
    6. Update the module docstring's op list (items after op 7).
  </action>
  <verify>`python3.6 -m py_compile aamatch/*.py`; `python3.6 -m unittest discover -s tests -v` ALL green (engine is cmd-tier — WSL untouched except the purity/audit gates); grep `grep -n "already has a recorded result" aamatch/engine.py` hits the guard.</verify>
  <done>record_scored refuses a second record with the pinned message; skip_molecule records + counts; advance_molecule/total_score/is_over exist as public ops.</done>
</task>

<task type="auto">
  <name>Task 2: advance_level + give_up/complete_game + endgame_summary read op</name>
  <files>aamatch/engine.py</files>
  <action>
    1. `advance_level()`: fail-closed ordering — `game = _current_game()`; if `game.game_over`: EngineError('engine.advance_level: the game is over'); `if _payload is None: raise EngineError(...no payload...)`; `nxt = game.current_level_index + 1`; `if nxt >= len(_payload['levels']): raise EngineError('engine.advance_level: no next level (%d of %d) -- the game should complete instead' % (nxt, len(_payload['levels'])))`. THEN: `placement.cleanup_game_objects()` -> `registry = materialize(_payload, nxt, ligand_content=_ligand_content)` -> `game.advance_level()` LAST (pure data cannot fail — a materialize failure leaves GameState consistent). Return the new registry. Docstring: the cleanup-order hazard residue + "timer anchor untouched by design (the game clock runs across levels)".
    2. `give_up(now=None)`: `game = _current_game()`; if `game.game_over`: EngineError('engine.give_up: the game is already over'); `game.giveup_count += 1`; `game.end_state = 'gave_up'`; `game.stop_timer(now)`; return `game.endgame_summary(self._molecule_counts())` where `_molecule_counts()` is a private helper `[(len(l['molecules'])) for l in _payload['levels']]` (payload None -> EngineError). The current molecule is NOT scored by give-up (spec.md:44-45 — partial store is Skip's only).
    3. `complete_game(now=None)`: same shape minus the counter: `game.end_state = 'completed'`; `game.stop_timer(now)`; return the summary. (Refuse when already over, same message class.)
    4. `endgame_summary()`: READ-only -> `_current_game().endgame_summary(self._molecule_counts())`; no mutation. (give_up/complete_game may delegate to this read after mutating.)
    5. Docstrings cite SCORE-05/06/07 + the 06-01 summary contract keys.
  </action>
  <verify>py_compile + full WSL suite green; grep `grep -n "def advance_level\|def give_up\|def complete_game\|def endgame_summary" aamatch/engine.py` shows all four.</verify>
  <done>advance_level atomically rebuilds level L+1 (cleanup -> materialize -> data advance) without touching the anchor; give_up/complete_game freeze the game and return the 06-01 summary dict; the read op exists.</done>
</task>

<task type="auto">
  <name>Task 3: SMOKE-15 creation + PART A (engine lifecycle E2E)</name>
  <files>smoke/smoke_15_lifecycle.py</files>
  <action>
    Create smoke/smoke_15_lifecycle.py in the SMOKE-04/08 house style (repo-root anchor via sys.argv first/cwd fallback, count-asserted checks, verdict line `=== SMOKE-15 %s ===`, restore blocks). PART A (ALWAYS tier — pure cmd drive):
    1. Baseline scene snapshot; `gamestart.start_game(activate=True)` (defaults; 2 molecules per level, 3 levels per setup_state.DEFAULTS difficulty 3 -> L*6 formula gives grid 3, types 1).
    2. Zero-score Confirm mechanics are sufficient for lifecycle asserts (document: score correctness is smoke_04's proven domain): `engine.record_scored(0, 0, required)` where required comes from the payload -> assert (0.0, formed []); `engine.is_over()` False; `engine.total_score()` 0.0.
    3. GUARD: second `engine.record_scored(0, 0, required)` raises EngineError with the EXACT pinned message ('already has a recorded result') — assert message text + GameState unchanged (molecule_scores len 1).
    4. `engine.skip_molecule(0, 1, required)` -> skip_count == 1, molecule_scores len 2.
    5. Molecule advance: `engine.advance_molecule()` -> current_molecule_index 2 -> EngineError on a THIRD advance_molecule? NO — advance_molecule is data-only (no range check; the wizard owns range logic) — document. Level advance: `engine.advance_level()` -> registry returned with level_index 1, fresh `_aam_*` names (count == level-1 molecule count), `engine.game_status()['current_level_index'] == 1`, current_molecule_index 0, and **timer_anchor UNCHANGED** (capture before/after — the D6 intent, engine-side half).
    6. give_up mid-level-1: anchor captured -> `summary = engine.give_up()` -> is_over True, end_state 'gave_up', giveup_count 1, final_time float >= 0, summary keys exactly the 06-01 contract (sorted equality), level_scores length == levels.
    7. Fresh game: `start_game(activate=True)` again -> game_status all-zero including the NEW fields (game_over False, end_state None, final_time None) -> complete_game() after recording all molecules via record_scored across levels (advance_molecule/advance_level drives) -> end_state 'completed', summary['molecules_completed'] == summary['molecules'], total == sum(level_scores).
    8. Restore: done pop + prefix cleanup -> baseline EXACTLY; `gamestart._last_start = None`.
    Keep the PART-letter structure so 06-05/06-06 append PART B/C.
  </action>
  <verify>`bash smoke/run_smoke.sh smoke/smoke_15_lifecycle.py` greps `=== SMOKE-15 PASS ===` with the PART A count printed and zero FAIL lines.</verify>
  <done>SMOKE-15 PART A proves the guarded record, skip counting, atomic level advance (anchor untouched), give-up/complete end states, and the summary contract — headlessly in real PyMOL.</done>
</task>

</tasks>

<verification>
- `python3.6 -m py_compile aamatch/*.py` && `python3.6 -m unittest discover -s tests -v` ALL green.
- `bash smoke/run_smoke.sh smoke/smoke_15_lifecycle.py` -> `=== SMOKE-15 PASS ===`.
- Regression: `bash smoke/run_smoke.sh smoke/smoke_04_e2e.py` still PASS (confirm/score_current byte-identical — the guard did NOT move into them).
- PROSE_PIN untouched (tests/test_code_audit.py green without edits).
</verification>

<success_criteria>
- One guard site covers Confirm+Skip re-record refusals; smoke_04/06/07 probe paths unaffected.
- Level advance = cleanup + materialize(L+1) + GameState.advance_level, anchor untouched, ligand_content retained for uploaded games.
- give_up/complete_game produce the exact 06-01 summary contract; SMOKE-15 PART A green.
</success_criteria>

<output>
After completion, create `.planning/phases/06-scoring-lifecycle-endgame/06-03-SUMMARY.md` (frontmatter: subsystem engine, provides the exact op signatures + refusal wording for 06-05/06-06/06-09).
</output>
