---
phase: 05-game-status-tab-start-sequence
plan: 08
type: execute
wave: 3
depends_on: ["05-02", "05-03", "05-06"]
files_modified: [aamatch/engine.py, aamatch/wizard.py, aamatch/game_window.py, smoke/smoke_11_window.py]
autonomous: true

must_haves:
  truths:
    - "The hint is CAPABILITY-LIVE: GameWizard.hint() computes candidates via capability.hint_candidate_slots(slots, live_ligand_profile, required) — NEVER reads slot['can_form'] (generation-time provenance, generator.py:453-456); DETECT-04 agreement by construction"
    - "THE TRAP IS CLOSED: _hint_impl registers EVERY candidate object into the ONE _color_store via wizard_core.ensure_snapshot BEFORE its first recolor (ensure_snapshot alone fires only in _select_slot today — without this, Done leaves hint colors stuck on hinted-never-selected objects, pitfall H-1)"
    - "Recolor = cmd.color(HINT_COLOR, '<obj> and elem C') per candidate object ONLY (redraw-safe per the 03-06 display-rebuild law — Executive.cpp:12440-12446 color-cache invalidate; never cmd.alter on the apply path, H-4; never unscoped 'elem C' — the ligand would recolor too, H-5)"
    - "Repeated presses are IDEMPOTENT (the candidate set is static within a molecule: resn x immutable profile x immutable required); hint-over-green coexists per-atom (orange carbons + green non-carbons, probe check 6); Done/switch/replace-on-restart restore is owned by the EXISTING cleanup via the shared store — zero new bookkeeping"
    - "The handler is the established contract: non-modal impl + _guard + isinstance gate with lazy relative import, SILENT no-op pre-GO (deferred-activation interplay, H-8); the empty-candidate case fail-closes into WizardError (solvability makes it unreachable — a silent no-op would hide a bug)"
  artifacts:
    - path: "aamatch/engine.py"
      provides: "ligand_profile_molecule(level_index, molecule_index) — mirrors detect_molecule's scoping (engine.py:361-392); recompute from the LIVE ligand object via _remap_ligand_bonds + capability.ligand_profile (byte-equal to generation-time; the ligand never changes during play)"
      contains: "def ligand_profile_molecule"
    - path: "aamatch/wizard.py"
      provides: "GameWizard.hint() -> _guard(self._hint_impl); returns plain data {'count': n, 'slot_ids': [...]}; NO new wizard attributes (contract 2)"
      contains: "def hint"
    - path: "aamatch/game_window.py"
      provides: "GameTab._guard (the 04-09 verbatim contract) + _on_hint/_hint_now + btn_hint connected + the info-box hint line 'Hint: %d eligible amino acid(s) highlighted.'"
      contains: "_hint_now"
    - path: "smoke/smoke_11_window.py"
      provides: "new hint PART before the Gate-A2 echo: pure-vs-cmd candidate agreement, per-object carbon-color asserts, idempotence, hint-over-green, whole-scene restore (incl. a hinted-never-selected object), pre-GO silent no-op via the dialog handler"
  key_links:
    - from: "aamatch/wizard.py _hint_impl"
      to: "aamatch/capability.hint_candidate_slots"
      via: "the pure battery's helpers (plan 05-02) consume the live profile (plan's engine op) — single typing home"
      pattern: "hint_candidate_slots"
    - from: "aamatch/wizard.py _hint_impl"
      to: "aamatch/wizard_core.ensure_snapshot + _color_store"
      via: "snapshot BEFORE recolor into the ONE store the existing cleanup restores (wizard.py:202-204)"
      pattern: "ensure_snapshot"
    - from: "aamatch/game_window.py _hint_now"
      to: "aamatch/wizard.py GameWizard.hint"
      via: "isinstance-gated dispatch; the tab logs the count line directly (no wizard round-trip)"
      pattern: "prior.hint()"
---

<objective>
The PLAY-05 Hint vertical slice: the cmd-tier capability op (live ligand profile + candidate computation + snapshot-before-recolor), the Qt button handler, and the headless color-mechanics proof.

Purpose: "Hint recolors carbon atoms of amino acids that could form one of the required interactions — recolor only, never lines/dots/geometry" (PLAY-05). The research's key finding: the predicate half ALREADY exists (capability.py:244-314); the design questions were where the ligand profile comes from (recompute live — byte-equal) and how recolor integrates with PLAY-01 bookkeeping (ONE store, snapshot BEFORE recolor — the (a) trap). This plan lands the whole slice in one coherent unit because the smoke PART proves the mechanics end-to-end.
Output: engine op + wizard method + handler + SMOKE-11 hint PART.
</objective>

<execution_context>
@~/.config/opencode/get-shit-done/workflows/execute-plan.md
@~/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/05-game-status-tab-start-sequence/05-RESEARCH-hint.md
@.planning/phases/04-qt-setup-window/04-09-SUMMARY.md
@.planning/STATE.md
@aamatch/engine.py
@aamatch/wizard.py
@aamatch/wizard_core.py
@aamatch/capability.py
@aamatch/game_window.py
@smoke/smoke_11_window.py
@smoke/smoke_07_wizard_loop.py

Key facts (verified, transcribe exactly):
- engine.ligand_profile_molecule (research C Pattern 2, verbatim shape): registry = _current_registry(); index guard (EngineError naming molecule_index + registry count); lig_object = molecules[index]['ligand'][0]; records = [r for r in geometry.extract_game_atoms() if r['object'] == lig_object]; lig_records, lig_bonds = _remap_ligand_bonds(records, [lig_object]); return capability.ligand_profile(lig_records, lig_bonds). Docstring: the 02-08 deviation-3 contract shape (engine.py:217); DETECT-04 by construction (the SAME capability.ligand_profile the generator consumed at generation time, generator._profile_of:655-676). Lazy sibling imports per the engine module law.
- wizard.py hint/_hint_impl (research C Pattern 3, verbatim shape): `def hint(self): self._guard(self._hint_impl)`; _hint_impl: lazy `from . import capability, engine`; required = self._required(); slots = self._payload['levels'][self._level_index]['molecules'][self._molecule_index]['grid']['slots']; profile = engine.ligand_profile_molecule(self._level_index, self._molecule_index); candidate_ids = capability.hint_candidate_slots(slots, profile, required); if not candidate_ids: raise WizardError('hint: no amino acid in the grid could form a required interaction (%s) -- the solvability guarantee was violated' % (required.get('mode'),)); for slot_id in candidate_ids: obj = self._objects_by_slot[slot_id]; wizard_core.ensure_snapshot(self._color_store, obj, self._atom_colors(obj)) THEN cmd.color(wizard_core.HINT_COLOR, '%s and elem C' % obj); cmd.refresh_wizard(); return {'count': len(candidate_ids), 'slot_ids': list(candidate_ids)}.
- ensure_snapshot is IDEMPOTENT (wizard_core.py:172-175: first call copies, later calls return False) — a slot first hinted then selected keeps its ORIGINAL pre-hint snapshot.
- wizard._guard (wizard.py:423-433): maps the ValueError family (EngineError/PlacementError/WizardError) to visible _error + refresh; unexpected exceptions propagate. The hint's WizardError lands on the wizard PANEL.
- GameTab._guard (the handler side — 04-09 verbatim contract): catch ONLY (ValueError, OSError) -> QtWidgets.QMessageBox.warning(self, 'AA-match', str(e)); anything else propagates. This is the tab's FIRST _guard (GameTab had none — the shell plan created no handlers).
- Handler (research C Pattern 4): _on_hint = thin _guard wrapper (NO box on success — the recolor IS the feedback, _on_start precedent setup_window.py:879-886); _hint_now: lazy `from pymol import cmd` + `from . import wizard as wizard_mod`; prior = cmd.get_wizard(); if not isinstance(prior, wizard_mod.GameWizard): return None (silent no-op pre-GO — H-8, PA-gui_game:142-145 precedent); result = prior.hint(); self._log('Hint: %d eligible amino acid(s) highlighted.' % result['count']); return result. Connect: self.btn_hint.clicked.connect(self._on_hint).
- PROSE_PIN / banned tokens: wizard.py + engine.py docstrings must add ZERO get_model/matrix_reset/get_object_ttt mentions (counts are exact); the hint does no atom-namespace double loops (the TOKEN-PAIR ban stays dormant).
- Color read-back recipe (pinned): cmd.iterate(obj, 'stored.append((ID, color))', space={'stored': rows}) then compare against cmd.get_color_index('orange') — wizard._atom_colors (wizard.py:208-215) + SMOKE-07's _color_map (smoke/smoke_07_wizard_loop.py:206-211) precedent; live-probed 14/14 PASS (tmp/probe_05_hint.py, 2026-09-18).
- SMOKE-11 conventions: insert the new hint PART before the Gate-A2 echo, re-letter the echo; the hint PART is T1a/T1b-safe (cmd-substance + dialog asserts); mind that PART G's rework (plan 05-09, NEXT wave) happens after — author this part against the CURRENT drive (start_game default activates; the deferred drive lands next wave).
</context>

<tasks>

<task type="auto">
  <name>Task 1: engine.ligand_profile_molecule</name>
  <files>aamatch/engine.py</files>
  <action>Add the op per the context facts (research C Pattern 2 verbatim shape) near detect_molecule (engine.py:361-392) so the scoping mirroring is obvious. Docstring: the live-recompute rationale (the engine does NOT retain the generation-time profile — ligand_data is a local in new_game, engine.py:275-278; the payload carries NO profile, level_spec.py:9-35); byte-equality argument (the ligand object is loaded once at materialize and never modified — movement is AA-only). Lazy sibling imports inside the body. Commit: `feat(05-08): engine.ligand_profile_molecule live-profile op`.
  </action>
  <verify>python3.6 -m py_compile aamatch/*.py && python3.6 -m unittest discover -s tests -v green (cmd tier: syntax + regression floor; the real proof rides the smoke PART)</verify>
  <done>The live-profile op exists, scoped to one molecule, mirroring detect_molecule</done>
</task>

<task type="auto">
  <name>Task 2: GameWizard.hint / _hint_impl — capability recolor with snapshot-before-color</name>
  <files>aamatch/wizard.py</files>
  <action>Add the methods per the context facts (research C Pattern 3 verbatim). Non-negotiables:
- ensure_snapshot BEFORE the first cmd.color per candidate (the (a) trap — comment it explicitly).
- Selection strings built ONLY from _objects_by_slot (molecule-scoped by construction — H-5).
- Empty candidates -> WizardError (fail-closed; unreachable by solvability).
- NO new wizard attributes (contract 2); candidates recomputed per press (static within a molecule — rule set (c)).
- Docstring: capability-live, never can_form; recolor-only (PLAY-04; the atom-color property is invisible to detection, geometry.py:134-138); returns plain data.
Commit: `feat(05-08): GameWizard.hint capability recolor`.
  </action>
  <verify>python3.6 -m py_compile aamatch/*.py && python3.6 -m unittest discover -s tests -v green (test_wizard_source.py's scans over wizard.py stay green: zero banned visual calls, zero banned-token mentions)</verify>
  <done>The wizard method exists with the trap fix and the _guard contract</done>
</task>

<task type="auto">
  <name>Task 3: GameTab handler + hint line + SMOKE-11 hint PART</name>
  <files>aamatch/game_window.py, smoke/smoke_11_window.py</files>
  <action>
1. game_window.py: add GameTab._guard (the 04-09 verbatim contract — first _guard on this class; comment cites it), _on_hint (thin _guard wrapper, no success box), _hint_now (isinstance gate + silent no-op pre-GO + prior.hint() + the pinned info-box line via self._log + return result), and the btn_hint connect in __init__ (replace the 'NOT connected' comment with the connect + a comment citing this plan).
2. smoke_11_window.py: insert ONE new hint PART before the Gate-A2 echo (re-letter the echo). Drive (mirrors the probe; the research's recipe steps 1-7):
   a. Pre-GO no-op via the dialog: gamestart.start_game(activate=False) + dlg.game_tab.start_countdown(wiz) -> dlg.game_tab._hint_now() returns None AND no atom color changed anywhere -> cancel_pending_start + cleanup (restore baseline).
   b. Main drive: `wiz = gamestart.start_game()` (default activates) -> expected candidates computed IN-SMOKE from the payload via the pure helpers (capability.hint_required_types + hint_candidate_slots over wiz._payload's molecule 0 slots + engine.ligand_profile_molecule(0, 0)) -> check the cmd path and the pure path AGREE after wiz.hint() (the returned slot_ids == the in-smoke tuple).
   c. Color asserts: per candidate object, every elem C atom color == cmd.get_color_index('orange') AND every non-carbon color unchanged from its pre-hint read (cmd.iterate recipe); per non-candidate object AND the ligand object: NO atom color changed.
   d. Re-press idempotence: second wiz.hint() -> color maps identical (static set).
   e. Hint-over-green: cmd.color('green', <one candidate object>) -> wiz.hint() -> carbons orange, non-carbons green on that object (probe check 6).
   f. Handler line: with the wizard still active, dlg.game_tab._hint_now() returns the dict AND _info_log.toPlainText() contains 'Hint: %d eligible amino acid(s) highlighted.' with the right count.
   g. Restore: canonical Done (cmd.set_wizard()) -> check EVERY snapshotted object's color map == its materialization colors — assert the WHOLE scene's color maps (H-1: the hinted-never-selected objects MUST be restored too) + _color_store cleared + cmd.get_wizard() is None + baseline scene exact.
   Update the header part list. Commit: `test(05-08): hint handler + SMOKE-11 hint part`.
  </action>
  <verify>bash smoke/run_smoke.sh smoke/smoke_11_window.py 120 — === SMOKE-11 PASS === with the hint part green and ALL prior parts still green; python3.6 -m unittest discover -s tests -v green; python3.6 -m py_compile aamatch/*.py clean</verify>
  <done>The hint recolors candidate carbons only, is idempotent, coexists with green, restores fully through the one store, and the tab handler no-ops pre-GO and logs the pinned line</done>
</task>

</tasks>

<verification>
- `python3.6 -m py_compile aamatch/*.py` — floor.
- `python3.6 -m unittest discover -s tests -v` — full WSL suite (incl. 05-02's battery + GEN-04 parity + the source scans).
- `bash smoke/run_smoke.sh smoke/smoke_11_window.py 120` — hint PART + all existing parts green.
- `bash smoke/run_smoke.sh smoke/smoke_08_starter.py 120` — seam regression.
- Grep aamatch/wizard.py for "can_form" — zero NEW reads (the pre-existing generator-side provenance note stays; the hint body must not touch it).
</verification>

<success_criteria>
- hint() returns {'count', 'slot_ids'} computed capability-live; carbons of every candidate recolor orange; nothing else changes.
- The (a) trap is closed: hinted-never-selected objects restore on Done (whole-scene color-map assert).
- The handler no-ops silently pre-GO and logs the pinned line post-GO.
- Repeated presses idempotent; hint-over-green coexists; restore via the ONE store.
</success_criteria>

<output>
After completion, create `.planning/phases/05-game-status-tab-start-sequence/05-08-SUMMARY.md`
</output>
