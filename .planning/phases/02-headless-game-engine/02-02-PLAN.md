---
phase: 02-headless-game-engine
plan: 02
type: tdd
wave: 1
depends_on: []
files_modified:
  - aamatch/vec3.py
  - aamatch/spatial.py
  - tests/test_vec3.py
  - tests/test_spatial.py
  - tests/test_purity.py
autonomous: true

must_haves:
  truths:
    - "3-vector math (add/sub/dot/cross/norm/dist/angle-at/plane-projection) works on plain float 3-tuples with stdlib math only"
    - "Cell-list candidate generation finds the same pair set as brute force on randomized clouds (>= 100 seeds) — pruning never misses a candidate"
    - "Both modules pass the purity gates (AST + clean subprocess) under python3.6"
  artifacts:
    - path: "aamatch/vec3.py"
      provides: "Pure 3-vector math on tuples"
      min_lines: 60
    - path: "aamatch/spatial.py"
      provides: "build_index + cross_pairs (cell-list) + brute_force_pairs oracle"
      min_lines: 60
    - path: "tests/test_spatial.py"
      provides: "Cell-list equivalence property test over >= 100 random seeds"
  key_links:
    - from: "aamatch/spatial.py"
      to: "aamatch/vec3.py"
      via: "dist() for pair filtering"
      pattern: "from .vec3 import"
    - from: "tests/test_purity.py"
      to: "PURE_MODULES"
      via: "append vec3 + spatial"
      pattern: "PURE_MODULES = \\['setup_state', 'level_spec', 'persistence', 'backup', 'paths', 'vec3', 'spatial'\\]"
---

<objective>
Build the detector's pure geometry primitives: `vec3.py` (3-vector math on tuples) and `spatial.py` (cell-list spatial pruning + brute-force oracle). These are the "vectorized, not loop-bound" foundation of DETECT-05's correctness half.

Purpose: The detector (02-06/07) consumes these instead of numpy — the purity gate forbids numpy in the pure layer; per-candidate math must be constant-time on tuples.
Output: Two new pure modules, registered in PURE_MODULES, with property-tested correctness.
</objective>

<execution_context>
@~/.config/opencode/get-shit-done/workflows/execute-plan.md
@~/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/02-headless-game-engine/02-RESEARCH-detection.md   # §5.1 module split (vec3/spatial), §5.2 pipeline, §6 test 5 (cell-list equivalence)
@.planning/research/PITFALLS.md                                      # Pitfall 3 (stdlib-only pure layer)
@aamatch/level_spec.py                                               # docstring pattern for pure modules (style reference)
@tests/test_purity.py                                                # PURE_MODULES + ALLOWED_STDLIB (Gate A/A2/B/D)
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: vec3.py — pure 3-vector math (RED -> GREEN -> REFACTOR)</name>
  <files>aamatch/vec3.py, tests/test_vec3.py</files>
  <action>
RED: write tests/test_vec3.py first covering, with exact expected values (hand-computed):
- `add(a,b)`, `sub(a,b)`, `scale(a,s)`, `dot(a,b)`, `cross(a,b)` on tuples, e.g. cross((1,0,0),(0,1,0)) == (0,0,1)
- `norm(a)` (length), `dist(a,b)`, `norm_squared` (sqrt-free variant used by hot loops), `unit(a)` (zero vector -> raises ValueError)
- `angle_at(p0, p1, p2)`: angle in radians at vertex p1 between (p0-p1) and (p2-p1); degenerate (zero-length arm) -> raises ValueError. Test the 90-degree and 180-degree cases.
- `plane_project(point, plane_point, unit_normal)` -> projected point on the plane (for offset tests: distance from original point to plane).
- All functions accept/return 3-tuples of floats; floats in, floats out.

GREEN: implement aamatch/vec3.py minimally: `import math` ONLY; every function is 1-3 lines of tuple arithmetic. NO classes, NO numpy, NO itertools (the recorded convention from the 02-01 gate doc: itertools stays OUT of ALLOWED_STDLIB — use comprehensions/explicit loops).

REFACTOR only if obvious duplication appears; tests must stay green.

Commit RED as `test(02-02): add failing vec3 tests`, GREEN as `feat(02-02): implement pure vec3 module`.
  </action>
  <verify>python3.6 -m unittest tests.test_vec3 -v  (all pass)</verify>
  <done>vec3 functions match hand-computed values; module imports with zero stubs under python3.6</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: spatial.py — cell-list pruning + brute-force oracle (RED -> GREEN -> REFACTOR)</name>
  <files>aamatch/spatial.py, tests/test_spatial.py</files>
  <action>
RED: write tests/test_spatial.py first:
- `cross_pairs(points_a, points_b, cutoff)` returns sorted list of (i, j) index pairs whose distance <= cutoff, where points are lists of (x,y,z) tuples. Hand-checkable case: a=[(0,0,0),(10,0,0)], b=[(1,0,0),(20,0,0)], cutoff=2.0 -> [(0,0)] only.
- Equivalence property test (the correctness half of DETECT-05, detection research §6.5): for 100 seeds, build two random clouds (seeded via random.Random(seed): 40 + 40 points in a 20-A box), assert `sorted(cross_pairs(a, b, 4.0)) == sorted(brute_force_pairs(a, b, 4.0))`. NEVER sample from a set (PYTHONHASHSEED) — use random.uniform on a fixed-order list.
- Boundary semantics: distance exactly == cutoff is INCLUDED (<=).
- Empty inputs -> [].
- Determinism: same inputs twice -> identical output list.

GREEN: implement aamatch/spatial.py:
- `build_index(points, cell)` -> dict mapping (i,j,k) int cell keys to lists of point indices; cell key = (int(x//cell), int(y//cell), int(z//cell)) — floor division handles negatives correctly.
- `cross_pairs(points_a, points_b, cutoff)`: bucket points_b with cell=cutoff; each point_a scans its own + 26 neighbor cells (3x3x3 via nested loops over dx,dy,dz in (-1,0,1) — NOT itertools.product, per the recorded convention); emit (i,j) when vec3.dist <= cutoff. Return sorted pairs.
- `brute_force_pairs(points_a, points_b, cutoff)`: O(N*M) double loop — the test-only oracle. Docstring MUST say "test oracle only — the detector must never call this outside tests" (the code audit in plan 02-15 greps for it).
- Imports: `math` NOT even needed here (vec3.dist does it) — only `from .vec3 import dist`. No other imports.

REFACTOR if the 27-cell scan has duplicated logic.

Commit RED then GREEN with scopes test(02-02)/feat(02-02).
  </action>
  <verify>python3.6 -m unittest tests.test_spatial -v  (all pass, incl. 100-seed equivalence)</verify>
  <done>cross_pairs == brute_force_pairs on 100 random seeds; pair generation is cell-list based (no full double loop in cross_pairs)</done>
</task>

<task type="auto">
  <name>Task 3: Register both modules in the purity gate</name>
  <files>tests/test_purity.py</files>
  <action>
Append 'vec3' and 'spatial' to PURE_MODULES in tests/test_purity.py so Gates A/B scan them. Do NOT touch ALLOWED_STDLIB (the recorded 02-01 decision: no itertools, no new roots — vec3 uses math [already whitelisted], spatial uses only vec3). Then run the FULL suite — this is the first registration of the phase; every later pure-module plan repeats this exact pattern for its own module.
  </action>
  <verify>python3.6 -m unittest discover -s tests -v  (FULL suite green incl. purity gates now scanning vec3+spatial)</verify>
  <done>Pure module count in PURE_MODULES is 7; Gate A/B/D green over the new modules</done>
</task>

</tasks>

<verification>
python3.6 -m py_compile aamatch/*.py && python3.6 -m unittest discover -s tests -v
</verification>

<success_criteria>
- vec3 + spatial exist, pure (Gate A/B green, no stubs), 100-seed cell-list equivalence proven
- PURE_MODULES = the 5 Phase-1 modules + vec3 + spatial
- No itertools anywhere in the new modules (grep returns nothing)
</success_criteria>

<output>
After completion, create `.planning/phases/02-headless-game-engine/02-02-SUMMARY.md`
</output>
