---
phase: 05-game-status-tab-start-sequence
plan: 03
type: tdd
wave: 1
depends_on: []
files_modified: [aamatch/wizard_core.py, tests/test_wizard_core.py]
autonomous: true

must_haves:
  truths:
    - "HINT_COLOR == 'orange' is a named constant in the PURE zero-imports module, next to HIGHLIGHT_COLOR 'green' — the hint color has ONE home and is pinned by test (05-RESEARCH-hint.md standard_stack + Q3)"
    - "'orange' is a registered named color on this PyMOL 2.5.0 build (Color.cpp:1039; live probe index 13) and is distinct from the green selection highlight and from default element coloring"
  artifacts:
    - path: "aamatch/wizard_core.py"
      provides: "HINT_COLOR = 'orange' beside HIGHLIGHT_COLOR (wizard_core.py:53)"
      contains: "HINT_COLOR"
    - path: "tests/test_wizard_core.py"
      provides: "the exact-constants battery gains the HINT_COLOR == 'orange' pin"
  key_links:
    - from: "tests/test_wizard_core.py"
      to: "aamatch/wizard_core.HINT_COLOR"
      via: "exact-constant pin (the same battery style that pins NUDGE_STEP/HIGHLIGHT_COLOR)"
      pattern: "HINT_COLOR"
---

<objective>
Pin the hint color BEFORE the recolor code exists: one named constant in the pure zero-imports module plus its exact-constants test pin, RED-first.

Purpose: `GameWizard._hint_impl` (plan 05-08) will recolor with `cmd.color(wizard_core.HINT_COLOR, '<obj> and elem C')`. Landing the constant as its own micro-plan keeps the wizard.py plan purely about mechanics, and the pin test makes any silent color change a test failure (the human may revisit the color at the Phase-5 checkpoint — that becomes a one-constant edit + one test update, never a hunt).
Output: `HINT_COLOR = 'orange'` in wizard_core.py + the test pin.
</objective>

<execution_context>
@~/.config/opencode/get-shit-done/workflows/execute-plan.md
@~/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/05-game-status-tab-start-sequence/05-RESEARCH-hint.md
@.planning/STATE.md
@aamatch/wizard_core.py
@tests/test_wizard_core.py

Key facts (verified, transcribe exactly):
- wizard_core.py is a ZERO-imports PURE module (PURE_MODULES member); a string constant needs no import (research C gate-impact: "a string constant needs nothing").
- HIGHLIGHT_COLOR = 'green' sits at wizard_core.py:53 (the PLAY-01 feedback color, v1 game.py:208-213 precedent). HINT_COLOR goes directly beside it.
- 'orange' prior art: [PA-OBS] PA-game:12 shipped 'orange' for hints ("distinct from green=found"); registered in the C layer (pymol-src/layer1/Color.cpp:1039 `reg_named_color("orange", 1.F, 0.5F, 0.F)`); live-probed on this build (tmp/probe_05_hint.py check 3: index 13, 14/14 PASS).
- Alternatives verified present if the human later prefers: magenta (8), salmon (9), pink (48) — recorded in the research; NOT built now.
- python3.6 floor; PROSE_PIN discipline (no banned-token mentions).
</context>

<feature>
  <name>HINT_COLOR constant + exact-constants pin</name>
  <files>aamatch/wizard_core.py, tests/test_wizard_core.py</files>
  <behavior>
- tests/test_wizard_core.py gains a pin (in the exact-constants battery area that already pins NUDGE_STEP/ROTATE_*/HIGHLIGHT_COLOR): `wizard_core.HINT_COLOR == 'orange'` and `isinstance(wizard_core.HINT_COLOR, str)`.
- RED: the test fails with AttributeError (no HINT_COLOR yet).
- GREEN: the constant exists beside HIGHLIGHT_COLOR with a comment carrying the provenance (Color.cpp:1039 named color; PA-game:12 prior-art hint color; distinct from HIGHLIGHT_COLOR 'green').
- wizard_core.py stays ZERO-imports (grep: no new import lines).
  </behavior>
  <implementation>
- wizard_core.py: add one constant line + provenance comment after HIGHLIGHT_COLOR (wizard_core.py:53). Nothing else moves.
- tests/test_wizard_core.py: add the pin to the existing constants test (or a one-test class beside it, matching the file's style).
  </implementation>
</feature>

<tasks>

<task type="tdd-red">
  <name>Task 1 (RED): the HINT_COLOR pin</name>
  <files>tests/test_wizard_core.py</files>
  <action>Add the pin test (exact string 'orange', matching the existing constants-battery style). Run — MUST fail with AttributeError. Commit: `test(05-03): add failing HINT_COLOR pin`.
  </action>
  <verify>python3.6 -m unittest tests.test_wizard_core -v exits nonzero naming HINT_COLOR (RED proven)</verify>
  <done>The pin exists and fails for the RIGHT reason</done>
</task>

<task type="tdd-green">
  <name>Task 2 (GREEN): the constant</name>
  <files>aamatch/wizard_core.py</files>
  <action>Add `HINT_COLOR = 'orange'` beside HIGHLIGHT_COLOR with the provenance comment (named color Color.cpp:1039; v1 PA-game:12 hint precedent; distinct from green). Run the test — MUST pass. Commit: `feat(05-03): HINT_COLOR orange constant`.
  </action>
  <verify>python3.6 -m unittest tests.test_wizard_core -v green; python3.6 -m py_compile aamatch/*.py clean; grep -c '^import\|^from' aamatch/wizard_core.py — unchanged (zero module-level imports)</verify>
  <done>HINT_COLOR pinned 'orange'; wizard_core still zero-imports; full constants battery green</done>
</task>

</tasks>

<verification>
- `python3.6 -m py_compile aamatch/*.py` — floor.
- `python3.6 -m unittest discover -s tests -v` — full suite green (wizard_core purity gates untouched: no new imports).
</verification>

<success_criteria>
- HINT_COLOR = 'orange' exists in the single pure home, pinned by test, beside HIGHLIGHT_COLOR.
- The human checkpoint (05-11) can adjust the color as a one-constant edit with a one-line test update.
</success_criteria>

<output>
After completion, create `.planning/phases/05-game-status-tab-start-sequence/05-03-SUMMARY.md`
</output>
