---
phase: 08-demo-curation-citations
plan: 05
type: execute
wave: 3
depends_on:
  - "08-04"
files_modified:
  - aamatch/data/MANIFEST.json
  - aamatch/data/ligands/demo-easy-1-aspirin.sdf
  - aamatch/data/ligands/demo-easy-1-benzoic-acid.sdf
  - aamatch/data/ligands/demo-easy-2-citric-acid.sdf
  - aamatch/data/ligands/demo-easy-2-acetate.sdf
  - aamatch/data/ligands/demo-easy-3-caffeine.sdf
  - scripts/demo_specs/demo-easy-1.json
  - scripts/demo_specs/demo-easy-2.json
  - scripts/demo_specs/demo-easy-3.json
autonomous: true
must_haves:
  truths:
    - "demo-easy-1, demo-easy-2, demo-easy-3 ship pre-downloaded single-record SDFs whose derived counts/sha256 match MANIFEST.json exactly (SMOKE-02 real loads prove it)"
    - "Each set's spec records per-entry provenance (CID, fetch URL, PDB complex + DOIs, PLIP ref, protonation why, license) transcribed from APPROVED 08-PROPOSALS.md rows only"
    - "MANIFEST.json regenerated via the committed builder (set-level license/provenance/tier/title filled); demo-dev-1's set payload byte-identical; never hand-edited"
    - "Each set committed atomically (SDFs + MANIFEST.json + spec in one commit per set)"
  artifacts:
    - path: "aamatch/data/MANIFEST.json"
      provides: "manifest_version 1 container with 4 sets (dev + 3 easy), curated sets carrying real license/provenance"
      contains: "demo-easy-1"
    - path: "scripts/demo_specs/demo-easy-1.json"
      provides: "Machine-readable approved curation input for demo-easy-1"
      contains: "demo-easy-1"
    - path: "scripts/demo_specs/demo-easy-2.json"
      provides: "Machine-readable approved curation input for demo-easy-2"
      contains: "demo-easy-2"
    - path: "scripts/demo_specs/demo-easy-3.json"
      provides: "Machine-readable approved curation input for demo-easy-3"
      contains: "demo-easy-3"
  key_links:
    - from: "scripts/demo_specs/*.json"
      to: "08-PROPOSALS.md approved rows"
      via: "set_ids/entry_ids/CIDs/URLs/PDB complexes transcribed verbatim from the approved proposal"
      pattern: "pubchem\\.ncbi\\.nlm\\.nih\\.gov"
    - from: "aamatch/data/MANIFEST.json"
      to: "aamatch/data/ligands/demo-easy-*.sdf"
      via: "sha256 + derived counts pinned by the builder in the same run"
      pattern: "sha256"
    - from: "SMOKE-02"
      to: "MANIFEST.json"
      via: "every new entry load-verified in real PyMOL BEFORE its commit"
      pattern: "SMOKE-02"
---

<objective>
Bundle the three Easy demo sets (GEN-06 tier slots: Easy ×3) through the committed 08-01 pipeline, one atomic commit per set, each gated by a SMOKE-02 run before commit.

Purpose: first curated content lands under the approved GATE — proving the propose → approve → fetch/commit protocol end-to-end on the simplest tier.
Output: 5 SDFs + 3 specs + regenerated MANIFEST.json (4 sets total), all committed atomically per set.
</objective>

<execution_context>
@~/.config/opencode/get-shit-done/workflows/execute-plan.md
@~/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/08-demo-curation-citations/08-PROPOSALS.md
@.planning/phases/08-demo-curation-citations/08-RESEARCH-sourcing.md
@.planning/phases/08-demo-curation-citations/08-RESEARCH-mechanics.md
@scripts/build_demos.py
@aamatch/data/MANIFEST.json
@tests/test_demo_data.py

**GATE precondition:** 08-PROPOSALS.md carries the recorded human approval. If it does not, STOP — this plan must not run.

Sets (from the approved proposal; tier 'easy'):
- demo-easy-1 'Everyday organics': aspirin (PubChem CID 2244) + benzoic-acid (CID 3979)
- demo-easy-2 'Metabolites & ions': citric-acid (CID 311) + acetate (CID 175, formal charge −1)
- demo-easy-3 'Caffeine': caffeine (CID 2519)
Fetch URL: `https://pubchem.ncbi.nlm.nih.gov/rest/pug/compound/cid/<CID>/SDF?record_type=3d` (WSL TLS to PubChem probe-verified 2026-09-24).

**BINDING LAWS (repeat per task):** NEVER hand-edit an SDF after manifest creation — regenerate via the builder. NEVER hand-edit MANIFEST.json. NEVER substitute a molecule beyond the approved proposal's listed alternates. If a fetch fails (non-200, multi-record, counts-line mismatch vs the proposal's recorded evidence): STOP that task, record the failure in the SUMMARY, and surface it — an approved ALTERNATE may be substituted only with the substitution recorded in the spec notes + SUMMARY as a deviation; anything else needs a new human decision.
</context>

<tasks>

<task type="auto">
  <name>Task 1: bundle demo-easy-1 (aspirin + benzoic acid)</name>
  <files>scripts/demo_specs/demo-easy-1.json, aamatch/data/ligands/demo-easy-1-aspirin.sdf, aamatch/data/ligands/demo-easy-1-benzoic-acid.sdf, aamatch/data/MANIFEST.json</files>
  <action>
1. Write `scripts/demo_specs/demo-easy-1.json` per the 08-01 spec schema: set_id 'demo-easy-1', tier 'easy', title from the approved proposal, license 'Public domain (NCBI PubChem)', set-level provenance {source_url: the PubChem REST base, license_url: 'https://www.ncbi.nlm.nih.gov/home/about/policies/', fetched: <today>, notes: <rationale from the proposal>}; two entries (aspirin CID 2244, benzoic-acid CID 3979) each with url (record_type=3d), protonation 'as-recorded', expect_atom_count from the proposal's recorded counts-line evidence, and the per-entry provenance dict (source_url, pdb_id 1OXR for aspirin / the proposal's pinned complex for benzoic-acid, entry_doi, paper_doi, plip ref, notes) transcribed from the APPROVED row.
2. Run `python3.6 scripts/build_demos.py --spec scripts/demo_specs/demo-easy-1.json --fetch` — the builder downloads both SDFs, refuses multi-record, derives the 14 keys, regenerates MANIFEST.json (demo-dev-1 payload untouched), re-parses OK.
3. Run `bash smoke/run_smoke.sh smoke/smoke_02_manifest.py 180` — every entry including the two new ones load-verifies (atoms/bonds multiset/charge/states/sha256/flags) in real PyMOL.
4. Run `python3.6 -m unittest tests.test_demo_data -v` — the data battery auto-covers the new set (curated license/provenance/tier + spec coverage now enforced for demo-easy-1).
5. Commit atomically: the 2 SDFs + MANIFEST.json + the spec — `feat(08-05): bundle demo-easy-1 (aspirin + benzoic acid)`.
  </action>
  <verify>bash smoke/run_smoke.sh smoke/smoke_02_manifest.py 180 (greps '=== SMOKE-02 PASS ===') && python3.6 -m unittest tests.test_demo_data -v (green) && python3.6 -c "import json,sys; c=json.load(open('aamatch/data/MANIFEST.json')); print(len(c['data']['sets']))" (4 sets)</verify>
  <done>demo-easy-1 committed atomically; SMOKE-02 + data battery green with the set present; dev set untouched.</done>
</task>

<task type="auto">
  <name>Task 2: bundle demo-easy-2 (citric acid + acetate)</name>
  <files>scripts/demo_specs/demo-easy-2.json, aamatch/data/ligands/demo-easy-2-citric-acid.sdf, aamatch/data/ligands/demo-easy-2-acetate.sdf, aamatch/data/MANIFEST.json</files>
  <action>
Same procedure as Task 1 for demo-easy-2: citric-acid (CID 311, neutral free acid per the ratified decision — its COOH groups are NOT salt-bridge-typed; the set's salt-bridge slot is acetate) + acetate (CID 175, formal_charge_sum expected −1 — the builder's M CHG derivation must yield −1; if the fetched record is the neutral acid instead, the charge check vs the proposal evidence fails: STOP and re-examine, do not proceed). Note: acetate's FILE is `ligands/demo-easy-2-acetate.sdf` (set-scoped name; the dev set's legacy `ligands/acetate.sdf` is a different, script-built file and must remain untouched). SMOKE-02 → data battery → atomic commit `feat(08-05): bundle demo-easy-2 (citric acid + acetate)`.
  </action>
  <verify>bash smoke/run_smoke.sh smoke/smoke_02_manifest.py 180 (PASS) && python3.6 -m unittest tests.test_demo_data -v (green) && python3.6 -c "import json; c=json.load(open('aamatch/data/MANIFEST.json')); s=[x for x in c['data']['sets'] if x['set_id']=='demo-easy-2'][0]; print([e['formal_charge_sum'] for e in s['entries']])" (includes -1)</verify>
  <done>demo-easy-2 committed atomically; acetate's −1 charge manifest-verified; legacy dev acetate.sdf untouched.</done>
</task>

<task type="auto">
  <name>Task 3: bundle demo-easy-3 (caffeine) + full-suite regression</name>
  <files>scripts/demo_specs/demo-easy-3.json, aamatch/data/ligands/demo-easy-3-caffeine.sdf, aamatch/data/MANIFEST.json</files>
  <action>
Same procedure for demo-easy-3: caffeine (CID 2519, single entry — the set's PDB complex anchor 3G6M confirmed in the proposal). SMOKE-02 → data battery → atomic commit `feat(08-05): bundle demo-easy-3 (caffeine)`. Then run the FULL WSL suite (`python3.6 -m unittest discover -s tests -v`) — the dropdown grouping (08-02) and de-hardcoded smokes (08-03) must hold with 4 sets / 7 entries present, and `scripts/build_demos.py --report-only` must exit 0 (specs ↔ manifest consistency).
  </action>
  <verify>bash smoke/run_smoke.sh smoke/smoke_02_manifest.py 180 (PASS) && python3.6 -m unittest discover -s tests -v (ALL green) && python3.6 scripts/build_demos.py --report-only (exit 0)</verify>
  <done>Easy tier complete (3 sets, 5 ligands) committed atomically; full suite green; report-only consistent.</done>
</task>

</tasks>

<verification>
- `python3.6 -m py_compile aamatch/*.py scripts/build_demos.py` — syntax floor.
- `python3.6 -m unittest discover -s tests -v` — full suite incl. purity gates.
- `bash smoke/run_smoke.sh smoke/smoke_02_manifest.py 180` — every entry of all 4 sets load-verifies.
- `python3.6 scripts/build_demos.py --spec scripts/demo_specs/demo-dev-1.json --dry-run` — dev set still reproduces exactly (proves the multi-set assembly never disturbs other sets).
- MANIFEST.json container still version 1, kind 'manifest'; manifest_version 1 (frozen schema untouched).
</verification>

<success_criteria>
- 3 curated easy sets committed, one commit per set, each gated by SMOKE-02 before commit.
- All fetched files single-record with derived counts matching the proposal's recorded evidence (expect_atom_count guards passed).
- Set-level license/provenance/title/tier real and data-battery-enforced; GEN-06 Easy ×3 slots filled.
</success_criteria>

<output>
After completion, create `.planning/phases/08-demo-curation-citations/08-05-SUMMARY.md`
</output>
