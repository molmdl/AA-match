---
phase: 04-qt-setup-window
plan: 08
type: execute
wave: 4
depends_on: ["04-04", "04-06"]
files_modified: [aamatch/upload.py, smoke/smoke_12_upload_e2e.py]
autonomous: true

must_haves:
  truths:
    - "prepare_uploaded_set turns split record strings into (rows, ligand_content) with one temp-load extraction per record, zero scene residue, and every row manifest-shaped with synthetic 'uploads/mol-00N.<ext>' keys"
    - "The read_sdfstr/read_mol2str parity with file loads is empirically proven in this build (atom/bond/charge counts identical) -- research OQ2 closed"
    - "A 2-segment MOL2 with a leading comment line is probed; the mechanical fallback rule applies on failure (multi-segment MOL2 refused with a message naming file + record count)"
    - "EXT-04 stays out: only the fail-closed minimum (format whitelist, non-empty split, single state per record, count >= 1, sha256 integrity, manifest-shaped row) is enforced"
  artifacts:
    - path: "aamatch/upload.py"
      provides: "cmd-tier prepare_uploaded_set(records, fmt) using the _ligand_data_for temp-object discipline"
      min_lines: 60
    - path: "smoke/smoke_12_upload_e2e.py"
      provides: "SMOKE-12: read/split/prepare/parity/MOL2-prove in real headless PyMOL + per-record timing print"
  key_links:
    - from: "aamatch/upload.py"
      to: "aamatch/game_file.py"
      via: "build_uploaded_row + validate_uploaded_rows (pure helpers do the row shaping; cmd tier only extracts)"
      pattern: "build_uploaded_row|validate_uploaded_rows"
    - from: "aamatch/upload.py"
      to: "aamatch/engine._remap_ligand_bonds"
      via: "bond remap reuse (02-09-probed walk-position mapping, nothing manifest-specific)"
      pattern: "_remap_ligand_bonds"
    - from: "aamatch/upload.py"
      to: "cmd.read_sdfstr / cmd.read_mol2str"
      via: "per-record string loads into get_unused_name('_aam_tmp') with delete-in-finally"
      pattern: "get_unused_name\\('_aam_tmp'\\)"
---

<objective>
Create the cmd-tier upload extraction module `aamatch/upload.py` and prove it end-to-end headlessly with SMOKE-12 (research upload_pipeline Steps 1-3 made executable): per-record temp-load extraction (records + bounding sphere + bond remap + capability profile), pure row building, and the empirical probes (parity, MOL2 multi-segment).

Purpose: this is the bridge between the pure split/row helpers (04-06) and the engine's ligand_content pipe (04-04). After this plan, an uploaded FILE can become exactly the `(candidates rows, ligand_content dict)` pair that `engine.new_game` and `start_game` already accept -- the window handler (04-10) is then pure glue.
Output: upload.py + SMOKE-12 PASS (+ probe outcomes recorded).
</objective>

<execution_context>
@~/.config/opencode/get-shit-done/workflows/execute-plan.md
@~/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/04-qt-setup-window/04-RESEARCH-export-upload.md
@.planning/STATE.md
@aamatch/upload.py
@aamatch/engine.py
@aamatch/geometry.py
@aamatch/capability.py
</context>

<decisions>
- MOL2 fallback rule (Decision 14): implement multi-segment support; SMOKE-12 probes a 2-segment MOL2 with a leading comment. If the probe section fails, prepare_uploaded_set switches to the documented fail-closed fallback -- refuse multi-segment MOL2 (message names the file + record count) -- and the smoke asserts THAT refusal instead. The decision rule is mechanical (probe outcome), not interpretive.
- OQ3 (MOL2 formal-charge category mapping) is informational-only and NOT probed here (structure fallbacks exist, capability.py:572-613); noted in the SUMMARY as open-documentation.
- Per-record extraction copies the proven `_ligand_data_for` discipline (engine.py:153-180): fresh `get_unused_name('_aam_tmp')`, extract scoped to this object, delete in finally, scene-unchanged assert at the end.
</decisions>

<tasks>

<task type="auto">
  <name>Task 1: aamatch/upload.py -- prepare_uploaded_set</name>
  <files>aamatch/upload.py</files>
  <action>
Create `aamatch/upload.py` (cmd tier -- module-level `from pymol import cmd` is legal here, exactly like engine.py/placement.py; docstring opens with the layer declaration "Layer: CMD TIER (never PURE_MODULES); per-record uploaded-molecule extraction..."; NO banned tokens):

`prepare_uploaded_set(records, fmt) -> (rows, ligand_content)`:
1. Validate fmt in ('sdf', 'mol2') else ValueError fail-closed.
2. Loop over records with index i:
   - `tmp = cmd.get_unused_name('_aam_tmp')`; in a try: `cmd.read_sdfstr(record, tmp)` or `cmd.read_mol2str(record, tmp)`; `if cmd.count_states(tmp) != 1: raise ValueError('upload record %d decoded to %d states (expected 1) -- the file may smuggle nested records' % (i + 1, n))`; `records_i = [r for r in geometry.extract_game_atoms() if r['object'] == tmp]`; `if not records_i: raise ValueError('upload record %d produced 0 atoms' % (i + 1))`; `center, radius = geometry.bounding_sphere(records_i)` (bounding result intentionally UNUSED for upload rows -- extraction parity with engine; keep the call ONLY if cheap? NO -- skip bounding_sphere here: rows need counts/flags, not geometry; the engine recomputes geometry from ligand_content at new_game time via _ligand_data_for. DO NOT call bounding_sphere in upload.py -- avoid dead work.); `lig_records, lig_bonds = engine._remap_ligand_bonds(records_i, [tmp])`; `profile = capability.ligand_profile(lig_records, lig_bonds)`.
   - finally: delete tmp if present; track the object-list snapshot before the loop and assert equality after EVERY iteration (temp never leaks, engine.py:173-180 pattern).
   - row = `game_file.build_uploaded_row(records_i, lig_bonds, profile, record_text=record, fmt=fmt, index=i)`; ligand_content[row['file']] = record.
3. `game_file.validate_uploaded_rows(rows)` once at the end (fail-closed before anything downstream).
4. Return (rows, ligand_content).
Sibling imports: module-level `from . import geometry, capability` is NOT safe for identity? geometry/capability are pure-free of cmd? geometry is CMD tier (imports pymol) but identity-safe as a plain module -- still, follow the house rule: lazy `from . import geometry, capability, engine, game_file` INSIDE the function (wizard.py:22-29 discipline, uniform).
Keep 3.6 syntax, %-format, no logging. Commit: `feat(04-08): upload.py per-record extraction pipeline`.
  </action>
  <verify>T0: `python3.6 -m py_compile aamatch/upload.py`; full WSL suite green (source gates only). Static: lazy sibling imports inside the function; temp-object delete in finally; no bounding_sphere call.</verify>
  <done>prepare_uploaded_set implemented on the proven extraction discipline.</done>
</task>

<task type="auto">
  <name>Task 2: SMOKE-12 -- parity probe, multi-record SDF E2E, MOL2 2-segment probe</name>
  <files>smoke/smoke_12_upload_e2e.py</files>
  <action>
Write `smoke/smoke_12_upload_e2e.py` (marker `=== SMOKE-12 PASS ===`; run `bash smoke/run_smoke.sh smoke/smoke_12_upload_e2e.py 240`). Parts:

- PART 1 (parity probe, research OQ2): `hasattr(cmd, 'read_sdfstr')` guard (abort with a clear FAIL line if absent). Load `aamatch/data/ligands/acetate.sdf` TWICE: once via `cmd.load(winpath, name_a)` (to_windows_path routing) and once via `cmd.read_sdfstr(file_text, name_b)` into fresh unused names; assert identical: count_atoms, count_states, and the `engine._remap_ligand_bonds` bond multiset (order-MULTISET comparison, SMOKE-02 convention) and formal-charge sums over extracted records. Delete both.
- PART 2 (multi-record SDF E2E): text = benzamide.sdf text + acetate.sdf text concatenated (2 records); records = game_file.split_sdf_records(text); check_upload_supply; rows, content = upload.prepare_uploaded_set(records, 'sdf'); assert len(rows) == 2; entry_ids 'mol-001'/'mol-002'; content keys 'uploads/mol-001.sdf'/'uploads/mol-002.sdf'; each row sha256 == sha256 of its record text; validate passes. THEN the full flow: `engine.new_game(validate_state(DEFAULTS), 999, candidates=rows, ligand_content=content)` + `engine.materialize(payload, 0, ligand_content=content)` -- asserts objects exist and counts match rows' atom_count. Done + cleanup + scene baseline assert.
- PART 3 (timing print, Decision 8 evidence): time prepare_uploaded_set over the 2 records (time.time() deltas), print 'upload extraction: %d records in %.3f s (per-record %.4f s)' -- the recorded datum for the cap value.
- PART 4 (MOL2 2-segment probe): build a minimal 2-segment MOL2 STRING in-script (each segment: `@<TRIPOS>MOLECULE` + name line + `@<TRIPOS>ATOM` with 2 atoms + `@<TRIPOS>BOND` with 1 bond; leading comment line before the FIRST segment). Split with game_file.split_mol2_segments -> 2 segments; try prepare_uploaded_set(segments, 'mol2'):
  - SUCCESS path: rows/content built (2 rows, .mol2 keys); materialize one level from them via new_game+candidates+ligand_content; cleanup; print 'MOL2-MULTI OK (2-segment split+load verified)'.
  - FAILURE path (read_mol2str cannot handle the segments): catch the exception, print 'MOL2-MULTI UNVERIFIED (<reason>) -- activating fallback'. This branch is MECHANICAL, two steps, both REQUIRED: (1) the EXECUTOR edits prepare_uploaded_set to enforce the fail-closed fallback -- refuse multi-segment MOL2 with FormatError 'upload %r carries %d MOL2 molecules -- this build accepts single-molecule MOL2 files only' (message names the file + record count); (2) the EXECUTOR then EDITS PART 4 itself so the fallback path ASSERTS the refusal exactly the way PART 2 asserts success: expect the ValueError-family exception from prepare_uploaded_set, assert str(exception) equals the verbatim refusal text (test file name + record count 2 substituted), print 'MOL2-MULTI REFUSED (fallback pinned)'. On the fallback path `=== SMOKE-12 PASS ===` is printed ONLY after that assert passes -- a catch-and-print 'UNVERIFIED' without the pinned-refusal assert is a FAILING smoke (the Decision-14 refusal must be pinned by an assert, never just observed). Commit any fallback edit separately: `fix(04-08): MOL2 multi-segment fallback per SMOKE-12 probe`.
- Print `=== SMOKE-12 PASS ===` (both paths reach it -- SUCCESS via the PART 4 build/materialize asserts, FAILURE via the pinned-refusal assert; the probe OUTCOME line is printed above it).
  </action>
  <verify>`bash smoke/run_smoke.sh smoke/smoke_12_upload_e2e.py 240` prints `=== SMOKE-12 PASS ===` plus the parity line, the E2E asserts, the timing datum, and a definite MOL2-MULTI verdict line (OK, or REFUSED with the fallback refusal pinned by assert). T0: py_compile; full WSL suite green.</verify>
  <done>The cmd-tier upload pipeline is proven on real headless PyMOL for SDF (E2E) and empirically probed for MOL2 (mechanical fallback if needed); research OQ1/OQ2 closed with recorded outcomes.</done>
</task>

</tasks>

<verification>
- T0: py_compile upload.py; full WSL suite green.
- T1a: SMOKE-12 PASS with parity asserts (atom/bond/charge identical file-vs-string), 2-record SDF E2E through new_game+materialize+cleanup, timing print, MOL2 probe verdict.
</verification>

<success_criteria>
An uploaded file text can be turned into generator-ready (rows, ligand_content) entirely by proven code: pure split + cmd extraction + pure rows, with scene hygiene (temp discipline) and the empirical probes recorded. The window handler now only orchestrates.
</success_criteria>

<output>
After completion, create `.planning/phases/04-qt-setup-window/04-08-SUMMARY.md` (frontmatter: affects [04-10 upload handler], subsystem cmd-tier upload; include the MOL2-MULTI verdict and timing datum verbatim).
</output>
