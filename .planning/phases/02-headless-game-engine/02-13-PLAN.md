---
phase: 02-headless-game-engine
plan: 13
type: execute
wave: 6
depends_on: ["02-04", "02-05", "02-08", "02-09"]
files_modified:
  - aamatch/placement.py
  - smoke/smoke_03_generate.py
autonomous: true

must_haves:
  truths:
    - "materialize(payload) creates exactly 1 ligand + n*n AA objects (fresh names via get_unused_name), sentinel-tags every atom (segi='AAM', b=-999.0), translates AAs to their effective grid poses (grid_pose + placement.offset, camera=0, world frame), and returns the slot->(object, atom ids) registry"
    - "reset_to_grid re-bakes every AA from its CURRENT pose back to the spec grid pose — never matrix_reset (probe-proven coordinate reverter)"
    - "SMOKE-03 proves generation+materialization count-asserted in real PyMOL: object growth, per-atom sentinels, pose agreement with the spec, zero temp leaks, clean teardown"
  artifacts:
    - path: "aamatch/placement.py"
      provides: "materialize, reset_to_grid, translate_to, transform_baked, cleanup_game_objects"
      min_lines: 150
    - path: "smoke/smoke_03_generate.py"
      provides: "Count-asserted generate+materialize smoke (SMOKE-03)"
      min_lines: 80
  key_links:
    - from: "aamatch/placement.py"
      to: "aamatch/capability.py"
      via: "AA_TOKENS fragment/resn mapping (ONE vocabulary)"
      pattern: "AA_TOKENS"
    - from: "aamatch/placement.py"
      to: "aamatch/paths.py"
      via: "to_windows_path(package_data_path('data', ligand.file)) before cmd.load"
      pattern: "to_windows_path"
    - from: "smoke/smoke_03_generate.py"
      to: "aamatch/generator.py"
      via: "generate on real fixture ligand then materialize"
      pattern: "generate"
---

<objective>
Build the cmd-tier materializer: payload -> real PyMOL objects (ligand load + per-slot AA fragments), sentinel/identity conventions, scripted baked placement + spec-replay reset, minimal cleanup — proven by SMOKE-03 headlessly.

Purpose: ROADMAP Phase-2 criterion 4's materialize stage (GEN-02/03 mechanics); establishes the naming/sentinel conventions Phase 4 Cleanup and Phase 7 reconstruction consume.
Output: aamatch/placement.py + passing SMOKE-03.
</objective>

<execution_context>
@~/.config/opencode/get-shit-done/workflows/execute-plan.md
@~/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/02-headless-game-engine/02-RESEARCH-materialization.md  # §2 AA materialization, §4 sentinels, §5 identity, §6 placement recipe + hazards, §8.2 SMOKE-03
@.planning/phases/02-headless-game-engine/02-RESEARCH-generation.md        # §5.2 pose formula + §9 payload (placement.offset semantics)
@aamatch/capability.py                                                     # AA_TOKENS
@aamatch/generator.py                                                      # payload producer
@aamatch/geometry.py                                                       # centroid_of/coords_of for pose asserts
@aamatch/paths.py                                                          # path helpers
</context>

<tasks>

<task type="auto">
  <name>Task 1: placement.py — materialize + registry</name>
  <files>aamatch/placement.py</files>
  <action>
Cmd tier (module-level `from pymol import cmd` legal; NEVER pure). Implement:
1. `materialize(payload)` -> registry dict {'molecules': [{'molecule_id', 'ligand': (object, sorted ids), 'slots': {slot_id: (object, sorted ids)}}], 'pre_game_names': snapshot}. Per molecule: ligand object name = cmd.get_unused_name('_aam_lig'); cmd.load(to_windows_path(package_data_path('data', ligand['file'])), lig_name) — UNIQUE name always (cmd.load appends states into existing names — probe-proven hazard); assert cmd.count_atoms(lig_name) == manifest? (no manifest here — assert > 0 and record); translate ligand by placement.offset (camera=0). Per slot: name = cmd.get_unused_name('_aam_aa'); frag = capability.AA_TOKENS[slot['aa']]['fragment']; cmd.fragment(frag, name, zoom=0); cmd.alter(name, "segi='AAM'; b=-999.0", space={}); cmd.sort(name); delta = effective_pose - geometry.centroid_of(name) (effective = grid_pose.position + placement.offset); cmd.translate(delta, name, state=1, camera=0) — BAKE world-frame coords (never the object-matrix path in Phase 2). Registry records (object, sorted cmd IDs via iterate uppercase ID). Count-assert after each step; NEVER cmd.create onto an existing object (Pitfall 7 — fresh fragment objects avoid it by construction).
2. `reset_to_grid(payload, registry)`: for each slot, current = geometry.coords_of(obj); target = effective grid pose; re-translate by (target - current_centroid) — spec REPLAY, re-bake; NEVER cmd.matrix_reset (probe-proven it REVERTS baked coords). Assert final centroid == effective pose within 1e-6.
3. `cleanup_game_objects()` -> counts: delete every name from get_names('objects') with the '_aam_' prefix ONLY (sentinel/prefix rule — never chemical filters); return {'deleted': n}. Docstring: full Cleanup-button semantics are Phase 4; this is the convention proof.
Docstring records: Phase-2 rule "bake coordinates, never matrices"; banned calls list (matrix_reset, get_object_ttt — segfault hazard, cmd.create-onto-existing, cmd.load-into-existing).
  </action>
  <verify>python3.6 -m py_compile aamatch/placement.py</verify>
  <done>Materialize/reset/cleanup implemented with count asserts + banned-call docstring</done>
</task>

<task type="auto">
  <name>Task 2: Scripted-placement helpers</name>
  <files>aamatch/placement.py</files>
  <action>
Add: `translate_to(object_name, position)` (delta from current centroid, camera=0 — the scripted-placement primitive) and `transform_baked(object_name, m16)` (cmd.transform_object(name, M, homogenous=1) — row-major 4x4, probe-verified bake) with a pose assertion helper usage pattern documented: snapshot geometry.coords_of -> transform -> assert after[i] == R.dot(before[i]) + t per atom within 1e-6; NEVER assert via get_object_matrix (non-identity history record). These are what SMOKE-04's scripted placement and Phase 3's game-driven moves build on.
  </action>
  <verify>python3.6 -m py_compile aamatch/placement.py</verify>
  <done>Baked-move primitives in place with assert-coordinates discipline</done>
</task>

<task type="auto">
  <name>Task 3: SMOKE-03 — count-asserted generate + materialize</name>
  <files>smoke/smoke_03_generate.py</files>
  <action>
smoke_03_generate.py per conventions (flush prints, per-check labels, final marker === SMOKE-03 PASS ===): load MANIFEST.json via read_json_file + parse_manifest_dict; candidates = enumerate_entries sorted; load ligand (benzamide) -> geometry.bounding_sphere -> ligand_data; setup = validate_state({'interaction_mode': 'block_exclusive', 'allowed_interactions': ['pi_stacking'], 'molecules_per_level': 1, 'difficulty_levels': 3}); payload = generator.generate(seed=42, ...); parse gates pass inside PyMOL. Snapshot cmd.get_names('objects'); materialize(payload); assert: object list grew by exactly 1 + n*n (n=3 -> 10 objects); every '_aam_aa*' object's atoms ALL have segi=='AAM' and b<-0 (cmd.iterate with space; b < 0 selector discipline); sentinel atom count == total game atom count; every AA object centroid == its effective grid pose within 1e-6 (geometry.coords_of); reset_to_grid after a deliberate translate_to perturbation restores the pose; cleanup_game_objects() -> object list == pre-game snapshot exactly. py_compile BEFORE the Windows run.
  </action>
  <verify>python3.6 -m py_compile smoke/smoke_03_generate.py && bash smoke/run_smoke.sh smoke/smoke_03_generate.py 180  (=== SMOKE-03 PASS ===)</verify>
  <done>SMOKE-03 green: generation, materialization, sentinels, poses, reset, teardown all count-asserted in real PyMOL</done>
</task>

</tasks>

<verification>
bash smoke/run_smoke.sh smoke/smoke_03_generate.py 180 && python3.6 -m unittest discover -s tests -v
</verification>

<success_criteria>
Materialization pipeline proven headlessly with zero temp leaks and exact pose agreement; conventions documented for Phase 4/7.
</success_criteria>

<output>
After completion, create `.planning/phases/02-headless-game-engine/02-13-SUMMARY.md`
</output>
