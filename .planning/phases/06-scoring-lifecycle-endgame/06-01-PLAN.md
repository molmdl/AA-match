---
phase: 06-scoring-lifecycle-endgame
plan: 01
type: tdd
wave: 1
depends_on: []
files_modified:
  - aamatch/game_state.py
  - tests/test_game_state.py
  - smoke/smoke_14_status_surface.py
autonomous: true
must_haves:
  truths:
    - "A game that ends (give-up or natural completion) carries game_over=True, an end_state ('completed'|'gave_up'), and a frozen final_time that survives to_dict/from_dict"
    - "Every recorded molecule has exactly one keyed score entry, so per-level aggregation is robust without flat-list slicing"
    - "The one-record guard is derivable: has_record(level, molecule) reports whether a molecule already produced a Confirm/Skip record"
    - "endgame_summary produces the complete SCORE-07 data payload (per-level scores, total, time, sizes, counters) fail-closed"
  artifacts:
    - path: "aamatch/game_state.py"
      provides: "game_over/end_state/final_time/score_per_molecule fields + stop_timer + has_record + endgame_summary"
      contains: "def stop_timer"
    - path: "tests/test_game_state.py"
      provides: "WSL battery for the lifecycle data layer (RED-first)"
      contains: "stop_timer"
    - path: "smoke/smoke_14_status_surface.py"
      provides: "evolved exact-key pin for engine.game_status() (11 keys)"
      contains: "score_per_molecule"
  key_links:
    - from: "aamatch/game_state.py record_molecule_result"
      to: "score_per_molecule[molecule_key(level, molecule)]"
      via: "same-call write beside molecule_scores.append"
      pattern: "score_per_molecule"
    - from: "engine.game_status() (later plan 06-03)"
      to: "GameState.to_dict()"
      via: "additive key growth"
      pattern: "game_over"
---

<objective>
GameState lifecycle data layer (SCORE-06/07/05 groundwork): end-state fields, the stopped-timer op, the one-record guard helper, the keyed per-molecule score store, and the pure endgame-summary builder — all WSL-TDD'd before any cmd/Qt tier touches them.

Purpose: 06-03's engine ops and 06-09's endgame screen consume exactly these shapes; building the pure layer first keeps every later tier a thin wrapper.
Output: grown GameState (lossless to_dict/from_dict), pure endgame_summary, evolved SMOKE-14 key pin.
</objective>

<execution_context>
@~/.config/opencode/get-shit-done/workflows/execute-plan.md
@~/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-scoring-lifecycle-endgame/06-RESEARCH.md
@aamatch/game_state.py
@aamatch/engine.py
@tests/test_game_state.py
@smoke/smoke_14_status_surface.py

Binding decisions (research §5 resolutions, final):
- D8 home: additive fields + pure builders live in game_state.py (established scoring home; no new PURE_MODULES entry).
- D11: keyed `score_per_molecule` store (written by record_molecule_result beside the flat list) — robust per-level aggregation for the endgame AND Phase-7 persistence.
- endgame-ui D9: `final_time` lives on GameState (Phase-7 can reconstruct a finished game's clock); the tab-side render consumes it.
- Q8: stop = capture final elapsed ONCE + freeze; the anchor itself stays untouched (it is the pause mechanism's home, game_state.py:193-224).

Purity: game_state.py imports ONLY `time` + `.setup_state` today (game_state.py:38-40) — keep it that way. PURE_MODULES registration unchanged (already gated at tests/test_purity.py:94). python3.6 floor, %-formatting, no dataclasses.
</context>

<feature>
  <name>GameState lifecycle data layer</name>
  <files>aamatch/game_state.py, tests/test_game_state.py, smoke/smoke_14_status_surface.py</files>
  <behavior>
    All cases plain-data in/out, WSL-runnable under python3.6:

    1. New fields zero-init: `GameState()` has `game_over == False`, `end_state is None`, `final_time is None`, `score_per_molecule == {}`.
    2. `stop_timer(now=None)`: with an anchor set (start_timer(1000.0)) and now=1065.0 -> final_time == 65.0 and game_over is True; the timer_anchor is UNCHANGED (still 1000.0 — the pause mechanism's home is not disturbed); with now=None the wall clock is used (assert final_time is a float >= 0); a never-started game (anchor None) -> final_time == 0.0 (0.0-tolerant, matching rebase_timer's totality); a now earlier than the anchor clamps at 0.0 (max(0.0, ...)).
    3. `has_record(level, molecule)`: False before any record; True after record_molecule_result(level, molecule, ...) — including the zero-score case (a 0.0 record with empty formed still writes the key, game_state.py:246-247, pinned by the existing test at tests/test_game_state.py:186-191).
    4. `record_molecule_result` ALSO writes `score_per_molecule[molecule_key(level, molecule)] = value` in the SAME call (the two views can never drift); the flat molecule_scores list behavior is byte-unchanged.
    5. to_dict/from_dict: the 4 new keys round-trip losslessly (score_per_molecule deep-copied per key); from_dict uses `.get` defaults (accept-older law: game_over .get('game_over', False), end_state .get('end_state'), final_time .get('final_time'), score_per_molecule .get('score_per_molecule', {})) — an OLD 7-key dict loads WITHOUT KeyError and yields the zero-init defaults for the new fields; to_dict output stays JSON-able.
    6. `endgame_summary(molecule_counts)` (GameState method): molecule_counts = list of per-level molecule counts (payload order). Returns the plain dict with EXACTLY these keys:
       'end_state' (str), 'level_scores' (list of float, one per level, SUM of that level's recorded scores via score_per_molecule), 'total' (float, == self.total_score), 'final_time' (float), 'levels' (int, len(molecule_counts)), 'molecules' (int, sum), 'molecules_completed' (int, len(score_per_molecule)), 'skip_count' (int), 'giveup_count' (int), 'ended_level' (int, current_level_index + 1), 'ended_molecule' (int, current_molecule_index + 1).
       Fail-closed ValueErrors naming the cause: empty molecule_counts; non-int entries; end_state None or unknown; for end_state 'completed': len(score_per_molecule) != sum(molecule_counts) (a completed game must have EVERY molecule recorded); for end_state 'gave_up': len(score_per_molecule) != sum(molecule_counts[:current_level_index]) + current_molecule_index (the current molecule at give-up is intentionally unrecorded — research Q5). Per-level recorded-count overflow (more records for a level than its molecule count) also refuses.
    7. Happy-path numeric case: a 2-level game with counts [2, 2], records 0.5/1.0/0.0/1.0 -> level_scores [1.5, 1.0], total 2.5, molecules 4, molecules_completed 4.
  </behavior>
  <implementation>
    - Add the 4 fields in __init__ (after formed_types_per_molecule, game_state.py:175) with the existing docstring style; extend the class docstring's field list.
    - stop_timer beside rebase_timer (after game_state.py:224): float-coerce like start_timer/rebase_timer; NEVER touch timer_anchor; docstring states "stop = capture once + freeze; the caller (the lifecycle op) owns when".
    - has_record: one-line key-presence check via molecule_key (the Q10 guard primitive).
    - record_molecule_result: add the score_per_molecule write beside the append (game_state.py:245-247).
    - endgame_summary as a METHOD (uses self state; molecule_counts passed in because the payload lives engine-side, not here). %-formatting only.
    - Deliberate test evolutions: tests/test_game_state.py round-trip tests (:194-231 pattern) grow the new keys; smoke/smoke_14_status_surface.py:206-208 exact-7-key engine.game_status() pin becomes the exact 11-key list ['current_level_index', 'current_molecule_index', 'formed_types_per_molecule', 'final_time', 'game_over', 'giveup_count', 'molecule_scores', 'score_per_molecule', 'skip_count', 'end_state', 'timer_anchor'] (sorted) — EDIT the pin + RE-RUN the smoke (bash smoke/run_smoke.sh smoke/smoke_14_status_surface.py, grep '=== SMOKE-14 PASS ==='); this is the research-flagged deliberate evolution.
    - Do NOT touch EVENT_KINDS notes or any other file.
  </implementation>
</feature>

<verification>
- RED first: `python3.6 -m unittest tests.test_game_state -v` fails on the new cases; commit `test(06-01): add failing tests for GameState lifecycle data layer`.
- GREEN: `python3.6 -m unittest tests.test_game_state -v` green; commit `feat(06-01): implement GameState lifecycle data layer`.
- Gates: `python3.6 -m py_compile aamatch/*.py` && `python3.6 -m unittest discover -s tests -v` ALL green (incl. tests/test_purity.py — no new modules, no stubs).
- `bash smoke/run_smoke.sh smoke/smoke_14_status_surface.py` greps `=== SMOKE-14 PASS ===` after the pin evolution.
- grep receipt: `grep -n "import" aamatch/game_state.py` shows ONLY time + .setup_state.
</verification>

<success_criteria>
- All 7 behavior cases pinned by WSL tests; 753+ WSL tests green; purity gates green.
- SMOKE-14 re-run PASS with the evolved 11-key pin.
- game_over/end_state/final_time/score_per_molecule survive to_dict->from_dict byte-losslessly with accept-older .get defaults.
</success_criteria>

<output>
After completion, create `.planning/phases/06-scoring-lifecycle-endgame/06-01-SUMMARY.md` (frontmatter: subsystem scoring, provides the endgame-summary dict contract + stop_timer/has_record shapes for 06-03/06-09).
</output>
