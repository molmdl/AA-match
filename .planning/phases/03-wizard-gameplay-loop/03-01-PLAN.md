---
phase: 03-wizard-gameplay-loop
plan: 01
type: tdd
wave: 1
depends_on: []
files_modified:
  - aamatch/wizard_core.py
  - tests/test_wizard_core.py
  - tests/test_purity.py
autonomous: true

must_haves:
  truths:
    - "A wizard-side reverse map {object_name: slot_id} is built from ONE molecule of the Phase-2 placement registry with fail-closed refusals on bad shapes (PLAY-01 pick identity precondition)"
    - "Camera-frame nudge directions convert to world-frame step vectors from cmd.get_view's rotation block (identity view is the pass-through identity; a known rotation maps hand-computed values)"
    - "Per-slot color snapshots are taken exactly once per slot and are idempotent afterwards; the restore view is a plain {ID: color} map (PLAY-01 feedback + PLAY-04 restore precondition)"
  artifacts:
    - path: "aamatch/wizard_core.py"
      provides: "build_slot_map, transient_selection, view_camera_to_world, ensure_snapshot, color_map, snapshot_objects + NUDGE_STEP / ROTATE_STEP_DEG / ROTATE_BUTTON_STEP_DEG / HIGHLIGHT_COLOR constants"
      min_lines: 100
    - path: "tests/test_wizard_core.py"
      provides: "RED-first unit battery over every pure helper (hand-computed expectations, fail-closed refusals)"
      min_lines: 100
  key_links:
    - from: "aamatch/wizard_core.py"
      to: "aamatch/placement.py"
      via: "build_slot_map consumes the materialize registry shape (slots = {slot_id: (object, ids)})"
      pattern: "registry\\['molecules'\\]"
    - from: "tests/test_purity.py"
      to: "aamatch/wizard_core.py"
      via: "PURE_MODULES registration gates the new module mechanically"
      pattern: "wizard_core"
---

<objective>
Create `aamatch/wizard_core.py` — the PURE logic half of the Phase-3 wizard (slot reverse-map, camera-to-world nudge math, selection-color snapshot bookkeeping, movement constants) — via a full RED→GREEN→REGISTER TDD cycle.

Purpose: every gameplay decision the wizard makes that is NOT a cmd call must be pure, WSL-testable logic (repo pattern: pure helpers in PURE_MODULES, cmd glue in the cmd-tier module). Building it first means plan 03-03's GameWizard wires proven functions instead of inventing math inline.
Output: aamatch/wizard_core.py (PURE_MODULES gated) + tests/test_wizard_core.py green.
</objective>

<execution_context>
@~/.config/opencode/get-shit-done/workflows/execute-plan.md
@~/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@AGENTS.md                                        # standing gates — binding (purity, 3.6 floor, no stubs)
@.planning/STATE.md                               # Phase-2 decisions + Phase-3 spawn notes
@.planning/phases/03-wizard-gameplay-loop/03-RESEARCH-wizard-interaction.md   # §5.3 reverse-map shape, §4 color snapshot/restore
@aamatch/placement.py                             # registry shape: {'molecules': [{'molecule_id', 'offset', 'ligand', 'slots': {slot_id: (object, sorted ids)}}]}
@aamatch/vec3.py                                  # house tuple-vector style (floats in/out) to imitate
@tests/test_purity.py                             # PURE_MODULES list + Gate A/B/D mechanics
</context>

<tasks>

<task type="auto">
  <name>Task 1: RED — failing test battery for every wizard_core helper</name>
  <files>tests/test_wizard_core.py</files>
  <action>
Create tests/test_wizard_core.py (unittest, python3.6-safe, imports aamatch.wizard_core at module top — the ImportError IS the RED proof; no sys.modules stubs, ever). Test cases (hand-computed expectations, no property machinery needed):

1. `build_slot_map(registry, molecule_index)`:
   - registry with molecules[m] whose 'slots' = {'r0c0': ('_aam_aa', [1,2,3]), 'r0c1': ('_aam_aa001', [1,2,3,4])} → {'_aam_aa': 'r0c0', '_aam_aa001': 'r0c1'} (EXACT dict equality).
   - Two molecules in the registry: only molecule_index's slots land in the map (the other molecule's objects are absent — Phase-3 scoping to molecule 0).
   - Fail-closed ValueError (message names the cause) for: registry without a 'molecules' list; molecule_index out of range; a slot entry that is not a 2-tuple with a string object name.
2. `transient_selection(name)` → True for 'sele', 'pk1'..'pk4', '_drag', '_anything'; False for 'mysel', 'ligand', '_aam_aa' is NOT a selection concern (still False is fine — the rule is: name == 'sele' or name in ('pk1','pk2','pk3','pk4') or name.startswith('_')).
3. `view_camera_to_world(view, step)` where view mimics cmd.get_view() (18 numbers; view[0:9] = row-major 3x3 rotation world→camera):
   - identity rotation block (1,0,0, 0,1,0, 0,0,1) + arbitrary trailing values → returns step unchanged as a 3-tuple of FLOATS (int inputs coerced — house vec3 contract).
   - rotation = Rz(+90 deg) block [0,-1,0, 1,0,0, 0,0,1] (maps world x̂ to camera ŷ): camera step (1,0,0) → world (0,-1,0); camera step (0,1,0) → world (1,0,0); camera step (0,0,1) → world (0,0,1). Compute by hand: world = Rᵀ·step.
   - view shorter than 9 numbers → ValueError naming the cause.
4. `ensure_snapshot(store, obj, rows)`: first call stores a COPY of rows [(ID, color), ...] and returns True; second call with DIFFERENT rows returns False and the store keeps the ORIGINAL snapshot (idempotent — the snapshot is taken BEFORE the first recolor and never overwritten).
5. `color_map(store, obj)` → {ID: color} dict for a stored slot, None for an absent one.
6. `snapshot_objects(store)` → sorted object-name list (deterministic; do NOT rely on dict insertion order — python3.6 does not guarantee it).
7. Constants exist and are pinned exact: NUDGE_STEP == 1.0, ROTATE_STEP_DEG == 10.0, ROTATE_BUTTON_STEP_DEG == 90.0, HIGHLIGHT_COLOR == 'green' (v1 recolor precedent, game.py:208-213).
Module docstring in the test explaining the TDD intent. Run: `python3.6 -m unittest tests.test_wizard_core -v` — MUST FAIL (ImportError). Commit: `test(03-01): add failing tests for pure wizard core helpers`.
  </action>
  <verify>python3.6 -m unittest tests.test_wizard_core -v 2>&1 | tail -5  (must show failure/ImportError)</verify>
  <done>Failing RED test battery committed; every helper has at least one hand-computed expectation and one fail-closed case</done>
</task>

<task type="auto">
  <name>Task 2: GREEN — implement aamatch/wizard_core.py + register in PURE_MODULES</name>
  <files>aamatch/wizard_core.py, tests/test_purity.py</files>
  <action>
Implement aamatch/wizard_core.py to make the battery green — PURE module: module-level imports = stdlib only (nothing beyond what the functions need; likely NOTHING at all), never pymol/Qt/numpy/dataclasses (Gate A scans every scope including function bodies). House style: % formatting (no f-strings), floats in/floats out via float() coercion, fail-closed ValueError whose message NAMES the cause, full module docstring (layer: PURE — must be registered in tests/test_purity.py PURE_MODULES; never import cmd here; consumed by aamatch/wizard.py which is CMD tier).

Functions per the Task-1 contract:
- `build_slot_map(registry, molecule_index)` — validate registry is a dict with a 'molecules' list and 0 <= int(molecule_index) < len(molecules); molecule dict must carry a 'slots' dict of {slot_id: (object_name, ids)} with a non-empty str object name; return {object_name: slot_id}.
- `transient_selection(name)` — the guard behind the canonical do_select→do_pick map: only transient selections ('sele', pk1..pk4, '_'-prefixed) may be deleted by the wizard's pick routing; a user's NAMED active selection must survive (recorded planner decision, RESEARCH-wizard §10.2: transient-only delete, accepting a leftover enabled named selection over user-data loss).
- `view_camera_to_world(view, step)` — view[0:9] = row-major 3x3 R (world→camera per PyMOL's get_view convention); return Rᵀ·step as 3-tuple of floats. len(view) < 9 → ValueError. NOTE for 03-04's smoke: the Rᵀ convention itself gets live-verified against a scripted cmd.set_view (if the live build proves the opposite convention, this function is the single fix site).
- `ensure_snapshot(store, obj, rows)` / `color_map(store, obj)` / `snapshot_objects(store)` — the PLAY-01 color feedback bookkeeping: snapshot BEFORE first recolor (lazy, per-slot), restore via {ID: color} maps, cleanup iterates snapshot_objects. Store dict is mutated in place (caller owns it; plain picklable data).
- Constants: NUDGE_STEP = 1.0 (Å per nudge press), ROTATE_STEP_DEG = 10.0 (keyboard rotate), ROTATE_BUTTON_STEP_DEG = 90.0 (panel rotate button), HIGHLIGHT_COLOR = 'green'.

Then register the module: edit tests/test_purity.py PURE_MODULES += 'wizard_core' (keep the alphabetical/grouped style of the list; add a one-line comment "# Phase 3 (03-01): wizard_core (pure wizard logic: slot map, view math, color bookkeeping)"). Gate A/B now scan it mechanically. REFACTOR only if an obvious improvement exists — tests MUST stay green.

Commits: `feat(03-01): implement pure wizard core helpers` then `test(03-01): register wizard_core in PURE_MODULES`.
  </action>
  <verify>python3.6 -m unittest tests.test_wizard_core -v && python3.6 -m py_compile aamatch/wizard_core.py && python3.6 -m unittest discover -s tests -v 2>&1 | tail -3</verify>
  <done>Full WSL suite green with wizard_core registered and gated; helpers match the hand-computed expectations exactly</done>
</task>

</tasks>

<verification>
python3.6 -m py_compile aamatch/*.py && python3.6 -m unittest discover -s tests -v 2>&1 | tail -3
(All green including the purity gates; wizard_core is now a gated PURE module.)
</verification>

<success_criteria>
- aamatch/wizard_core.py exists as a PURE module (stdlib-only imports, Gate A/B scan it), registered in tests/test_purity.py PURE_MODULES.
- Every helper carries its hand-computed unit expectations (RED-first proven) and fail-closed refusals.
- The camera→world convention (Rᵀ·step over get_view's rotation block) is pinned by unit tests and marked for live confirmation in SMOKE-07.
</success_criteria>

<output>
After completion, create `.planning/phases/03-wizard-gameplay-loop/03-01-SUMMARY.md`
</output>
