---
phase: 08-demo-curation-citations
plan: 07
type: execute
wave: 5
depends_on:
  - "08-06"
files_modified:
  - aamatch/data/MANIFEST.json
  - aamatch/data/ligands/demo-challenge-1-chloramphenicol.sdf
  - aamatch/data/ligands/demo-challenge-1-quinine.sdf
  - aamatch/data/ligands/demo-veryhard-1-folic-acid.sdf
  - aamatch/data/ligands/demo-veryhard-1-thyroxine.sdf
  - aamatch/data/ligands/demo-veryhard-2-heme.sdf
  - scripts/demo_specs/demo-challenge-1.json
  - scripts/demo_specs/demo-veryhard-1.json
  - scripts/demo_specs/demo-veryhard-2.json
autonomous: true
must_haves:
  truths:
    - "All ~9 curated tier slots ship (Easy ×3, Hard ×3, Challenge ×1, Very challenging ×2) — GEN-06's slot count met"
    - "The flag-system branches become real: chloramphenicol (C–Cl ×2) + thyroxine (C–I ×4) make halogen_present TRUE, HEM (Fe) makes metal_present TRUE — element-scan verified by the builder AND load-verified by SMOKE-02"
    - "All 10 sets coexist in one regenerated MANIFEST.json with every earlier set byte-identical; each new set committed atomically"
    - "7-type interaction diversity is supply-real: every canonical interaction type has at least one capable ligand in the bundle (recorded in the SUMMARY from proposal mapping)"
  artifacts:
    - path: "aamatch/data/MANIFEST.json"
      provides: "The full curated manifest: 10 sets / 18 ligands (16 curated + 2 dev), container version 1, frozen schema"
      contains: "demo-veryhard-2"
    - path: "aamatch/data/ligands/demo-veryhard-2-heme.sdf"
      provides: "The metal-bearing ligand (Fe) — first exercise of the detector's metal gate with real data"
    - path: "scripts/demo_specs/demo-challenge-1.json"
      provides: "Approved curation input for demo-challenge-1"
      contains: "demo-challenge-1"
    - path: "scripts/demo_specs/demo-veryhard-1.json"
      provides: "Approved curation input for demo-veryhard-1"
      contains: "demo-veryhard-1"
    - path: "scripts/demo_specs/demo-veryhard-2.json"
      provides: "Approved curation input for demo-veryhard-2"
      contains: "demo-veryhard-2"
  key_links:
    - from: "scripts/demo_specs/demo-{challenge,veryhard}-*.json"
      to: "08-PROPOSALS.md approved rows"
      via: "transcription of approved rows only (CIDs 5959/3034034/135398658/5819 + RCSB HEM)"
      pattern: "demo-(challenge|veryhard)"
    - from: "aamatch/data/ligands/demo-veryhard-2-heme.sdf"
      to: "manifest metal_present"
      via: "builder element-scan sets metal_present true (Fe in DETECT-03 approved list)"
      pattern: "metal_present"
---

<objective>
Bundle the final tier slots — Challenge ×1 and Very challenging ×2 (GEN-06) — including the halogen debut (chloramphenicol, thyroxine) and the metal debut (heme/HEM via RCSB), completing the ~9-set curated manifest.

Purpose: these sets exercise the two detector flag branches with REAL data for the first time (the 02-01 provisional-threshold caveat's trigger) and complete the tier vocabulary in the grouped dropdown.
Output: 5 SDFs + 3 specs + regenerated MANIFEST.json (10 sets), committed atomically per set.
</objective>

<execution_context>
@~/.config/opencode/get-shit-done/workflows/execute-plan.md
@~/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/08-demo-curation-citations/08-PROPOSALS.md
@.planning/phases/08-demo-curation-citations/08-RESEARCH-sourcing.md
@scripts/build_demos.py
@aamatch/data/MANIFEST.json

**GATE precondition:** 08-PROPOSALS.md approval recorded. STOP otherwise.

Sets:
- demo-challenge-1 'Halogen & alkaloids' (tier 'challenge'): chloramphenicol (PubChem CID 5959, C–Cl ×2 → halogen_present true) + quinine (CID 3034034)
- demo-veryhard-1 'Big & iodinated' (tier 'very_challenging'): folic-acid (CID 135398658, 38 heavy → medium) + thyroxine (CID 5819, C–I ×4 → halogen_present true)
- demo-veryhard-2 'Metalloporphyrin' (tier 'very_challenging'): heme — RCSB CCD ideal SDF `https://files.rcsb.org/ligands/view/HEM_ideal.sdf` (Fe in molecule → metal_present true; license 'CC0 1.0 (wwPDB)'; PDB complex + paper DOI pinned in the proposal)

**BINDING LAWS (repeat per task):** NEVER hand-edit an SDF or MANIFEST.json. NEVER substitute beyond approved alternates; record any approved-alternate use in spec notes + SUMMARY. Fetch failure = STOP and surface.
Chemistry guardrails: C–F is never a halogen donor (DETECT-03) — no fluoro compound is a halogen carrier; halogen flags must come from Cl/Br/I element scan. HEM's metal flag comes from FE in the approved metal list. Thyroxine's iodine count (4× C–I) goes in the spec notes (proposal evidence cross-check). If the HEM ideal SDF's counts/atoms mismatch the proposal's recorded evidence, STOP — the CCD name/identity was flagged UNVERIFIED at research time and must have been confirmed during 08-04; a mismatch now means the proposal evidence was wrong: surface it, do not guess.
</context>

<tasks>

<task type="auto">
  <name>Task 1: bundle demo-challenge-1 (chloramphenicol + quinine) — halogen debut</name>
  <files>scripts/demo_specs/demo-challenge-1.json, aamatch/data/ligands/demo-challenge-1-chloramphenicol.sdf, aamatch/data/ligands/demo-challenge-1-quinine.sdf, aamatch/data/MANIFEST.json</files>
  <action>
Spec per schema: tier 'challenge', license 'Public domain (NCBI PubChem)', entries chloramphenicol (CID 5959; PDB complex 4CLA from the proposal) + quinine (CID 3034034; 9NDX). Run `python3.6 scripts/build_demos.py --spec scripts/demo_specs/demo-challenge-1.json --fetch`; VERIFY the derived halogen_present is True for chloramphenicol and False for quinine, and metal_present False for both. `bash smoke/run_smoke.sh smoke/smoke_02_manifest.py 180` — SMOKE-02's element-scan cross-check must agree with the manifest flags (this is the first real exercise of the halogen branch). `python3.6 -m unittest tests.test_demo_data -v`. Atomic commit `feat(08-07): bundle demo-challenge-1 (chloramphenicol + quinine)`.
  </action>
  <verify>bash smoke/run_smoke.sh smoke/smoke_02_manifest.py 180 (PASS) && python3.6 -c "import json; c=json.load(open('aamatch/data/MANIFEST.json')); s=[x for x in c['data']['sets'] if x['set_id']=='demo-challenge-1'][0]; print([(e['entry_id'], e['halogen_present']) for e in s['entries']])" (chloramphenicol True, quinine False)</verify>
  <done>demo-challenge-1 committed atomically; halogen flag branch exercised with real data for the first time.</done>
</task>

<task type="auto">
  <name>Task 2: bundle demo-veryhard-1 (folic acid + thyroxine)</name>
  <files>scripts/demo_specs/demo-veryhard-1.json, aamatch/data/ligands/demo-veryhard-1-folic-acid.sdf, aamatch/data/ligands/demo-veryhard-1-thyroxine.sdf, aamatch/data/MANIFEST.json</files>
  <action>
Same procedure: tier 'very_challenging'; folic-acid (CID 135398658; 4KMZ) + thyroxine (CID 5819 — halogen_present True via 4× C–I; record the iodine count in spec notes vs proposal evidence). SMOKE-02 (iodine branch: 'I' is in the approved halogen list) → data battery → atomic commit `feat(08-07): bundle demo-veryhard-1 (folic acid + thyroxine)`.
  </action>
  <verify>bash smoke/run_smoke.sh smoke/smoke_02_manifest.py 180 (PASS) && python3.6 -m unittest tests.test_demo_data -v (green)</verify>
  <done>demo-veryhard-1 committed atomically; iodine halogen branch exercised.</done>
</task>

<task type="auto">
  <name>Task 3: bundle demo-veryhard-2 (heme/HEM) + full-suite regression</name>
  <files>scripts/demo_specs/demo-veryhard-2.json, aamatch/data/ligands/demo-veryhard-2-heme.sdf, aamatch/data/MANIFEST.json</files>
  <action>
Same procedure with the RCSB source: spec url `https://files.rcsb.org/ligands/view/HEM_ideal.sdf`, license 'CC0 1.0 (wwPDB)', license_url 'https://www.wwpdb.org/about/usage-policies', PDB complex per the proposal's pinned heme entry. VERIFY derived metal_present True (FE element) and size_class 'medium'. SMOKE-02 (metal branch first real exercise) → data battery → atomic commit `feat(08-07): bundle demo-veryhard-2 (heme)`. Then FULL WSL suite + `python3.6 scripts/build_demos.py --report-only` (exit 0; all 10 specs consistent) + dev-set dry-run exact + a final `git log --oneline` check that the phase has one atomic commit per set.
  </action>
  <verify>bash smoke/run_smoke.sh smoke/smoke_02_manifest.py 180 (PASS) && python3.6 -m unittest discover -s tests -v (ALL green) && python3.6 -c "import json; c=json.load(open('aamatch/data/MANIFEST.json')); sets=c['data']['sets']; print(len(sets), sorted(s['tier'] for s in sets))" (10 sets; tier counts 3 easy/3 hard/1 challenge/2 very_challenging + dev easy) && python3.6 scripts/build_demos.py --report-only (exit 0)</verify>
  <done>All ~9 curated slots + dev set live in one manifest; metal branch exercised; full suite green; proposal → approve → fetch/commit protocol complete end-to-end.</done>
</task>

</tasks>

<verification>
- `python3.6 -m py_compile aamatch/*.py scripts/build_demos.py` — syntax floor.
- `python3.6 -m unittest discover -s tests -v` — full suite incl. purity gates.
- `bash smoke/run_smoke.sh smoke/smoke_02_manifest.py 180` — all 10 sets / 18 entries (16 curated + 2 dev) load-verify (counts/charge/states/sha256/flags).
- Manifest tier census: exactly 3 easy + 3 hard + 1 challenge + 2 very_challenging curated sets (+ dev set).
- metal_present True ≥ 1 entry; halogen_present True ≥ 2 entries (chloramphenicol, thyroxine).
</verification>

<success_criteria>
- GEN-06 slot inventory complete: Easy ×3, Hard ×3, Challenge, Very challenging ×2 — pre-downloaded and committed.
- Interaction diversity supply: all 7 types have capable ligands (mapping recorded in SUMMARY from the proposal).
- Every set atomically committed through the builder with SMOKE-02 gating; zero hand-edited data files.
</success_criteria>

<output>
After completion, create `.planning/phases/08-demo-curation-citations/08-07-SUMMARY.md`
</output>
