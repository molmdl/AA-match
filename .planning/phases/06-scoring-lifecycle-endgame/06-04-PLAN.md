---
phase: 06-scoring-lifecycle-endgame
plan: 04
type: execute
wave: 1
depends_on: []
files_modified:
  - aamatch/gamestart.py
  - smoke/smoke_08_starter.py
autonomous: true
must_haves:
  truths:
    - "The start composition can frame ANY molecule index of a materialized level, not only molecule 0"
    - "start_game's default path is byte-identical (SMOKE-08 re-green unchanged) — the generalization is additive"
    - "The wizard (06-05) can re-frame the camera onto a newly advanced molecule through ONE public call"
  artifacts:
    - path: "aamatch/gamestart.py"
      provides: "public compose_molecule_view(registry, molecule_index=0) + molecule_index params on the three helpers"
      contains: "def compose_molecule_view"
    - path: "smoke/smoke_08_starter.py"
      provides: "PART 5: molecule_index=1 compose proof + default-path byte-identity re-run"
      contains: "compose_molecule_view"
  key_links:
    - from: "aamatch/gamestart.py start_game"
      to: "compose_molecule_view(registry)"
      via: "the :405-407 inline sequence extracted verbatim (single home)"
      pattern: "compose_molecule_view"
    - from: "aamatch/gamestart.py compose helpers"
      to: "registry['molecules'][molecule_index]"
      via: "molecule_index default 0 replaces the hardcoded 0"
      pattern: "molecule_index"
---

<objective>
Gamestart compose-seam generalization (SCORE-03 groundwork): the three molecule-0-hardcoded compose helpers gain a `molecule_index=0` default parameter, and the start_game compose sequence is extracted into ONE public `compose_molecule_view(registry, molecule_index=0)` the Phase-6 wizard lifecycle calls on every molecule/level advance.

Purpose: gamestart.py:105-108 explicitly defers advance-time re-framing to Phase 6; this plan builds that seam so the wizard's advance ops (06-05) can frame molecule N without duplicating composition logic.
Output: generalized helpers + extracted compose function + SMOKE-08 PART 5.
</objective>

<execution_context>
@~/.config/opencode/get-shit-done/workflows/execute-plan.md
@~/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-scoring-lifecycle-endgame/06-RESEARCH.md
@aamatch/gamestart.py
@smoke/smoke_08_starter.py

Seam map (verified):
- `_active_molecule_selection(registry)` gamestart.py:181-196 — `registry['molecules'][0]` at :193.
- `_frame_ligand_above_grid(registry)` gamestart.py:199-254 — molecule 0 at :216.
- `_move_ligand_in_front(registry)` gamestart.py:257-320 — molecule 0 in its body; idempotent no-op when already in front (:316-317).
- start_game compose sequence gamestart.py:405-407: `_move_ligand_in_front(registry)` -> `_frame_ligand_above_grid(registry)` -> `cmd.zoom(_active_molecule_selection(registry), buffer=5.0)`.
- `activate_game(wiz)` gamestart.py:323-339 — re-anchors the timer at :339 (start_timer(time.time())).

DELIBERATE DEVIATION from research D6 (record here and in the SUMMARY): the research proposed an additive `activate_game(wiz, anchor_timer=True)` param because its design pushed a NEW GameWizard at level advance (which would re-anchor). The adopted Phase-6 design (06-05) REBINDS THE SAME WIZARD INSTANCE at level advance and never calls activate_game mid-game — so the re-anchor hazard is structurally avoided and the param would be dead code. activate_game stays byte-identical; the no-reanchor property is ASSERTED in SMOKE-15 PART B (plan 06-05). The timer-continuation intent of D6 is honored; only the mechanism differs.

Laws: 03-07 framing laws hold per-molecule (zoom LAST, active-molecule-only frame, geometry-side front-offset); `_move_ligand_in_front`'s translate is chemistry-read-invariant (translation-invariant ligand_profile — engine.py:417-426 argument). Module identity: no new imports needed. python3.6 floor, %-formatting.
</context>

<tasks>

<task type="auto">
  <name>Task 1: molecule_index params + compose_molecule_view extraction</name>
  <files>aamatch/gamestart.py</files>
  <action>
    1. Add `molecule_index=0` as the trailing parameter of `_active_molecule_selection`, `_frame_ligand_above_grid`, and `_move_ligand_in_front`; replace each hardcoded `registry['molecules'][0]` (:193, :216, and the one inside _move_ligand_in_front's body) with `registry['molecules'][molecule_index]`. Update each docstring: "molecule_index defaults to 0 (the start path); the Phase-6 lifecycle passes the advanced index".
    2. Add the PUBLIC function `compose_molecule_view(registry, molecule_index=0)` placed after `_move_ligand_in_front` (:320), body EXACTLY the start_game:405-407 sequence parameterized:
       `_move_ligand_in_front(registry, molecule_index)` ; `_frame_ligand_above_grid(registry, molecule_index)` ; `cmd.zoom(_active_molecule_selection(registry, molecule_index), buffer=5.0)`. Docstring: the 03-07 framing laws + "the ONE compose home for starts AND Phase-6 molecule/level advances".
    3. Replace start_game:405-407 with `compose_molecule_view(registry)` (byte-identical behavior — same three calls, same order, default index 0).
    4. Update the module docstring's PHASE 5/6 NOTE (:105-108): re-framing is now AVAILABLE for any molecule index via compose_molecule_view (Phase 6 consumes it); the note's history stays.
    - Do NOT touch _last_start capture (:413-416), activate_game (:323-339), or any signature callers rely on beyond the additive default.
  </action>
  <verify>`python3.6 -m py_compile aamatch/*.py` green; `python3.6 -m unittest discover -s tests -v` ALL green (test_package_skeleton/test_wizard_source AST gates unaffected); grep `grep -n "molecules\]\[0\]" aamatch/gamestart.py` returns ZERO hits (all three hardcoded sites gone).</verify>
  <done>The three helpers are index-parameterized with default 0; compose_molecule_view exists public; start_game delegates; zero remaining hardcoded molecule-0 index reads in compose code.</done>
</task>

<task type="auto">
  <name>Task 2: SMOKE-08 regression + PART 5 (index-1 compose proof)</name>
  <files>smoke/smoke_08_starter.py</files>
  <action>
    1. Re-run SMOKE-08 UNCHANGED first (`bash smoke/run_smoke.sh smoke/smoke_08_starter.py`) — the default path must stay byte-identical (the compose extraction is behavior-neutral). All existing PARTs keep passing.
    2. Append PART 5 (ALWAYS tier, count-asserted, SMOKE-08 house style):
       a. `start_game(setup=sentinel-demo, seed=4242, activate=False)` (deferred prepare — the PART H shape).
       b. Compose molecule 1 EXPLICITLY: `gamestart.compose_molecule_view(registry_from_wiz, 1)`; assert: cmd.zoom happened (view changed vs pre-compose), molecule 0's objects are NOT in the zoom target (read the zoomed selection indirectly: capture `gamestart._active_molecule_selection(registry, 1)` == ' or '.join of ONLY molecule-1 object names — the string assert is deterministic), and molecule 0's ligand object position is UNCHANGED by the index-1 compose (geometry.centroid_of before/after — the front-offset only moved molecule 1's ligand).
       c. Idempotence: call compose_molecule_view(registry, 1) AGAIN -> molecule 1's ligand centroid UNCHANGED (the need <= 0 early return, :316-317).
       d. Default-index equivalence: compose_molecule_view(registry) == compose_molecule_view(registry, 0) on a fresh materialize (centroid + view equality).
       e. Restore: prefix cleanup -> baseline EXACTLY; `gamestart._last_start = None`.
    3. Keep the check-counter discipline (every PART prints its PASS/FAIL count; the verdict line stays `=== SMOKE-08 %s ===`).
  </action>
  <verify>`bash smoke/run_smoke.sh smoke/smoke_08_starter.py` greps `=== SMOKE-08 PASS ===` with PART 5 checks included and zero FAIL lines.</verify>
  <done>SMOKE-08 green including PART 5: index-1 compose frames molecule 1 only, is idempotent, and the default path is byte-identical.</done>
</task>

</tasks>

<verification>
- `python3.6 -m py_compile aamatch/*.py` && `python3.6 -m unittest discover -s tests -v` ALL green.
- `bash smoke/run_smoke.sh smoke/smoke_08_starter.py` -> `=== SMOKE-08 PASS ===` (all prior PARTs + new PART 5).
- PROSE_PIN: no docstring adds/removes banned-token mentions (tests/test_code_audit.py untouched — gamestart has no banned-token prose today; keep it that way).
</verification>

<success_criteria>
- compose_molecule_view(registry, molecule_index=0) is the single public compose home; start_game uses it; SMOKE-08 proves byte-identity + the index-1 case.
- 06-05 can re-frame any molecule with one call — no composition logic will be duplicated in the wizard.
</success_criteria>

<output>
After completion, create `.planning/phases/06-scoring-lifecycle-endgame/06-04-SUMMARY.md` (frontmatter: subsystem gamestart, provides compose_molecule_view signature for 06-05; records the D6 anchor_timer deviation rationale).
</output>
