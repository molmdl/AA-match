---
phase: 01-bootstrap-pure-foundation
plan: 09
type: execute
wave: 3
depends_on: ["01-01", "01-07"]
files_modified: []
autonomous: false
must_haves:
  truths:
    - "The repo-root package aamatch/ is plugin-path install-ready: no __pycache__ present at install time, Windows path of aamatch/__init__.py recorded for the human"
    - "A human installs via Plugin Manager directory install (pointing the dialog at aamatch/__init__.py) and, after restarting PyMOL, sees 'AA-match' under the Plugins menu and the placeholder line in the console (INSTALL-01)"
    - "The human explicitly confirms (or amends) the adopted setup defaults: molecules_per_level 2/min 1/cap 10, difficulty_levels 3/min 1/cap 9, interaction_mode 'unset'"
  artifacts:
    - path: ".planning/phases/01-bootstrap-pure-foundation/01-09-SUMMARY.md"
      provides: "recorded human verdict (menu present, click feedback, console clean) + the plugin path used + defaults decision"
  key_links:
    - from: "Plugin Manager install flow (directory install: dialog pointed at aamatch/__init__.py)"
      to: "aamatch/__init__.py::__init_plugin__"
      via: "installation.py:299-308 copytree of the package dir; installation.py:339-345 loads immediately in-session; restart re-runs GUI autoload"
      pattern: "AA-match"
---

<objective>
Prepare the repo-root package for Plugin-Manager **directory install via the plugin path** (automated) and run the [HUMAN] install checkpoint (Phase-1 success criterion 4 / INSTALL-01): Plugin-Manager install → restart → "AA-match" menu entry → placeholder prints. The same checkpoint records the human's confirmation of the adopted setup defaults.

Purpose: Menu registration is NOT assertable headless (HAVE_QT=False → `addmenuitemqt` raises, loader catches cleanly — by design). Only a human in a real GUI session can prove INSTALL-01. This is a by-design verification step, not a gap. No build artifact is needed: `installation.py:299-308` installs a package directory directly when the file dialog is pointed at its `__init__.py`.

Output: plugin path prepared (pycache-free repo package, Windows path recorded) + recorded human verdict (menu present, click feedback, console clean, defaults confirmed).
</objective>

<execution_context>
@~/.config/opencode/get-shit-done/workflows/execute-plan.md
@~/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/01-bootstrap-pure-foundation/01-RESEARCH-plugin-install.md   # §1.1 install contract, §2.4 exact human steps, §4 pitfalls E/F/G/H/I
@.planning/phases/01-bootstrap-pure-foundation/01-05-SUMMARY.md                 # the adopted defaults being confirmed
@.planning/phases/01-bootstrap-pure-foundation/01-07-SUMMARY.md                 # headless proof already passed
</context>

<tasks>

<task type="auto">
  <name>Task 1: Prepare the plugin-path install target (WSL, automated)</name>
  <files>aamatch/__pycache__ (removed)</files>
  <action>
From the repo root:

1. `python3.6 -c "import shutil; shutil.rmtree('aamatch/__pycache__', ignore_errors=True)"` — REQUIRED: directory install does `shutil.copytree(odir, mod_dir)` UNFILTERED (`installation.py:306`), so any `aamatch/__pycache__` present at install time (regenerated by the 01-07 headless smoke and WSL test runs) would be copied verbatim into the startup dir. Run this immediately before handing off to the human. NOTE: `rm` is denied in opencode.json — use the shutil one-liner exactly as written.
2. Verify the repo package is the complete install source: `ls aamatch` shows only shipped package files (`.py` modules; no `__pycache__` after step 1) — the directory-install branch copies the WHOLE dir (`installation.py:299-308`), and per the 01-01 layout decision `tests/` and `smoke/` live at repo root, so nothing extraneous rides along.
3. Verify + record the Windows-visible install path for the human: `/mnt/c/Users/nglok/Desktop/WORKDIR/molmdl/AA-match/aamatch/__init__.py` must exist; its Windows form is `C:\Users\nglok\Desktop\WORKDIR\molmdl\AA-match\aamatch\__init__.py`. The Plugin Manager dialog must be pointed at `__init__.py` (NOT the directory): the file dialog offers only `*.py` + archives — no directory mode (`legacysupport.py:60-66`), and the install branch keys on the file name being `__init__.py` (`installation.py:299-301`).
  </action>
  <verify>`ls aamatch` lists the shipped package files with zero `__pycache__`; `/mnt/c/Users/nglok/Desktop/WORKDIR/molmdl/AA-match/aamatch/__init__.py` exists (Windows path resolves)</verify>
  <done>Repo package is plugin-path install-ready: pycache-free, Windows path of `aamatch/__init__.py` recorded</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>
The AA-match plugin skeleton (Phase 1), installed from the repo package via the plugin path (directory install): `aamatch` package with metadata-first `__init__.py`, `__init_plugin__` registering the "AA-match" Plugins-menu item via `addmenuitemqt`, and a Qt-free print placeholder. The headless smoke (01-07) already proved the package loads clean under real Windows PyMOL; this checkpoint proves the GUI install + menu wiring that headless mode cannot assert.
  </what-built>
  <how-to-verify>
1. Launch PyMOL 2.5.0 (GUI, Windows conda env) as you normally would.
2. Open **Plugins → Plugin Manager**.
3. Click **"Install from local file"** and pick `C:\Users\nglok\Desktop\WORKDIR\molmdl\AA-match\aamatch\__init__.py` — pointing at the package's `__init__.py` is the directory install: the whole `aamatch/` dir is copied to the startup dir (`installation.py:299-308`).
4. If a **"Select plugin directory"** dialog appears (only when more than one startup path exists): pick `%APPDATA%\pymol\startup` if offered, else accept the default. If the chosen directory is not writable, PyMOL itself offers to create `%APPDATA%\pymol\startup` — accept. (Avoid letting it install into the conda env's own startup dir — that copy is lost on env changes.)
5. Expect the dialog **"Success: Plugin 'aamatch' has been installed."** The menu item is already live in this session.
6. **Restart PyMOL** (required by INSTALL-01's restart leg). After restart, GUI startup autoloads plugins.
7. Verify: the **Plugins menu shows "AA-match"** (directly under the top-level Plugins menu, below Plugin Manager). Click **AA-match** → the PyMOL console prints: `AA-match 0.1.0: plugin skeleton OK — game UI arrives in Phase 4.`
8. If the menu item is MISSING after restart: check the PyMOL console for `Unable to initialize plugin 'aamatch'` — load errors never crash PyMOL, they only print. Report the console text.
9. **Defaults confirmation** (research proposals adopted in 01-05 — confirm or amend):
   - `molecules_per_level`: default **2**, min **1**, cap **10**
   - `difficulty_levels`: default **3**, min **1**, cap **9**
   - `interaction_mode` (when the user has not chosen): **"unset"** → the generator picks a random required set
   These become frozen schema constants for Phases 2+; changing them later is a version-bump event.
  </how-to-verify>
  <resume-signal>Type "approved" (menu present + placeholder printed + defaults confirmed) — or list what failed / which defaults to change.</resume-signal>
</task>

</tasks>

<verification>
- Plugin path prepared: `aamatch/__pycache__` removed before the human install (copytree is unfiltered, `installation.py:306`) and the Windows path of `aamatch/__init__.py` recorded
- Human verdict recorded in the phase UAT/summary: menu present Y/N, click feedback Y/N, console clean Y/N
- Defaults confirmed or amendment list captured
</verification>

<success_criteria>
- INSTALL-01 proven: Plugin-Manager directory install from the repo package path (`C:\...\AA-match\aamatch\__init__.py`) works, restart re-registers "AA-match" under the Plugins menu, clicking it prints the placeholder line
- Adopted defaults explicitly human-approved (or amendments recorded as follow-up todos)
</success_criteria>

<output>
After completion, create `.planning/phases/01-bootstrap-pure-foundation/01-09-SUMMARY.md` (include the human verdict + the plugin path used + defaults decision verbatim)
</output>
