---
phase: 02-headless-game-engine
plan: 14
type: execute
wave: 7
depends_on: ["02-08", "02-09", "02-10", "02-13"]
files_modified:
  - aamatch/engine.py
  - smoke/smoke_04_e2e.py
autonomous: true

must_haves:
  truths:
    - "generate -> materialize -> scripted placement -> detect -> score runs headlessly end-to-end in real PyMOL with count assertions at every step (ROADMAP criterion 4)"
    - "A scripted pose that forms the required interaction scores 1.0; grid poses score 0.0; rotating the WHOLE scene re-detects identically (no camera/frame leak); reset_to_grid restores grid poses and zero score"
    - "Engine ops are the composition root Phases 3-7 call: new_game / materialize / place_aa / reset_to_grid / detect / score — no Qt anywhere"
  artifacts:
    - path: "aamatch/engine.py"
      provides: "new_game, materialize, place_aa, reset_to_grid, detect, score_current, confirm"
      min_lines: 120
    - path: "smoke/smoke_04_e2e.py"
      provides: "The count-asserted E2E smoke (parts A/B/C)"
      min_lines: 120
  key_links:
    - from: "aamatch/engine.py"
      to: "aamatch/detector.py"
      via: "geometry extraction -> pure detect"
      pattern: "detector.detect"
    - from: "aamatch/engine.py"
      to: "aamatch/game_state.py"
      via: "score_current updates GameState via score(required, results)"
      pattern: "game_state.score"
    - from: "smoke/smoke_04_e2e.py"
      to: "aamatch/engine.py"
      via: "engine ops drive the whole E2E"
      pattern: "import aamatch.engine|from aamatch import engine"
---

<objective>
Build the headless engine (composition root over pure generator/detector/scoring + cmd placement/geometry) and prove the full generate -> place -> detect -> score loop end-to-end in real PyMOL with rotation invariance and spec-replay reset (SMOKE-04).

Purpose: ROADMAP Phase-2 success criterion 4 ([HEADLESS] E2E) — the entire phase's payoff.
Output: aamatch/engine.py + passing SMOKE-04.
</objective>

<execution_context>
@~/.config/opencode/get-shit-done/workflows/execute-plan.md
@~/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/02-headless-game-engine/02-RESEARCH-materialization.md  # §6 placement mechanics + §7.2 engine ops + §7.3 E2E data flow + §8.2 SMOKE-04 design
@aamatch/generator.py, @aamatch/detector.py, @aamatch/game_state.py       # pure surfaces consumed
@aamatch/placement.py, @aamatch/geometry.py                               # cmd surfaces consumed
@aamatch/manifest.py, @aamatch/persistence.py, @aamatch/paths.py          # data + file discipline
</context>

<tasks>

<task type="auto">
  <name>Task 1: engine.py — headless operation set</name>
  <files>aamatch/engine.py</files>
  <action>
Cmd tier composition root (module-level `from pymol import cmd` legal; NO Qt anywhere — grep-gated by the standing purity hygiene). Ops (count-asserted, plain-data in/out, per 02-RESEARCH-materialization.md §7.2):
1. `new_game(setup, seed)` -> (payload, manifest_entries): read aamatch/data/MANIFEST.json (read_json_file + parse_manifest_dict); candidates = enumerate_entries; for demo mode: load the setup's demo set entries (v1: single-set dev manifest — filter by set_id if setup.demo_set_id set, else all); for each molecule ligand_data needed by generate: load ligand into a _aam_tmp name -> geometry.bounding_sphere -> DELETE the temp object (object list must be unchanged across new_game — assert); then payload = generator.generate(seed, validate_state(setup), candidates_sorted, ligand_data, setup['difficulty_levels']); write nothing (save-to-file is a caller concern via persistence).
2. `materialize(payload)` -> registry (delegates to placement.materialize; keeps the registry on the engine instance for later ops).
3. `place_aa(slot_id, position)` -> translate_to over the registry's object for that slot (count-asserted).
4. `reset_to_grid()` -> placement.reset_to_grid(payload, registry).
5. `detect()` -> (records, bonds): geometry.extract_game_atoms() + geometry.ligand_bonds(lig_object from registry) -> detector.detect(records, bonds).
6. `score_current(level_index, molecule_index, required)` -> (score, formed_types): game_state.score(required, records) + record into a module-level GameState; returns plain data for a future UI.
7. `confirm(level_index, molecule_index, required)` -> detect + score_current composition returning (records, score, formed_types) — the Phase-3 Confirm handler will wrap exactly this.
Docstring: state split (spec = single source of truth; runtime in GameState; PyMOL holds atoms + sentinels only).
  </action>
  <verify>python3.6 -m py_compile aamatch/engine.py && python3.6 -m unittest discover -s tests -v  (purity untouched)</verify>
  <done>Engine ops implemented; no Qt imports; object-list hygiene asserted in new_game</done>
</task>

<task type="auto">
  <name>Task 2: SMOKE-04 parts A + B — happy path + rotation invariance</name>
  <files>smoke/smoke_04_e2e.py</files>
  <action>
smoke_04_e2e.py per conventions (flush prints; per-check labels `SMOKE-04 <label> PASS|FAIL <detail>`; final marker === SMOKE-04 PASS === only if ALL parts pass). PART A (happy path): engine.new_game(setup=block_exclusive ['pi_stacking'], molecules 1, D 3, seed 42) -> engine.materialize -> assert object growth; assert score 0.0 from the grid (detect + score with no placement); find the required PHE slot (role=='required', can_form==['pi_stacking']); compute the ligand benzene-ring center from extracted ligand records (use capability typing on records + bonds to get ring atom indices, mean their coords); compute the PHE ring center from its extracted side-chain ring atoms (AA_RESIDUES['PHE']['rings'][0]); engine.place_aa(slot, ligand_ring_center - phe_ring_center + (0, 0, 4.5) + phe_current_position_offset) i.e. translate the AA so its ring center lands 4.5 A directly above the ligand ring center (offset 0.0 < 2.0; normals parallel) -> detect -> assert >= 1 pi_stacking record with aa side == the placed object -> score_current == 1.0. PART B (rotation invariance, probe pattern from materialization research §6.3): cmd.rotate('z', 37.0, '_aam_*', camera=0, origin=ligand ring center) — BAKES every game object equally; re-detect -> assert the formed-record set equals Part A's (same types/objects/sides; metrics within 1e-6) and score still 1.0. py_compile BEFORE the Windows run.
  </action>
  <verify>python3.6 -m py_compile smoke/smoke_04_e2e.py && bash smoke/run_smoke.sh smoke/smoke_04_e2e.py 240  (marker in output)</verify>
  <done>E2E happy path + pipeline-level rotation invariance green in real PyMOL</done>
</task>

<task type="auto">
  <name>Task 3: SMOKE-04 part C — reset replay + full gate</name>
  <files>smoke/smoke_04_e2e.py</files>
  <action>
PART C (reset): engine.reset_to_grid() -> every AA object centroid == its effective spec grid pose within 1e-6; detect -> ZERO pi_stacking records (grid is beyond the gap) and score_current == 0.0; cleanup via placement.cleanup_game_objects() -> object list == pre-game snapshot. Then run the FULL WSL suite + both earlier smokes to confirm no regressions.
  </action>
  <verify>bash smoke/run_smoke.sh smoke/smoke_04_e2e.py 240 && bash smoke/run_smoke.sh smoke/smoke_03_generate.py 180 && python3.6 -m unittest discover -s tests -v</verify>
  <done>SMOKE-04 fully green (A+B+C); SMOKE-03 still green; WSL suite green</done>
</task>

</tasks>

<verification>
bash smoke/run_smoke.sh smoke/smoke_04_e2e.py 240 && python3.6 -m unittest discover -s tests -v
</verification>

<success_criteria>
ROADMAP Phase-2 criterion 4 ([HEADLESS] count-asserted E2E) is green end-to-end, including rotation invariance and spec-replay reset.
</success_criteria>

<output>
After completion, create `.planning/phases/02-headless-game-engine/02-14-SUMMARY.md`
</output>
