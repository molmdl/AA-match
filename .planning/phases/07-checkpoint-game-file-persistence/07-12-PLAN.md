---
phase: 07-checkpoint-game-file-persistence
plan: 12
type: execute
wave: 8
depends_on: [07-11]
files_modified: []
autonomous: false

must_haves:
  truths:
    - "[HUMAN] Save writes a real .aamz via the Game tab's Save button (dialog feel, default name, extension append) and the game keeps running after the save"
    - "[HUMAN] save -> quit -> relaunch -> load restores placed positions/orientations, scores, counters, and a timer that continues from the saved elapsed — the full criterion-1 human half on setenv.bat PyMOL"
    - "[HUMAN] The Import button loads a previously exported game file and reconstructs the initial game; stale/foreign files are refused with clear message boxes (criterion 2)"
    - "[GATE] Console is clean during save/load (no Session-Warning, no tracebacks); restore works after a plugin reload; the installed-identity pickle round-trip is exercised"
  artifacts:
    - path: ".planning/phases/07-checkpoint-game-file-persistence/07-12-SUMMARY.md"
      provides: "The human verdict table per criterion + any fix-batch record"
      contains: "VERDICT"
  key_links:
    - from: "Human GUI session"
      to: "ROADMAP criteria 1-3"
      via: "the [HUMAN]/[GATE] halves that no headless smoke can prove (dialogs, console cleanliness, relaunch, plugin reload)"
      pattern: "checkpoint"
---

<objective>
The [HUMAN] consolidated GUI checkpoint for Phase 7: verify ROADMAP criteria 1 ([HUMAN] half), 2, and 3 ([GATE]) on real Windows PyMOL 2.5.0 (setenv.bat, 01-09 plugin-path install), record verdicts, and apply any fix-batch.

Purpose: dialogs, console cleanliness, a real quit->relaunch, and a plugin reload are not headlessly provable. This checkpoint closes PERSIST-02, PERSIST-03, and SCORE-08's human halves.
Output: verdict table in 07-12-SUMMARY.md (+ fix-batch commit if needed).
</objective>

<execution_context>
@~/.config/opencode/get-shit-done/workflows/execute-plan.md
@~/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/07-checkpoint-game-file-persistence/07-11-SUMMARY.md
@.planning/phases/07-checkpoint-game-file-persistence/07-RESEARCH-import.md
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Checkpoint: Phase 7 GUI verification (criteria 1-3)</name>
  <what-built>
The complete Phase-7 persistence stack: Game-tab Save button (full-session .pse + sidecar in one .aamz), Import button dispatching game files vs checkpoints, sentinel-first checkpoint resume (positions/orientations/scores/counters/timer), wizard pickle restore, version-gate refusals — all headlessly proven by SMOKE-17/18/19/20 (see 07-11-SUMMARY.md verdicts).
  </what-built>
  <how-to-verify>
Run on Windows PyMOL 2.5.0 via setenv.bat (repo on the plugin path). Console baseline = the 04-14 recorded stock-plugin warnings; anything beyond that is a finding.

1. **Save flow (criterion 1, part 1):** Plugins -> AA-match -> Start a game (defaults). Move/rotate an AA (panel buttons/keys), run Hint, Skip one molecule. Game tab -> Save -> the dialog opens with default name `game.aamz` + filter 'AA-match Checkpoint (*.aamz)'; accept into a folder of your choice. EXPECT: the file exists; the info box logs 'Game saved to <path>.'; NO success popup; the game keeps running (timer still ticking, wizard still active); console clean.
2. **Save -> quit -> relaunch -> load (criterion 1, part 2):** note the scores/skip count/timer reading -> QUIT PyMOL entirely -> relaunch via setenv.bat -> Game tab -> Import -> pick the saved .aamz. EXPECT: the scene reappears exactly (placed positions AND orientations), the info box re-arms (resumed line + level/required lines), the timer continues from approximately the saved elapsed (not zero, not the wall-clock gap), scores/counters shown via the wizard panel match. Play on: move an AA and Confirm — scoring continues. Console clean throughout the load (NO 'Session-Warning: unable to restore wizard.').
3. **Import a game file (criterion 2, part 1):** Setup tab -> generate + export a game (.aamatch.json). Game tab -> Import -> pick the exported file. EXPECT: countdown 3-2-1 -> GO, fresh timer from zero, fresh scores, 'Game imported: <path>.' logged after the countdown arms.
4. **Refusals (criterion 2, part 2):** Import -> pick a random non-AA-match file (e.g. a .txt or a .pse). EXPECT a clear message box (not a traceback). If convenient, also try a hand-edited .aamatch.json with a bumped version or a stale detector stamp -> the version/detector message box names the cause.
5. **Plugin reload + installed identity (criterion 3):** with a game running, save a checkpoint, then restart PyMOL and load it (step-2 flow already covers relaunch). For the identity half: confirm the load works from the INSTALLED plugin session (the 01-09 plugin-path install is the running identity — this whole session IS the installed-identity exercise). If a 'Session-Warning' ever appears, record the exact console text.
6. **Console + robustness sweep:** zero AA-match tracebacks across all steps; double-Import mid-game replaces cleanly (old game discarded like Start).
  </how-to-verify>
  <resume-signal>Type "approved" or describe issues (per-step verdicts welcome: "1 pass, 2 pass, ...")</resume-signal>
</task>

<task type="auto">
  <name>Task 2: Record verdicts + fix-batch if needed</name>
  <files></files>
  <action>
Record the per-step verdict table into `.planning/phases/07-checkpoint-game-file-persistence/07-12-SUMMARY.md` (verdict, exact human quotes where given, console observations vs the 04-14 baseline). If the human reported issues: apply the fix-batch (Rule 1 auto-fixes for bugs; anything architectural STOPs per Rule 4), re-run the affected smokes + the WSL suite, and record the fix commit. Update the must-have truths that the human confirmed.
  </action>
  <verify>The SUMMARY contains the verdict table; if fixes were applied: py_compile + WSL suite + affected smokes re-green, fix commit hash recorded</verify>
  <done>Phase-7 criteria 1 ([HUMAN] half), 2, 3 ([GATE]) carry recorded human verdicts; any fix-batch is committed and re-verified</done>
</task>

</tasks>

<verification>
1. The human verdict table exists with per-criterion coverage (criterion 1 human half = steps 1-2; criterion 2 = steps 3-4; criterion 3 = steps 2/5 + console cleanliness).
2. Any fix-batch is committed with re-green proofs.
</verification>

<success_criteria>
- All three ROADMAP Phase-7 criteria now carry BOTH mechanical (07-11 battery) and [HUMAN] verdict coverage.
- PERSIST-02, PERSIST-03, SCORE-08 are human-verified end to end.
</success_criteria>

<output>
After completion, create `.planning/phases/07-checkpoint-game-file-persistence/07-12-SUMMARY.md`
</output>
