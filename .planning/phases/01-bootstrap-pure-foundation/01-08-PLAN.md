---
phase: 01-bootstrap-pure-foundation
plan: 08
type: execute
wave: 3
depends_on: ["01-02", "01-03", "01-04", "01-05", "01-06"]
files_modified:
  - tests/test_purity.py
  - AGENTS.md
autonomous: true
must_haves:
  truths:
    - "Purity is ENFORCED, not aspired: an AST import scan fails the suite if any pure module imports pymol/Qt/numpy/dataclasses (module level OR function bodies)"
    - "The zero-stubs proof is a clean-subprocess import test: every pure module imports under bare python3.6 with cwd=repo-root"
    - "A negative control proves the gate can fail (synthetic bad source is flagged)"
    - "AGENTS.md carries the standing gates + rules every later phase inherits"
  artifacts:
    - path: "tests/test_purity.py"
      provides: "Gate A (AST import scan over the 5 pure modules + lazy-__init__ rule), Gate B (clean-subprocess import), Gate D (py_compile via subprocess), negative control"
      min_lines: 100
    - path: "AGENTS.md"
      provides: "standing Phase-1 gates section: commands, purity rationale, two version-gates rule, module-identity rule, dev-loop rule"
      contains: "python3.6 -m unittest discover"
  key_links:
    - from: "tests/test_purity.py Gate A"
      to: "aamatch/{setup_state,level_spec,persistence,backup,paths}.py"
      via: "ast.walk over ALL Import/ImportFrom nodes (immune to docstring false positives)"
      pattern: "FORBIDDEN"
    - from: "tests/test_purity.py Gate B"
      to: "aamatch/__init__.py"
      via: "subprocess import proves the package has zero module-level pymol/Qt imports (no stubs needed)"
      pattern: "import aamatch"
---

<objective>
Enforce the purity contract mechanically (Phase-1 success criterion 2) and install the standing rules into AGENTS.md so every later phase inherits them: AST import gate + clean-subprocess import gate + 3.6 syntax gate over ALL pure modules, documented with the prior-art false-positive warning.

Purpose: "Purity" that is only prose regresses silently (the prior art needed MagicMock stubs because `__init__.py` imported Qt at module level — the exact anti-pattern PITFALL 1 forbids). These gates protect every later phase; AGENTS.md makes them standing policy.

Output: `tests/test_purity.py` (runs as part of the normal suite) + updated root AGENTS.md.
</objective>

<execution_context>
@~/.config/opencode/get-shit-done/workflows/execute-plan.md
@~/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/01-bootstrap-pure-foundation/01-RESEARCH-pure-foundation.md   # R8 gates (verbatim), P1/P2/P10 pitfalls, A2 stdlib whitelist, G1/G3 prior-art lessons
@.planning/research/PITFALLS.md                                                 # Pitfall 1 (Qt tiers), 3 (stdlib purity)
@AGENTS.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Write tests/test_purity.py (AST gate + subprocess gate + syntax gate + negative control)</name>
  <files>tests/test_purity.py</files>
  <action>
Stdlib-only unittest file (this file itself must import NOTHING forbidden — it imports `ast, os, subprocess, sys, unittest` and the `aamatch` pure modules list as STRINGS). Include the `sys.path.insert(0, ...)` bootstrap. Constants:

- `PURE_MODULES = ['setup_state', 'level_spec', 'persistence', 'backup', 'paths']` (import targets: `aamatch.<name>`)
- `FORBIDDEN = {'pymol', 'numpy', 'dataclasses', 'PyQt5', 'PySide2', 'tkinter', 'Pmw', 'pmg_tk'}`
- `ALLOWED_STDLIB = {'json', 'os', 'sys', 'tempfile', 'time', 'hashlib', 'random', 'copy', 'math', 'io', 're', 'collections', 'errno', 'ast', 'zipfile', 'shutil', 'unittest', 'datetime'}` (A2 list + sys)

**Gate A — AST import scan (one test over all pure modules):** for each pure module, read `aamatch/<mod>.py`, `ast.parse`, walk EVERY node; for each `ast.Import`/`ast.ImportFrom` (module level AND inside function bodies — research B7's rule):
- absolute root (first name of the dotted module) must NOT be in FORBIDDEN;
- absolute roots must be in ALLOWED_STDLIB ∪ {'aamatch'} (intra-package absolute) — anything else fails;
- relative imports (level > 0) must resolve to one of the PURE_MODULES (or '.'), never to a cmd/Qt module.
Assert failure messages name the file + the offending import (so regressions are greppable).

**Gate A2 — lazy-`__init__` rule (separate test):** `ast.parse` of `aamatch/__init__.py`; assert NO `ast.Import`/`ast.ImportFrom` in `tree.body` (module level). Function-level pymol imports inside `__init_plugin__` are ALLOWED — that is the design (R8.5).

**Gate B — clean-subprocess import (one test, loop subTests):** for each pure module: `subprocess.run([sys.executable, '-c', 'import aamatch.' + mod], cwd=REPO_ROOT, capture_output=True, timeout=30)`; assert returncode == 0; on failure include stderr tail in the assertion message. NEVER assert `sys.modules` cleanliness in-process (P10: this process has already imported the package; subprocess exit codes are the only honest proof).

**Gate D — 3.6 syntax (one test, loop subTests):** for each `.py` file in `aamatch/`: `subprocess.run([sys.executable, '-m', 'py_compile', path], cwd=REPO_ROOT, capture_output=True)`; assert returncode == 0.

**Negative control (one test):** run the Gate-A checker FUNCTION against a synthetic source string containing `import pymol` and `from PyQt5 import QtWidgets` (and a docstring saying "from PyQt5 import" to prove docstring immunity) — assert it flags the two REAL imports and does NOT flag the docstring (P2 immunity proof). The checker must therefore be a pure function in the test file (e.g. `find_bad_imports(src) -> list[str]`), callable on strings, not only on files.

Do NOT create `tests/stubs.py` and do NOT stub sys.modules ANYWHERE — the whole point is zero stubs.
  </action>
  <verify>`python3.6 -m unittest tests.test_purity -v` → all gates green</verify>
  <done>AST scan + subprocess import + py_compile gates green over all 5 pure modules; negative control proves the gate can fail and ignores docstrings</done>
</task>

<task type="auto">
  <name>Task 2: Document standing gates in AGENTS.md</name>
  <files>AGENTS.md</files>
  <action>
Add a new section to root `AGENTS.md` (after the "GSD workflow" section, before "Parallel subagent execution") titled `## AA-match standing gates (Phase 1 — inherit into every phase)`. Content:

1. **Commands (run from repo root):**
   - `python3.6 -m py_compile aamatch/*.py` — syntax floor (3.6; catches 3.7+ syntax).
   - `python3.6 -m unittest discover -s tests -v` — ALL WSL tests INCLUDING the purity gates (`tests/test_purity.py`).
   - `bash smoke/run_smoke.sh smoke/<script>.py` — headless Windows PyMOL proof (greps `=== SMOKE-0N PASS ===`; exit codes cannot carry verdicts through cmd.exe).
2. **Purity contract:** pure modules (`setup_state`, `level_spec`, `persistence`, `backup`, `paths`) import stdlib + other pure modules ONLY — no `pymol`, no Qt, no numpy, no dataclasses, at module level OR inside any function body. `aamatch/__init__.py` keeps ZERO module-level imports (lazy imports inside `__init_plugin__`/handlers). Tests import pure modules with ZERO sys.modules stubs — do NOT add stubs (ARCHITECTURE.md's stub pattern is superseded for AA-match; the enforced gates are `tests/test_purity.py`).
3. **Grep-gate warning (Gate C, documented-only):** token greps fire on prose — the prior art "hit a false positive on a docstring that said 'from PyQt5 import'". The AST gate is the enforced mechanism; greps are for human plan checks with inspection, never blind-fail.
4. **Two version gates, never conflate:** format `version` (container + level-spec payload) refuses only NEWER and accepts older (additive-only evolution, `.get` defaults on read); `detector_version` refuses ANY mismatch (exact match — changed detection semantics make specs unsolvable). Constants live in `aamatch/persistence.py` (`FORMAT_VERSION`) and `aamatch/level_spec.py` (`DETECTOR_VERSION`, `LEVEL_SPEC_VERSION`).
5. **Module identity:** after a real install the plugin imports as `pmg_tk.startup.aamatch`; in smokes/direct imports it is `aamatch`. NEVER mix both import mechanisms in one PyMOL session (two module objects → duplicate singletons later).
6. **Dev loop:** headless smokes run against the REPO copy (repo is Windows-visible on /mnt/c — no staging step). Plugin-Manager installs are for human checkpoints only; an installed copy never sees repo edits until reinstalled.
7. **Windows env:** point to `.planning/phases/01-bootstrap-pure-foundation/windows-env-versions.md` for the recorded Python/Qt/PyMOL/numpy versions and the cmd.load path-probe results.

Do NOT delete or rewrite any existing AGENTS.md content — this is an additive section.
  </action>
  <verify>`grep -c "standing gates" AGENTS.md` ≥ 1; the three commands appear verbatim in AGENTS.md</verify>
  <done>Standing rules committed where every future session/agent reads them</done>
</task>

<task type="auto">
  <name>Task 3: Full-suite green run + commit</name>
  <files>tests/test_purity.py</files>
  <action>
Run `python3.6 -m unittest discover -s tests -v` from repo root — EVERYTHING must be green (skeleton contract, persistence, backup, paths, setup_state, level_spec, purity gates). Fix nothing outside this plan's files if a failure appears; if another plan's test fails, report it in the summary instead. Commit: `feat(01-08): enforce purity gates (AST + clean-subprocess) + document standing rules`.
  </action>
  <verify>full discover run → OK, zero failures</verify>
  <done>Phase-1 success criterion 2 mechanically proven and permanent</done>
</task>

</tasks>

<verification>
- `python3.6 -m unittest discover -s tests -v` → OK (all suites + purity gates)
- Temporarily adding `import numpy` to any pure module (scratch check) would fail Gate A — negative control already proves this without touching real files
- AGENTS.md contains the standing-gates section with the three commands
</verification>

<success_criteria>
- Purity enforced: AST scan (all import nodes, all bodies), clean-subprocess zero-stubs proof, 3.6 py_compile — all green and part of the normal suite
- Negative control demonstrates gate sensitivity + docstring immunity
- AGENTS.md carries the standing gates, two-version-gates rule, module-identity rule, dev-loop rule, env-versions pointer
</success_criteria>

<output>
After completion, create `.planning/phases/01-bootstrap-pure-foundation/01-08-SUMMARY.md`
</output>
