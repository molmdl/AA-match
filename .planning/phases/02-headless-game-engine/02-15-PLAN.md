---
phase: 02-headless-game-engine
plan: 15
type: execute
wave: 8
depends_on: ["02-14"]
files_modified:
  - smoke/smoke_05_perf.py
  - tests/test_code_audit.py
autonomous: true

must_haves:
  truths:
    - "On the largest bundled molecule + a max 9x9 grid, geometry-extract and detect complete inside the Phase-2 budget (detect < 100 ms asserted; extract+detect < 1000 ms asserted) with per-stage timings printed"
    - "The no-naive-pair-loop audit is enforced mechanically: brute_force_pairs is callable only from tests; candidate generation routes through spatial.cross_pairs; cmd.get_model appears nowhere"
    - "Generated specs carry detector_version and a stale-stamped spec is refused by the parse gate inside the same smoke"
  artifacts:
    - path: "smoke/smoke_05_perf.py"
      provides: "HEADLESS perf smoke (SMOKE-05) with staged timings + count asserts"
      min_lines: 90
    - path: "tests/test_code_audit.py"
      provides: "AST-level vectorization/pruning audit (permanent suite member)"
      min_lines: 60
  key_links:
    - from: "smoke/smoke_05_perf.py"
      to: "aamatch/manifest.py"
      via: "largest_entry over enumerate_entries selects the perf target"
      pattern: "largest_entry"
    - from: "tests/test_code_audit.py"
      to: "aamatch/detector.py + spatial.py"
      via: "AST audit of pruning discipline"
      pattern: "cross_pairs|brute_force_pairs"
---

<objective>
Close DETECT-05: the HEADLESS perf smoke on the largest bundled molecule (staged extract/detect timings inside the budget) plus the mechanical vectorization/code audit and the detector-version stamp proof.

Purpose: ROADMAP Phase-2 success criterion 5 — the phase's last gate before the GUI phases.
Output: passing SMOKE-05 + a permanent AST audit test.
</objective>

<execution_context>
@~/.config/opencode/get-shit-done/workflows/execute-plan.md
@~/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/02-headless-game-engine/02-RESEARCH-detection.md        # §5.2 pipeline + §8 perf budgets + §8.3 smoke design + §9.9 audit list
@.planning/phases/02-headless-game-engine/02-RESEARCH-materialization.md  # §9 perf smoke design + budget numbers
@.planning/research/PITFALLS.md                                            # Pitfall 15 (budgets; get_model trap)
@aamatch/manifest.py, @aamatch/generator.py, @aamatch/detector.py, @aamatch/geometry.py, @aamatch/placement.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Mechanical code audit (AST test)</name>
  <files>tests/test_code_audit.py</files>
  <action>
tests/test_code_audit.py (pure; ast + os only, mirrors test_purity.py's style):
1. detector.py source: contains a call to spatial.cross_pairs (candidate generation routes through the cell list); does NOT call brute_force_pairs anywhere (the oracle is tests-only); contains NO nested for-loop pair where the OUTER loop iterates atom records and the INNER loop iterates atom records over the full sets — implement pragmatically: parse the AST, walk function bodies, flag any function containing an ast.For whose body contains another ast.For whose iter call chain mentions 'atoms' or 'records' — then assert the flagged set is EMPTY (if a legitimate nested pattern ever appears it must be renamed/refactored, keeping this gate strict).
2. spatial.py: cross_pairs implementation contains NO O(N*M) double loop over the full input lists (the only double loop allowed is the 3x3x3 neighbor scan over dx/dy/dz in (-1,0,1)); brute_force_pairs exists and its docstring says 'test oracle'.
3. aamatch/*.py: the string 'get_model' appears in NO module (grep via source read — Pitfall 15 OOM trap); 'get_object_ttt' appears in NO module (probe-proven SEGFAULT hazard — banned); 'matrix_reset' appears in NO module (probe-proven coordinate reverter — banned in Phase 2).
Keep assertions precise so the suite stays a honest audit, not noise.
  </action>
  <verify>python3.6 -m unittest tests.test_code_audit -v</verify>
  <done>Audit green: pruning via cell list only, oracle tests-only, banned APIs absent</done>
</task>

<task type="auto">
  <name>Task 2: SMOKE-05 — headless perf smoke</name>
  <files>smoke/smoke_05_perf.py</files>
  <action>
smoke_05_perf.py per conventions (flush prints; labels; final marker === SMOKE-05 PASS ===): read MANIFEST.json; targets = enumerate_entries; biggest = manifest.largest_entry(targets) (max heavy_atom_count); build a max-grid game: setup D=10 (tier 9 -> grid_n 9 -> 81 AAs), mode block_exclusive ['pi_stacking'] (benzamide is the largest fixture and supports it), molecules 1, seed 7; new_game via engine (temp-object hygiene asserted); materialize (assert 82 objects); scripted placement forming >= 1 pi_stacking (same ring-center computation as SMOKE-04 part A). TIMING (stdlib time.time, 3 decimal places): t1 = geometry.extract_game_atoms() + ligand_bonds wall; t2 = detector.detect wall; print 'SMOKE-ENV perf extract_ms=<int> detect_ms=<int> atoms=<int>' (ONE line, single spaces). ASSERT: detect_ms < 100 AND (extract_ms + detect_ms) < 1000 (Phase-2 gate, materialization research §9; the 100 ms detect figure is PITFALLS' stated budget). Count-assert: >= 1 pi_stacking record formed. DETECTOR-VERSION STAMP: payload['detector_version'] == DETECTOR_VERSION; then mutate a deep-copied payload's detector_version to 'det-0' -> make_level_spec_container + parse_level_spec_dict -> MUST raise FormatError (stale games refused — ROADMAP criterion 5's stamp proof, executed headlessly). teardown: cleanup_game_objects -> pre-game snapshot. py_compile BEFORE the Windows run.
  </action>
  <verify>python3.6 -m py_compile smoke/smoke_05_perf.py && bash smoke/run_smoke.sh smoke/smoke_05_perf.py 300  (marker + SMOKE-ENV perf line in output)</verify>
  <done>Perf inside budget on the largest molecule + 81-AA grid; stamp refusal proven; marker-gated pass</done>
</task>

<task type="auto">
  <name>Task 3: Phase-final gate — all smokes + full suite</name>
  <files>smoke/smoke_05_perf.py</files>
  <action>
Run the complete Phase-2 verification battery in order: py_compile all aamatch/*.py; full WSL suite (purity gates + invariants + invariance battery + code audit); SMOKE-01, SMOKE-02, SMOKE-03, SMOKE-04, SMOKE-05 via the generalized runner. Fix any regression this exposes; if a perf number exceeds budget, profile extract vs detect and optimize the pure pipeline (candidate pruning) — NEVER by weakening the audit or the budget.
  </action>
  <verify>python3.6 -m py_compile aamatch/*.py && python3.6 -m unittest discover -s tests -v && for n in 01 02 03 04 05; do bash smoke/run_smoke.sh smoke/smoke_${n}_*.py 300 || exit 1; done</verify>
  <done>All 5 smokes green; full WSL suite green — Phase 2's five success criteria are demonstrably satisfied</done>
</task>

</tasks>

<verification>
python3.6 -m py_compile aamatch/*.py && python3.6 -m unittest discover -s tests -v && bash smoke/run_smoke.sh smoke/smoke_05_perf.py 300
</verification>

<success_criteria>
ROADMAP Phase-2 criterion 5 ([HEADLESS] perf inside budget, vectorized/spatially-pruned code-audited, detector-version stamp) is green — Phase 2 complete.
</success_criteria>

<output>
After completion, create `.planning/phases/02-headless-game-engine/02-15-SUMMARY.md`
</output>
