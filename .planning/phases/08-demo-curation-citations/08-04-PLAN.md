---
phase: 08-demo-curation-citations
plan: 04
type: execute
wave: 2
depends_on:
  - "08-01"
files_modified:
  - .planning/phases/08-demo-curation-citations/08-PROPOSALS.md
  - .planning/STATE.md
autonomous: false
must_haves:
  truths:
    - "Every proposed demo candidate carries: set_id/tier/entry_id, molecule + formula + heavy-atom count, exact fetch URL + fetch evidence, size_class, interaction-type rationale, halogen/metal flags, PDB complex provenance (ID + entry DOI + paper DOI + PLIP reference), protonation record with why, license with policy URL, selection rationale, and approved alternates"
    - "Every UNVERIFIED research item is either resolved by an explicit verify step in this plan or explicitly deferred to the human with the exact check to perform — never silently assumed"
    - "Human approval is recorded in 08-PROPOSALS.md header AND as a dated STATE.md decision line BEFORE any candidate data is fetched into the repo or committed"
    - "All open design decisions are recorded with defaults the human can override: multi-state policy, tier vocabulary + dev-set visibility, file-naming convention, large-bucket fallback, citrate ionization form, payload provenance '', context-complex shipping (no), cleanup-scene fixture (local ions+water, 1OXR cited as analog), DETECT-03 revisit trigger"
  artifacts:
    - path: ".planning/phases/08-demo-curation-citations/08-PROPOSALS.md"
      provides: "The GATE proposal: 9 set sections + per-candidate tables + decisions record + approval header"
      min_lines: 150
    - path: ".planning/STATE.md"
      provides: "Dated [GATE] APPROVED decision line (DETECT-03 precedent)"
      contains: "GATE"
  key_links:
    - from: "08-PROPOSALS.md rows"
      to: "scripts/demo_specs/*.json (written in 08-05..08-07)"
      via: "same set_ids/entry_ids/CIDs/URLs — specs transcribe approved rows only"
      pattern: "demo-(easy|hard|challenge|veryhard)-"
    - from: "STATE.md approval line"
      to: "08-05..08-07 fetch/commit tasks"
      via: "approval PRECEDES any candidate-data fetch/commit (protocol ordering)"
      pattern: "APPROVED"
---

<objective>
Produce the [GATE] proposal document for the ~9 curated demo sets and obtain recorded human approval BEFORE any fetch/commit of candidate data (ROADMAP criterion 1; GEN-06 + HELP-02 protocol; PITFALL 14 citation truthfulness).

Purpose: content is gated by truthfulness, not code. This plan re-verifies every candidate's citation evidence live, records the open design decisions with defaults, and ends in a blocking human-approval checkpoint.
Output: `08-PROPOSALS.md` (proposal + decisions + approval record) + the STATE.md approval line.

**FETCH BOUNDARY (binding):** proposal-time URL evidence fetches (property JSON, SDF counts-line reads, RCSB search/Data API) are sanctioned — they ARE the proposal evidence, per the sourcing handoff ("every row's URLs re-fetched during the plan"). What is FORBIDDEN before the approval line exists: downloading any candidate SDF into the repo, writing any file under `aamatch/data/`, and any git commit of candidate data.
</objective>

<execution_context>
@~/.config/opencode/get-shit-done/workflows/execute-plan.md
@~/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/08-demo-curation-citations/08-RESEARCH-sourcing.md
@.planning/phases/08-demo-curation-citations/08-RESEARCH-mechanics.md
@aamatch/data/MANIFEST.json

Candidate universe (from sourcing RQ2 — VERIFIED live 2026-09-24 unless noted):
- demo-easy-1 (easy): Aspirin CID 2244 (C9H8O4, 13 heavy, small) + Benzoic acid CID 3979 (C7H6O2, 7 heavy)
- demo-easy-2 (easy): Citric acid CID 311 (C6H8O7, 6 heavy) + Acetate anion CID 175 (C2H3O2−, 2 heavy, −1)
- demo-easy-3 (easy): Caffeine CID 2519 (C8H10N4O2, 14 heavy)
- demo-hard-1 (hard): Benzamidine CID 2332 (C7H8N2, 9 heavy) + Guanidinium CID 32838 (CH6N3+, 4 heavy — 3D SDF UNVERIFIED, property only)
- demo-hard-2 (hard): Glutamic acid CID 33032 (C5H9NO4, 5 heavy) + Acetylcholine CID 187 (C7H16NO2+, 9 heavy — DEFAULT pick over imidazolium CID 444234 because its 3D SDF fetch is VERIFIED 200)
- demo-hard-3 (hard): ATP CID 5957 (C10H16N5O13P3, 31 heavy, medium) + NAD CID 5892 (C21H27N7O14P2, 44 heavy, medium)
- demo-challenge-1 (challenge): Chloramphenicol CID 5959 (C11H12Cl2N2O5, 20 heavy; C–Cl ×2) + Quinine CID 3034034 (C20H24N2O2, 24 heavy)
- demo-veryhard-1 (very_challenging): Folic acid CID 135398658 (C19H19N7O6, 38 heavy, medium) + Thyroxine CID 5819 (C15H11I4NO4, 21 heavy; C–I ×4)
- demo-veryhard-2 (very_challenging): Heme — RCSB CCD HEM ideal SDF (C34H32FeN4O4, 43 heavy, medium; Fe in molecule → metal_present)

URL patterns (VERIFIED): PubChem 3D SDF `https://pubchem.ncbi.nlm.nih.gov/rest/pug/compound/cid/<CID>/SDF?record_type=3d`; PubChem property `.../property/MolecularFormula,MolecularWeight,Title,Charge/JSON`; RCSB ideal SDF `https://files.rcsb.org/ligands/view/<CCD>_ideal.sdf` (plain `.sdf` 404s); RCSB search `https://search.rcsb.org/rcsbsearch/v2/query?json=<urlencoded>`; Data API `https://data.rcsb.org/rest/v1/core/entry/<PDBID>`; entry DOI `10.2210/pdbXXXX/pdb` (resolves; live-proven with 1OXR).
</context>

<tasks>

<task type="auto">
  <name>Task 1: re-fetch candidate evidence + write 08-PROPOSALS.md candidate tables</name>
  <files>.planning/phases/08-demo-curation-citations/08-PROPOSALS.md</files>
  <action>
Create `08-PROPOSALS.md`. Header: phase, date, status `PENDING APPROVAL`, and the fetch-boundary statement. Then one section per set (order above), each with a per-candidate table in the RQ8 artifact-1 shape (adapted: sha256-at-fetch is recorded at BUNDLING time by the committed builder, so the evidence column here carries URL + fetch date + counts-line read):

| field | content |
|---|---|
| set_id / tier / entry_id | demo-easy-1 / easy / aspirin |
| molecule + formula + heavy atoms | Aspirin, C9H8O4, 13 heavy (small) |
| source + fetch URL | PubChem CID 2244; exact REST URL; record_type=3d |
| evidence | webfetch <date>: HTTP 200, single record, counts line 'atoms bonds' values |
| interaction types + why | per capability.py rules, e.g. h_bond (COOH donor + ester O acceptors), pi_stacking (benzene ring), hydrophobic |
| halogen/metal flags | expected manifest flags (aspirin: false/false) |
| PDB complex provenance | PDB ID + entry DOI 10.2210/pdbXXXX/pdb + primary paper DOI (Data API `pdbx_database_id_DOI` with `rcsb_is_primary: Y`) + PLIP report reference for that PDB ID |
| protonation record | 'as-recorded' + why (e.g. acetate anion CID 175 chosen over acetic acid: deprotonated form is the salt-bridge carrier) |
| license | 'Public domain (NCBI PubChem)' with policy URLs (ncbi.nlm.nih.gov/home/about/policies/ + ftp.ncbi.nlm.nih.gov/pubchem/README); HEM set: 'CC0 1.0 (wwPDB)' with wwPDB usage-policy URL |
| selection rationale | educational coverage / tier fit / diversity slot in the 9-set plan |
| alternates | the sourcing table's alternates, explicitly listed as APPROVED substitutes |

Evidence work (webfetch each; record VERIFIED/UNVERIFIED honestly):
1. For every PubChem candidate: property endpoint (confirm CID → Title/Formula/Charge) + SDF endpoint (confirm 200, ONE `$$$$` record, counts-line atom/bond numbers — record them; they become the builder's `expect_atom_count` guards).
2. Guanidinium CID 32838: 3D SDF was property-only at research time — fetch it now. If 200 + single record: mark VERIFIED and record counts. If 404/multi-record: mark UNVERIFIED-3D and flag its approved alternate (from the proposal table) as the likely substitute.
3. HEM: fetch `HEM_ideal.sdf` (200 + counts line) AND confirm the CCD name field reads 'HEME' in the header (research left the name field unread); via the verified RCSB search API + Data API, pin ONE classic heme-bearing PDB entry and record its title, entry DOI, primary paper DOI, and PLIP reference.
4. For every candidate whose PDB complex is only search-level (caffeine 3G6M, benzamidine 1S0R, citrate 1O7X, chloramphenicol 4CLA, quinine 9NDX, folate 4KMZ) or missing entirely (benzoic acid, acetate, guanidinium, glutamic acid, acetylcholine, ATP, NAD, thyroxine): run the search API query, take the top hit, CONFIRM the ligand is actually present via the Data API entry record (ligand inventory / nonpolymer entities), then record entry DOI + primary paper DOI. Only 1OXR (aspirin) is fully pre-verified.
5. UNVERIFIED citation items: try `https://pubchemdocs.ncbi.nlm.nih.gov/publications` (PubChem canonical citation) and one PDBe per-entry URL — if JS-walled again, mark UNVERIFIED and list them in the checkpoint's human browser-check steps (do NOT cite them as verified).
6. File-naming convention: record `ligands/<set_id>-<entry_id>.sdf` for all curated files (collision-free with the dev set's legacy acetate.sdf).
  </action>
  <verify>grep -c 'demo-' 08-PROPOSALS.md covers all 9 set_ids; every candidate row has 'evidence' with a fetch date; UNVERIFIED items are explicitly marked (grep 'UNVERIFIED' shows them, not silently absent); guanidinium 3D status recorded one way or the other</verify>
  <done>08-PROPOSALS.md carries 9 set sections with complete per-candidate tables; every claim marked VERIFIED carries a same-session fetch; every UNVERIFIED item names the exact human check.</done>
</task>

<task type="auto">
  <name>Task 2: record the open decisions with defaults</name>
  <files>.planning/phases/08-demo-curation-citations/08-PROPOSALS.md</files>
  <action>
Append a `## Decisions` section listing each open item with its DEFAULT (the human may override any at approval):
1. Multi-state policy (RQ7): ONE molecule record per SDF file, `states_expected: 1`, conformer/protonation chosen at proposal time and recorded here; bundler refuses multi-record sources (never collapses).
2. Tier vocabulary: manifest tokens exactly `easy | hard | challenge | very_challenging`; display labels Easy/Hard/Challenge/Very challenging; TIER_ORDER as shipped in 08-02.
3. Dev-set visibility: demo-dev-1 REMAINS in the manifest and dropdown under Easy (default); removing it is an explicit human override.
4. Large-bucket supply: accept the generator's NEAREST-BUCKET fallback as the designed carrier (curated set = 12 small / 4 medium / 0 large predicted); a true ≥60-heavy ligand (vancomycin 2D / RCSB large CCD) is an optional stretch, NOT required.
5. Citrate ionization: free acid CID 311 'as-recorded' (capability's COOH-never-anion guard means its carboxylates are NOT salt-bridge-typed); demo-easy-2's salt-bridge slot is carried by acetate (−1, verified). Deprotonated-citrate sourcing is rejected for v1 (ChemSpider-free ChEBI molfile is 2D-only).
6. Payload provenance: saved-game payload ligand blocks keep `provenance: ''` (generator.py:864 unchanged); citations live in the manifest set-level fields + specs + DATA_SOURCES.md.
7. Context complexes: NOT shipped (outside the frozen manifest format; no runtime consumer); the messy scene for the cleanup field test is a LOCAL script-built ions+water fixture (smoke_21), with 1OXR cited in DATA_SOURCES.md as the real-world analog.
8. DETECT-03 revisit trigger: RECORDED AS TRIGGERED-FOR-CHECK — the curated data brings the detector its first real metal (HEM Fe) and halogen (chloramphenicol C–Cl, thyroxine C–I) ligands; the headless coverage check runs in 08-11; any threshold surprise becomes a DETECTOR_VERSION bump PROPOSAL in a new plan (never a silent edit).
9. Entry counts: 16 curated ligands across 9 sets (per the table: 2+2+1+2+2+2+2+2+1), size distribution predicted 12 small / 4 medium / 0 large; 18 total including the dev set's 2; supply re-measured in 08-10.
10. Set titles (dropdown labels, amendable): 'Everyday organics' / 'Metabolites & ions' / 'Caffeine' / 'Trypsin classics' / 'Charged messengers' / 'Cofactors' / 'Halogen & alkaloids' / 'Big & iodinated' / 'Metalloporphyrin'.
  </action>
  <verify>grep -c 'DEFAULT' 08-PROPOSALS.md >= 10 (each decision has a default); decisions reference the binding repo laws (count_states==1; DETECTOR_VERSION bump policy)</verify>
  <done>All 10 decisions recorded with explicit defaults; nothing left implicit for content plans to assume.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>
The [GATE] proposal for all ~9 curated demo sets (08-PROPOSALS.md): per-candidate evidence tables (re-fetched live this session), provenance chains (PDB complex + DOIs + PLIP), licenses (PubChem public domain / wwPDB CC0), protonation records, selection rationales, approved alternates, and 10 recorded default decisions.
  </what-built>
  <how-to-verify>
  1. Read `.planning/phases/08-demo-curation-citations/08-PROPOSALS.md` — one section per set; check each candidate's IDs/URLs/rationale are complete and honest (VERIFIED vs UNVERIFIED).
  2. Spot-check 2-3 fetch URLs in a real browser (e.g. the aspirin property endpoint and `https://files.rcsb.org/ligands/view/HEM_ideal.sdf`).
  3. Human browser checks for the JS-walled items (if still marked UNVERIFIED): https://pubchem.ncbi.nlm.nih.gov/docs/disclaimer (confirm the submitter-rights caveat does not apply to our chosen drug-like CIDs) and https://pubchemdocs.ncbi.nlm.nih.gov/publications (PubChem canonical citation) — tell the executor the verdicts so they can be recorded as VERIFIED (or stay flagged).
  4. Confirm or override each of the 10 recorded decisions (especially: dev-set visibility, citrate form, large-bucket acceptance, DETECT-03 trigger wording).
  5. ONLY after you are satisfied: approval unlocks 08-05..08-07 (fetch/commit). No candidate data has been downloaded into the repo or committed at this point.
  </how-to-verify>
  <resume-signal>Type "approved" to record approval and unblock content plans — or list the changes/substitutions needed (the proposal will be amended and re-presented).</resume-signal>
</task>

<task type="auto">
  <name>Task 3: record the approval (post-checkpoint)</name>
  <files>.planning/phases/08-demo-curation-citations/08-PROPOSALS.md, .planning/STATE.md</files>
  <action>
After the human responds: write `APPROVED <date>, human` (plus any decision overrides verbatim) into the 08-PROPOSALS.md header, and add the dated decision line to STATE.md's Decisions section following the DETECT-03 precedent, e.g.: `- (08-04, HUMAN gate <date>) DEMO-CURATION [GATE] APPROVED — 9 sets / 16 curated ligands (18 total incl. the dev set's 2) per 08-PROPOSALS.md; multi-state = one record per file (states_expected 1); tier tokens easy|hard|challenge|very_challenging; dev set stays listed; <any overrides>`. Commit both files (`docs(08-04): record demo-curation GATE approval`). If the human requested changes instead, amend the proposal, re-present the checkpoint, and do NOT write the approval line until approved.
  </action>
  <verify>grep -q 'APPROVED' 08-PROPOSALS.md && grep -q 'GATE] APPROVED' .planning/STATE.md</verify>
  <done>Approval recorded in both places BEFORE any content-plan fetch/commit; overrides (if any) captured verbatim.</done>
</task>

</tasks>

<verification>
- 08-PROPOSALS.md: 9 set sections, complete candidate tables, decisions with defaults, approval header.
- STATE.md: dated [GATE] APPROVED line referencing 08-PROPOSALS.md.
- No file under aamatch/data/ modified; no SDF downloaded; git log shows only docs commits for this plan.
</verification>

<success_criteria>
- Every GEN-06/HELP-02 protocol element present per candidate: PDB/SDF IDs, protonation/interaction sources, selection rationale, license, approval-before-fetch.
- Approval is RECORDED, not verbal: proposal header + STATE.md line, dated.
- 08-05..08-07 can transcribe specs mechanically from approved rows (no re-research).
</success_criteria>

<output>
After completion, create `.planning/phases/08-demo-curation-citations/08-04-SUMMARY.md`
</output>
