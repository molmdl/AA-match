---
phase: 05-game-status-tab-start-sequence
plan: 09
type: execute
wave: 4
depends_on: ["05-05", "05-06"]
files_modified: [aamatch/setup_window.py, smoke/smoke_11_window.py]
autonomous: true

must_haves:
  truths:
    - "The WINDOW is the only start path and it ORCHESTRATES the deferred sequence (tab switch is window-driven; cmd tier stays Qt-free): _start_impl = collect/build_state/seed-policy (UNCHANGED, ordering law preserved) -> _pop_game_wizard() -> start_game(activate=False) -> tabs.setCurrentWidget(game_tab) -> game_tab.start_countdown(wiz) -> return wiz"
    - "The prior GameWizard is popped by the WINDOW BEFORE start_game(activate=False) (the 04-11 isinstance-pop pattern, deletion excluded) so the countdown window is wizard-free and stale-wizard clicks cannot hit deleted objects (pitfall P-3); start_game's internal conditional-replace decision stays untouched for direct callers (SMOKE-08 teeth)"
    - "_on_cleanup cancels any pending countdown FIRST (cancel_pending_start) so Cleanup mid-countdown can never fire a stale GO over deleted objects (pitfall P-2)"
    - "SMOKE-11 PART G is REWORKED as a documented deliberate evolution (03-05 precedent): after _start_impl the pending wizard is NOT on the stack; G2 reads the PENDING wizard's seed 31337 BEFORE its GO; the GO step is driven directly, then wizard-active asserts run; the restore block cancels the pending countdown before popping"
  artifacts:
    - path: "aamatch/setup_window.py"
      provides: "_pop_game_wizard() helper (isinstance-pop, no deletion — _cleanup_now refactored to reuse it, behavior byte-identical), _start_impl deferred sequence, _on_cleanup cancel-first"
      contains: "_pop_game_wizard"
    - path: "smoke/smoke_11_window.py"
      provides: "reworked PART G: deferred drive (pending-not-on-stack, G2 pending-seed, direct GO, active+anchor asserts, cancel-before-restore)"
  key_links:
    - from: "aamatch/setup_window._start_impl"
      to: "aamatch/gamestart.start_game(activate=False)"
      via: "the window's prepare step; the cmd tier never learns about tabs (research Q2 answer (c))"
      pattern: "activate=False"
    - from: "aamatch/setup_window._start_impl"
      to: "aamatch/game_window.GameTab.start_countdown"
      via: "the tab switch + countdown hand-off — the window is the sequencer"
      pattern: "start_countdown"
    - from: "aamatch/setup_window._on_cleanup"
      to: "aamatch/game_window.GameTab.cancel_pending_start"
      via: "cancel BEFORE the guarded cleanup (P-2)"
      pattern: "cancel_pending_start"
---

<objective>
Wire the window-driven start sequence: the Setup window's Start button now pops any prior GameWizard, prepares the game WITHOUT activating it, switches to the Game status tab, and hands off to the cancellable 3-2-1 countdown — completing the deferred-activation design.

Purpose: this is the seam where SETUP-11's observable sequence becomes real ("switches to the Game status tab, and counts down 3-2-1 before play begins"). The research is explicit that the window drives the sequence (prior-art pattern PA-__init__:242-261; the cmd tier must never import the Qt tier — ARCHITECTURE.md:20). PART G's rework is the documented deliberate evolution of the SMOKE-11 start drive (the established PART-letter pattern, 03-05 precedent — new parts extend, existing parts are reworked only when their drive contract changes, which this is).
Output: setup_window sequence rework + PART G rework.
</objective>

<execution_context>
@~/.config/opencode/get-shit-done/workflows/execute-plan.md
@~/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/05-game-status-tab-start-sequence/05-RESEARCH-window-start-timer.md
@.planning/phases/04-qt-setup-window/04-13-SUMMARY.md
@.planning/phases/04-qt-setup-window/04-11-SUMMARY.md
@.planning/STATE.md
@aamatch/setup_window.py
@aamatch/gamestart.py
@aamatch/game_window.py
@smoke/smoke_11_window.py

Key facts (verified, transcribe exactly):
- _start_impl today (setup_window.py:886-933): collect -> upload_ready_for -> build_state (known_set_ids) -> Decision-4 seed policy (_last_export reuse or random.randint(0, 2**31-1) + candidates/content by source mode) -> gamestart.start_game(setup=state, seed=seed, candidates=..., ligand_content=...). The ORDERING LAW (docstring-carried): build_state refusals are caught by _guard with the scene UNTOUCHED — the pre-check's whole point. PRESERVE all of it; only the final call changes.
- _cleanup_now today (setup_window.py:784-816): lazy `from . import placement, wizard`; prior = cmd.get_wizard(); if isinstance(prior, wizard.GameWizard): cmd.set_wizard(); result = placement.cleanup_game_objects(); return result['deleted']. Extract the first half into `def _pop_game_wizard(self):` (same lazy imports, same isinstance gate, returns whether a GameWizard was popped) and have _cleanup_now call it — behavior byte-identical (PART E re-greens).
- The reworked _start_impl tail: `self._pop_game_wizard()` -> `wiz = gamestart.start_game(setup=state, seed=seed, candidates=candidates, ligand_content=ligand_content, activate=False)` -> `self.tabs.setCurrentWidget(self.game_tab)` -> `self.game_tab.start_countdown(wiz)` -> `return wiz` (the assertion handle for smokes — was None). Docstring: the deferred sequence + why the window pops BEFORE prepare (P-3: the old wizard's slot map points at objects start_game's cleanup deletes; clicks during the countdown must not hit it) + the ordering law unchanged.
- _on_cleanup: `self.game_tab.cancel_pending_start()` FIRST, then the existing `deleted = self._guard(self._cleanup_now)` + the count box. (The game_tab attribute always exists — constructed in __init__ since 05-06.)
- PART G today (smoke_11_window.py:569-680): G1 _start_impl -> cmd.get_wizard() active + scene grew + payload seed int; G2 _last_export seed 31337 replay -> cmd.get_wizard() seed check; G3 spinbox change breaks the reuse guard; restore = cmd.set_wizard() + cleanup + baseline exact; PART G-alt (probe-FAIL degradation) keeps the pure pre-check chain.
- The rework (research gate-impact item ii): G1: after dlg._start_impl() -> check dlg.game_tab._pending_wizard is a GameWizard; check cmd.get_wizard() is NOT that wizard (wizard-free countdown window); scene-grew check stays; THEN drive the GO step directly (dlg.game_tab._begin_play()) -> check cmd.get_wizard() IS that wizard + engine._current_game().timer_anchor is a float; check the wizard's _saved_msm captured (the ORDER-LAW snapshot happens at GO because the push happens then — research Q4). G2: set _last_export (seed 31337) -> _start_impl -> read dlg.game_tab._pending_wizard._payload['seed'] == 31337 BEFORE its GO -> then drive GO for the new wizard. G3 unchanged. Restore block: dlg.game_tab.cancel_pending_start() BEFORE cmd.set_wizard() + cleanup + baseline exact. PART G-alt keeps the pure chain (unchanged).
- setup_window.py module-level `from pymol import cmd` already exists (04-11 Decision 17) — reuse it; no new module-level imports (game_window is imported lazily in __init__ since 05-06).
- PROSE_PIN discipline: no banned-token mentions added.
</context>

<tasks>

<task type="auto">
  <name>Task 1: _pop_game_wizard helper + deferred _start_impl + cleanup cancel</name>
  <files>aamatch/setup_window.py</files>
  <action>
1. Extract _pop_game_wizard() from _cleanup_now's first half (context facts); refactor _cleanup_now to call it — byte-identical behavior.
2. Rework _start_impl's tail to the deferred sequence (context facts) — everything before the final call (collect/build_state/seed policy) stays byte-identical; update the docstring (deferred sequence, P-3 rationale, ordering law preserved, returns the wizard).
3. _on_cleanup: cancel_pending_start() first.
Commit: `feat(05-09): window-driven deferred start sequence`.
  </action>
  <verify>python3.6 -m py_compile aamatch/*.py && python3.6 -m unittest discover -s tests -v green</verify>
  <done>The window orchestrates pop -> prepare(unactivated) -> tab switch -> countdown; cleanup cancels first</done>
</task>

<task type="auto">
  <name>Task 2: SMOKE-11 PART G rework</name>
  <files>smoke/smoke_11_window.py</files>
  <action>Rework PART G per the context facts (G1 deferred asserts + direct GO; G2 pending-seed-before-GO; G3 unchanged; restore cancels first). Header comment records the deliberate evolution (03-05 precedent: the drive contract changed with the deferred design — the CHECKS survive, the DRIVE sequence moves). Keep the part's T1b probe-gated header and the PART G-alt degradation path. Update the header docstring part list. Commit: `test(05-09): rework SMOKE-11 PART G for the deferred sequence`.
  </action>
  <verify>bash smoke/run_smoke.sh smoke/smoke_11_window.py 120 — === SMOKE-11 PASS === with reworked G green and ALL other parts green</verify>
  <done>The smoke proves: pending wizard not on stack during the countdown; GO activates + anchors; seed replay via the pending wizard; exact restore</done>
</task>

<task type="auto">
  <name>Task 3: full gate sweep</name>
  <files>[]</files>
  <action>Run the standing gates. Confirm SMOKE-08 still green (the default activate path is untouched by the window work — the window now passes activate=False but SMOKE-08's direct calls use the default). Commit (if fixups): `test(05-09): gate sweep green`.
  </action>
  <verify>python3.6 -m py_compile aamatch/*.py && python3.6 -m unittest discover -s tests -v && bash smoke/run_smoke.sh smoke/smoke_08_starter.py 120 && bash smoke/run_smoke.sh smoke/smoke_11_window.py 120 && bash smoke/run_smoke.sh smoke/smoke_14_status_surface.py 120 — all green</verify>
  <done>All standing gates green with the sequence wired</done>
</task>

</tasks>

<verification>
- `python3.6 -m py_compile aamatch/*.py` — floor.
- `python3.6 -m unittest discover -s tests -v` — full WSL suite.
- `bash smoke/run_smoke.sh smoke/smoke_11_window.py 120` — reworked PART G + all parts green.
- `bash smoke/run_smoke.sh smoke/smoke_08_starter.py 120` — default path regression.
- Grep setup_window.py for 'activate=False' — exactly one call site (the window's prepare step).
</verification>

<success_criteria>
- Start via the window: prior wizard popped -> game prepared unactivated -> tab switches -> countdown starts -> GO activates + anchors.
- Cleanup during a countdown cancels it (no stale GO).
- PART G reworked as a documented deliberate evolution; every other part untouched.
</success_criteria>

<output>
After completion, create `.planning/phases/05-game-status-tab-start-sequence/05-09-SUMMARY.md`
</output>
