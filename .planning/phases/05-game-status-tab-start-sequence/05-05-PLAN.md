---
phase: 05-game-status-tab-start-sequence
plan: 05
type: execute
wave: 1
depends_on: []
files_modified: [aamatch/gamestart.py, smoke/smoke_11_window.py]
autonomous: true

must_haves:
  truths:
    - "start_game(activate=False) prepares the FULL game (cleanup -> new_game -> materialize -> compose/framing) and returns the GameWizard WITHOUT pushing it — cmd.get_wizard() is not that wizard (no pre-GO play window, spec.md:34 'countdown ... then start the game')"
    - "gamestart.activate_game(wiz) is the ONLY GO-time activation: re-evaluates the conditional replace AT ACTIVATION (the stack can change during the 3-second countdown — 03-05 decision re-checked, not cached), pushes the wizard, and starts the timer from zero via engine._current_game().start_timer(time.time())"
    - "gamestart._last_start holds the materialization INPUT tuple {'setup' (deep-copied), 'seed', 'candidates', 'ligand_content'} after every successful start — captured BEFORE any wizard mutation, NOT a v1-style backup object (the inputs fully regenerate the scene; do NOT port the backup machinery)"
    - "The DEFAULT path (activate=True) is byte-identical: SMOKE-08's 26 checks incl. the msm ORDER-LAW teeth stay green unchanged"
  artifacts:
    - path: "aamatch/gamestart.py"
      provides: "activate kwarg on start_game + activate_game(wiz) + module-level _last_start store (cmd tier, NO Qt)"
      contains: "activate_game"
    - path: "smoke/smoke_11_window.py"
      provides: "new ALWAYS-tier T1a PART before the Gate-A2 echo: deferred-activation direct drive (unactivated return, activate_game push + anchor, _last_start round-trip) with exact-scene restore"
  key_links:
    - from: "aamatch/gamestart.activate_game"
      to: "aamatch/game_state.GameState.start_timer"
      via: "engine._current_game().start_timer(time.time()) — ONE anchor home, set at GO (start_timer was never called in production before Phase 5)"
      pattern: "start_timer"
    - from: "aamatch/setup_window._start_impl (plan 05-09)"
      to: "aamatch/gamestart.start_game(activate=False)"
      via: "the window's deferred sequence calls the seam with activate=False then drives activate_game at GO"
      pattern: "activate=False"
---

<objective>
The cmd-tier half of SETUP-11's deferred start: split "prepare the game" from "start play" inside the one-call seam, and store the initial state for Phase-6 Restart.

Purpose: v1 activates its PickWizard only at GO (PA-gui_game:266-269) and the spec reads "countdown 3, 2, 1, then start the game" — with today's bundle, the player could pick/move/confirm during the countdown (pitfall P-1). The minimal-churn shape (research Q2/Q3): an `activate=True` kwarg whose default preserves every existing caller byte-for-byte, plus `activate_game(wiz)` as the single GO-time activation home, plus the `_last_start` input-tuple store that Phase 6's Restart will replay (ROADMAP:159). The window (plan 05-09) orchestrates: pop prior wizard -> start_game(activate=False) -> switch tab -> countdown -> GO -> activate_game.
Output: extended gamestart.py + a headless T1a proof in SMOKE-11.
</objective>

<execution_context>
@~/.config/opencode/get-shit-done/workflows/execute-plan.md
@~/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/05-game-status-tab-start-sequence/05-RESEARCH-window-start-timer.md
@.planning/STATE.md
@aamatch/gamestart.py
@aamatch/wizard.py
@aamatch/engine.py
@smoke/smoke_11_window.py
@smoke/smoke_08_starter.py

Key facts (verified, transcribe exactly):
- start_game (aamatch/gamestart.py:285-339): cleanup_game_objects -> spec = dict(DEFAULTS if setup is None else setup) -> engine.new_game(spec, seed, candidates=..., ligand_content=...) -> engine.materialize(payload, 0, ligand_content=...) -> prior = cmd.get_wizard() -> GameWizard(payload, registry, 0, 0) -> _move_ligand_in_front + _frame_ligand_above_grid + cmd.zoom (compose BEFORE activate) -> wiz.activate(replace=(1 if isinstance(prior, GameWizard) else 0)) -> print -> return wiz.
- The deferred shape (research Pattern 2): the activate block becomes `if activate: activate_game(wiz)`; activate_game re-evaluates replace at GO: `replace = 1 if isinstance(cmd.get_wizard(), GameWizard) else 0; wiz.activate(replace=replace); engine._current_game().start_timer(time.time())`. Lazy relative imports inside the method (module-identity law, wizard.py pattern).
- _last_start store (research Pattern 4 + Q3): `{'setup': copy.deepcopy(spec), 'seed': seed, 'candidates': candidates, 'ligand_content': ligand_content}` — set AFTER new_game+materialize (and the compose) SUCCEED, BEFORE the activate branch (a failed start leaves the store holding the last successfully BUILT game's tuple). Mirrors Decision-4's _last_export shape (setup_window.py:872-874) but lives in gamestart — a later export must never corrupt the restart source. DEEP-copy the setup (allowed_interactions list aliasing — the 04-09 _reset_impl note). `import copy` inside the function (3.6-safe, cmd tier may use stdlib freely).
- msm ORDER LAW untouched: with activate=True (default) the internal prior capture + conditional replace behave exactly as today (SMOKE-08's teeth, 03-05). With the deferred path, the WINDOW pops the prior GameWizard itself (plan 05-09, pitfall P-3), so at GO replace evaluates 0 over an empty stack / a user wizard — stack-native dormancy, consistent with 03-05.
- SMOKE-08 (smoke/smoke_08_starter.py) is UNCHANGED by this plan — its 3 direct start_game calls (:192/:485/:530) use the default. SMOKE-10's direct calls (:225/:285) likewise.
- Gate discipline: gamestart.py is cmd tier (module-level `from pymol import cmd` legal; NEVER PURE_MODULES; SCANNED_MODULES member). PROSE_PIN: gamestart.py currently carries ZERO banned-token mentions — new docstrings must keep it that way (refer to "the banned matrix calls" if ever needed, wizard.py:66-69 convention). python3.6 floor.
- SMOKE-11 part conventions: `check(name, cond, detail)` helper + REC dict + parts wrapped in try/except; ALWAYS-tier parts run without the probe gate; insert new parts BEFORE the Gate-A2 echo (PART H today) and re-letter the echo per the established renumber pattern (04-10/04-12/04-13 precedent — the echo letter shifts).
</context>

<tasks>

<task type="auto">
  <name>Task 1: gamestart deferred-activation seam + _last_start store</name>
  <files>aamatch/gamestart.py</files>
  <action>
1. `start_game(setup=None, seed=42, candidates=None, ligand_content=None, activate=True)` — add the kwarg. Everything up to and including the compose block is UNCHANGED. Replace the unconditional activate with `if activate: activate_game(wiz)`. Keep the status print + `return wiz` unconditional (the print describes the prepared game; the deferred path still prints — the GO activation is silent).
2. NEW `def activate_game(wiz):` — module-level function, docstring: "Push the prepared GameWizard + anchor the timer (Phase 5 GO step). The conditional replace is re-evaluated HERE (the stack can change during the countdown — 03-05's decision re-checked, not cached)." Body: lazy `from . import engine`; `replace = 1 if isinstance(cmd.get_wizard(), GameWizard) else 0`; `wiz.activate(replace=replace)`; `engine._current_game().start_timer(time.time())`. `import time` at module level or lazy — gamestart already has stdlib access; keep module imports minimal and consistent with the file's current shape.
3. NEW module-level `_last_start = None` with a docstring comment: the initial-state store (SETUP-11; Phase 6 Restart replays it verbatim through start_game — ROADMAP:159); NOT a v1-style backup object (the materialization INPUT tuple fully regenerates the scene — placement.py 02-13 docstring).
4. In start_game, after the compose block succeeds and BEFORE the activate branch: `global _last_start; _last_start = {'setup': copy.deepcopy(spec), 'seed': seed, 'candidates': candidates, 'ligand_content': ligand_content}` with `import copy` (lazy, inside the function). Docstring gains: the activate kwarg semantics (default True preserves every existing caller byte-for-byte; False defers activation to activate_game for the countdown window) + the _last_start capture note.
5. Layer docstring: extend the module header's seam description with the deferred-activation note (the Qt window orchestrates pop -> prepare(activate=False) -> tab switch -> countdown -> activate_game).
NO Qt anywhere; NO banned-token mentions; python3.6 floor.
  </action>
  <verify>python3.6 -m py_compile aamatch/gamestart.py && python3.6 -m unittest discover -s tests -v (full suite green — nothing in WSL imports gamestart, this is the syntax+regression floor) && bash smoke/run_smoke.sh smoke/smoke_08_starter.py 120 — === SMOKE-08 PASS === (26 checks, default path byte-identical)</verify>
  <done>start_game(activate=False) compiles and the default path is proven byte-identical by SMOKE-08's unchanged green run</done>
</task>

<task type="auto">
  <name>Task 2: SMOKE-11 deferred-activation T1a PART</name>
  <files>smoke/smoke_11_window.py</files>
  <action>Insert ONE new ALWAYS-tier PART (T1a, cmd-substance — no dialog needed) immediately BEFORE the Gate-A2 echo part; re-letter the echo per the established pattern. Header comment cites the deferred-activation design (05-RESEARCH-window-start-timer.md Pattern 2). Drive:
1. baseline = cmd.get_names('objects'); `wiz = gamestart.start_game(activate=False)` (import gamestart lazily per the smoke's existing style).
2. check: the returned wizard is a GameWizard AND cmd.get_wizard() is NOT wiz (unactivated — the countdown window is wizard-free, P-1's warning-signals assert).
3. check: the scene grew (_aam_* objects) — preparation is complete even though activation is deferred.
4. check: gamestart._last_start is a dict with EXACTLY the keys {'setup','seed','candidates','ligand_content'}; _last_start['setup'] is not the same object as the dict passed (deep-copy proof via `is not` on a sentinel dict the smoke passes in); _last_start['seed'] is a real int.
5. Drive GO: `gamestart.activate_game(wiz)` -> check: cmd.get_wizard() IS wiz; check: engine._current_game().timer_anchor is a float (the anchor started from zero); check: the wizard's _saved_msm was captured post-push (isinstance int) — the ORDER LAW fires at GO because the push happens then (research Q4).
6. Restore: cmd.set_wizard() (done pop) + placement.cleanup_game_objects() -> check: baseline scene restored EXACTLY + cmd.get_wizard() is None. Also reset gamestart._last_start = None in the restore block so later parts start clean.
Update the header docstring's part list; keep every existing part's checks byte-identical. Commit: `test(05-05): SMOKE-11 deferred-activation T1a part`.
  </action>
  <verify>bash smoke/run_smoke.sh smoke/smoke_11_window.py 120 — === SMOKE-11 PASS === with the new part's checks in the output and ALL prior parts still green (A/B/C/D/E/F/F2/G/H letters may have shifted for the echo only)</verify>
  <done>The deferred path + GO activation + anchor + _last_start round-trip are headlessly proven; existing parts untouched</done>
</task>

<task type="auto">
  <name>Task 3: full gate sweep</name>
  <files>[]</files>
  <action>Run the complete standing gate set (AGENTS.md gates 1). Confirm gamestart.py still carries ZERO banned-token mentions (tests/test_code_audit.py PROSE_PIN is exact-count). Commit (if any fixups): `test(05-05): gate sweep green`.
  </action>
  <verify>python3.6 -m py_compile aamatch/*.py && python3.6 -m unittest discover -s tests -v && bash smoke/run_smoke.sh smoke/smoke_08_starter.py 120 && bash smoke/run_smoke.sh smoke/smoke_11_window.py 120 — all green</verify>
  <done>All standing gates green with the seam extended</done>
</task>

</tasks>

<verification>
- `python3.6 -m py_compile aamatch/*.py` — floor.
- `python3.6 -m unittest discover -s tests -v` — full WSL suite.
- `bash smoke/run_smoke.sh smoke/smoke_08_starter.py 120` — default path byte-identical (26 checks).
- `bash smoke/run_smoke.sh smoke/smoke_11_window.py 120` — new T1a part + all existing parts green.
- Grep aamatch/gamestart.py for 'Qt'/'QtWidgets' — zero hits (cmd-tier law).
</verification>

<success_criteria>
- start_game(activate=False) returns a fully-prepared UNACTIVATED wizard; activate_game is the single GO-time activation home (conditional replace re-evaluated + anchor set from zero).
- _last_start stores the deep-copied input tuple on every successful start, ready for Phase-6 Restart replay.
- SMOKE-08 unchanged-green (byte-identical default); SMOKE-11 carries the new T1a proof.
</success_criteria>

<output>
After completion, create `.planning/phases/05-game-status-tab-start-sequence/05-05-SUMMARY.md`
</output>
