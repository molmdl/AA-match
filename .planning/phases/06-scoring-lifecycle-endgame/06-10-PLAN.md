---
phase: 06-scoring-lifecycle-endgame
plan: 10
type: execute
wave: 8
depends_on: ["06-01", "06-02", "06-03", "06-04", "06-05", "06-06", "06-07", "06-08", "06-09"]
files_modified: []
autonomous: false
must_haves:
  truths:
    - "A human has played a full lifecycle on real PyMOL: confirm/debrief/advance, level escalation, skip/give-up warnings, endgame screen, restart/reset — all five ROADMAP criteria carry [HUMAN] verdicts"
    - "The pinned Phase-6 wording (event lines, warnings, endgame block) is human-CONFIRMED or amended in-session"
    - "The timer freeze under the NEW warning modals is human-confirmed (P-5 class, new dialog instances)"
  artifacts:
    - path: ".planning/phases/06-scoring-lifecycle-endgame/06-10-SUMMARY.md"
      provides: "the per-step verdict table + any fix-batch record"
      contains: "APPROVED"
  key_links:
    - from: "06-10 checkpoint verdicts"
      to: "ROADMAP Phase-6 success criteria 1-5"
      via: "one consolidated GUI session covering all five"
      pattern: "SCORE-0"
---

<objective>
[HUMAN] consolidated GUI checkpoint for Phase 6: all five ROADMAP success criteria verified on the real Windows PyMOL 2.5.0 (setenv.bat conda env, 01-09 plugin-path install), plus the wording confirmation for every pinned Phase-6 string.

Purpose: the not-headlessly-provable set — modal look/feel, timer freeze under the new warning boxes, on-top endgame modal, dropdown feel, restart/reset feel, and human approval of the permanent wording pins.
Output: 06-10-SUMMARY.md verdict table (+ any in-session fix batch).
</objective>

<execution_context>
@~/.config/opencode/get-shit-done/workflows/execute-plan.md
@~/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-scoring-lifecycle-endgame/06-RESEARCH-endgame-ui.md
@.planning/phases/06-scoring-lifecycle-endgame/06-08-SUMMARY.md
@.planning/phases/06-scoring-lifecycle-endgame/06-09-SUMMARY.md
@aamatch/status_text.py
@spec.md

Session recipe (04-14/05-11 precedent): Windows PyMOL 2.5.0 via `setenv.bat`; Plugins -> AA-match; the repo root is on the plugin path (live edits, no reinstall). Console baseline = the stock-PyMOL plugin-init warnings recorded at 04-14 (pre-existing, unrelated).
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>The complete Phase-6 scoring lifecycle: Confirm scoring/debrief/advance (tab + panel), level escalation with scene rebuild and continued timer, Skip/Give-Up dropdown with spec-required confirmation warnings and correct partial-score/endgame semantics, the endgame modal (per-level scores, total, stopped timer, winning message, time, sizes, skip/give-up counts), Restart replaying the stored initial state, and Reset returning AAs to grid with orientations kept. All mechanical behavior is SMOKE-proven (06-09's battery); this session verifies look, feel, and the human-only behaviors.</what-built>
  <how-to-verify>
    1. **Confirm + debrief + advance (SCORE-01/02):** Start a game (Setup tab). Move/rotate AAs on molecule 1, then press the Game tab's **Confirm**. EXPECT: info box shows 'Molecule 1 of 2 scored X.XX (total X.XX).' followed by the debrief lines (Formed/Missing, numbers only — no geometry), then the next molecule's level line; the wizard panel header advances; the required label updates. A second Confirm on the NEW molecule works normally. (The re-Confirm refusal 'This molecule already has a recorded result (scored or skipped) -- use Restart to replay the game.' is engine-level and proven headlessly by SMOKE-15 PART A — after a real advance a second Confirm always lands on the NEXT molecule, so it is unreachable in the GUI by design; no human observation needed.)
    2. **Level escalation (SCORE-03):** Finish all molecules of level 1. EXPECT: 'Level 2, molecule 1 of 2.' logged, a NEW grid materialized (different/more slots at higher difficulty), the camera framed on the new molecule, and the **timer CONTINUES** (does NOT reset to 0:00).
    3. **Skip warning + semantics (SCORE-05):** Open the Skip / Give Up dropdown -> Skip Molecule. EXPECT warning box 'Skip this molecule?' with the pinned text; **the timer freezes while the box is open**; Yes -> 'Skipped molecule N of M (partial score X.XX, total Y.YY).' and advance; No -> play resumes unchanged. Skipping with nothing formed stores 0.00 and advances (mercy, no refusal).
    4. **Give Up + endgame screen (SCORE-06/07):** Dropdown -> Give Up... -> warning 'Give up?'; Yes -> the endgame sequence: 'Game ended at level...' line, the endgame block lines in the info box, the 1 Hz timer STOPS (label frozen at the exact final time), the wizard panel is gone (colors restored), and a modal appears **ABOVE the 3D viewer** with the headline, per-level scores, total, time, molecules/levels, and skip/give-up counts. The scene (`_aam_*` objects) stays for inspection.
    5. **Natural end + winning message (SCORE-07):** Restart, then play/skip through ALL molecules of ALL levels via the tab Confirm. EXPECT the SAME endgame screen with the 'You win! All 3 level(s) finished in M:SS.' headline.
    6. **Restart (SCORE-09):** From the endgame state (or mid-game), press **Restart**. EXPECT: 'Game restarted.' then 'Get ready... 3 2 1 GO!', a rebuilt scene, timer at 0:00 during the countdown and advancing from zero at GO, all scores/counters zeroed, required label fresh.
    7. **Reset (SCORE-10):** Mid-game, move AND rotate an AA, then press **Reset**. EXPECT: 'Amino acids reset to grid positions (orientations kept).' in the info box; every AA back at its grid position; the ROTATION is kept (orientation visibly unchanged); the selection stays green; the timer keeps running.
    8. **Wording approval (permanent pins):** Read the actual strings shown in steps 1-7 against status_text.py's builders. Verdict per string: CONFIRMED or AMENDED (amendments are applied in-session as a fix batch with the WSL pin updated).
    9. **Console:** zero tracebacks and zero stray AA-match prints across the whole session (stock-PyMOL init warnings = recorded baseline).
  </how-to-verify>
  <resume-signal>Type "approved" or list failing step numbers + what you saw (fix batch follows, then re-verify the failed steps only).</resume-signal>
</task>

</tasks>

<verification>
- Every ROADMAP Phase-6 success criterion (1-5) has an explicit [HUMAN] verdict in the summary table.
- Wording verdicts recorded for: the 5 poll-emitted event lines, game_restarted, both warning boxes, and the endgame block/headline.
- Any fix batch: applied + WSL suite + affected smokes re-green + re-verified in-session (05-11 precedent).
</verification>

<success_criteria>
- 06-10-SUMMARY.md records APPROVED (or approved-after-fix) with the per-step table; Phase 6 is ready for the phase verifier.
</success_criteria>

<output>
After completion, create `.planning/phases/06-scoring-lifecycle-endgame/06-10-SUMMARY.md` (frontmatter: subsystem ui/verification; the checkpoint verdict record — 05-11's structure: per-step verdicts + fix-batch section + wording verdicts).
</output>
