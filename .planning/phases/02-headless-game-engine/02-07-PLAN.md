---
phase: 02-headless-game-engine
plan: 07
type: tdd
wave: 6
depends_on: ["02-06"]
files_modified:
  - aamatch/detector.py
  - tests/test_detector.py
autonomous: true

must_haves:
  truths:
    - "pi_stacking fires for both parallel and T-shaped orientations under one criterion set (5.5 A / 30 deg / 2.0 A offset)"
    - "cation_pi detects BOTH directions (AA-cation over ligand ring, AA-ring under ligand cation) and applies the ligand-cation anti-artifact rule"
    - "halogen detects only (AA O/N/S acceptor) x (ligand C-X donor, X in Cl/Br/I) within both angle windows; metal detects only when the ligand carries an approved metal, distance-only"
    - "detect() exposes the complete 7-type canonical result list; ring-center/normal math is vector-exact at criterion boundaries"
  artifacts:
    - path: "aamatch/detector.py"
      provides: "Full 7-type detection pipeline with canonical results"
      min_lines: 350
  key_links:
    - from: "aamatch/detector.py (pi/cation-pi)"
      to: "aamatch/vec3.py"
      via: "ring centers/normals + projections"
      pattern: "plane_project|cross|angle_at"
    - from: "tests/test_detector.py"
      to: "setup_state.INTERACTION_TYPES"
      via: "all 7 types produce records; canonical order matches the enum"
      pattern: "INTERACTION_TYPES"
---

<objective>
Complete the detector: pi_stacking, cation_pi, halogen bond, metal coordination — the four geometry-heavy types — and finalize the canonical 7-type result surface.

Purpose: DETECT-01 + DETECT-02 complete; the partner-side record contract consumed by scoring (02-10) and the E2E smoke (02-14) becomes final here.
Output: detector.py covering all 7 types, boundary-tested.
</objective>

<execution_context>
@~/.config/opencode/get-shit-done/workflows/execute-plan.md
@~/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@docs/DETECTION_THRESHOLDS.md                                        # rows 3, 4, 6, 7 — the frozen criteria
@.planning/phases/02-headless-game-engine/02-RESEARCH-detection.md   # §2.3/2.4/2.6/2.7 mechanics, §3 rows 3/4/6/7, §4 roles, OQ-4 anti-artifact rule
@aamatch/detector.py                                                 # pipeline from 02-06 (extend, do not restructure)
@aamatch/capability.py, @aamatch/thresholds.py, @aamatch/vec3.py
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: pi_stacking + cation_pi (RED -> GREEN)</name>
  <files>aamatch/detector.py, tests/test_detector.py</files>
  <action>
RED: scripted-geometry tests first.
- pi_stacking: ligand benzene ring (6 atoms, kekule bonds) with center at origin, normal +z. AA PHE ring placed (a) parallel at z=4.5, projected center offset 1.0 A -> FORMED, metrics record center distance + angle + offset, sub-type 'P' recorded as a METRIC only; (b) parallel at z=5.6 -> NOT formed; (c) T-shaped: PHE ring plane perpendicular (normal in xy), ring center 5.0 A away, offset < 2.0 -> FORMED with metric 'T'; (d) parallel but offset 2.5 A -> NOT formed; (e) normals 45 deg off parallel (neither within 30 of parallel nor perpendicular) -> NOT formed. AA ring atoms from capability AA_RESIDUES['PHE']['rings'][0]; AA side-chain-only restriction holds (backbone atoms never join a ring test).
- cation_pi: (a) ligand benzene ring + AA LYS NZ (charged ammonium) 5.5 A above ring center, projected offset 1.0 -> FORMED, aa role 'cation', direction 'aa_cation_over_lig_ring'; (b) ligand ammonium + AA PHE ring 5.5 A -> FORMED, direction 'aa_ring_under_lig_cation' (D4 both directions); (c) 6.5 A -> NOT; (d) projected offset 2.5 -> NOT; (e) anti-artifact rule (OQ-4, ligand-cation case only): ligand tertiary amine whose substituent-plane normal is ~parallel to the ring normal (amine stacked 'through' the ring) -> NOT formed; AA-side LYS has NO such veto (documented asymmetry).

GREEN: extend detector.py: ring features already exist; add _pi_stacking (single uniform test: center dist < 5.5 AND (normals within 30 of parallel OR within 30 of perpendicular) AND projected-center offset < 2.0) and _cation_pi (dist <= 6.0 AND projected charge offset < 2.0; direction recorded; PLIP asymmetric anti-artifact rule when the LIGAND carries the cation). All cutoffs from thresholds.

Commit: `feat(02-07): pi_stacking + cation_pi (TDD)`.
  </action>
  <verify>python3.6 -m unittest tests.test_detector -v</verify>
  <done>Both orientations and both directions detect; boundaries exact; anti-artifact veto proven</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: halogen + metal + canonical completion (RED -> GREEN)</name>
  <files>aamatch/detector.py, tests/test_detector.py</files>
  <action>
RED:
- halogen: ligand C-Cl donor; AA SER OG acceptor. (a) A···Cl 3.5 A, donor angle ∠(A···Cl-C) = 165 deg, acceptor angle ∠(O-A···Cl... careful with vertex conventions: ∠(Y-A···X) at the acceptor A = 120 deg) -> FORMED; (b) donor angle 100 deg (outside 135-195) -> NOT; (c) acceptor angle 60 deg (outside 90-150) -> NOT; (d) ligand C-F donor -> NO candidates AT ALL (C-F excluded at typing; assert no record even at perfect geometry); (e) AA-side "donor" attempt (ligand O accepting from an AA C-Cl) -> impossible: enumeration is (AA-acceptor x ligand-donor) only — assert an AA halogen produces nothing (spec's side rule, detection research §4).
- metal: ligand carries ZN (element in approved list) + AA HIS ring N at 2.5 A -> FORMED (distance-only; aa role 'chelator'); same geometry with NO metal in ligand -> NO record and _metal not even enumerated (gating via capability.ligand_has_metal — detection research §7.6); 3.2 A -> NOT; metal element not in METAL_ELEMENTS (e.g. 'NA') -> no record.
- Canonical completion: a scene triggering h_bond + pi_stacking + hydrophobic returns records ordered by (type-position in INTERACTION_TYPES, aa object, aa atom_ids, lig atom_ids); unknown/extra types impossible (closed set); total record count asserted.

GREEN: implement _halogen (two angle windows from thresholds; enumerate ONLY (AA acceptor O/N/S) x (ligand donor C-X, X in {CL,BR,I})) and _metal (distance-only; gated by ligand_has_metal; AA donors = side-chain N/O/S of chelator residues). Wire all 7 type tests into detect(atom_records, ligand_bonds) -> canonical list. Keep MIN_DIST 0.5 guard + covalent exclusion (intra-side bonded atoms never pair — use the ligand bond block; AA side has no bond block: exclude identical atom ids and rely on cross-side enumeration).

Commit: `feat(02-07): halogen + metal + canonical 7-type surface (TDD)`.
  </action>
  <verify>python3.6 -m unittest tests.test_detector -v</verify>
  <done>All 7 types boundary-tested; side rules structurally enforced; canonical deterministic output</done>
</task>

<task type="auto">
  <name>Task 3: Full-suite gate</name>
  <files>tests/test_detector.py</files>
  <action>
Run the full suite (no new files; detector already registered in 02-06). Confirm no regression in tests/test_capability, tests/test_spatial, tests/test_thresholds.
  </action>
  <verify>python3.6 -m unittest discover -s tests -v</verify>
  <done>Full WSL suite green with the complete 7-type detector</done>
</task>

</tasks>

<verification>
python3.6 -m py_compile aamatch/*.py && python3.6 -m unittest discover -s tests -v
</verification>

<success_criteria>
DETECT-01 + DETECT-02 complete in the pure layer: 7 types, explicit partner sides, D1 restriction, canonical records — all WSL-green.
</success_criteria>

<output>
After completion, create `.planning/phases/02-headless-game-engine/02-07-SUMMARY.md`
</output>
