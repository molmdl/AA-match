---
phase: 08-demo-curation-citations
plan: 08
type: execute
wave: 6
depends_on:
  - "08-07"
files_modified:
  - smoke/smoke_02_manifest.py
  - smoke/smoke_21_demo_cleanup.py
autonomous: true
must_haves:
  truths:
    - "SMOKE-02 asserts full curated coverage: the canonical 10 set_ids exist, every set has ≥1 entry, and both flag branches (metal_present TRUE, halogen_present TRUE) appear at least once across the bundle"
    - "On a messy user scene (protein fragment + waters + ion), running a curated-set game and then placement.cleanup_game_objects() restores the EXACT original object set — names AND per-object atom counts (PITFALL 13 proven in the field)"
    - "All game objects during the field test are '_aam_'-prefixed and no 'AAM' segi atom exists outside game objects"
  artifacts:
    - path: "smoke/smoke_02_manifest.py"
      provides: "Set/tier coverage + flag-branch asserts appended to the every-manifest-id loop"
      contains: "metal_present"
    - path: "smoke/smoke_21_demo_cleanup.py"
      provides: "ROADMAP criterion-4 field test: messy-scene cleanup restoration, headless"
      min_lines: 120
  key_links:
    - from: "smoke/smoke_21_demo_cleanup.py"
      to: "aamatch.placement.cleanup_game_objects"
      via: "prefix-only deletion proven against a non-game messy scene (pre-registered snapshot assert, smoke_04_e2e.py:477-486 pattern)"
      pattern: "cleanup_game_objects"
    - from: "smoke/smoke_21_demo_cleanup.py"
      to: "tmp/messy_scene.pdb"
      via: "inline PDB string written to git-ignored tmp/ + cmd.load via to_windows_path (probe-proven universal path; no read_pdbstr dependency)"
      pattern: "to_windows_path"
    - from: "smoke/smoke_02_manifest.py"
      to: "aamatch/data/MANIFEST.json"
      via: "set/tier census computed from the parsed payload, not literals"
      pattern: "manifest_sets_grouped|set_ids"
---

<objective>
Extend SMOKE-02 to assert curated coverage (sets/tiers/flag branches) and add the messy-scene cleanup field smoke (`smoke_21`) that proves ROADMAP criterion 4's second clause: "on a ligand+ions+water demo, Cleanup leaves exactly the original object set".

Purpose: criterion 4 [HEADLESS] closes here — every demo load-verifies against the manifest AND cleanup isolation is proven against a water/ion-carrying scene (the exact prior-art hazard class of PITFALL 13).
Output: extended smoke_02 + new smoke_21, both PASS headless.
</objective>

<execution_context>
@~/.config/opencode/get-shit-done/workflows/execute-plan.md
@~/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/08-demo-curation-citations/08-RESEARCH-mechanics.md
@smoke/smoke_02_manifest.py
@smoke/smoke_04_e2e.py
@aamatch/placement.py
@aamatch/geometry.py
@.planning/phases/08-demo-curation-citations/08-PROPOSALS.md

Design decisions already ratified (08-PROPOSALS): context complexes are NOT shipped; the messy scene is a LOCAL script-built ions+water fixture; 1OXR (aspirin + Ca²⁺) is cited in DATA_SOURCES.md as the real-world analog. The research's read_pdbstr sketch is replaced by the more robust route: write the inline PDB string to git-ignored `tmp/messy_scene.pdb` and `cmd.load` it via `to_windows_path` (cmd.load is the repo's probe-proven universal path — avoids the UNVERIFIED read_pdbstr surface entirely; deviation documented in SUMMARY).
</context>

<tasks>

<task type="auto">
  <name>Task 1: SMOKE-02 curated-coverage extension</name>
  <files>smoke/smoke_02_manifest.py</files>
  <action>
Append coverage asserts after the manifest parse (data-relative — computed from the parsed payload, following the check() discipline):
1. Set census: every expected set_id present — the canonical list {'demo-dev-1', 'demo-easy-1', 'demo-easy-2', 'demo-easy-3', 'demo-hard-1', 'demo-hard-2', 'demo-hard-3', 'demo-challenge-1', 'demo-veryhard-1', 'demo-veryhard-2'} (these are stable curation contracts, pinned like the tier vocabulary); every set has ≥ 1 entry; tier census == {easy: 4, hard: 3, challenge: 1, very_challenging: 2} counting the dev set in easy.
2. Flag-branch coverage: at least one entry with metal_present True (heme) and at least one with halogen_present True (chloramphenicol/thyroxine) — the per-entry loop's element-scan cross-checks therefore exercise both branches every run.
3. Curated truthfulness echo (runtime side): every non-dev set's license non-empty (mirrors tests/test_demo_data.py in the PyMOL tier).
Keep the per-entry loop itself unchanged. The generic loop is already the criterion-4 "every demo loads with counts matching the manifest" machine (research Q6).
  </action>
  <verify>python3.6 -m py_compile smoke/smoke_02_manifest.py && bash smoke/run_smoke.sh smoke/smoke_02_manifest.py 180 (greps '=== SMOKE-02 PASS ===' with the new coverage checks printed PASS)</verify>
  <done>SMOKE-02 PASS with set census + flag-branch + license-echo asserts green on the full curated manifest.</done>
</task>

<task type="auto">
  <name>Task 2: smoke_21_demo_cleanup.py — messy-scene cleanup field test</name>
  <files>smoke/smoke_21_demo_cleanup.py</files>
  <action>
Create `smoke/smoke_21_demo_cleanup.py` following the house smoke skeleton (repo-root anchor via sys.argv-first/cwd-fallback, one-line flushed prints, sole verdict marker `=== SMOKE-21 PASS ===`, python3.6-safe). Parts:
- PART A (messy scene): write an inline PDB string to `tmp/messy_scene.pdb` (git-ignored) containing a small protein fragment (e.g. 4-6 residues with ATOM records), 3 HOH HETATM records, and 1 Na+ ION HETATM record — then `cmd.load(to_windows_path(...), 'user_complex')`. Snapshot `pre_names = cmd.get_names('objects')` and `pre_counts = {n: cmd.count_atoms(n) for n in pre_names}`; assert the messy names do NOT start with the game prefix (geometry.GAME_PREFIX == '_aam_').
- PART B (curated game): parse the manifest via the pure chain; pick demo-easy-1's first entry (data-relative, the 1OXR-aspirin analog row); build a setup via setup_state.validate_state({'source_mode': 'demo', 'demo_set_id': 'demo-easy-1', 'molecules_per_level': 1, 'difficulty_levels': 1, 'interaction_mode': 'unset'}); `payload, rows = engine.new_game(setup, <fixed seed>, candidates=[that row])` (single-candidate games MUST set molecules_per_level=1 — 04-04 law); `registry = placement.materialize(payload, 0)`; assert all newly-created objects start with '_aam_' and at least one atom everywhere carries segi 'AAM'.
- PART C (cleanup restoration): `placement.cleanup_game_objects()`; then assert `post == pre_names` (exact name-set equality — the sanctioned pre-registered-snapshot pattern per 03-05 law) AND `cmd.count_atoms(n) == pre_counts[n]` for every n in pre_names (PITFALL 13's own suggested proof) AND no '_aam_'-prefixed object remains AND no atom outside the pre-game objects carries segi 'AAM'.
Follow smoke-authoring discipline: compute every value into a variable BEFORE it appears in a check-detail string; tolerance 1e-6 for any coordinate compare (none strictly needed here). Deviation from the research sketch (tmp-file cmd.load instead of read_pdbstr) documented in the header comment.
  </action>
  <verify>python3.6 -m py_compile smoke/smoke_21_demo_cleanup.py && bash smoke/run_smoke.sh smoke/smoke_21_demo_cleanup.py 180 (greps '=== SMOKE-21 PASS ===')</verify>
  <done>SMOKE-21 PASS: messy scene restored exactly after a curated-set game's cleanup; zero game-object leakage.</done>
</task>

<task type="auto">
  <name>Task 3: regression pass over the new smoke pair</name>
  <files>smoke/smoke_21_demo_cleanup.py</files>
  <action>
Re-run SMOKE-02 and SMOKE-21 back-to-back (shared PyMOL boot is not a thing — two separate runs), plus SMOKE-04 (the pre-existing cleanup-assert precedent) to prove no cross-effect, and the full WSL suite. Fix and re-run until green (Rule 1). Commit tasks separately (`feat(08-08): ...`).
  </action>
  <verify>bash smoke/run_smoke.sh smoke/smoke_02_manifest.py 180 && bash smoke/run_smoke.sh smoke/smoke_21_demo_cleanup.py 180 && bash smoke/run_smoke.sh smoke/smoke_04_e2e.py 180 (all three PASS markers) && python3.6 -m unittest discover -s tests -v (green)</verify>
  <done>Criterion 4's both clauses machine-proven headless; no regression in the E2E precedent smoke.</done>
</task>

</tasks>

<verification>
- `bash smoke/run_smoke.sh smoke/smoke_02_manifest.py 180` — PASS with coverage asserts.
- `bash smoke/run_smoke.sh smoke/smoke_21_demo_cleanup.py 180` — PASS (messy-scene restoration).
- `python3.6 -m unittest discover -s tests -v` — full suite green.
- tmp/messy_scene.pdb NOT committed (git-ignored; verify with `git status --short`).
</verification>

<success_criteria>
- SMOKE-02 = the complete every-manifest-id + curated-coverage machine (criterion 4 clause 1).
- SMOKE-21 = PITFALL 13 proven in the field against a waters+ions scene (criterion 4 clause 2).
- Both verdicts reproducible with a single command each.
</success_criteria>

<output>
After completion, create `.planning/phases/08-demo-curation-citations/08-08-SUMMARY.md`
</output>
