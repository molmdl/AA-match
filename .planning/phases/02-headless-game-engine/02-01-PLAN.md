---
phase: 02-headless-game-engine
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - docs/DETECTION_THRESHOLDS.md
autonomous: false

must_haves:
  truths:
    - "A single in-repo document transcribes every threshold row with its verified published source (PLIP/ProLIF/BINANA) and records in-house resolutions of disagreements"
    - "The AA capability table is transcribed side-chain-only with explicit per-row provenance marks ([V-SRC]/[RESOLVE]/[HUMAN])"
    - "Coordination decisions D1, OQ-1, OQ-3, OQ-4, OQ-5, OQ-7, halogen C-F exclusion, and the single-typing-home (capability.py) + no-itertools decisions are recorded verbatim"
    - "Explicit human approval with date + approver is recorded in the document before any detector code freezes"
  artifacts:
    - path: "docs/DETECTION_THRESHOLDS.md"
      provides: "Approved threshold table (10 rows) + capability table (20 AAs x 7 types) + policy decisions + approval record"
      min_lines: 150
  key_links:
    - from: "docs/DETECTION_THRESHOLDS.md"
      to: "aamatch/thresholds.py (02-06) and aamatch/capability.py (02-05)"
      via: "constants transcribed row-by-row from this document"
      pattern: "DETECTOR_VERSION bump policy"
---

<objective>
Create the human-approved detection threshold + capability document (DETECT-03 — the phase's [GATE] critical path). This document is the single source of truth that every downstream plan transcribes into code (thresholds.py 02-06, capability.py 02-05, generator mode semantics 02-08, scoring 'any' semantics 02-10).

Purpose: The project's truthfulness rule forbids inventing chemistry. All values below were verified from PLIP/ProLIF/BINANA sources on 2026-09-06 (see the three 02-RESEARCH-*.md files); this gate approves our ADOPTION of them, records the in-house resolutions, and freezes D1 + OQ-1.
Output: docs/DETECTION_THRESHOLDS.md with a recorded human approval.
</objective>

<execution_context>
@~/.config/opencode/get-shit-done/workflows/execute-plan.md
@~/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/02-headless-game-engine/02-RESEARCH-detection.md   # §3 threshold DRAFT table, §4 partner sides, §7 chemistry policy, §10 open questions
@.planning/phases/02-headless-game-engine/02-RESEARCH-generation.md  # §2 capability table DRAFT + policy decisions D1-D4, §4.1 OQ-1 mode semantics
@.planning/REQUIREMENTS.md                                           # DETECT-03 exact text
@.planning/ROADMAP.md                                                # Phase 2 success criterion 1
</context>

<tasks>

<task type="auto">
  <name>Task 1: Write the threshold + capability + policy document</name>
  <files>docs/DETECTION_THRESHOLDS.md</files>
  <action>
Create `docs/DETECTION_THRESHOLDS.md` (create `docs/` if absent) with these sections, transcribing VERIFIED values from the two research files — do NOT invent, round, or "improve" any number:

1. **Header**: purpose (DETECT-03 gate), verification date (2026-09-06), the three source repos + versions + fetched files, and the three DOIs exactly as cited in 02-RESEARCH-detection.md §1.2 (PLIP doi:10.1093/nar/gkaf361; ProLIF doi:10.1186/s13321-021-00548-6; BINANA doi:10.1016/j.jmgm.2011.01.004).

2. **Approved threshold table** — all 10 rows of 02-RESEARCH-detection.md §3 verbatim (type, adopted criterion, source, rationale/rejected alternatives). Each row gets an `Approval:` placeholder line ("date: ____ approver: ____") to be filled at Task 2.

3. **AA capability table** — all 20 rows of 02-RESEARCH-generation.md §2.3 with their per-cell provenance marks. Apply the following RESOLUTIONS (these are the recommended options from the research; the human confirms or vetoes them at Task 2):
   - His = neutral (donor+acceptor, ring for pi/cation-pi ring role, NOT a salt-bridge cation in v1) [OQ-3 / D2]
   - Hydrophobic pedagogical set = ALA VAL LEU ILE PRO PHE MET TRP (Cys/Tyr/Gly excluded) with Kyte & Doolittle 1982 citation (doi:10.1016/0022-2836(82)90515-0)
   - Asp/Gln metal-chelator: INCLUDE (BINANA N/O/S rule beats PLIP's omission); Cys(S) metal: INCLUDE per PLIP+BINANA; Met thioether metal: EXCLUDE
   - Met thioether S as H-bond acceptor: EXCLUDE in v1 — must-record DETECT-03 disagreement (BINANA "S atoms can act as acceptors" → yes; ProLIF acceptor SMARTS has no aliphatic S → no; research recommends INCLUDE-marked-weak). Rationale for excluding: weak thioether acceptor; excluded in v1, revisit only as a versioned capability-table change (DETECTOR_VERSION bump). The h_bond-acceptor AA set is therefore {ASN, ASP, CYS, GLN, GLU, HIS, SER, THR, TYR}
   - Halogen bond: C-F donors EXCLUDED (ProLIF precedent); donors ligand-side only; AA-side acceptors O/N/S
   - Metal element list v1 = {MG, ZN, FE, CA, MN, CU, NI, CO, CD} [OQ-7]

4. **Policy decisions** — one numbered subsection each, stating decision + rationale + who it binds:
   - **D1 (side-chain-only)**: capability table is side-chain-only; the detector MUST restrict H-bond donor/acceptor (and hydrophobic, residue-name-based) matching to side-chain atoms of grid AAs. Binds: capability.py (02-05), detector.py (02-06/07), generator tests.
   - **Single typing home**: `aamatch/capability.py` is the ONLY atom/residue typing module (the detection research's proposed `chem_types.py` is merged into it). Binds all consumers (generator, detector, Hint later). DETECT-04 holds by construction.
   - **OQ-1 (mode semantics)**: exclusive = "any interaction formed" scoped by allowed_interactions; exclusive + empty allowed list -> GenerationError at generate time; unset + empty allowed list -> draw from all 7 types; block_exclusive + unsupported checked type -> refuse naming the type. Binds: generator derive_required (02-08), score 'any' semantics (02-10).
   - **OQ-4**: keep PLIP's ligand-side-only tertiary-amine anti-artifact rule for cation-pi (asymmetric v1).
   - **OQ-5**: hydrophobic excluded from `unset`-mode required-type sampling (generator policy; detection unchanged).
   - **Purity convention**: `itertools` is NOT added to ALLOWED_STDLIB — pure modules use comprehensions/nested loops instead (deliberate decision; binds every Phase-2 pure-module plan).
   - **Bump policy**: any change to threshold rows 1-10 or capability typing requires bumping DETECTOR_VERSION in aamatch/level_spec.py (exact-match gate).

5. **Chemistry policy summary** (from detection research §7, 1-2 lines each): waters excluded; alt-conf keep ''/'A' only; fail-closed donor typing (O/N/S without attached H is not a donor); state-1 only; metal gating via ligand_has_metal; covalent exclusions; MIN_DIST 0.5.

Do NOT modify any .py file in this plan — the gate approves the document first.
  </action>
  <verify>grep -c "Approval:" docs/DETECTION_THRESHOLDS.md returns >= 10; grep for each DOI returns a match; grep "D1" and "OQ-1" both match; python3.6 -m py_compile aamatch/*.py still green (no code touched)</verify>
  <done>Document exists with all 10 threshold rows (each with source + rationale), 20-AA capability table with resolutions, all policy decisions, and per-row approval placeholders</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>The in-repo threshold + capability document (docs/DETECTION_THRESHOLDS.md) transcribing verified PLIP/ProLIF/BINANA values, the resolved capability table, and the D1/OQ-1 policy decisions.</what-built>
  <how-to-verify>
    1. Open docs/DETECTION_THRESHOLDS.md.
    2. Spot-check 2-3 threshold rows against 02-RESEARCH-detection.md §3 (values must match the verified research exactly).
    3. Confirm the capability resolutions you agree with (His neutral, hydrophobic set, Met thioether-S acceptor EXCLUDED in v1, C-F exclusion, metal list, D1 side-chain-only, OQ-1 mode semantics). Veto any row/decision you disagree with.
    4. This approval is the DETECT-03 [GATE]: detector code in later plans freezes against this table.
  </how-to-verify>
  <resume-signal>Type "approved" to record approval, or describe the rows/decisions to change (Task 3 then amends the document and re-presents).</resume-signal>
</task>

<task type="auto">
  <name>Task 2: Record the approval</name>
  <files>docs/DETECTION_THRESHOLDS.md</files>
  <action>
Fill every `Approval:` placeholder with the real date (today) and approver ("human gate, phase-2 plan 02-01 checkpoint"). If the human requested changes in the checkpoint, apply them to the affected rows/decisions FIRST, then record approval. Add a final "Approval record" section: date, approver, and the sentence "Detector implementation (plans 02-05..02-08) freezes against this table; any later change requires a DETECTOR_VERSION bump."
  </action>
  <verify>grep -c "date: 20" docs/DETECTION_THRESHOLDS.md >= 10 and no empty "approver: ____" placeholders remain (grep -c "____" returns 0)</verify>
  <done>Approval recorded per row; document is the frozen source of truth for plans 02-05 through 02-08</done>
</task>

</tasks>

<verification>
- docs/DETECTION_THRESHOLDS.md exists with >= 10 approval records, 3 DOIs, D1 + OQ-1 decisions
- No code files modified (gate precedes implementation by design)
- `python3.6 -m unittest discover -s tests -v` still green (nothing broke)
</verification>

<success_criteria>
Phase-2 success criterion 1 ([GATE]) is satisfiable: the threshold document exists in-repo with citations, in-house resolutions, and recorded human approval before detector code freezes.
</success_criteria>

<output>
After completion, create `.planning/phases/02-headless-game-engine/02-01-SUMMARY.md`
</output>
