---
phase: 05-game-status-tab-start-sequence
plan: 02
type: tdd
wave: 1
depends_on: []
files_modified: [aamatch/capability.py, tests/test_capability.py, tests/test_generator_invariants.py]
autonomous: true

must_haves:
  truths:
    - "hint_required_types resolves the required dict to a type tuple: mode 'any' -> ALL 7 canonical INTERACTION_TYPES (scoring-honest vocabulary, game_state.py:74-75); mode 'list' -> the item types in GIVEN order; unknown mode / empty items / unknown item type -> ValueError (05-RESEARCH-hint.md Pattern 1)"
    - "hint_candidate_slots returns the tuple of slot_ids whose residue_capabilities(slot['aa'], profile) intersects the required types — capable DISTRACTORS are included, incapable slots excluded, payload (row-major) order deterministic; the hint NEVER reads slot['can_form'] (generation-time provenance, generator.py:453-456)"
    - "GEN-04 parity invariant: for real generated payloads, every role='required' slot's aa appears in hint_candidate_slots(slots, profile, required) — solvability and the hint agree by construction, pinned forever"
  artifacts:
    - path: "aamatch/capability.py"
      provides: "hint_required_types(required) + hint_candidate_slots(slots, profile, required) — pure, fail-closed, zero new imports"
      contains: "hint_candidate_slots"
    - path: "tests/test_capability.py"
      provides: "the hint predicate battery (types resolution + candidates over the existing profile fixtures)"
    - path: "tests/test_generator_invariants.py"
      provides: "the GEN-04 parity invariant over real generator payloads (several seeds x modes)"
  key_links:
    - from: "aamatch/capability.py hint_candidate_slots"
      to: "aamatch/capability.py residue_capabilities (capability.py:307-314)"
      via: "the ONE typing home — the same predicate the generator's solvability (generator.py:492-496) and the detector (detector.py:160-161) consume"
      pattern: "residue_capabilities"
---

<objective>
The pure "could form" half of PLAY-05: two fail-closed helpers in the ONE typing home (capability.py) that resolve the required dict to types and intersect per-residue capability over the payload slots — TDD'd in WSL before any cmd-tier recolor exists.

Purpose: the hint must be IDENTICAL to what `allocate_slots` guaranteed solvable (same `aa_capable`, same profile) and to what `score` credits — by construction, not convention (DETECT-04). The predicates already exist (capability.aa_capable:244-304, residue_capabilities:307-314 whose docstring already names the Hint as an intended consumer); only ~50 lines of composition are new, which is exactly what makes this a clean RED-first battery.
Output: two new pure functions + the WSL battery + the GEN-04 parity invariant.
</objective>

<execution_context>
@~/.config/opencode/get-shit-done/workflows/execute-plan.md
@~/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/05-game-status-tab-start-sequence/05-RESEARCH-hint.md
@.planning/STATE.md
@aamatch/capability.py
@aamatch/generator.py
@aamatch/setup_state.py
@tests/test_capability.py
@tests/test_generator_invariants.py

Key facts (verified, transcribe exactly):
- `aa_capable(resn, itype, ligand_profile)` (capability.py:244-304) is polarity-aware and direction-refined; `residue_capabilities(resn, ligand_profile)` (capability.py:307-314) returns the full per-residue type set — the hint consumes ONLY these two plus the new helpers; it must NOT call ligand_support/ligand_has_metal separately (that would duplicate the gating inside aa_capable and invite drift).
- Mode semantics (research C, pinned): exclusive -> {'mode': 'any', 'items': []} (generator.py:396) -> hint types = all 7; block_exclusive -> {'mode': 'list', 'items': checked types} (generator.py:409-410); unset -> {'mode': 'list', 'items': sampled subset} (generator.py:433-435).
- `slot['aa']` IS the canonical uppercase resn (generator.py:844 'aa': slot['aa']; placement.py:110-116); slots live at payload['levels'][L]['molecules'][M]['grid']['slots'] as plain dicts with 'slot_id' and 'aa' (generator.py:840-850).
- `can_form` is generation-time provenance, NOT the hint source (generator.py:453-456 pins this in its docstring — pitfall H-2).
- Solvability guarantees: allocate_slots refuses a required type with zero capable AAs (generator.py:494-496) and draws required AAs from capable ones (generator.py:492-493) — hence the parity invariant is EXPECTED to hold for any generated payload.
- tests/test_generator_invariants.py already imports capability + generate and carries CANDIDATES/LIGAND_DATA fixtures with capability.ligand_profile-shaped profiles keyed by (set_id, entry_id) (the 02-08 ligand_data contract).
- Purity: capability.py is already in PURE_MODULES; NO new imports at all (the helpers need none — research C gate-impact: "no new imports at all"); python3.6 floor; PROSE_PIN discipline (no banned-token mentions).
</context>

<feature>
  <name>capability.hint_required_types + capability.hint_candidate_slots (+ GEN-04 parity)</name>
  <files>aamatch/capability.py, tests/test_capability.py, tests/test_generator_invariants.py</files>
  <behavior>
All cases expect-before-fn (python3.6, unittest, zero stubs):

hint_required_types(required):
- {'mode': 'any', 'items': []} -> tuple(INTERACTION_TYPES) — all 7 canonical types in canonical order
- {'mode': 'list', 'items': [h_bond x1, pi_stacking x2]} -> ('h_bond', 'pi_stacking') in GIVEN order
- non-dict required -> ValueError naming the type found
- unknown mode -> ValueError naming the mode and the expected set (any, list)
- 'list' with missing/empty items -> ValueError naming the cause (empty items is the 'any'-mode representation)
- 'list' with an unknown item type -> ValueError naming the unknown type(s)
- wording mirrors the game_state.score / required_summary refusal family (research C's sketch is the wording source)

hint_candidate_slots(slots, profile, required):
- slots = the payload's grid.slots list (plain dicts with 'slot_id' and 'aa')
- returns the tuple of slot_ids where residue_capabilities(slot['aa'], profile) & required_types is non-empty, in PAYLOAD order (row-major, deterministic)
- a capable DISTRACTOR slot is INCLUDED (its resn is capable even though can_form == [] — pitfall H-2)
- an incapable slot is EXCLUDED; a required type the ligand cannot support yields no candidates for it (aa_capable gates on the profile)
- non-list slots -> ValueError; a slot without 'slot_id' or 'aa' -> ValueError naming the slot
- hand-built profile fixtures (reuse tests/test_capability.py's existing profile builders) covering all 7 types: required-slot capable; capable distractor included; incapable excluded; unsupported type -> nothing; deterministic order; malformed shapes refuse

GEN-04 parity invariant (tests/test_generator_invariants.py):
- for several (seed x interaction_mode) generated payloads (reuse the file's existing generate + CANDIDATES + LIGAND_DATA fixtures; profile = ligand_data[(set_id, entry_id)]['profile']): every slot with role == 'required' has its slot_id in hint_candidate_slots(slots, profile, required)
- ALSO (belt-and-braces agreement pin, tests/test_capability.py): for every resn in AA_RESIDUES and a representative profile, residue_capabilities(resn, profile) == {t for t in INTERACTION_TYPES if aa_capable(resn, t, profile)}
  </behavior>
  <implementation>
aamatch/capability.py — EXTEND (research C Pattern 1, transcribe the sketches):
- hint_required_types(required): non-dict -> ValueError; mode 'any' -> tuple(INTERACTION_TYPES); mode 'list' -> empty-items check then unknown-type check then tuple of item types; else ValueError with the unknown-mode wording. Zero new imports.
- hint_candidate_slots(slots, profile, required): non-list -> ValueError; per slot shape-check ('slot_id' and 'aa' present); caps = residue_capabilities(slot['aa'], profile); caps & frozenset(hint_required_types(required)) non-empty -> append slot_id; return tuple in payload order.
- Docstrings carry the DETECT-04 argument (same predicate as solvability + scoring) and the can_form prohibition.
- tests/test_capability.py: new TestCase class(es) appended — the helper battery + the agreement pin (reuse the file's profile fixtures).
- tests/test_generator_invariants.py: new TestCase class — GEN-04 parity over several seeds/modes from the existing fixtures.

RED first: battery + invariant written -> run -> MUST fail (AttributeError: no hint_required_types). GREEN: implement the two functions -> all green.
  </implementation>
</feature>

<tasks>

<task type="tdd-red">
  <name>Task 1 (RED): hint predicate battery + GEN-04 parity invariant</name>
  <files>tests/test_capability.py, tests/test_generator_invariants.py</files>
  <action>Append to tests/test_capability.py: TestHintRequiredTypes (any->7 canonical order; list->given order; non-dict/unknown-mode/empty-items/unknown-type -> ValueError), TestHintCandidateSlots (hand-built profiles over the existing fixtures: required-slot capable; capable distractor INCLUDED; incapable excluded; ligand-unsupported type yields nothing; payload-order determinism; non-list slots + missing-key slots -> ValueError), TestResidueCapabilitiesAgreement (the per-type aa_capable union pin for all AA_RESIDUES). Append to tests/test_generator_invariants.py: TestGen04HintParity — several (seed, mode) payloads via the file's existing fixtures; for each molecule, profile looked up from ligand_data by the molecule's ligand (set_id, entry_id); assert every role=='required' slot_id ∈ hint_candidate_slots(...). Run — MUST FAIL (AttributeError: hint_required_types / hint_candidate_slots missing). Commit: `test(05-02): add failing hint predicate battery + GEN-04 parity invariant`.
  </action>
  <verify>python3.6 -m unittest tests.test_capability tests.test_generator_invariants -v exits nonzero naming the missing functions (RED proven); python3.6 -m py_compile tests/test_capability.py tests/test_generator_invariants.py clean</verify>
  <done>The battery + invariant exist, compile under 3.6, and fail for the RIGHT reason</done>
</task>

<task type="tdd-green">
  <name>Task 2 (GREEN): implement the two capability helpers</name>
  <files>aamatch/capability.py</files>
  <action>Implement hint_required_types + hint_candidate_slots per <implementation> (research C Pattern 1 sketches verbatim where given). Zero new imports; docstrings carry the DETECT-04 single-home argument + the can_form prohibition + the GEN-04 note. Run the battery — MUST pass. Commit: `feat(05-02): implement hint_required_types + hint_candidate_slots`.
  </action>
  <verify>python3.6 -m unittest tests.test_capability tests.test_generator_invariants -v — all green (incl. GEN-04 parity over the generated payloads)</verify>
  <done>Both helpers fail-closed and payload-order-deterministic; the parity invariant proves required slots ⊆ candidates on real payloads</done>
</task>

<task type="auto">
  <name>Task 3: full regression sweep</name>
  <files>[]</files>
  <action>No gate edits needed (capability.py is already registered in PURE_MODULES; NO new imports anywhere — research C gate-impact). Run the full WSL suite + the 3.6 syntax floor. Commit (if any docstring polish happened): `test(05-02): regression sweep green`.
  </action>
  <verify>python3.6 -m py_compile aamatch/*.py && python3.6 -m unittest discover -s tests -v — all green including purity gates</verify>
  <done>Full WSL suite green; zero gate-file changes required by this plan</done>
</task>

</tasks>

<verification>
- `python3.6 -m py_compile aamatch/*.py` — 3.6 floor.
- `python3.6 -m unittest discover -s tests -v` — full suite green.
- Grep capability.py for 'can_form' — only the PRE-EXISTING generator-side mentions may exist; the NEW helper docstrings must reference the prohibition without reading the field (grep the two new functions' bodies for `['can_form']` / `.get('can_form')` — zero hits).
- Grep tests/test_generator_invariants.py for the new parity class — present and green.
</verification>

<success_criteria>
- hint_required_types: any -> all 7 canonical; list -> items in given order; four fail-closed refusals pinned.
- hint_candidate_slots: capability-live intersection, payload order, distractors included, can_form never read.
- GEN-04 parity invariant green over real generated payloads (the designed alarm for future capability-table edits).
- Full WSL suite green; no gate edits.
</success_criteria>

<output>
After completion, create `.planning/phases/05-game-status-tab-start-sequence/05-02-SUMMARY.md`
</output>
