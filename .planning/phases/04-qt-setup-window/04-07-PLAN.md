---
phase: 04-qt-setup-window
plan: 07
type: execute
wave: 3
depends_on: ["04-05", "04-02"]
files_modified: [aamatch/setup_window.py, smoke/smoke_11_window.py]
autonomous: true

must_haves:
  truths:
    - "The window shows the full 7-field form: 2-page source selector (demo dropdown / upload browse+path label), molecules + difficulty spinboxes whose ranges are imported from the frozen setup_state constants, 3-way mode radios with a context label that restates the current mode's meaning, and 7 checkboxes in canonical INTERACTION_TYPES order -- all with tooltips"
    - "collect_state/apply_state round-trips a validate_state-normalized dict losslessly; apply_state tolerates missing keys and suppresses cascading recompute via a _loading guard"
    - "The demo dropdown is populated from the bundled MANIFEST.json using the exact engine.py:196-198 read shape; a FormatError degrades to a placeholder item + visible warning label, never a crash"
    - "The form never re-implements validation (the pure layer owns validate/clamp); spinbox ranges == clamp ranges so collect -> validate is lossless"
  artifacts:
    - path: "aamatch/setup_window.py"
      provides: "full form: _build_source_selector, _build_spinboxes, _build_mode_group, _build_interactions, collect_state, apply_state"
      min_lines: 220
    - path: "smoke/smoke_11_window.py"
      provides: "SMOKE-11 PART B-extension: apply_state/collect_state round-trip + widget mutation drive (T1b, probe-gated)"
  key_links:
    - from: "aamatch/setup_window.py"
      to: "aamatch/setup_state.py"
      via: "lazy import of MOLECULES_MIN/CAP/DEFAULT, DIFFICULTY_*, INTERACTION_TYPES, DEFAULTS (ranges mirror frozen constants exactly -- never hardcoded)"
      pattern: "MOLECULES_(MIN|CAP|DEFAULT)|DIFFICULTY_|INTERACTION_TYPES"
    - from: "aamatch/setup_window.py"
      to: "aamatch/setup_form.manifest_sets"
      via: "dropdown rows from the pure helper (set_id as userData, title as label)"
      pattern: "manifest_sets"
    - from: "aamatch/setup_window.py"
      to: "MANIFEST.json"
      via: "read_json_file + parse_manifest_dict + paths.package_data_path -- the exact engine.py:196-198 shape"
      pattern: "parse_manifest_dict"
---

<objective>
Build the complete 7-field form inside SetupWindow (SETUP-02..06 widget side) with lossless collect/apply, plus per-widget tooltips (Decision 7). Button handlers remain for plans 04-09..04-13.

Purpose: the form is the user's view of the frozen 7-field model; every later handler reads it through collect_state and writes it through apply_state. Getting the collect/apply round-trip and the range-mirroring right here is what makes Save/Load/Randomize/Start behave (research form_field_specs ruling).
Output: full form + round-trip proof in SMOKE-11.
</objective>

<execution_context>
@~/.config/opencode/get-shit-done/workflows/execute-plan.md
@~/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/04-qt-setup-window/04-RESEARCH-setup-form-seams.md
@.planning/phases/04-qt-setup-window/04-05-SUMMARY.md
@.planning/STATE.md
@aamatch/setup_window.py
@aamatch/setup_form.py
@aamatch/setup_state.py
</context>

<decisions>
- Widget structure mirrors the prior-art stacked-pages pattern (gui_setup.py:89-96): a QGroupBox 'Molecule source' with 2 QRadioButtons + a QStackedWidget; the non-active page's values stay intact underneath (three coexisting state fields, setup_state.py:55-57).
- Checkboxes are ENABLED in ALL THREE modes (the allowed list is the sampling vocabulary in unset mode -- generator.py:413-414; disabling would silently discard the user's restriction, research mode_widget_design).
- The metal/halogen "(no bundled molecule carries this)" annotation is OMITTED (Decision 7 -- Phase 8 changes the data anyway).
- Dropdown populated ONCE at construction; FormatError caught -> placeholder '(no bundled sets)' item with userData '' + a visible warning label (Decision 16).
- Check the 04-01 probe verdict before writing SMOKE-11 parts (same rule as 04-05).
</decisions>

<tasks>

<task type="auto">
  <name>Task 1: source selector + demo dropdown + upload widgets</name>
  <files>aamatch/setup_window.py</files>
  <action>
Inside SetupWindow, add `_build_source_selector(self)` called from __init__ into the form-area layout:

1. QGroupBox 'Molecule source'; two QRadioButtons: self.src_demo ('Bundled demo set') and self.src_upload ('Upload my molecules...'), tooltips: demo = 'Generate from a demo set bundled with AA-match.'; upload = 'Generate from your own SDF or MOL2 file (one file; multi-record files give several molecules).'
2. QStackedWidget (self.source_stack): page 0 = demo page: a QComboBox (self.demo_combo) + a QLabel self.source_note (for placeholder/warning text, initially empty). page 1 = upload page: a QPushButton self.btn_browse ('Browse...', tooltip 'Pick an SDF or MOL2 molecule file (single file; a multi-record file provides several molecules).') + a read-only QLabel self.upload_path_label (elide long paths via setText with the raw path; tooltip shows the full text).
3. Radio toggled signals switch the stack (src_demo.toggled -> setCurrentIndex 0/1). btn_browse is created here but NOT connected (04-10 connects it).
4. Dropdown population in `_populate_demo_sets(self)`: lazy `from . import manifest, paths` + `from .persistence import read_json_file`; read exactly like engine.new_game (engine.py:196-198): `payload = manifest.parse_manifest_dict(read_json_file(paths.package_data_path('data', 'MANIFEST.json')))`; `from . import setup_form` (lazy); rows = setup_form.manifest_sets(payload); for each (set_id, title, tier): `self.demo_combo.addItem(label, set_id)` where label = title (fallback set_id), optional ' (tier)' suffix; `setCurrentIndex(0)`. On `Exception as e` (FormatError from a newer manifest, manifest.py:146-155): clear the combo, add one item '(no bundled sets)' with userData '', and set self.source_note text to 'Could not read the bundled demo list: %s' % e (window must never crash the menu action). Store nothing else.
5. Class attribute `self._uploaded = None` initialized in __init__ (the session-only ingested-upload slot; populated by 04-10, read by collect_state).
Commit: `feat(04-07): source selector + demo dropdown + upload widgets`.
  </action>
  <verify>T0: py_compile + full WSL suite green (no logic measurable in WSL -- Qt tier is source-gated only). Static: no `from PyQt5`, no `.exec_(` on the dialog, no widget-side clamping logic (grep for 'setRange' shows ONLY imported constants).</verify>
  <done>Source selector, dropdown (manifest-fed with degrade path), upload widgets exist and radio->stack wiring works.</done>
</task>

<task type="auto">
  <name>Task 2: spinboxes + mode group + interaction checkboxes + tooltips</name>
  <files>aamatch/setup_window.py</files>
  <action>
1. `_build_spinboxes(self)` (lazy `from . import setup_state` INSIDE the method -- sibling discipline):
   - QGroupBox 'Game size': QSpinBox self.molecules_spin, `setRange(setup_state.MOLECULES_MIN, setup_state.MOLECULES_CAP)`, `setValue(setup_state.MOLECULES_DEFAULT)` (frozen constants -- NEVER literal 1/10/2, form_field_specs ruling); label 'Small molecules per level' + tooltip 'How many small molecules each level contains (default 2; range mirrors the frozen 1..10 bounds).'
   - QSpinBox self.difficulty_spin, `setRange(setup_state.DIFFICULTY_MIN, setup_state.DIFFICULTY_CAP)`, `setValue(setup_state.DIFFICULTY_DEFAULT)`; label 'Difficulty levels' + tooltip 'How many difficulty levels per game (each level is harder: bigger grid, more required interactions).'
2. `_build_mode_group(self)`: QGroupBox 'Required interactions'; 3 QRadioButtons in a QButtonGroup-free layout (radio exclusivity is automatic within a parent): self.mode_exclusive ('Exclusive (any allowed)' tooltip 'The game accepts ANY of the checked interactions.'), self.mode_block ('Block-exclusive (checked set)' tooltip 'The player must form EXACTLY the checked interactions.'), self.mode_unset ('Unset (random)' tooltip 'The game randomizes the required set from the checked types (hydrophobic is excluded from random picks).'); default checked = mode_unset (frozen DEFAULTS interaction_mode 'unset').
   - Below the radios, QLabel self.mode_context_label whose text updates on every mode toggle (three exact strings from research mode_widget_design: exclusive -> 'The game accepts ANY of the checked interactions'; block_exclusive -> 'The player must form EXACTLY the checked interactions'; unset -> 'The game randomizes the required set from the checked types (hydrophobic excluded from random picks).').
3. `_build_interactions(self)`: QGroupBox 'Allowed interactions'; 7 QCheckBoxes built by iterating the CANONICAL list (lazy `from .setup_state import INTERACTION_TYPES`): self.interaction_checks = list of (type_str, QCheckBox) in that order; display labels: 'Hydrogen bond', 'Salt bridge', 'Pi-stacking', 'Cation-pi', 'Hydrophobic', 'Halogen bond', 'Metal coordination' (UI-only labels; stored values are the enum strings); each checkbox tooltip = one line explaining the interaction in game terms (spec UI standard 'clear but sufficient'; e.g. halogen: 'Halogen bond: a Cl/Br/I on the molecule accepting from a donor on the amino acid.'). Checkboxes are enabled in every mode (Decision: never disabled).
4. All three builders called from __init__ in order; the form area packs them vertically with the button row below (existing).
Commit: `feat(04-07): spinboxes, mode radios + context label, 7 interaction checkboxes`.
  </action>
  <verify>T0: py_compile; full WSL suite green; grep: setRange calls reference setup_state attributes only (no numeric literals); 7 checkboxes built from INTERACTION_TYPES order.</verify>
  <done>Every SETUP-02..06 widget exists with frozen-constant ranges, canonical checkbox order, and tooltips everywhere.</done>
</task>

<task type="auto">
  <name>Task 3: collect_state/apply_state round-trip + SMOKE-11 form parts</name>
  <files>aamatch/setup_window.py, smoke/smoke_11_window.py</files>
  <action>
1. `collect_state(self) -> dict` (PA-setup:539-557 shape): returns the plain dict:
   {'source_mode': 'upload' if self.src_upload.isChecked() else 'demo',
    'demo_set_id': str(self.demo_combo.currentData() or ''),
    'upload': ({'path': self._uploaded['path'], 'sha256': self._uploaded['sha256']} if self._uploaded else None),
    'molecules_per_level': int(self.molecules_spin.value()),
    'difficulty_levels': int(self.difficulty_spin.value()),
    'interaction_mode': ('exclusive' if self.mode_exclusive.isChecked() else 'block_exclusive' if self.mode_block.isChecked() else 'unset'),
    'allowed_interactions': [t for (t, cb) in self.interaction_checks if cb.isChecked()]}
2. `apply_state(self, state)` (PA-setup:559-617 shape): set `self._loading = True` FIRST (initialized False in __init__); try: with missing-key tolerance via `state.get(key, default)` for every field -- source radio + stack page; demo combo: `findData(set_id)` -> setCurrentIndex (fall back to index 0 if not found, and set source_note 'set %r is not in the bundled list' when the id is non-empty and unfound); spinboxes via setValue(int(...)); mode radios; checkboxes via setChecked(type in allowed); upload page label from state.get('upload') path if present (label text only -- _uploaded is session-only and NOT restored); finally: `self._loading = False`. The _loading guard exists so future valueChanged hooks (none yet) can early-return; keep the flag + comment.
3. SMOKE-11 PART B extension (only per probe verdict, same rule as 04-05): after the existing parts -- `dlg.apply_state(<defaults dict built from aamatch.setup_state.DEFAULTS via dict copy>)` (import setup_state in the smoke; it is pure and importable headless); `state = dlg.collect_state()`; `from aamatch import setup_state` + assert `setup_state.validate_state(state) == setup_state.validate_state(dict(setup_state.DEFAULTS))` (round-trip identity through the pure authority); mutate widgets programmatically: molecules_spin.setValue(4), difficulty_spin.setValue(2), mode_block.setChecked(True), check h_bond + pi_stacking, src_upload.setChecked(True) then back to src_demo; collect again and assert each value landed; assert demo_combo carries userData 'demo-dev-1' (bundled manifest today). NO modals. Extend the marker logic: parts fail -> no SMOKE-11 PASS.
Commit: `test(04-07): collect/apply round-trip + SMOKE-11 form drive`.
  </action>
  <verify>T0: py_compile + full WSL suite green. T1b (probe-PASS): `bash smoke/run_smoke.sh smoke/smoke_11_window.py 120` prints `=== SMOKE-11 PASS ===` including the round-trip asserts. T1a (probe-FAIL): PART B-extension skipped and marked for [HUMAN].</verify>
  <done>The form is a lossless view of the 7-field model, proven headlessly as far as the probe allows.</done>
</task>

</tasks>

<verification>
- T0: py_compile + full WSL suite green; source gates (no PyQt5 import, no exec_ on the dialog, no numeric spinbox literals).
- T1b (probe-gated): SMOKE-11 round-trip + widget mutation asserts PASS.
- collect -> validate_state is identity for every field (ranges mirror clamps exactly).
</verification>

<success_criteria>
The window's form is complete (SETUP-02..06 widget side): dropdown of bundled sets with degrade path, upload widgets, frozen-constant spinboxes, 3-way mode with context label, 7 canonical checkboxes, tooltips, and a lossless collect/apply round-trip. No validation logic leaked into widgets.
</success_criteria>

<output>
After completion, create `.planning/phases/04-qt-setup-window/04-07-SUMMARY.md` (frontmatter: affects [04-09..04-13 handlers], subsystem qt-window).
</output>
