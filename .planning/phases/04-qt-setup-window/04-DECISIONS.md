# Phase 4 Decisions Record

All planner decisions (yolo mode), taken per research recommendations. Alternatives recorded where a real choice existed. Executor- binding: plans cite these; do not re-litigate during execution.

| # | Question | Decision | Alternative rejected | Where |
|---|----------|----------|----------------------|-------|
| 1 | Qt module name (research Q6) | `aamatch/setup_window.py` | `gui_setup.py` (v1 mental-model name) | 04-05 |
| 2 | Setup-file extension (form Q1) | `.aam.setup.json` (filter `AA-match Setup (*.aam.setup.json);;All Files (*)`, auto-append when missing) | `.bcm.setup.json` analog only | 04-09; human one-line confirm in 04-15 |
| 3 | Export filename/extension (export OQ5) | default name `game.aamatch.json`, filter `AA-match Game (*.aamatch.json);;All Files (*)`, auto-append when missing | zip / folder / `.pse`+sidecar — all rejected by research game_file_design | 04-12; human one-line confirm in 04-15 |
| 4 | Seed policy (form Q2 + export flow 8) | Generate: fresh random per export (`random.randint(0, 2**31 - 1)` — matches `generator._SUB_SEED_MAX`, generator.py:286), seed displayed in the success message. Start: fresh random seed from the CURRENT form — EXCEPT when `self._last_export['setup'] == state` (Start-after-Generate with unchanged form), then the exported `(setup, seed, candidates, ligand_content)` tuple is reused so the game played IS the game shared. Upload mode additionally requires session-ingested content whose path/sha256 match `state['upload']`. | Always-42 (every game identical); unconditionally replaying a stale export after form edits (surprising) | 04-12 (export side), 04-13 (start side) |
| 5 | Randomize fix-up target (form Q3) | `setup_form.usable_randomized_state(seed, demo_set_id=<current dropdown selection>, fallback '')` — preserves the user's chosen content; `''` = all sets when no real set selected | Overwriting with `''` always | 04-02 (helper), 04-09 (handler) |
| 6 | Cleanup mid-game (form Q4) | Cleanup handler pops the wizard FIRST iff `isinstance(cmd.get_wizard(), wizard.GameWizard)` via canonical `cmd.set_wizard()` None-pop (wizard.py:31-39; C layer runs its cleanup + auto-resumes prior wizard); NEVER pops a user wizard | Disabling the button while a game is active | 04-11 |
| 7 | Tooltips scope (form Q5) | Cheap tooltips on every widget + the mode context label are IN SCOPE (04-07). Full help panel stays Phase 9 (HELP-01). The metal/halogen "(no bundled molecule carries this)" checkbox annotation is OMITTED — Phase 8 changes the data anyway; tooltip text stays static | Full help panel now; capability annotation | 04-07 |
| 8 | `UPLOAD_MAX_RECORDS` (export OQ6) | `50`, a named constant in `aamatch/game_file.py`, enforced by pure `check_upload_supply` (refusal names the file + cap + perf rationale). SMOKE-12 prints per-record extraction timing; revisit the value only if 50 records demonstrably exceed the 30 s Generate budget | 20 (too tight for educator sets); unlimited (500-record uploads run 500 temp-load extractions per Generate) | 04-06 (constant), 04-08 (timing print) |

## Additional decisions taken while planning

| # | Question | Decision | Rationale |
|---|----------|----------|-----------|
| 9 | Multi-file upload vs frozen field shape | v1 upload = ONE file per upload action (`QFileDialog.getOpenFileName` single-select; a new upload REPLACES the slot). The frozen setup field is `upload = None \| {'path','sha256'}` (setup_state.py:57, shape-checked :137-142); supporting multiple files requires a schema change = version-bump event (01-09 law). One multi-record SDF = "a set of small molecules" (spec.md:14). Multi-file select = v2/EXT-04 territory. | 04-10 |
| 10 | `build_state` return shape | `setup_form.build_state(form_values, known_set_ids=()) -> validated_state`, RAISING `ValueError` with user-facing messages on the three fatal pre-checks (empty-allowed in exclusive/block_exclusive — worded like generator.py:386-401; upload mode without session-ready content; `demo_set_id` not in `known_set_ids`). NO `(state, warnings)` tuple: all three conditions warrant refusal, and fail-closed beats a warning the user can click past. The empty-allowed pre-check firing BEFORE `start_game` is the point — `start_game` cleans the scene before `new_game` can refuse (gamestart.py:239). | 04-02 |
| 11 | Headless vs modal error paths | Smokes drive SUCCESS paths only; `_guard` (QMessageBox.warning) paths are [HUMAN]-verified (a modal in a headless probe hangs the smoke — P5). Every handler is factored as non-modal `_X_impl`/module-level function (dialog wrapper + logic), so T1b drives logic without modals. | 04-05, all handler plans |
| 12 | `base64` stdlib whitelist | `tests/test_purity.py` `ALLOWED_STDLIB` gains `'base64'` (deliberate one-line test evolution in 04-03, rationale documented in the commit). Required by `encode_ligand_files`/`decode_ligand_files`. | 04-03 |
| 13 | Upload pipeline module homes | Pure: `game_file.read_upload_source` (file read + sha256 + format detect), split helpers, row builder, row validator, cap. Cmd tier: NEW `aamatch/upload.py` — `prepare_uploaded_set(records, fmt)` (per-record temp-load extraction loop reusing the `_ligand_data_for` discipline). Keeps engine.py stable and setup_window.py lean. | 04-06, 04-08 |
| 14 | MOL2 multi-segment edge cases (export OQ1) | `split_mol2_segments` implemented (split at column-0 `@<TRIPOS>MOLECULE`); SMOKE-12 probes a 2-segment MOL2 WITH a leading comment line. If the probe fails, the documented fail-closed fallback activates: `prepare_uploaded_set` refuses multi-segment MOL2 (message names the file + record count) — both branches written out in 04-08; decision rule is mechanical (probe outcome). | 04-08 |
| 15 | New smoke numbering | 09 = Qt offscreen probe (04-01) · 10 = ligand_content plumbing (04-04) · 11 = setup window (04-05, grown by 04-07/09/10/11/12/13) · 12 = upload E2E (04-08). Probe script prints BOTH `=== PROBE PASS (platform=...) ===` (research verdict marker) and `=== SMOKE-09 PASS ===` (run_smoke.sh grep contract). | 04-01, 04-04, 04-05, 04-08 |
| 16 | Dropdown population timing | Manifest read at dialog construction (the exact engine.py:196-198 read shape); `FormatError` caught → placeholder item + visible warning label (window must never crash the menu action — form research dropdown_and_upload_state). No re-read on re-open (v1: one bundled set; Phase 8 revisits). | 04-07 |
| 17 | `setup_window.py` cmd import | Handlers need `pymol.cmd` only for Cleanup (`cmd.get_wizard`/`cmd.set_wizard`); `from pymol import cmd` added at module level in 04-11 (legal in Qt tier per research), not in the 04-05 shell. Sibling aamatch imports stay lazy-inside-methods everywhere (wizard.py:22-29 discipline). | 04-05, 04-11 |
| 18 | materialize ligand format for uploads | Payload `ligand` blocks carry no `format` key (generator.py:851-861); `placement.materialize` derives the reader from the synthetic file key's EXTENSION (`'uploads/mol-00N.sdf'`) — fail-closed `PlacementError` for any other extension. No generator schema change. | 04-04 |
| 19 | Two sha256 hashes never conflated | State field `upload['sha256']` = hash of the uploaded FILE bytes (Load-moved-file warning; setup_state.py:27-30). Row `sha256` + `ligand_files` integrity = hash of the embedded RECORD TEXT (what import verifies). Both documented in game_file docstrings + tests. | 04-06, 04-03 |
| 20 | Read `_check_entry` reuse | `validate_uploaded_rows` reuses `manifest._check_entry`-family internals (manifest.py:191-295) via same-package import — single home for the 14-key rules; the absolute-path refusal reuses manifest.py:267-275 rules. | 04-06 |

## Deliberate test evolution inventory (never silent)

| Test file | Change | Plan |
|-----------|--------|------|
| `tests/test_purity.py` | `PURE_MODULES` += `'setup_form'` | 04-02 |
| `tests/test_purity.py` | `PURE_MODULES` += `'game_file'`; `ALLOWED_STDLIB` += `'base64'` | 04-03 |
| `tests/test_package_skeleton.py:110-154` | `run_plugin_gui` AST contract REPLACED: `from . import setup_window` + `return setup_window.open_window()` (03-05 precedent of deliberate contract replacement) | 04-05 |
| `tests/test_wizard_source.py:51` | `SCANNED_MODULES` += `'setup_window.py'` (recorded growth protocol) | 04-05 |
| `smoke/smoke_08_starter.py` | Re-pointed to call `gamestart.start_game()` directly (the menu function now opens a Qt window); all 31 checks preserved | 04-05 |
| `tests/test_code_audit.py` | NO change — new modules' docstrings simply never mention `get_model`/`matrix_reset`/`get_object_ttt` (P9 prose discipline) | all |
