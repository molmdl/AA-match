---
phase: 07-checkpoint-game-file-persistence
plan: 06
type: execute
wave: 3
depends_on: [07-03, 07-04]
files_modified: [aamatch/game_window.py, smoke/smoke_16_tab.py]
autonomous: true

must_haves:
  truths:
    - "The Game tab has a 'Save' button (attribute btn_save_game) inserted before the stretch — the row order becomes Hint, Confirm, Skip/Give Up, Restart, Reset, Save and the stretch stays LAST"
    - "Save with no live GameWizard is a SILENT no-op BEFORE any box (gate-first law) — covering pre-GO, no-game, and post-endgame-popped states"
    - "Save captures the elapsed BEFORE the file dialog opens and passes it into the impl (deterministic sidecar value, the v1 capture doctrine)"
    - "A successful save logs 'Game saved to <path>.' via the pure builder — NO success box (the info-box line IS the feedback)"
    - "The .aamz file auto-appends the extension and lands at the chosen path with both members"
  artifacts:
    - path: "aamatch/game_window.py"
      provides: "btn_save_game + _on_save_game (thin modal wrapper) + _save_game_to(path, elapsed) (non-modal impl)"
      contains: "btn_save_game"
    - path: "smoke/smoke_16_tab.py"
      provides: "New PART: save-button T1b drive (labels, tooltip, stretch-last, impl drive, gate no-op, elapsed-capture ordering)"
      contains: "btn_save_game"
  key_links:
    - from: "aamatch/game_window.py _save_game_to"
      to: "aamatch/gamestart.capture_checkpoint_snapshot / save_checkpoint"
      via: "impl = capture(elapsed) -> save(path, data); the Qt tier never touches engine/wizard privates"
      pattern: "capture_checkpoint_snapshot"
    - from: "aamatch/game_window.py _on_save_game"
      to: "status_text.game_saved_line"
      via: "handler-logged AFTER _guard success; no QMessageBox"
      pattern: "game_saved_line"
---

<objective>
Wire the Game-tab Save button (SCORE-08 UI): `btn_save_game` + gate-first thin wrapper + non-modal impl driving the 07-04 seams, plus the SMOKE-16 T1b extension.

Purpose: the user-facing half of "save button checkpoints the game" (spec.md:46). The impl composition is thin — the container/zip work is done (07-02/07-04); this plan is the Qt-tier wiring per the _X_impl law and the D7 button-row law.
Output: button + wrapper + impl + smoke part.

**RECORDED DECISIONS (yolo):**
1. **Save gating on a game_over state = ALLOW** (carried from 07-04 Decision 3): the gate is ONLY the wizard-live isinstance check; a panel-ended-but-live wizard saves losslessly (game_over/end_state/final_time round-trip in game_state). No new refusal wording.
2. **Default Save filename = 'game.aamz'**, filter `'AA-match Checkpoint (*.aamz);;All Files (*)'`, extension auto-append `.aamz` (the 04-09 Decision-2 law; 04-RESEARCH OQ-5 precedent leaves the default to plan time).
3. **No success box** — the v1 game-tab Save precedent logged the line only (PA-__init__:786-787); the Game-tab feedback convention is "the info-box line IS the feedback".
</objective>

<execution_context>
@~/.config/opencode/get-shit-done/workflows/execute-plan.md
@~/.config/opencode/get-shit-done/templates/summary.md
@.planning/phases/05-game-status-tab-start-sequence/05-06-SUMMARY.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/phases/07-checkpoint-game-file-persistence/07-RESEARCH-import.md
@aamatch/game_window.py
@aamatch/status_text.py
@aamatch/gamestart.py
@smoke/smoke_16_tab.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: btn_save_game + _on_save_game wrapper + _save_game_to impl</name>
  <files>aamatch/game_window.py</files>
  <action>
1. **Button creation** (after btn_reset_grid's block, before `layout.addLayout(btn_row)`): `self.btn_save_game = QtWidgets.QPushButton('Save', self)` with tooltip `'Save the running game (PyMOL session + game state) to a checkpoint file.'` inserted via `btn_row.insertWidget(btn_row.count() - 1, self.btn_save_game)` — the stretch stays LAST (the 05-06 OQ-1 no-reflow law; the class docstring at game_window.py:130-133 explicitly reserves this slot). Connect: `self.btn_save_game.clicked.connect(self._on_save_game)` next to the other connects.

2. **Wrapper `_on_save_game`** — thin, gate-first, owns the ONLY modals (the 06-07/06-09 shape):
   - GATE FIRST, silent no-op BEFORE any box: `from pymol import cmd; from . import wizard as wizard_mod` (lazy, module-identity law); `if not isinstance(cmd.get_wizard(), wizard_mod.GameWizard): return` — covers pre-GO (wizard-free countdown P-1), no-game, and post-endgame-popped. NO game_over refusal (Recorded Decision 1).
   - Capture elapsed BEFORE the dialog: `elapsed = self._compute_elapsed()` (the tab's own <=1 s-coarse live read, game_window.py:344-355).
   - `path, _filter = QtWidgets.QFileDialog.getSaveFileName(self, 'Save AA-match Game', 'game.aamz', 'AA-match Checkpoint (*.aamz);;All Files (*)')`; `if not path: return` (cancel = safe no-op — nothing to undo, the tick's modal freeze already handled the clock).
   - Extension auto-append: `if not path.endswith('.aamz'): path += '.aamz'` (04-09 Decision-2 law).
   - `final = self._guard(lambda: self._save_game_to(path, elapsed))`; `if final is not None: self._log(status_text.game_saved_line(final))` — NO success box.

3. **Impl `_save_game_to(self, path, elapsed)`** — NON-MODAL, box-free (the smoke-99 law), a pure function of (path, elapsed):
   ```python
   def _save_game_to(self, path, elapsed):
       """Non-modal save impl (Phase 7 SCORE-08). The elapsed arrives
       PRE-DIALOG-captured (the v1 doctrine, PA-persistence.py:62-68):
       the impl writes a value that does not depend on whether a 1 Hz
       tick fired during the dialog. The clock itself needs NO manual
       stop/rebase bracket -- the tick's activeModalWidget branch
       freezes it under the dialog (human-verified 05-11)."""
       from . import gamestart
       data = gamestart.capture_checkpoint_snapshot(elapsed)
       return gamestart.save_checkpoint(path, data)
   ```
   NO engine/wizard private access from the tab (the grep law — game_window.py must keep zero `engine._game`/`engine._payload`/wizard-private hits).
  </action>
  <verify>python3.6 -m py_compile aamatch/*.py && python3.6 -m unittest discover -s tests -v</verify>
  <done>Button wired with the pinned label/tooltip/insert-before-stretch; wrapper gate-first with pre-dialog elapsed capture; impl box-free driving the 07-04 seams; success logs game_saved_line</done>
</task>

<task type="auto">
  <name>Task 2: SMOKE-16 new PART — save-button T1b drive</name>
  <files>smoke/smoke_16_tab.py</files>
  <action>
Add a new PART (next letter after the existing ones; keep ALL 67 existing checks green):
1. Fresh GameTab (the SMOKE-14 standalone recipe): `btn_save_game` exists with label exactly `'Save'`, non-empty tooltip; `btn_row` stretch is LAST after the insertion (indexOf(stretchItem) == count-1); the button is connected (has listeners — the smoke may assert via driving the wrapper's no-op path).
2. Gate-first no-op: with NO live GameWizard, `_on_save_game()` produces NOTHING (no box — offscreen smoke would hang on a modal per smoke-99; the no-op returns before any dialog; assert via a returned-None sentinel or by observing no state change — follow the existing wrapper-gate no-op assert pattern used for Hint/Reset in this smoke).
3. Elapsed-capture ordering: monkeypatch-free deterministic proof — set the live game's anchor via `engine._current_game().start_timer(time.time() - 75.0)` on a started game, call the impl directly `_save_game_to(tmp_path, 75.0)` and assert the written sidecar's `elapsed_at_save == 75.0` (deterministic-value proof: the impl is a pure function of its args). Use `checkpoint.read_checkpoint_zip` to verify the written zip (both members + elapsed).
4. Wrapper dialog path stays [HUMAN]-only (no getSaveFileName drive headless — document in a comment).
Restore block: prefix cleanup + `_last_start = None` + `_timer.stop()` (the Rule-2 law).
  </action>
  <verify>bash smoke/run_smoke.sh smoke/smoke_16_tab.py 240 — ALL parts PASS (existing 67 + new save part)</verify>
  <done>SMOKE-16 green with the new save part: labels/tooltip/stretch-law pinned, gate no-op proven, the pre-dialog elapsed value lands verbatim in the sidecar</done>
</task>

</tasks>

<verification>
1. `python3.6 -m py_compile aamatch/*.py` + full WSL suite green.
2. `bash smoke/run_smoke.sh smoke/smoke_16_tab.py 240` PASS (all parts).
3. SMOKE-14 regression (GameTab construction touched indirectly): `bash smoke/run_smoke.sh smoke/smoke_14_status_surface.py 240` PASS.
</verification>

<success_criteria>
- The Save button is live on the Game tab per the house wiring laws (gate-first wrapper, box-free impl, insert-before-stretch, D7 naming).
- The sidecar's elapsed is the pre-dialog captured value (deterministic), and the written .aamz verifies through the real gates.
</success_criteria>

<output>
After completion, create `.planning/phases/07-checkpoint-game-file-persistence/07-06-SUMMARY.md`
</output>
