---
phase: 04-qt-setup-window
plan: 05
type: execute
wave: 2
depends_on: ["04-01"]
files_modified: [aamatch/setup_window.py, aamatch/__init__.py, tests/test_package_skeleton.py, tests/test_wizard_source.py, smoke/smoke_08_starter.py, smoke/smoke_11_window.py]
autonomous: true

must_haves:
  truths:
    - "Plugins -> AA-match opens a modeless SetupWindow via open_window() (create-if-None, show/raise_/activateWindow, return the dialog) and the __init__.py keeps ZERO module-level imports (Gate A2)"
    - "Double menu fire reuses and raises the SAME dialog instance; the module-level _window singleton prevents GC; there is deliberately NO closeEvent override (default close hides; re-open is reuse-and-raise)"
    - "run_plugin_gui's recorded AST contract is deliberately REPLACED (03-05 precedent): the test now pins 'from . import setup_window' + 'return setup_window.open_window()'"
    - "SMOKE-08 is deliberately re-pointed to call gamestart.start_game() directly (the menu function now opens a Qt window); all 31 checks preserved"
    - "The Qt module is NEVER in PURE_MODULES and never imported by WSL tests (T0 AST source-gates only, zero sys.modules stubs)"
  artifacts:
    - path: "aamatch/setup_window.py"
      provides: "Qt-tier module: module-level pymol.Qt imports (legal+required here), _window singleton, open_window(), SetupWindow(QDialog) shell with the 7-button row created in spec order (unconnected) and minimum-width 420"
      min_lines: 80
    - path: "smoke/smoke_11_window.py"
      provides: "SMOKE-11 shell parts: import proof (always) + open_window construct/reuse/close-reopen drive (only if the 04-01 probe verdict is PASS)"
      min_lines: 40
  key_links:
    - from: "aamatch/__init__.py run_plugin_gui"
      to: "aamatch/setup_window.open_window"
      via: "lazy relative import inside the function body (Gate A2 + module-identity law)"
      pattern: "from \\. import setup_window"
    - from: "tests/test_package_skeleton.py"
      to: "aamatch/__init__.py"
      via: "replaced AST contract pinning the new seam"
      pattern: "setup_window"
    - from: "tests/test_wizard_source.py"
      to: "aamatch/setup_window.py"
      via: "SCANNED_MODULES grows one line ('setup_window.py') per the recorded growth protocol"
      pattern: "'setup_window\\.py'"
---

<objective>
Create the Qt-tier shell `aamatch/setup_window.py` (modeless lifecycle only -- the FORM fields come in plan 04-07) and rewire the Plugins-menu entry point, including the three deliberate test evolutions this rewire breaks (research P9/P10). The 04-01 probe verdict decides whether SMOKE-11 gets widget-construction parts (T1b) or stays import-only (T1a).

Purpose: this is the architectural keystone of SETUP-01 (modeless window; survives minimize/re-open). Every borrowed v1 pattern (PA-__init__:3-5 singleton, :141-153 open trio, no closeEvent) lands here once, correctly.
Output: shell module + rewired menu + updated contracts + SMOKE-11 shell.
</objective>

<execution_context>
@~/.config/opencode/get-shit-done/workflows/execute-plan.md
@~/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/04-qt-setup-window/04-RESEARCH-qt-window-architecture.md
@.planning/phases/03-wizard-gameplay-loop/03-05-SUMMARY.md
@.planning/STATE.md
@aamatch/__init__.py
@tests/test_package_skeleton.py
@tests/test_wizard_source.py
@smoke/smoke_08_starter.py
</context>

<decisions>
- Probe dependence: read `.planning/phases/04-qt-setup-window/04-01-SUMMARY.md` line "Probe verdict: ...". PASS -> SMOKE-11 includes widget parts (T1b). FAIL -> SMOKE-11 is import-only (T1a) and widget verification stays with the [HUMAN] checkpoints; note the degradation in the SUMMARY.
- Shell scope: window lifecycle + empty dialog + the 7-button row (created in spec order with tooltips, NOT connected -- each handler plan connects its own button). No form fields here (04-07).
- Module-level `from pymol import cmd` is NOT added in this plan (only 04-11's cleanup handler needs cmd; added there -- Decision 17).
</decisions>

<tasks>

<task type="auto">
  <name>Task 1: setup_window.py shell -- singleton, open_window, minimal dialog + button row</name>
  <files>aamatch/setup_window.py</files>
  <action>
Create `aamatch/setup_window.py`:

1. Module docstring (layer declaration, wizard.py:3-6 shape, NO banned tokens get_model/matrix_reset/get_object_ttt anywhere): "aamatch.setup_window -- Qt-tier modeless setup window (Phase 4). Layer: Qt TIER (NEVER in PURE_MODULES; never imported by WSL tests -- covered by AST source gates only). Module-level pymol.Qt imports are legal and REQUIRED here (the class definition needs QtWidgets at class-creation time). Sibling aamatch modules are imported LAZILY inside handler methods (module-identity law, wizard.py:22-29 contract). The window is MODELESS: show()/raise_()/activateWindow(), NEVER .exec_() on the main dialog (modal children only: QFileDialog/QMessageBox). The module-level _window singleton prevents GC and makes re-open reuse-and-raise; there is deliberately NO closeEvent override (default close hides; the singleton keeps the object alive -- SETUP-01's survive-minimize/re-open mechanism)."
2. `from pymol.Qt import QtWidgets, QtCore, QtGui` at module level (never `from PyQt5 import ...` -- banned repo-wide; code against the wrapper shim).
3. `_window = None` at module scope (the GC-prevention singleton; PA-__init__:3-5 receipt).
4. `def open_window():` -- global _window; if None construct SetupWindow(); then `_window.show(); _window.raise_(); _window.activateWindow(); return _window` (return = assertion handle, 03-05 pattern; PA-__init__:141-153 trio verbatim).
5. `class SetupWindow(QtWidgets.QDialog):` -- `__init__(self)` with NO parent (modeless requirement), `super().__init__()`, `setWindowTitle('AA-match Setup')`, `setMinimumWidth(420)` (PA-__init__ precedent).
6. Layout skeleton: a top QVBoxLayout holding (a) a form-area placeholder QWidget with its own QVBoxLayout (form fields added in 04-07), (b) a stretch, (c) the button row: QHBoxLayout with the 7 QPushButton instances created IN SPEC ORDER (spec.md:21) with object attributes self.btn_reset, self.btn_randomize, self.btn_save_setup, self.btn_load_setup, self.btn_generate_export, self.btn_cleanup, self.btn_start; labels exactly 'Reset', 'Randomize', 'Save Setup', 'Load Setup', 'Generate and export', 'Cleanup model', 'Start'. Tooltips from spec meanings (spec.md:22-28), e.g. btn_reset.setToolTip('Restore the default settings.') etc. Buttons are created but NOT connected (each handler plan connects its own; no dead stub handlers).
7. NO closeEvent override anywhere (add a comment saying exactly why, citing SETUP-01 + the v1 grep-zero receipt).
8. NO other methods. 3.6 syntax, %-format if any formatting, no logging/print.
Commit: `feat(04-05): setup_window Qt-tier shell (modeless singleton, open_window, 7-button row)`.
  </action>
  <verify>T0: `python3.6 -m py_compile aamatch/setup_window.py` (Gate D scans it automatically); grep gate: no `exec_(` on SetupWindow, no `from PyQt5`, no closeEvent; `python3.6 -m unittest discover -s tests -v` still green (nothing wired yet, nothing broken).</verify>
  <done>Shell module exists with the exact borrowed lifecycle pattern and the spec-ordered button row.</done>
</task>

<task type="auto">
  <name>Task 2: Rewire __init__ + replace the AST contract + grow SCANNED_MODULES + re-point SMOKE-08</name>
  <files>aamatch/__init__.py, tests/test_package_skeleton.py, tests/test_wizard_source.py, smoke/smoke_08_starter.py</files>
  <action>
1. `aamatch/__init__.py`: ONLY the `run_plugin_gui` body + docstring change (metadata block first lines stay byte-identical; __version__ stays 0.1.0; __init_plugin__ untouched):
   ```python
   def run_plugin_gui():
       """Open the Phase-4 modeless Qt setup window (Plugins -> AA-match).

       Returns the SetupWindow dialog (assertion handle, 03-05 pattern).
       The import is lazy (Gate A2: zero module-level imports here). The
       window delegates Start to gamestart.start_game -- the SAME seam
       this function used in Phase 3. ValueError-family refusals are
       caught per button by the window's _guard; unexpected exceptions
       propagate fail-closed."""
       from . import setup_window
       return setup_window.open_window()
   ```
2. `tests/test_package_skeleton.py`: REPLACE the test at :110-154 (`test_run_plugin_gui_launches_gamestart_lazily`) with `test_run_plugin_gui_opens_setup_window_lazily` -- same AST mechanics, new contract: a lazy relative ImportFrom with alias name == 'setup_window' INSIDE run_plugin_gui's body, and a Return whose value is a Call with func.attr == 'open_window' on Name 'setup_window'; keep the no-print assert; update the docstring (03-05 precedent: deliberate contract replacement, 03-05-SUMMARY.md:103-109 -- the seam MOVED, the test follows the seam). Keep `test_entry_points_exist` unchanged.
3. `tests/test_wizard_source.py`: append `'setup_window.py'` to SCANNED_MODULES (:48-51, one line + comment "04-05: Qt setup window (the new cmd/Qt-tier UI module)").
4. `smoke/smoke_08_starter.py`: re-point to the seam (P10): replace every `aamatch.run_plugin_gui()` call with `aamatch.gamestart.start_game()` (import gamestart at the top of the script alongside aamatch); update the header docstring (the Plugins-menu function now opens the Phase-4 Qt window; the smoke proves the SEAM directly); ALL 31 checks and their asserts unchanged (restart paths, instance markers, msm law, determinism).
Run the full WSL suite + the two headless smokes.
Commit: `feat(04-05): rewire run_plugin_gui to the setup window; replace skeleton AST contract; grow SCANNED_MODULES; re-point SMOKE-08`.
  </action>
  <verify>T0: full WSL suite green (the replaced contract test passes; Gate A2 untouched and green; SCANNED_MODULES growth green). T1a: `bash smoke/run_smoke.sh smoke/smoke_08_starter.py 240` prints `=== SMOKE-08 PASS ===` via the direct seam calls.</verify>
  <done>Menu entry re-pointed; every recorded test contract updated deliberately, none worked around.</done>
</task>

<task type="auto">
  <name>Task 3: SMOKE-11 shell parts + full gates</name>
  <files>smoke/smoke_11_window.py</files>
  <action>
Write `smoke/smoke_11_window.py` (verdict marker `=== SMOKE-11 PASS ===`; run via `bash smoke/run_smoke.sh smoke/smoke_11_window.py 120`). Parts:

- PART A (ALWAYS, T1a): `import aamatch.setup_window` succeeds headless (the module-level Qt binding loads -- PyQt5 QtCore/QtGui/QtWidgets as a unit, pymol-src/Qt/__init__.py:28); `callable(aamatch.setup_window.open_window)`; `_window` starts None; source sanity: the module text contains 'from pymol.Qt import' and does NOT contain 'from PyQt5' (read via the repo-root anchor).
- PART B (ONLY if 04-01 SUMMARY verdict is PASS -- check the file; if the file is missing or verdict is FAIL, print 'PART B SKIPPED (probe verdict)' and continue): `dlg = aamatch.setup_window.open_window()`; assert it is a QtWidgets.QDialog instance; assert `aamatch.setup_window.open_window() is dlg` (reuse, not duplicate); `dlg.close()`; `aamatch.setup_window.open_window() is dlg` again (hide + re-open = same instance -- SETUP-01 mechanism); assert the 7 buttons exist with the spec order labels (btn_reset .. btn_start, `text()` checks); `QtWidgets.QApplication.instance().processEvents()`; NO `.exec_()` anywhere in the script (P5). Finish with `dlg.close()`.
- PART C (T0-in-smoke, always): re-run the T0 gates' verdict inside PyMOL's interpreter for the record: import aamatch; assert Gate A2 shape (aamatch/__init__ source has no module-level `from . import` outside function bodies -- simple line-based sanity echoing the WSL gate).
Print `=== SMOKE-11 PASS ===`.
Commit: `test(04-05): SMOKE-11 window shell proof (import + optional widget-construction parts)`.
  </action>
  <verify>`bash smoke/run_smoke.sh smoke/smoke_11_window.py 120` prints `=== SMOKE-11 PASS ===` (T1a always; T1b parts per probe verdict). Full WSL suite green.</verify>
  <done>The window shell is proven in real headless PyMOL to the extent the probe allows; the rest is explicitly deferred to [HUMAN].</done>
</task>

</tasks>

<verification>
- T0: py_compile setup_window.py; full WSL suite green (replaced skeleton contract + grown SCANNED_MODULES + untouched Gate A2).
- T1a: SMOKE-08 PASS (re-pointed), SMOKE-11 PART A PASS.
- T1b (probe-PASS only): SMOKE-11 PART B PASS (construct, reuse, close/reopen same instance).
- Metadata block of aamatch/__init__.py byte-identical; __version__ 0.1.0.
</verification>

<success_criteria>
SETUP-01's mechanics exist and are proven to the headless limit: modeless open trio, GC-safe singleton, reuse-and-raise, no closeEvent, lazy rewiring with zero module-level imports, and every broken recorded test deliberately replaced. The window opens EMPTY (form fields are 04-07) -- that intermediate state is fine mid-phase.
</success_criteria>

<output>
After completion, create `.planning/phases/04-qt-setup-window/04-05-SUMMARY.md` (frontmatter: affects [04-07 form, 04-09..04-13 handlers], subsystem qt-window; note the probe verdict used and whether PART B ran).
</output>
