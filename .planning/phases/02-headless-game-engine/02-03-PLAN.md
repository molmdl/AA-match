---
phase: 02-headless-game-engine
plan: 03
type: tdd
wave: 2
depends_on: ["02-01"]
files_modified:
  - aamatch/manifest.py
  - aamatch/persistence.py
  - tests/test_manifest.py
autonomous: true

must_haves:
  truths:
    - "A versioned manifest container (kind='manifest') parses and validates: every entry carries file/format/sha256/counts/flags, and malformed entries are refused with clear FormatError messages"
    - "Entries enumerate deterministically (sorted by set_id, entry_id) and the largest-by-heavy-atom-count entry is derivable without special-cased fields"
    - "'manifest' is a legal container kind; all other existing kinds and refusal messages unchanged"
  artifacts:
    - path: "aamatch/manifest.py"
      provides: "MANIFEST_VERSION, parse_manifest_dict, enumerate_entries, largest_entry"
      min_lines: 80
    - path: "tests/test_manifest.py"
      provides: "Validation + enumeration + refusal tests"
      min_lines: 60
  key_links:
    - from: "aamatch/manifest.py"
      to: "aamatch/persistence.py"
      via: "check_container(container, 'manifest')"
      pattern: "check_container"
    - from: "aamatch/persistence.py"
      to: "KINDS"
      via: "additive 'manifest' kind"
      pattern: "KINDS = \\('setup', 'level_spec', 'game', 'checkpoint', 'manifest'\\)"
---

<objective>
Create the pure manifest module: load/validate/enumerate the bundled-data manifest (aamatch/data/MANIFEST.json lands in plan 02-04). Adds 'manifest' to the persistence KINDS (additive — refuse-newer semantics already handled by the container gate).

Purpose: GEN-02 supply proof + PITFALL 11.3 anti-drift: counts recorded in the manifest are the contract every-manifest-id smoke (SMOKE-02, plan 02-04) asserts. The generator (02-08) consumes manifest entries as its candidate list.
Output: aamatch/manifest.py (pure) + KINDS extension + tests.
</objective>

<execution_context>
@~/.config/opencode/get-shit-done/workflows/execute-plan.md
@~/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/02-headless-game-engine/02-RESEARCH-materialization.md  # §3 manifest format + per-entry schema (verbatim field list)
@aamatch/persistence.py                                                   # container gate + FormatError style
@aamatch/paths.py                                                         # package_data_path (referenced in docstrings only; resolution is cmd-tier)
@.planning/phases/02-headless-game-engine/02-01-SUMMARY.md                # approved flag semantics (metal list, halogen C-F exclusion)
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: persistence KINDS gains 'manifest' (additive)</name>
  <files>aamatch/persistence.py</files>
  <action>
RED: add a test to tests/test_manifest.py (create file): make_container('manifest', {...}) succeeds; check_container({...kind='manifest'...}, 'manifest') passes; an unknown kind still refuses with the canonical message.

GREEN: in aamatch/persistence.py change KINDS to ('setup', 'level_spec', 'game', 'checkpoint', 'manifest') and add one docstring line ("'manifest': bundled demo-data manifest (Phase 2); see aamatch/manifest.py"). NOTHING else changes — refusal messages are built from KINDS dynamically. If any Phase-1 test pins the exact KINDS tuple, update that assertion minimally and note it as a deviation in the SUMMARY.

Commit: `feat(02-03): add manifest container kind`.
  </action>
  <verify>python3.6 -m unittest tests.test_manifest tests.test_persistence -v  (both green)</verify>
  <done>'manifest' is a first-class kind; all Phase-1 persistence tests still green</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: manifest.py — parse, validate, enumerate (RED -> GREEN)</name>
  <files>aamatch/manifest.py, tests/test_manifest.py</files>
  <action>
RED first. Tests cover, using a fixture dict built in the test (copy the exact entry schema from 02-RESEARCH-materialization.md §3.2 — entry_id/file/format/sha256/protonation/atom_count/heavy_atom_count/bond_count/bond_order_counts/formal_charge_sum/states_expected/metal_present/halogen_present/size_class):

1. parse_manifest_dict on a valid container (manifest_version=1, one set with 2 entries) returns the payload unchanged (passthrough, P9-style, input never mutated).
2. Container gate: wrong magic / kind='level_spec' / newer container version -> persistence.FormatError with the CANONICAL messages from check_container (reuse it — do not duplicate).
3. manifest_version missing or > 1 -> FormatError "unsupported manifest version ... (expected <= 1)".
4. Entry validation — each refusal names the offending entry: missing required key; format not in ('sdf','mol2'); sha256 not a 64-char lowercase hex string; atom_count/bond_count/heavy_atom_count/states_expected not real ints (bool refused — reuse the _is_int pattern from level_spec.py); bond_order_counts not a dict of digit-string -> positive-int; formal_charge_sum not an int; size_class not in ('small','medium','large'); metal_present/halogen_present not bools; file not a forward-slash package-relative path (refuse backslashes and absolute paths — PITFALL 2: never store Windows or absolute paths in the manifest).
5. enumerate_entries(payload) -> flat list of dicts, each = entry fields + 'set_id', sorted by (set_id, entry_id) — deterministic across calls.
6. largest_entry(entries) -> the entry with max heavy_atom_count (ties -> first in sorted order). This is the perf-smoke target selector (no special-cased field).
7. Purity sanity is covered by the gate suite once registered (Task 3).

GREEN: implement aamatch/manifest.py. Module docstring states: PURE; imports persistence only; paths/file loading stay cmd-tier; manifest_version follows refuse-newer/accept-older like FORMAT_VERSION; counts are the contract (PITFALL 11.3). Validation helpers mirror level_spec.py's style (plain functions, FormatError with entry context).

Commit: `feat(02-03): manifest parse/validate/enumerate (TDD)`.
  </action>
  <verify>python3.6 -m unittest tests.test_manifest -v  (all pass)</verify>
  <done>Valid manifests parse passthrough; every malformed variant refused with a message naming the entry and field; enumeration + largest_entry deterministic</done>
</task>

<task type="auto">
  <name>Task 3: Register manifest in PURE_MODULES + full suite</name>
  <files>tests/test_purity.py</files>
  <action>
Append 'manifest' to PURE_MODULES in tests/test_purity.py (same pattern as plan 02-02). No ALLOWED_STDLIB changes (manifest imports json? NO — parsing is dict-level; file reading stays with the caller/cmd tier exactly like level_spec. Imports: `from .persistence import FormatError, check_container` only).
  </action>
  <verify>python3.6 -m unittest discover -s tests -v  (FULL suite green; Gates A/B now scan manifest)</verify>
  <done>PURE_MODULES = 8 modules; full suite green</done>
</task>

</tasks>

<verification>
python3.6 -m py_compile aamatch/*.py && python3.6 -m unittest discover -s tests -v
</verification>

<success_criteria>
- manifest.py pure-validated: schema, deterministic enumeration, largest_entry selector
- 'manifest' kind accepted; all Phase-1 container behavior intact
- Gates A/B/D green including the new module
</success_criteria>

<output>
After completion, create `.planning/phases/02-headless-game-engine/02-03-SUMMARY.md`
</output>
