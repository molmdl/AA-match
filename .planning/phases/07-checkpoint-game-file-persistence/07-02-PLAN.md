---
phase: 07-checkpoint-game-file-persistence
plan: 02
type: execute
wave: 1
depends_on: []
files_modified: [aamatch/checkpoint.py, tests/test_checkpoint.py, tests/test_purity.py]
autonomous: true

must_haves:
  truths:
    - "A checkpoint container (kind='checkpoint') built by build_checkpoint_data parses back through parse_checkpoint_data with ALL four version gates applied (container header, checkpoint_format_version refuse-newer, embedded game five-gate chain replayed verbatim incl. the detector exact-match gate, GameState light validator)"
    - "reconcile_registry keeps a sidecar entry ONLY when (object, sorted ids) verify exactly against the observed atom sweep — a non-verifying entry is DROPPED, never registered (never-ghost-entry law)"
    - "The current level must reconcile COMPLETELY (every molecule's ligand + all payload-expected slots) or the resume is refused with a message naming the first missing piece"
    - "The .aamz zip writes atomically (temp + os.replace) and read_checkpoint_zip refuses missing members / corrupt zips BEFORE any extraction hand-off"
  artifacts:
    - path: "aamatch/checkpoint.py"
      provides: "Pure checkpoint module: schema constants, build_checkpoint_data, parse_checkpoint_data (4-gate chain), reconcile_registry, write_checkpoint_zip/read_checkpoint_zip"
      exports: ["build_checkpoint_data", "parse_checkpoint_data", "reconcile_registry", "write_checkpoint_zip", "read_checkpoint_zip", "CHECKPOINT_VERSION", "PSE_MEMBER", "SIDECAR_MEMBER"]
      min_lines: 200
    - path: "tests/test_checkpoint.py"
      provides: "WSL unit battery: round-trip, every refusal message pinned verbatim, reconcile semantics, zip I/O"
      min_lines: 150
    - path: "tests/test_purity.py"
      provides: "checkpoint registered in PURE_MODULES (gated)"
      contains: "'checkpoint'"
  key_links:
    - from: "aamatch/checkpoint.py parse_checkpoint_data"
      to: "aamatch/game_file.parse_game_data"
      via: "the embedded 'game' block is replayed through the FULL five-gate chain (wrap: make_container('game', data['game'])) — detector_version EXACT-match refusals apply to checkpoints"
      pattern: "parse_game_data"
    - from: "aamatch/checkpoint.py reconcile_registry"
      to: "observed (object, sorted ids) sweep"
      via: "entry kept iff object in observed AND sorted(observed[object]) == ids; lists rebuilt as tuples matching placement.materialize registry shape"
      pattern: "reconcile_registry"
---

<objective>
Create `aamatch/checkpoint.py` — the pure checkpoint-schema module (stdlib-only, python3.6, no dataclasses) — owning the sidecar schema, the four-gate parse chain, sentinel-first reconcile_registry, and the `.aamz` zip I/O, with a full WSL test battery.

Purpose: this is the data foundation every later Phase-7 plan consumes (save seams 07-04, resume 07-09, dispatch 07-10). The container reuses Phase-1's versioned container with the already-reserved kind `checkpoint` (`persistence.py:37`); `zipfile` is already whitelisted in `ALLOWED_STDLIB`. Design is fixed by 07-RESEARCH-state.md §2/§4/§5/§7 (sidecar schema, reconstruction gates, version-gate chain, zip layout).
Output: new pure module + tests/test_checkpoint.py + PURE_MODULES registration.
</objective>

<execution_context>
@~/.config/opencode/get-shit-done/workflows/execute-plan.md
@~/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/phases/07-checkpoint-game-file-persistence/07-RESEARCH-state.md
@.planning/phases/07-checkpoint-game-file-persistence/07-RESEARCH-pse.md
@aamatch/persistence.py
@aamatch/game_file.py
@aamatch/game_state.py
@aamatch/placement.py
@tests/test_purity.py
@tests/test_game_file.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: RED — tests/test_checkpoint.py (failing)</name>
  <files>tests/test_checkpoint.py</files>
  <action>
Write the test battery FIRST (module does not exist yet — import fails = RED). Reuse test_game_file.py's payload-fixture pattern for a chain-valid minimal payload (the five-gate chain must pass on the embedded game block). Cover, with EVERY refusal message pinned verbatim:

1. **Round-trip:** build_checkpoint_data(make_game_data output, GameState().to_dict(), elapsed, registry, wizard_books, candidates) -> parse_checkpoint_data(make_container('checkpoint', data)) returns setup/payload/ligand_texts/game_state/elapsed_at_save/registry/wizard/candidates with payload IDENTICAL (embed-don't-regenerate law) and game_state lossless.
2. **Gate 2 — checkpoint_format_version** (the game_file.py:135-143 pattern): missing/invalid -> `checkpoint data is missing or has an invalid 'checkpoint_format_version'`; newer -> `unsupported checkpoint format version %d (expected <= %d). Please update AA-match.`
3. **Gate 3 — embedded game gates replayed VERBATIM:** missing 'game' -> `checkpoint data is missing 'game'`; a stale detector_version ('det-0' re-stamp) inside the embedded level_spec raises the EXACT level_spec message (`unsupported detector_version ... stale or newer game spec - regenerate it with a current AA-match generator`); a wrong embedded game_format_version raises the exact game_file message.
4. **Gate 4 — GameState light validator:** missing required base key -> `checkpoint data is missing 'game_state.<key>'`; len(molecule_scores) != len(score_per_molecule) -> refusal naming the one-record invariant; an `L<i>M<j>` key out of the embedded payload's level/molecule bounds -> refusal; skip_count/giveup_count non-int (bool included) -> refusal; elapsed_at_save negative or non-float -> refusal naming the rewind-the-clock cause (None allowed).
5. **reconcile_registry semantics:** keep entry iff object in observed AND sorted(observed[object]) == ids; a ghost entry (object absent) is DROPPED and reported, never registered; an id-mismatch entry is DROPPED and reported; observed objects with no sidecar entry reported as missing-from-sidecar; rebuilt registry restores TUPLE shapes `(name, ids_tuple)` for ligand/slots (sidecar JSON carries lists) and carries `pre_game_names: []`; completeness gate — a missing ligand or any missing slot of the current level raises FormatError naming the first missing piece (pin the wording you implement, e.g. `checkpoint scene is missing game object %r (molecule %s, slot %s) -- the saved session no longer matches this checkpoint`).
6. **Zip I/O:** write_checkpoint_zip then read_checkpoint_zip round-trip returns (pse_path, parsed data) with the extracted pse bytes byte-identical to the input; missing `state.json` -> `not an AA-match archive (missing state.json)`; missing `game.pse` -> `archive missing game.pse (cannot reconstruct)`; corrupt/non-zip bytes -> FormatError (not BadZipFile); the sidecar member is the FULL container JSON (magic/kind='checkpoint'/version/data) so check_container gate 1 applies on read.
7. **elapsed/rebase integration:** parse output elapsed_at_save feeds GameState.rebase_timer(now, elapsed) -> anchor == now - elapsed (game_state import, pure).
  </action>
  <verify>python3.6 -m unittest tests.test_checkpoint -v — collection fails on ImportError (RED confirmed: aamatch/checkpoint.py absent)</verify>
  <done>test_checkpoint.py exists with every refusal message pinned verbatim; RED state confirmed (ImportError)</done>
</task>

<task type="auto">
  <name>Task 2: GREEN — aamatch/checkpoint.py implementation</name>
  <files>aamatch/checkpoint.py</files>
  <action>
Create the pure module. Imports: stdlib ONLY from ALLOWED_STDLIB (`json`, `os`, `tempfile`, `zipfile`) + pure siblings (`persistence`, `game_file`, `game_state` — dependency direction B8). NO dataclasses (3.7+), NO pymol/Qt/numpy at module level OR in any function body (the enforced purity gate).

Constants: `CHECKPOINT_VERSION = 1`, `PSE_MEMBER = 'game.pse'`, `SIDECAR_MEMBER = 'state.json'`.

**build_checkpoint_data(game_data, game_state, elapsed_at_save, registry, wizard_books=None, candidates=None, created_at='')** -> data dict per 07-RESEARCH-state.md §2:
- `checkpoint_format_version: CHECKPOINT_VERSION`, `created_at`, `generator: 'AA-match'`
- `game`: game_data VERBATIM (the full make_game_data output — setup/seed/level_spec/ligand_files/game_format_version)
- `candidates`: passthrough (None for demo games; rows for uploads — the Restart-after-restore replay input)
- `game_state`: VERBATIM (the 02-10 wrap-don't-reshape law — never reshape to_dict output)
- `elapsed_at_save`: float or None (the re-anchor authority; caller clamps >= 0)
- `registry`: CURRENT LEVEL ONLY — convert materialize-registry tuples to JSON lists: ligand `(name, ids)` -> `[name, [ids...]]`, slots `{slot_id: (name, ids)}` -> `{slot_id: [name, [ids...]]}`; DROP `pre_game_names` (zero functional consumers — grep-verified; rebuilt as [] on restore)
- `wizard`: wizard_books verbatim (repair block for the rebuild path)

**parse_checkpoint_data(container)** — the four-gate chain IN ORDER, every refusal BEFORE any caller scene mutation:
1. `container = persistence.check_container(container, 'checkpoint')` (gate 1 free).
2. checkpoint_format_version gate (Task-1 wordings; refuse-newer / accept-older with .get defaults).
3. `game` block: missing -> refuse; then `parsed = game_file.parse_game_data(persistence.make_container('game', data['game']))` — the full five-gate chain replayed VERBATIM (zero new version logic; detector exact-match applies).
4. game_state light validator (Task-1 rules; fail-closed FormatError naming the key; bounds derived from `parsed['payload']['levels']`).
5. registry/wizard blocks: additive .get defaults; registry `level_index` must equal game_state['current_level_index'] (consistency refusal); wizard must be a dict when present.
Return `{'setup', 'payload', 'ligand_texts', 'game_state', 'elapsed_at_save', 'registry', 'wizard', 'candidates'}`.

**reconcile_registry(sidecar_registry, observed, payload, level_index)** -> (rebuilt_registry, report):
- observed = {object_name: sorted_ids} from the loaded scene (caller sweeps).
- Match rule per molecule row: ligand/slot entry KEPT iff `object in observed` AND `sorted(observed[object]) == ids` (ids are the identity contract — placement.py:51-53, SMOKE-17 proved 360/360 stable).
- NEVER-GHOST-ENTRY LAW: a non-verifying entry is DROPPED, never registered (v1 registry.py missing_from_pse semantics); reported in `report['dropped']`; observed objects with no sidecar entry -> `report['missing_from_sidecar']`.
- Rebuilt registry shape EXACTLY matches placement.materialize: `{'level_index': int, 'pre_game_names': [], 'molecules': [{'molecule_id', 'offset', 'ligand': (name, tuple(ids)), 'slots': {slot_id: (name, tuple(ids))}}]}` (lists -> tuples; JSON-safe round trip).
- COMPLETENESS GATE: expected shape derived pure from `payload['levels'][level_index]['molecules']` (molecule_ids + slot ids); every molecule's ligand + ALL slots must be kept -> else FormatError naming the first missing piece. Future levels need NOTHING (only one level ever materialized — placement.py:64-66; advance_level re-materializes).

**write_checkpoint_zip(zip_path, data, tmp_pse_path)**: sidecar bytes = `json.dumps(persistence.make_container('checkpoint', data), indent=2, sort_keys=True, allow_nan=False).encode('utf-8')` (byte-stable, the write_json_atomic discipline); write to a temp zip in the target dir (tempfile.mkstemp) via `zipfile.ZipFile(fd, 'w', zipfile.ZIP_DEFLATED)`: `zf.write(tmp_pse_path, arcname=PSE_MEMBER)` + `zf.writestr(SIDECAR_MEMBER, sidecar_bytes)`; `os.replace(tmp, zip_path)`; temp removed on any failure (the write_json_atomic pattern, persistence.py:91-113).

**read_checkpoint_zip(zip_path)** -> (tmp_pse_path, data): open zip (BadZipFile -> FormatError `not an AA-match archive (unreadable zip: %s)`); namelist gate: missing state.json -> `not an AA-match archive (missing state.json)`; missing game.pse -> `archive missing game.pse (cannot reconstruct)`; parse the sidecar through parse_checkpoint_data BEFORE extraction hands off (the v1 read_bcmz order — parse gates precede extraction); extract game.pse into `tempfile.mkdtemp()`; return (extracted_path, data). Caller owns the rmtree.

Docstring: module-level statement of the schema (07-RESEARCH-state.md §2), the two-gates-law extension (a THIRD artifact with its own refuse-newer constant, never conflated with FORMAT_VERSION/GAME_VERSION/DETECTOR_VERSION), and the embed-don't-regenerate law.
  </action>
  <verify>python3.6 -m unittest tests.test_checkpoint -v — ALL green (the Task-1 battery passes)</verify>
  <done>checkpoint.py implements the full schema/gates/reconcile/zip surface; every Task-1 test green; python3.6-compatible (no dataclasses, no f-strings required by house style — %-formatting)</done>
</task>

<task type="auto">
  <name>Task 3: PURE_MODULES registration + full-suite green</name>
  <files>tests/test_purity.py</files>
  <action>
Add `'checkpoint'` to PURE_MODULES in tests/test_purity.py (the 01-08 law: new pure modules must be registered to be gated) with a one-line provenance comment in the established style (`# Phase 7 (07-02): checkpoint ...` matching the existing per-phase comments). Run the FULL suite — the AST scan must find ZERO forbidden imports in checkpoint.py (all scopes), the clean-subprocess import must pass, and the negative-control pin must stay at exactly 2 findings.
  </action>
  <verify>python3.6 -m unittest discover -s tests -v — full suite green including test_purity (checkpoint gated, negative control unchanged)</verify>
  <done>PURE_MODULES includes 'checkpoint'; full WSL suite green; purity gate actively enforced on the new module</done>
</task>

</tasks>

<verification>
1. `python3.6 -m py_compile aamatch/*.py` green.
2. `python3.6 -m unittest discover -s tests -v` — full suite green; tests/test_checkpoint.py covers round-trip + every refusal verbatim + reconcile semantics + zip I/O.
3. Grep-check: `grep -n "dataclass\\|import pymol\\|from pymol" aamatch/checkpoint.py` -> zero hits (Gate C is documented-only; the AST gate is the enforcement).
</verification>

<success_criteria>
- checkpoint.py is the single pure home for the checkpoint schema, four-gate parse chain, sentinel-first reconcile, and .aamz zip I/O.
- The embedded game block replays game_file.parse_game_data verbatim — a stale detector stamp refuses a checkpoint exactly as it refuses a game file.
- Registry round-trips tuple-shaped; never-ghost-entry + current-level completeness laws enforced and pinned by tests.
</success_criteria>

<output>
After completion, create `.planning/phases/07-checkpoint-game-file-persistence/07-02-SUMMARY.md`
</output>
