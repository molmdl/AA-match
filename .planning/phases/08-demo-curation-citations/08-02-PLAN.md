---
phase: 08-demo-curation-citations
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - aamatch/setup_form.py
  - aamatch/setup_window.py
  - tests/test_setup_form.py
  - smoke/smoke_11_window.py
autonomous: true
must_haves:
  truths:
    - "The demo dropdown renders sets grouped by tier (Easy → Hard → Challenge → Very challenging display order) with QComboBox separators BETWEEN groups, and only between groups (single-group manifests produce zero separators — today's dev-only manifest is visually byte-identical to before)"
    - "Separator rows are non-selectable and carry NO itemData — currentData()/known-ids can never yield the string 'None' as a set id"
    - "A stale saved demo_set_id falls back to the first SELECTABLE (data) row, never onto a separator"
    - "setup_form.manifest_sets stays byte-identical (existing pinned tests untouched and passing)"
  artifacts:
    - path: "aamatch/setup_form.py"
      provides: "TIER_ORDER + TIER_LABELS + manifest_sets_grouped (pure, PURE_MODULES member)"
      contains: "manifest_sets_grouped"
    - path: "aamatch/setup_window.py"
      provides: "_known_demo_set_ids helper + grouped separator population + first-selectable-row fallback"
      contains: "_known_demo_set_ids"
    - path: "tests/test_setup_form.py"
      provides: "TestManifestSetsGrouped battery (order, within-group sort, Other-group, empty)"
    - path: "smoke/smoke_11_window.py"
      provides: "T1b offscreen separator/data-row assertions + mirror-loop fix"
  key_links:
    - from: "aamatch/setup_window.py _populate_demo_sets"
      to: "aamatch/setup_form.manifest_sets_grouped"
      via: "groups drive addItem rows + insertSeparator between groups"
      pattern: "manifest_sets_grouped"
    - from: "aamatch/setup_window.py (export + start known_ids sites, ~927/1003)"
      to: "_known_demo_set_ids"
      via: "both itemData loops replaced by the None-skipping helper"
      pattern: "_known_demo_set_ids\\(\\)"
    - from: "smoke/smoke_11_window.py"
      to: "setup_window._known_demo_set_ids"
      via: "part-G mirror loop uses the same helper (no independent str(itemData) loop)"
      pattern: "_known_demo_set_ids"
---

<objective>
Group the demo dropdown by tier (ROADMAP criterion 2: "the setup dropdown lists all curated sets grouped by tier") — pure grouping in the PURE layer, minimal Qt diff, and the three separator hazards the mechanics research identified (known-ids 'None' pollution, setCurrentIndex(0) fallback landing on a separator, smoke_11 mirror loops).

Purpose: with 10 sets shipping in 08-05..08-07, a flat set_id-sorted dropdown becomes unreadable; grouping must land BEFORE content so the curated sets appear grouped from day one.
Output: grouped dropdown proven offscreen (T1b) with curated-data-relative teeth.
</objective>

<execution_context>
@~/.config/opencode/get-shit-done/workflows/execute-plan.md
@~/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/08-demo-curation-citations/08-RESEARCH-mechanics.md
@aamatch/setup_form.py
@aamatch/setup_window.py
@tests/test_setup_form.py
@smoke/smoke_11_window.py

Tier vocabulary (sourcing research handoff #5, to be ratified at the 08-04 GATE): manifest tokens exactly `easy | hard | challenge | very_challenging`; display order Easy → Hard → Challenge → Very challenging. The dev set (demo-dev-1, tier 'easy') stays listed unless the GATE decides otherwise.
</context>

<tasks>

<task type="auto">
  <name>Task 1: pure grouping — TIER_ORDER/TIER_LABELS/manifest_sets_grouped (TDD)</name>
  <files>aamatch/setup_form.py, tests/test_setup_form.py</files>
  <action>
RED first: add `tests/test_setup_form.py::TestManifestSetsGrouped` covering: (a) multi-tier payload → groups in TIER_ORDER sequence with display labels 'Easy'/'Hard'/'Challenge'/'Very challenging'; (b) within-group rows sorted by set_id (reuse the manifest_sets sort); (c) rows with tier missing/'' or not in the vocabulary collapse into ONE final ('Other', rows) group; (d) empty groups omitted entirely (a payload with only easy sets yields exactly [('Easy', rows)] — zero separators at runtime today); (e) empty/{} payload → []; (f) non-dict payload → []. Run — must FAIL (function absent).

GREEN: in `aamatch/setup_form.py` add:
```python
TIER_ORDER = ('easy', 'hard', 'challenge', 'very_challenging')
TIER_LABELS = {'easy': 'Easy', 'hard': 'Hard',
               'challenge': 'Challenge',
               'very_challenging': 'Very challenging'}

def manifest_sets_grouped(payload):
    """Return dropdown GROUPS [(display_label, rows)] for a manifest payload.

    rows are the manifest_sets tuples [(set_id, title, tier)] (already
    sorted by set_id); groups appear in TIER_ORDER sequence, empty groups
    omitted; tiers outside TIER_ORDER (incl. missing/'') land in a final
    'Other' group, omitted when empty. Non-dict payload -> []. No I/O.
    """
```
Implementation: one `manifest_sets(payload)` call, then tier-partition list comprehensions preserving row order. Do NOT modify `manifest_sets` — it stays byte-identical (pinned by the untouched `TestManifestSets`). Run the new tests green + the whole file green.
  </action>
  <verify>python3.6 -m unittest tests.test_setup_form -v (new class green; TestManifestSets unchanged and green)</verify>
  <done>manifest_sets_grouped pinned by tests; manifest_sets byte-identical; setup_form still PURE (Gate A untouched — no new imports).</done>
</task>

<task type="auto">
  <name>Task 2: Qt separators + three hazard fixes</name>
  <files>aamatch/setup_window.py, smoke/smoke_11_window.py</files>
  <action>
In `aamatch/setup_window.py`:
1. `_populate_demo_sets` (~325-352): replace the flat addItem loop with the grouped loop — `groups = setup_form.manifest_sets_grouped(payload)`; for each (label, rows): if not the first group, `self.demo_combo.insertSeparator(self.demo_combo.count())`; then addItem per row exactly as today (label `title (tier)`, userData set_id). Keep `setCurrentIndex(0)` on the success path — safe because separators are only ever inserted BETWEEN groups, so index 0 is always a data row. Keep the failure placeholder path unchanged.
2. NEW method `_known_demo_set_ids(self)`: returns `tuple(str(combo.itemData(i)) for i in range(combo.count()) if combo.itemData(i) is not None)` with a docstring naming the hazard (a separator's None itemData stringified to 'None' would let build_state accept demo_set_id == 'None'). Replace BOTH itemData loops at ~927-928 (export) and ~1003-1004 (start) with `known_ids = self._known_demo_set_ids()`.
3. `apply_state` fallback (~556-560): replace `setCurrentIndex(0)` with the first SELECTABLE data row: `next((i for i in range(self.demo_combo.count()) if self.demo_combo.itemData(i) is not None), -1)` — a stale saved set id must never land on a separator (that would silently mean 'all sets' via currentData() → None → '').
4. `collect_state` (~519) and `_randomize_impl` (~681): add a one-line comment each noting correctness depends on the current index never being a separator — guaranteed by popup non-selectability + fix 3. No behavior change.

In `smoke/smoke_11_window.py`:
5. Part G mirror loop (~729-730): replace the raw `tuple(str(dlg.demo_combo.itemData(i)) ...)` with `known_g = dlg._known_demo_set_ids()`.
6. Add a data-relative T1b part (extend PART B or add PART H, following the file's check() discipline — compute values into variables BEFORE the check-detail string): parse the bundled manifest via the pure chain (read_json_file + parse_manifest_dict, same imports the engine uses), compute `groups = setup_form.manifest_sets_grouped(payload)`, then assert: (a) combo data-row count == total rows across groups; (b) separator count == (len(groups) - 1) if len(groups) > 1 else 0; (c) for every index i, `combo.itemData(i) is None` implies the row is a separator (guard model access in try/except — `combo.model().item(i)` on the default QStandardItemModel, flag `Qt.ItemIsEnabled` cleared for separators; never call a possibly-throwing API inside an eager check-detail string); (d) `findData('demo-dev-1')` still resolves and `currentData()` round-trips.
  </action>
  <verify>python3.6 -m py_compile aamatch/setup_form.py aamatch/setup_window.py smoke/smoke_11_window.py && python3.6 -m unittest discover -s tests -v (all green)</verify>
  <done>Grouped population live; both known_ids sites + smoke_11 use the None-skipping helper; stale-id fallback lands on a data row.</done>
</task>

<task type="auto">
  <name>Task 3: offscreen T1b proof — smoke_11 headless run</name>
  <files>smoke/smoke_11_window.py</files>
  <action>
Run the extended smoke headlessly (T1b tier per the 04-01 probe verdict): `bash smoke/run_smoke.sh smoke/smoke_11_window.py 180`. With the current dev-only manifest this asserts the degenerate case (1 group, 0 separators, behavior identical to pre-change) AND all pre-existing parts stay green (findData selections, collect/apply round-trips, known-ids paths). Grep the marker `=== SMOKE-11 PASS ===`. If any part fails, fix and re-run before committing (Rule 1). Commit each task separately.
  </action>
  <verify>bash smoke/run_smoke.sh smoke/smoke_11_window.py 180 && (output greps '=== SMOKE-11 PASS ===') && python3.6 -m unittest discover -s tests -v</verify>
  <done>SMOKE-11 PASS offscreen with separator/data-row assertions in place; full WSL suite green; the real-viewer grouped-dropdown visual check remains for 08-11 [HUMAN] (by design).</done>
</task>

</tasks>

<verification>
- `python3.6 -m py_compile aamatch/*.py` — purity-adjacent syntax floor.
- `python3.6 -m unittest discover -s tests -v` — full suite incl. purity gates (setup_form still PURE, no new imports).
- `bash smoke/run_smoke.sh smoke/smoke_11_window.py 180` — SMOKE-11 PASS marker.
</verification>

<success_criteria>
- Grouped dropdown logic shipped pure-side + Qt-side with all three separator hazards closed.
- manifest_sets byte-identical; test_setup_form grows without touching pinned tests.
- Offscreen proof green; curated-set behavior (multiple groups → separators) is data-relative and will activate automatically as 08-05..08-07 land.
</success_criteria>

<output>
After completion, create `.planning/phases/08-demo-curation-citations/08-02-SUMMARY.md`
</output>
