---
phase: 07-checkpoint-game-file-persistence
plan: 07
type: execute
wave: 4
depends_on: [07-03, 07-05]
files_modified: [aamatch/game_window.py, smoke/smoke_19_import.py]
autonomous: true

must_haves:
  truths:
    - "The Game tab has an 'Import' button (btn_import) inserted before the stretch, after Save — the stretch stays LAST"
    - "Import works with NO game live (no upfront gate) and replaces a mid-game one exactly like Start (P-2 cancel first, P-3 pop, cleanup inside the seam)"
    - "The imported game is materialized from the EMBEDDED payload (fresh GameState zeros, timer from zero at GO) — never regenerated"
    - "'Game imported: <path>.' logs AFTER the countdown arms (D7 law)"
    - "Restart routes payload-direct when _last_start carries 'payload' (the imported uploaded game restarts CORRECTLY)"
    - "Every game-file refusal surfaces verbatim through _guard with its existing house message (foreign, newer, misfiled, unparseable, wrong game version, stale detector stamp, sha256 mismatch, upload-without-content)"
  artifacts:
    - path: "aamatch/game_window.py"
      provides: "btn_import + _on_import (gate-free wrapper) + _import_game_from(path) (P-2/P-3 deferred replay) + _restart_now payload branch"
      contains: "btn_import"
    - path: "smoke/smoke_19_import.py"
      provides: "SMOKE-19: import E2E (payload-direct, countdown, GO) + restart-payload-branch + the full refusal battery with exact-message asserts"
      contains: "SMOKE-19"
  key_links:
    - from: "aamatch/game_window.py _import_game_from"
      to: "gamestart.start_game_from_payload"
      via: "parse gates FIRST (refusals leave the scene untouched) -> cancel_pending_start -> _pop_game_wizard -> start_game_from_payload(activate=False) -> start_countdown -> log after arm"
      pattern: "start_game_from_payload"
    - from: "aamatch/game_window.py _restart_now"
      to: "gamestart.start_game_from_payload"
      via: "payload branch: _last_start['payload'] present -> payload-direct replay instead of start_game regeneration"
      pattern: "_last_start"
---

<objective>
Wire the Game-tab Import button for game files (PERSIST-02) and route Restart through the payload-direct branch (the c2 decision).

Purpose: spec.md:39 "import button to import a game prepared by (3.5) Generate and export". The export side is complete and round-trip-proven (game_file.py; SMOKE-11 PART F2); the parse chain is the 04-12-proven five-gate consumer contract. This plan wires the tab; the checkpoint-kind dispatch grows later (07-10) — the filter here is game-files-only.

**RECORDED DECISIONS (yolo):**
1. **Import filter at this plan = `'AA-match Game (*.aamatch.json);;All Files (*)'`** (the EXACT export filter). The `.aamz` checkpoint extension + kind dispatch land in 07-10 — no dead branch now.
2. **NO confirmation warning for mid-game Import** — house law: spec warnings exist ONLY for Skip/Give Up (spec.md:43-45; restart-reset D9). Import mid-game discards the running game exactly like Start.
3. **Restart payload routing (c2):** `_restart_now` branches on `'payload' in gamestart._last_start` -> `start_game_from_payload(...)`; legacy `start_game(...)` kept as the defensive fallback branch for a payload-less tuple (behavior-identical for demo games; the payload branch is asserted taken in SMOKE-19).
</objective>

<execution_context>
@~/.config/opencode/get-shit-done/workflows/execute-plan.md
@~/.config/opencode/get-shit-done/templates/summary.md
@.planning/phases/06-scoring-lifecycle-endgame/06-08-SUMMARY.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/phases/07-checkpoint-game-file-persistence/07-RESEARCH-import.md
@aamatch/game_window.py
@aamatch/gamestart.py
@aamatch/game_file.py
@aamatch/persistence.py
@aamatch/paths.py
@smoke/smoke_19_import.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: btn_import + _on_import wrapper + _import_game_from impl</name>
  <files>aamatch/game_window.py</files>
  <action>
1. **Button** (after btn_save_game's block): `self.btn_import = QtWidgets.QPushButton('Import', self)` (no naming collision anywhere -> bare name per the D7 logic that left btn_restart bare), tooltip `'Load a game file exported by Generate and export and start playing it.'`, inserted `btn_row.insertWidget(btn_row.count() - 1, self.btn_import)` (row: Hint, Confirm, Skip/Give Up, Restart, Reset, Save, Import — stretch LAST). Connect: `self.btn_import.clicked.connect(self._on_import)`.

2. **Wrapper `_on_import`** — thin, GATE-FREE (Import is legal with NO game live — it CREATES one; the v1 import ran with any prior state, PA-__init__:790-812):
   - `path, _f = QtWidgets.QFileDialog.getOpenFileName(self, 'Import AA-match Game', '', 'AA-match Game (*.aamatch.json);;All Files (*)')`; `if not path: return` (silent cancel).
   - `self._guard(lambda: self._import_game_from(path))` — refusals surface verbatim (no catch-and-humanize layer; the 04-13/06-08 convention).
   - NO success box: the countdown + armed wizard + the after-arm log line ARE the feedback (the _on_start precedent).

3. **Impl `_import_game_from(self, path)`** — NON-MODAL, refusal-first:
   - Parse + gates FIRST (pure — a foreign/corrupt file never touches the session): `from . import persistence, game_file, paths, gamestart`; `parsed = game_file.parse_game_data(persistence.load_container(paths.to_windows_path(path), 'game'))` -> `{'setup', 'payload', 'ligand_texts'}`.
   - `self.cancel_pending_start()` FIRST after parse (P-2 belt-and-braces — an in-flight countdown's GO must never activate an orphaned wizard over the imported game; the _restart_now:708 shape).
   - `self._pop_game_wizard()` (P-3 — the live GameWizard pops with its cleanup; a user wizard is never popped).
   - `wiz = gamestart.start_game_from_payload(parsed['payload'], ligand_content=(parsed['ligand_texts'] or None), setup=parsed['setup'], activate=False)` — the 07-05 seam (cleanup -> materialize -> adopt fresh GameState -> wizard -> compose). NO seed arg: the seam derives seed from payload['seed'] (the spec's authoritative seed).
   - `self.start_countdown(wiz)` (box clear, 'Get ready...').
   - Log AFTER the arm (D7 law): `self._log(status_text.game_imported_line(path))` — the countdown's box clear would wipe a pre-arm line.
   - Timer from ZERO at GO via the existing `_begin_play` -> `activate_game` (a game file carries no elapsed; the sidecar elapsed belongs to the checkpoint path only).
  </action>
  <verify>python3.6 -m py_compile aamatch/*.py && python3.6 -m unittest discover -s tests -v</verify>
  <done>Import button wired gate-free; impl is refusal-first (parse before any scene touch) with P-2/P-3 ordering, payload-direct seam, D7 after-arm logging</done>
</task>

<task type="auto">
  <name>Task 2: _restart_now payload branch</name>
  <files>aamatch/game_window.py</files>
  <action>
Edit `_restart_now` (the 06-08 law chain: `_last_start is None` refusal FIRST -> cancel_pending_start -> _pop_game_wizard -> start). Replace the direct `start_game(setup=..., seed=..., candidates=..., ligand_content=..., activate=False)` call with the branch:

```python
ls = gamestart._last_start
if 'payload' in ls:
    wiz = gamestart.start_game_from_payload(
        ls['payload'], ligand_content=ls['ligand_content'],
        setup=ls['setup'], candidates=ls['candidates'],
        activate=False)
else:
    wiz = gamestart.start_game(
        setup=ls['setup'], seed=ls['seed'],
        candidates=ls['candidates'],
        ligand_content=ls['ligand_content'], activate=False)
```

Everything else stays EXACTLY (the refusal-first order, the D7 `game_restarted_line()` after `start_countdown`, the OSError/ValueError residue through _guard). Docstring: note the payload branch is the c2 decision — imported uploaded games replay the EMBEDDED payload (never the bundled-manifest fallback, engine.py:290-302); the legacy branch is defensive (a tuple without 'payload' cannot occur post-07-05 but keeps 06-08 semantics for direct test constructions).
  </action>
  <verify>python3.6 -m py_compile aamatch/*.py && python3.6 -m unittest discover -s tests -v</verify>
  <done>Restart prefers the payload-direct replay; refusal-first + D7 ordering unchanged</done>
</task>

<task type="auto">
  <name>Task 3: SMOKE-19 — import E2E + restart branch + refusal battery</name>
  <files>smoke/smoke_19_import.py</files>
  <action>
New single-run smoke (repo-root anchor, PART-lettered, count-asserted, `=== SMOKE-19 PASS ===`, restore block per the house shape):

- **PART A (fixture):** build a real game file into `tmp/smoke fixtures/aam_pse19/`: `gamestart.start_game(seed=4242, activate=False)` (a real payload via the engine) -> `data = game_file.make_game_data(setup, engine._payload, None, created_at=...)` -> `persistence.save_container(path, 'game', data)` (or drive `setup_window._export_game_to`-equivalent assembly directly — the SMOKE-11 PART F2 shape). ALSO write refusal fixtures: a foreign JSON, a container with version 99, a 'game'-kind container with game_format_version 99, JSON with a syntax error, a payload re-stamped 'det-0', a tampered ligand_files (sha256 mismatch), an upload-payload-without-content row.
- **PART B (import E2E via the tab):** fresh GameTab (offscreen) -> drive `tab._import_game_from(path)` directly (no dialog) -> assert: prior game objects cleaned + fresh `_aam_*` generation (instance-stamped marker band per the 03-05 law), `engine.game_status()` fresh zeros, the countdown armed (`tab._pending_wizard` is the returned wizard / the countdown active per the SMOKE-16 drive pattern), `engine._payload` IS the embedded payload object, `game_imported` line last in `_info_log` (D7), cancel PendingStart + timer stopped in the restore block. Then drive GO (`tab._begin_play()` or the countdown expiry pattern used by SMOKE-16) -> timer anchored from zero, wizard active.
- **PART C (restart payload branch):** from the imported game, stamp the marker band, drive `tab._restart_now()`-equivalent (via `_on_restart`'s impl path) -> assert the payload branch ran (fresh GameState zeros, regenerated-equivalent scene from the SAME payload — object names/ids equal to pre-restart), `game_restarted_line` after the arm.
- **PART D (refusal battery, exact messages):** for each fixture, drive `tab._import_game_from(bad_path)` (wrap in try/except or expect the raise — the impl raises; _guard boxes are wrapper-side [HUMAN]) and assert the EXACT message strings: foreign (`not an AA-match file (magic=...`), newer container (`unsupported AA-match format version 99 ...`), misfiled kind, unparseable JSON, wrong game_format_version, stale detector stamp (`unsupported detector_version 'det-0' ... stale or newer ...`), sha256 mismatch, upload-without-content. Assert the scene is UNTOUCHED after each refusal (parse-first law: object count unchanged).
- Restore block + final marker.
  </action>
  <verify>bash smoke/run_smoke.sh smoke/smoke_19_import.py 240 — greps `=== SMOKE-19 PASS ===`, exit 0</verify>
  <done>SMOKE-19 green: payload-direct import E2E with countdown + D7 line, restart payload branch proven, every refusal message asserted verbatim with the scene untouched</done>
</task>

</tasks>

<verification>
1. `python3.6 -m py_compile aamatch/*.py` + full WSL suite green.
2. `bash smoke/run_smoke.sh smoke/smoke_19_import.py 240` PASS.
3. SMOKE-16 regression (game_window.py touched): `bash smoke/run_smoke.sh smoke/smoke_16_tab.py 240` PASS (incl. the 07-06 save part).
</verification>

<success_criteria>
- Import reconstructs the initial game from the embedded payload (fresh state, timer from zero, countdown, D7 line) without any regeneration.
- Restart replays payload-direct; the uploaded-game manifest-fallback hazard is closed.
- All eight refusal classes refuse with their exact house messages before touching the scene.
</success_criteria>

<output>
After completion, create `.planning/phases/07-checkpoint-game-file-persistence/07-07-SUMMARY.md`
</output>
