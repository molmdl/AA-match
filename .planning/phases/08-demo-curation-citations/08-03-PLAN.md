---
phase: 08-demo-curation-citations
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - smoke/smoke_02_manifest.py
  - smoke/smoke_03_generate.py
  - smoke/smoke_05_perf.py
autonomous: true
must_haves:
  truths:
    - "smoke_02 and smoke_03 no longer pin an absolute entry count — they pass on the dev-only manifest AND after the manifest grows in 08-05..08-07"
    - "smoke_05's perf target is the largest entry BY DATA (largest_entry + max-heavy cross-check), never by the literal name 'benzamide'"
    - "All three smokes still PASS headless on the current dev-only tree (no behavior regression)"
  artifacts:
    - path: "smoke/smoke_02_manifest.py"
      provides: "data-relative manifest-parse check (>= 2 entries)"
      contains: "len(entries) >= 2"
    - path: "smoke/smoke_03_generate.py"
      provides: "data-relative manifest-parse check"
      contains: "len(entries) >= 2"
    - path: "smoke/smoke_05_perf.py"
      provides: "largest-entry selection asserted by max-heavy cross-check only"
  key_links:
    - from: "smoke_02/03/05"
      to: "aamatch/data/MANIFEST.json"
      via: "pure parse chain inside PyMOL (unchanged); only the hardcoded literals are removed"
      pattern: "enumerate_entries"
---

<objective>
De-hardcode the three smokes that pin manifest-growth-sensitive literals (mechanics research Q6 table): entry-count checks in smoke_02/smoke_03 and the largest-entry name literal in smoke_05.

Purpose: every content plan re-runs SMOKE-02 per set; these literals would false-fail the moment the first curated set lands. This is independent machinery work — no curated data required.
Output: three data-relative smokes, proven green on the current tree.
</objective>

<execution_context>
@~/.config/opencode/get-shit-done/workflows/execute-plan.md
@~/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/08-demo-curation-citations/08-RESEARCH-mechanics.md
@smoke/smoke_02_manifest.py
@smoke/smoke_03_generate.py
@smoke/smoke_05_perf.py

Smoke-authoring discipline (house law): never call cmd APIs that may throw inside an eager check-detail string; print verdicts on one flushed line; the marker `=== SMOKE-NN PASS ===` is the sole verdict carrier.
</context>

<tasks>

<task type="auto">
  <name>Task 1: smoke_02 + smoke_03 entry-count literals → data-relative</name>
  <files>smoke/smoke_02_manifest.py, smoke/smoke_03_generate.py</files>
  <action>
smoke_02_manifest.py:78-79: change `check('manifest parse', len(entries) == 2, 'entries=%d' % len(entries))` to `len(entries) >= 2` (keep the same check name and detail format). Rationale comment: the manifest grows additively (Phase 8 curation); the anti-drift proof is the per-entry loop below, not an absolute count.
smoke_03_generate.py:100-101: same change. The benzamide candidate restriction (rows filtered by entry_id, ~102-106) stays — it is already manifest-growth-proof per the research (block_exclusive refuses checked-but-unsupported types on aromatic-less molecules; SMOKE-03 stays seed-robust only by that restriction, 02-13 law).
Do not change anything else in either file.
  </action>
  <verify>python3.6 -m py_compile smoke/smoke_02_manifest.py smoke/smoke_03_generate.py && grep -n "len(entries) >= 2" smoke/smoke_02_manifest.py smoke/smoke_03_generate.py</verify>
  <done>Both count checks are data-relative; no other lines touched.</done>
</task>

<task type="auto">
  <name>Task 2: smoke_05 largest-entry literal → data-relative</name>
  <files>smoke/smoke_05_perf.py</files>
  <action>
smoke_05_perf.py:181-188: the check `biggest['entry_id'] == 'benzamide'` pins the perf target by NAME. Remove that literal term, keeping `biggest is not None and biggest['heavy_atom_count'] == max(row['heavy_atom_count'] for row in entries)` (the max-heavy cross-check already proves largest_entry semantics; DETECT-05 targets the largest bundled molecule whatever its name). The `candidates=[biggest]` game construction (~205) is already data-relative — leave it. Note in a comment: with the Phase-8 manifest the largest entry is expected to be a medium ligand (NAD/heme class, aromatic — the pi_stacking block_exclusive game below needs its ligand ring; the feature extraction at ~259 fail-closes visibly if a future largest entry lacks one).
Do not change anything else.
  </action>
  <verify>python3.6 -m py_compile smoke/smoke_05_perf.py && grep -c "benzamide" smoke/smoke_05_perf.py (returns 0)</verify>
  <done>'benzamide' no longer appears in smoke_05; largest-entry selection is purely data-driven.</done>
</task>

<task type="auto">
  <name>Task 3: prove all three smokes green on the current tree</name>
  <files>smoke/smoke_02_manifest.py</files>
  <action>
Run headless Windows PyMOL for each modified smoke (bash smoke/run_smoke.sh smoke/<script>.py; pass TIMEOUT 180 for smoke_05's tier-9 grid battery per the 02-04 precedent) and grep each PASS marker. Any failure = Rule-1 fix + re-run before commit. Commit each task separately with `fix(08-03): ...` messages. Then run the full WSL suite to confirm zero cross-effects.
  </action>
  <verify>bash smoke/run_smoke.sh smoke/smoke_02_manifest.py && bash smoke/run_smoke.sh smoke/smoke_03_generate.py && bash smoke/run_smoke.sh smoke/smoke_05_perf.py 180 (all three print their PASS markers) && python3.6 -m unittest discover -s tests -v (green)</verify>
  <done>SMOKE-02/03/05 PASS on the dev-only manifest with data-relative assertions; suite green.</done>
</task>

</tasks>

<verification>
- grep confirms: no `== 2` entry-count literals, no 'benzamide' in smoke_05.
- All three smokes print PASS markers headless.
- Full WSL suite green (`python3.6 -m unittest discover -s tests -v`).
</verification>

<success_criteria>
- The smokes are manifest-growth-proof: adding curated sets in 08-05..08-07 cannot false-fail them.
- Zero behavior change on the current tree (same PASS verdicts as before the edit).
</success_criteria>

<output>
After completion, create `.planning/phases/08-demo-curation-citations/08-03-SUMMARY.md`
</output>
