---
phase: 04-qt-setup-window
plan: 09
type: execute
wave: 4
depends_on: ["04-07"]
files_modified: [aamatch/setup_window.py, smoke/smoke_11_window.py]
autonomous: true

must_haves:
  truths:
    - "Reset restores exactly the frozen DEFAULTS (deep-copied, never aliased) and the widgets reflect them"
    - "Randomize produces a USABLE random configuration: setup_form.usable_randomized_state with the current dropdown selection preserved (fallback ''), applied back through apply_state so source_mode/upload reflection is automatic"
    - "Save Setup writes a versioned 'setup' container via persistence.save_setup_file with the .aam.setup.json extension auto-appended; Load Setup reads it back through load_setup_file and applies it"
    - "All four handlers surface ValueError-family errors through the _guard pattern (QMessageBox.warning, modal CHILD -- legal) and let unexpected exceptions propagate"
  artifacts:
    - path: "aamatch/setup_window.py"
      provides: "_on_reset, _on_randomize, _on_save_setup + _save_setup_to(path), _on_load_setup + _load_setup_from(path), shared _guard"
      min_lines: 60
    - path: "smoke/smoke_11_window.py"
      provides: "SMOKE-11 PART C: save->load round-trip, reset, randomize drives (T1b, probe-gated; success paths only -- no modals)"
  key_links:
    - from: "aamatch/setup_window.py _on_randomize"
      to: "aamatch/setup_form.usable_randomized_state"
      via: "the Randomize fix-up (the 'demo-%04x' trap never reaches Start)"
      pattern: "usable_randomized_state"
    - from: "aamatch/setup_window.py _save_setup_to"
      to: "aamatch/persistence.save_setup_file"
      via: "to_windows_path routing + validate-on-save (the versioned container, never raw json.dump)"
      pattern: "save_setup_file"
    - from: "aamatch/setup_window.py"
      to: "aamatch/paths.to_windows_path"
      via: "every disk path routes the guard (QFileDialog Windows paths pass through unchanged by design)"
      pattern: "to_windows_path"
---

<objective>
Wire the four SETUP-07 buttons (Reset, Randomize, Save Setup, Load Setup) with the error-surfacing contract. This plan also introduces the shared `_guard` helper that every later handler reuses, and the `_X_impl` factoring rule (dialog wrapper + non-modal logic) that keeps headless driving possible.

Purpose: SETUP-07 is one ROADMAP criterion (Reset restores defaults; Randomize produces a valid random configuration; Save/Load round-trip the versioned setup file). The Randomize trap (randomize_state synthesizes 'demo-%04x' matching no manifest set) dies here via the 04-02 helper.
Output: four working buttons + SMOKE-11 PART C.
</objective>

<execution_context>
@~/.config/opencode/get-shit-done/workflows/execute-plan.md
@~/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/04-qt-setup-window/04-RESEARCH-setup-form-seams.md
@.planning/phases/04-qt-setup-window/04-07-SUMMARY.md
@aamatch/setup_window.py
@aamatch/setup_form.py
@aamatch/persistence.py
</context>

<decisions>
- Error-surfacing contract (research error_surfacing): shared `self._guard(self, fn)`-style helper -- catch ValueError (FormatError/GenerationError/EngineError/PlacementError/WizardError are all subclasses) AND OSError on file-op handlers -> `QtWidgets.QMessageBox.warning(self, 'AA-match', str(e))` verbatim (str(e) is already user-facing); ANY other exception propagates (bug surfacing, never silent swallow -- wizard _guard precedent wizard.py:423-433).
- Headless modal safety: smokes drive ONLY success paths (a QMessageBox in a headless probe hangs until timeout -- P5); error surfacing is verified at the [HUMAN] checkpoints (Decision 11).
- Extension auto-append (Decision 2): if the chosen path does not end with '.aam.setup.json', append it (prior-art gui_setup.py:651-652).
</decisions>

<tasks>

<task type="auto">
  <name>Task 1: _guard + Reset + Randomize handlers</name>
  <files>aamatch/setup_window.py</files>
  <action>
1. Add to SetupWindow:
   ```python
   def _guard(self, fn):
       """Run fn() mapping the ValueError family + OSError to a modal
       warning box (modal CHILD -- allowed, PITFALL 4). str(e) is shown
       verbatim: every house refusal already names its cause. Unexpected
       exceptions PROPAGATE (bug surfacing, never a silent swallow --
       wizard.py:423-433 precedent)."""
       try:
           return fn()
       except (ValueError, OSError) as e:
           QtWidgets.QMessageBox.warning(self, 'AA-match', str(e))
           return None
   ```
2. `_on_reset(self)`: `self._guard(self._reset_impl)`. `_reset_impl`: lazy `import copy` + `from . import setup_state`; `self.apply_state(copy.deepcopy(setup_state.DEFAULTS))` (NEVER alias the module dict -- the allowed list is mutable; research SETUP-07 seam). Connect self.btn_reset.clicked -> self._on_reset.
3. `_on_randomize(self)`: `self._guard(self._randomize_impl)`. `_randomize_impl`: lazy `from . import setup_form`; current = `str(self.demo_combo.currentData() or '')` (Decision 5: keep the current dropdown selection; '' when the placeholder/no-selection); `state = setup_form.usable_randomized_state(demo_set_id=current)`; `self.apply_state(state)` (apply_state reflects source_mode 'demo' + upload None back into the widgets automatically). Connect self.btn_randomize.clicked -> self._on_randomize.
No logging; 3.6 syntax; lazy sibling imports inside methods.
Commit: `feat(04-09): Reset + Randomize handlers (guard pattern, randomize fix-up)`.
  </action>
  <verify>T0: py_compile + full WSL suite green. Static: deepcopy used for DEFAULTS; usable_randomized_state called with the combo's currentData; _guard wording matches the contract.</verify>
  <done>Reset and Randomize are wired; the trap is structurally dead.</done>
</task>

<task type="auto">
  <name>Task 2: Save Setup + Load Setup handlers</name>
  <files>aamatch/setup_window.py</files>
  <action>
1. `_on_save_setup(self)`: `path, _ = QtWidgets.QFileDialog.getSaveFileName(self, 'Save AA-match Setup', '', 'AA-match Setup (*.aam.setup.json);;All Files (*)')` (modal CHILD -- legal); `if path: self._guard(lambda: self._save_setup_to(path))`. `_save_setup_to(path)`: lazy `from . import persistence, paths`; `if not path.endswith('.aam.setup.json'): path = path + '.aam.setup.json'`; `persistence.save_setup_file(paths.to_windows_path(path), self.collect_state())` (validate-on-save; OSError propagates to _guard -- it is caught there); success: `QtWidgets.QMessageBox.information(self, 'AA-match', 'Setup saved to %s' % path)`. Connect self.btn_save_setup.clicked -> self._on_save_setup.
2. `_on_load_setup(self)`: `path, _ = QtWidgets.QFileDialog.getOpenFileName(self, 'Load AA-match Setup', '', 'AA-match Setup (*.aam.setup.json);;All Files (*)')`; `if path: self._guard(lambda: self._load_setup_from(path))`. `_load_setup_from(path)`: lazy imports; `state = persistence.load_setup_file(paths.to_windows_path(path))` (validate-on-load; the 4 FormatError classes -- foreign/newer/misfiled/unparseable -- arrive with clear messages); `self.apply_state(state)`; success message 'Setup loaded from %s'. Connect self.btn_load_setup.clicked -> self._on_load_setup.
3. Documented caveat (docstring + comment): a loaded setup with source_mode 'upload' restores path/sha256 LABELS only; the molecules themselves are session-only -- Start/Export refuse via build_state's upload_ready pre-check until re-ingested (04-10 wires that).
Commit: `feat(04-09): Save/Load Setup handlers (versioned container, extension auto-append)`.
  </action>
  <verify>T0: py_compile + full WSL suite green. Static: every disk path routes to_windows_path; extension appended; _guard wraps both.</verify>
  <done>All four SETUP-07 buttons wired through the versioned-container discipline.</done>
</task>

<task type="auto">
  <name>Task 3: SMOKE-11 PART C -- save/load round-trip, reset, randomize</name>
  <files>smoke/smoke_11_window.py</files>
  <action>
Extend SMOKE-11 (probe-gated T1b, success paths only):
- PART C1 (reset): `dlg._reset_impl()` (call the non-modal impl directly -- no dialog, no box); `collect_state()` equals the defaults-normalized dict (compare against setup_state.validate_state(dict(setup_state.DEFAULTS))).
- PART C2 (randomize): set the dropdown to 'demo-dev-1' if present; `dlg._randomize_impl()`; `state = dlg.collect_state()`; assert `setup_state.validate_state(state) == state` (normalized output is stable); assert source_mode 'demo' and upload None; assert demo_set_id == 'demo-dev-1' (Decision 5) -- the trap string ('demo-0...') must NOT be collectible.
- PART C3 (save/load round-trip): tmp = `os.path.join(tempfile.gettempdir(), 'aamatch_smoke11_setup.aam.setup.json')`; mutate widgets (spinboxes 4/2, block_exclusive + h_bond checked); `dlg._save_setup_to(tmp)`; assert the file exists and `persistence.read_json_file(tmp)` has kind 'setup' and version 1; `dlg._reset_impl()` (widgets back to defaults); `dlg._load_setup_from(tmp)`; `collect_state()` equals the mutated dict again (round-trip); os.remove(tmp). Use lazy imports in the smoke; NO modals (impl methods only).
Commit: `test(04-09): SMOKE-11 PART C setup-button drives`.
  </action>
  <verify>T1b (probe-PASS): `bash smoke/run_smoke.sh smoke/smoke_11_window.py 120` prints `=== SMOKE-11 PASS ===` with PART C asserts green. T1a (probe-FAIL): PART C skipped and marked [HUMAN] (the pure wrappers save/load_setup_file are already WSL-tested -- the glue is checkpoint-covered). T0: full WSL suite green.</verify>
  <done>SETUP-07's logic is proven headlessly to the probe's limit; the pure persistence round-trip was already WSL-proven.</done>
</task>

</tasks>

<verification>
- T0: py_compile; full WSL suite green.
- T1b (probe-gated): SMOKE-11 PART C PASS (reset identity, randomize usability + id preservation, save->load round-trip).
- Static: _guard catches (ValueError, OSError) only; message boxes are modal children; unexpected exceptions propagate.
</verification>

<success_criteria>
SETUP-07 is functionally complete: Reset restores frozen defaults, Randomize always yields a usable configuration preserving the chosen content, Save/Load round-trip the versioned setup file with the decided extension, and every failure surfaces as a user-facing message without ever swallowing a bug.
</success_criteria>

<output>
After completion, create `.planning/phases/04-qt-setup-window/04-09-SUMMARY.md` (frontmatter: affects [04-14 checkpoint A], subsystem qt-window handlers).
</output>
