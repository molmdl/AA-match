---
phase: 05-game-status-tab-start-sequence
plan: 10
type: execute
wave: 4
depends_on: ["05-01", "05-06", "05-07"]
files_modified: [aamatch/game_window.py, smoke/smoke_14_status_surface.py]
autonomous: true

must_haves:
  truths:
    - "The info box is an EVENT LOG, not a state mirror: tab-owned actions log directly (start sequence, hint line); the 1 Hz poll appends only CHANGES via pure status_text.status_events — first observation silent (the start sequence already logged the level line), molecule/level change -> level line, selection change -> 'Selected: slot %s (%s).', new error -> 'ERROR: %s' with sticky dedupe; NO wizard-panel mirroring, no movement/nudge lines, no timestamps (04-15 clarification: the panel is the imperative surface)"
    - "The required-interactions LABEL is reference info refreshed on molecule change (and reset to 'Required: -' when no GameWizard is live) — never a log line, never scrolled away; rendered by status_text.required_display (both modes pinned)"
    - "_begin_play logs the FIRST level line + sets the first required label from the PENDING wizard's get_status() (instant, ordered right after 'GO!', no poll race — pitfall 4) and seeds _last_status so the poll's first observation is silent"
    - "The poll reads ONLY public accessors: cmd.get_wizard() + isinstance gate with LAZY relative import inside the method (module-identity law, setup_window.py:797-799 precedent) + w.get_status() — never engine._game/_payload/wizard privates, never callbacks registered on the wizard (pickle contract)"
  artifacts:
    - path: "aamatch/game_window.py"
      provides: "_refresh_status() + _last_status plain-dict store + _on_tick hook (after the timer-label half; skipped during the modal-pause branch) + _begin_play status content"
      contains: "_refresh_status"
    - path: "smoke/smoke_14_status_surface.py"
      provides: "T1b status-surface extension: info-box read-only + _log appends, both required modes from LIVE games, scripted-pick 'Selected:' line, no-selection-move 'ERROR:' line + sticky dedupe, no-score-lines reserve"
  key_links:
    - from: "aamatch/game_window.py _on_tick"
      to: "aamatch/game_window._refresh_status"
      via: "the poll piggybacks on A's 1 Hz timer (no separate timer, no push callbacks)"
      pattern: "_refresh_status"
    - from: "aamatch/game_window.py _refresh_status"
      to: "aamatch/status_text.status_events"
      via: "the pure diff owns all event semantics; the Qt tier never interprets state"
      pattern: "status_events"
    - from: "aamatch/game_window.py _refresh_status"
      to: "aamatch/wizard.GameWizard.get_status"
      via: "the public read accessor (plan 05-07) behind the isinstance gate"
      pattern: "get_status"
---

<objective>
Wire the status surface (SCORE-04's dynamic half): the 1 Hz poll-diff that turns the Game tab's info box into an event log and keeps the required label truthful, consuming the pure builders (05-01) and the accessors (05-07) on the GameTab skeleton (05-06).

Purpose: PyMOL exposes NO observable wizard-refresh hook (SRC wizarding.py:130-142 — refresh_wizard is an internal C dispatch) and push callbacks onto the wizard are FORBIDDEN (session save pickles the whole wizard stack — SRC wizarding.py:175-180). Polling at 1 Hz, piggybacked on the elapsed timer, is the realistic sync (research Q5). All wording lives in status_text (WSL-pinned); the tab stays a dumb renderer.
Output: _refresh_status + _begin_play content + SMOKE-14 T1b parts.
</objective>

<execution_context>
@~/.config/opencode/get-shit-done/workflows/execute-plan.md
@~/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/05-game-status-tab-start-sequence/05-RESEARCH-status-surface.md
@.planning/phases/05-game-status-tab-start-sequence/05-01-PLAN.md
@.planning/phases/04-qt-setup-window/04-09-SUMMARY.md
@.planning/STATE.md
@aamatch/game_window.py
@aamatch/status_text.py
@aamatch/wizard.py
@aamatch/setup_window.py
@smoke/smoke_14_status_surface.py
@smoke/smoke_07_wizard_loop.py

Key facts (verified, transcribe exactly):
- _refresh_status (research B Pattern 2, verbatim shape): lazy `from pymol import cmd` + `from . import status_text, wizard` INSIDE the method; w = cmd.get_wizard(); if not isinstance(w, wizard.GameWizard): self._required_label.setText('Required: -'); self._last_status = None; return. Else: state = w.get_status(); for line in status_text.status_events(self._last_status, state): self._log(line); prev = self._last_status; if prev is None or prev.get('molecule_id') != state.get('molecule_id'): self._required_label.setText(status_text.required_display(state['required'])); self._last_status = state.
- _on_tick gains `self._refresh_status()` AFTER the timer-label half; the modal-pause branch returns early so the poll is skipped while frozen (research B placement table).
- _begin_play extension (research B Pattern 1): after gamestart.activate_game(self._pending_wizard): state = self._pending_wizard.get_status() (BEFORE clearing pending); self._log(status_text.level_molecule_line(state)); self._required_label.setText(status_text.required_display(state['required'])); self._last_status = state; then self._pending_wizard = None; timer stop/start.
- __init__ gains `self._last_status = None` (the poll's baseline slot — added by THIS plan, not the shell).
- Pitfall 2 (module identity): the isinstance gate's lazy relative import is MANDATORY — a module-level `from .wizard import GameWizard` would create a second module object under the installed identity and silently fail the gate.
- Pitfall 6 (pre-GO): the poll starts after GO (_begin_play starts the timer); during the countdown the tab reads _pending_wizard only for start-sequence content — never conflate with cmd.get_wizard() (None pre-GO).
- SMOKE-14 T1b recipe (research B verification tiers): construct the GameTab STANDALONE (game_window.GameTab(None) — QWidget(None) is a legal top-level offscreen; no dialog needed) OR reuse the dialog path; drive a game via start_game(activate=False) + tab.start_countdown + direct _countdown_tick calls to GO (the 05-06 recipe); scripted pick via the SMOKE-07 recipe cmd.select('sele', '<slot object> and name CA') + do_select('sele'); movement refusal via the wizard's public move handler with no selection.
- The no-score-lines reserve is enforced by the WSL battery (05-01's result-not-fingerprinted cases); the smoke asserts the cheap structural form: no 'score' substring appears in the info box across the driven flows.
- SMOKE-14 already exists (05-07 created it with T1a parts) — EXTEND it (new T1b part), never rewrite existing parts.
</context>

<tasks>

<task type="auto">
  <name>Task 1: _refresh_status + _on_tick hook + _begin_play status content</name>
  <files>aamatch/game_window.py</files>
  <action>
1. __init__: add `self._last_status = None` (comment: the poll's plain-data baseline; the DIALOG is never pickled — a plain dict is free, pitfall 1).
2. Add _refresh_status() per the context facts (Pattern 2 verbatim; lazy imports inside the method).
3. _on_tick: append self._refresh_status() after the label half (the modal branch already returns early).
4. _begin_play: insert the status content between activate_game and the pending-clear (context facts; Pattern 1 verbatim). Update the method docstring: the first level line + first label are START-SEQUENCE-owned (instant, ordered; the poll only reports changes — pitfall 4).
5. Docstring discipline: no banned-token mentions; python3.6 floor.
Commit: `feat(05-10): status poll-diff wiring (info box + required label)`.
  </action>
  <verify>python3.6 -m py_compile aamatch/*.py && python3.6 -m unittest discover -s tests -v green</verify>
  <done>The poll exists on the tick; _begin_play owns the first observation; the tab never touches privates</done>
</task>

<task type="auto">
  <name>Task 2: SMOKE-14 T1b status-surface extension</name>
  <files>smoke/smoke_14_status_surface.py</files>
  <action>EXTEND smoke_14 with ONE new T1b part (probe-gated header; keep PART 1 byte-identical). Drive:
1. tab = game_window.GameTab(None) (standalone; the 04-01/04-05 T1b recipe: QT_QPA_PLATFORM=offscreen before any pymol.Qt import — the smoke already sets it).
2. check: _info_log.isReadOnly(); _log('test line') lands in toPlainText(); _timer_label '0:00'; _required_label 'Required: -'.
3. Deferred start: wiz = gamestart.start_game(activate=False); tab.start_countdown(wiz); drive _countdown_tick() x4 (GO). check: the level line 'Level 1, molecule 1 of %d.' IS in the box (from _begin_play); _required_label shows a required_display string (not 'Required: -'); tab._last_status is a plain dict.
4. First-observation silence: call tab._refresh_status() immediately; check the box gained NO new lines (prev seeded by _begin_play — pitfall 4).
5. Selection event: scripted pick (SMOKE-07 recipe: cmd.select('sele', '<first slot object> and name CA'); wiz.do_select('sele')) -> tab._refresh_status() -> check the box gained exactly one 'Selected: slot ' line; re-run _refresh_status() -> no new line (same-slot dedupe).
6. Error event + sticky dedupe: pop the selection context (cmd.deselect(); the wizard keeps its slot — use a SECOND fresh game instead): cmd.set_wizard() + cleanup; start_game() again (default activates; fresh wizard, no selection); tab2 path: since no countdown is pending, drive the poll via a fresh GameTab: tab2 = GameTab(None); tab2._refresh_status() -> check: _required_label == 'Required: -' and _last_status is None (no wizard? NO — a wizard IS active: check the label shows the requirement and _last_status set — the no-wizard branch needs NO active wizard: assert it in the PRE-game state instead, BEFORE the first start_game of this part: fresh GameTab + _refresh_status -> 'Required: -'). THEN with the live wizard: wiz.move_left() (public handler; no selection -> _guard sets _error) -> tab2._refresh_status() -> check exactly one new line startswith 'ERROR: '; call wiz.move_left() again -> _refresh_status -> NO new line (sticky dedupe, pitfall 3).
7. Both required modes: restart with an EXCLUSIVE setup (build a validated setup dict via setup_state.validate_state({'interaction_mode': 'exclusive', ...defaults})) -> deferred start + countdown drive on a fresh tab -> check _required_label == 'Required: any 1 interaction'; restart with an UNSET setup (DEFAULTS) -> check the label matches 'Required: %d interaction(s): ' % len(items) prefix.
8. Reserve: check 'score' not in the info-box text across all driven flows.
9. Restore: cancel_pending_start + cmd.set_wizard() + cleanup + baseline exact.
Update the header part list + docstring. Commit: `test(05-10): SMOKE-14 T1b status-surface drive`.
  </action>
  <verify>bash smoke/run_smoke.sh smoke/smoke_14_status_surface.py 120 — === SMOKE-14 PASS === with the T1b part green and PART 1 still green</verify>
  <done>The event log, both required modes, the diff rules, and the dedupe are proven against a LIVE game headlessly</done>
</task>

</tasks>

<verification>
- `python3.6 -m py_compile aamatch/*.py` — floor.
- `python3.6 -m unittest discover -s tests -v` — full WSL suite (status_text battery + everything).
- `bash smoke/run_smoke.sh smoke/smoke_14_status_surface.py 120` — T1a + T1b green.
- `bash smoke/run_smoke.sh smoke/smoke_11_window.py 120` — SMOKE-11 regression (game_window.py gained methods; the existing parts must stay green).
- Grep aamatch/game_window.py for 'engine._game'/'_payload' — zero hits (public accessors only).
</verification>

<success_criteria>
- The info box logs: start sequence (05-06), level line + required label at GO, selection changes, new errors (sticky-deduped) — and NOTHING else (no mirroring, no movement lines, no scores).
- The required label tracks the live molecule's requirement in both modes and resets when no game wizard is live.
- The poll is 1 Hz piggybacked, public-accessor-only, module-identity-safe.
</success_criteria>

<output>
After completion, create `.planning/phases/05-game-status-tab-start-sequence/05-10-SUMMARY.md`
</output>
