---
phase: 04-qt-setup-window
plan: 04
type: execute
wave: 1
depends_on: []
files_modified: [aamatch/engine.py, aamatch/placement.py, aamatch/gamestart.py, smoke/smoke_10_upload_flow.py]
autonomous: true

must_haves:
  truths:
    - "engine.new_game, engine._ligand_data_for, placement.materialize, and gamestart.start_game all accept ligand_content=None and behave byte-identically to today when it is None (every existing call site and test unchanged)"
    - "A synthetic 'uploads/mol-001.sdf'-keyed candidate row plus a ligand_content dict flows through new_game, materialize, and start_game headlessly, materializing the uploaded molecule from the embedded text (never a package path)"
    - "A synthetic-key row WITHOUT ligand_content fails closed with a clear EngineError, never a silent wrong molecule"
  artifacts:
    - path: "aamatch/engine.py"
      provides: "ligand_content param on _ligand_data_for + new_game (read_sdfstr/read_mol2str from string, count_states==1 assert)"
    - path: "aamatch/placement.py"
      provides: "ligand_content param on materialize (extension-derived reader, fail-closed)"
    - path: "aamatch/gamestart.py"
      provides: "ligand_content pass-through on start_game (the one-call seam stays complete)"
    - path: "smoke/smoke_10_upload_flow.py"
      provides: "SMOKE-10: uploaded-flow plumbing proven in real headless PyMOL"
  key_links:
    - from: "aamatch/engine.py"
      to: "cmd.read_sdfstr / cmd.read_mol2str"
      via: "string-based load when row['file'] is a ligand_content key (importing.py:931-959/1038-1069, same C parser as file loads)"
      pattern: "read_sdfstr|read_mol2str"
    - from: "aamatch/gamestart.py"
      to: "aamatch/engine.py"
      via: "start_game(setup, seed, candidates, ligand_content) passes ligand_content through to new_game"
      pattern: "ligand_content=ligand_content"
---

<objective>
Thread the additive `ligand_content=None` parameter through the cmd-tier game pipeline (research GAP steps 5-6 of 04-RESEARCH-export-upload.md upload_pipeline): `engine._ligand_data_for`, `engine.new_game`, `placement.materialize`, `gamestart.start_game`. Default `None` keeps every existing call site byte-identical. Prove the uploaded-flow plumbing headlessly with SMOKE-10.

Purpose: this is the pipe that makes uploads (plans 04-08/04-10) and Start-after-upload (04-13) work, and it is exactly the mechanism Phase 7 reuses for imported games (phase_7_contract item 5: "Phase 4 must land the ligand_content params + helpers so Phase 7 adds zero cmd-tier loading code"). Cmd tier is T1a-provable headless with NO QApplication, so this plan is probe-independent (wave 1).
Output: 4 additive signatures + SMOKE-10 PASS.
</objective>

<execution_context>
@~/.config/opencode/get-shit-done/workflows/execute-plan.md
@~/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/04-qt-setup-window/04-RESEARCH-export-upload.md
@.planning/STATE.md
@aamatch/engine.py
@aamatch/placement.py
@aamatch/gamestart.py
@smoke/smoke_04_e2e.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: engine -- ligand_content on _ligand_data_for + new_game</name>
  <files>aamatch/engine.py</files>
  <action>
1. `_ligand_data_for(row, names_before, ligand_content=None)` (engine.py:144-180): inside the try, BEFORE the existing `cmd.load` block, add the string path:

   if ligand_content is not None and row['file'] in ligand_content:
       text = ligand_content[row['file']]
       fmt = row.get('format')
       if fmt == 'mol2':
           cmd.read_mol2str(text, tmp_name)
       elif fmt == 'sdf':
           cmd.read_sdfstr(text, tmp_name)
       else:
           raise EngineError(
               'new_game: uploaded ligand %r has unsupported format %r '
               '(expected sdf or mol2)' % (identity, fmt))
       if cmd.count_states(tmp_name) != 1:
           raise EngineError(
               'new_game: uploaded ligand %r decoded to %d state(s) -- '
               'records must be split one molecule per content entry'
               % (identity, cmd.count_states(tmp_name)))

   then fall through to the SHARED extraction (records -> bounding_sphere -> _remap_ligand_bonds -> profile). The atom_count cross-check against the row (engine.py:158-165) stays ACTIVE for the string path too (rows carry atom_count). The existing `cmd.load(winpath, ...)` path runs when the key is absent -- byte-identical for demo sets.
2. `new_game(setup, seed, candidates=None, ligand_content=None)` (engine.py:183): new keyword param; docstring documents it ("dict mapping synthetic file keys such as 'uploads/mol-001.sdf' to molecule record text; None = package-resolved manifest flow"); pass-through at the `_ligand_data_for(row, names_before, ligand_content)` call site (engine.py:220-223).
3. Docstring notes on both: default None keeps every existing call site and test unchanged; the string path never touches `package_data_path` (uploaded records are embedded, so absolute paths are structurally impossible -- the os.path.join hazard from the research). Keep 3.6 syntax, %-format, no logging. NO other engine changes.
  </action>
  <verify>T0: `python3.6 -m py_compile aamatch/engine.py`; full WSL suite green (engine is cmd tier, exercised by WSL tests only via source gates; behavioral proof is Task 3's smoke).</verify>
  <done>engine accepts and routes ligand_content; the None path is provably untouched.</done>
</task>

<task type="auto">
  <name>Task 2: placement + gamestart -- materialize and start_game params</name>
  <files>aamatch/placement.py, aamatch/gamestart.py</files>
  <action>
1. `placement.materialize(payload, level_index=0, ligand_content=None)`: at the ligand-load site (placement.py:303-310), add the string path BEFORE the package-path resolution:

   if ligand_content is not None and ligand['file'] in ligand_content:
       text = ligand_content[ligand['file']]
       ext = os.path.splitext(str(ligand['file']))[1].lstrip('.').lower()
       if ext == 'mol2':
           cmd.read_mol2str(text, lig_name)
       elif ext == 'sdf':
           cmd.read_sdfstr(text, lig_name)
       else:
           raise PlacementError(
               'materialize: molecule %r ligand file %r has unsupported '
               'extension %r (expected sdf or mol2)'
               % (molecule_id, ligand['file'], ext))
       if cmd.count_states(lig_name) != 1:
           raise PlacementError(
               'materialize: molecule %r ligand decoded to %d state(s)'
               % (molecule_id, cmd.count_states(lig_name)))

   else the existing path (byte-identical). The extension-derived reader is the recorded decision (04-DECISIONS.md #18): payload ligand blocks carry NO 'format' key (generator.py:851-861), so the synthetic key's extension is the only carrier; anything else fail-closes. Sentinel-tag / translate / registry flow below stays shared. `os` is already imported.
2. `gamestart.start_game(setup=None, seed=42, candidates=None, ligand_content=None)` (gamestart.py:221): new keyword param with a docstring line ("dict of synthetic file key to molecule text for uploaded games; None = bundled-manifest flow"); pass-through at the `engine.new_game(spec, seed, candidates=candidates)` call (gamestart.py:241) so it becomes `engine.new_game(spec, seed, candidates=candidates, ligand_content=ligand_content)`.
3. `engine.materialize` delegate (the wrapper near engine.py:238): add the same keyword pass-through so start_game's param reaches placement.
  </action>
  <verify>T0: `python3.6 -m py_compile aamatch/placement.py aamatch/gamestart.py aamatch/engine.py`; grep confirms `ligand_content` appears in all four signatures and no existing default changed (`candidates=None` untouched).</verify>
  <done>The whole seam (start_game -> new_game -> materialize) carries ligand_content end-to-end with None defaults.</done>
</task>

<task type="auto">
  <name>Task 3: SMOKE-10 -- uploaded-flow plumbing proven headlessly</name>
  <files>smoke/smoke_10_upload_flow.py</files>
  <action>
Write `smoke/smoke_10_upload_flow.py` following the repo smoke header conventions (repo-root anchor via sys.argv, per smoke_01_bootstrap.py; verdict by printed marker). Run via `bash smoke/run_smoke.sh smoke/smoke_10_upload_flow.py 240`. Real headless PyMOL, NO QApplication needed (T1a tier).

Script design:
- PART 1 (synthetic uploaded flow): read `aamatch/data/ligands/benzamide.sdf` bytes; `text = raw.decode('utf-8')`. Build ONE synthetic manifest-shaped row by copying the benzamide row from MANIFEST.json (read via `aamatch.persistence.read_json_file` + `aamatch.manifest.parse_manifest_dict` + `enumerate_entries`, pick the benzamide row) then rewriting: `set_id='uploaded'`, `entry_id='mol-001'`, `file='uploads/mol-001.sdf'`, `sha256=hashlib.sha256(text.encode('utf-8')).hexdigest()` (record-text hash; Decision 19 in 04-DECISIONS.md), keeping atom_count / bond counts / size fields. `ligand_content = {'uploads/mol-001.sdf': text}`.
- `payload, rows = engine.new_game(setup_state.validate_state(dict(setup_state.DEFAULTS, demo_set_id='uploaded')), 12345, candidates=[row], ligand_content=ligand_content)` -- asserts: returns normally; payload['seed'] == 12345; the level-0 molecule ligand block reads source 'uploaded' and file 'uploads/mol-001.sdf'.
- `registry = engine.materialize(payload, 0, ligand_content=ligand_content)` -- asserts: the ligand object exists; `cmd.count_atoms(lig_object)` equals row['atom_count']; sentinel segi 'AAM'.
- Full start path: on a clean scene, `wiz = gamestart.start_game(setup=<same validated setup>, seed=777, candidates=[row], ligand_content=ligand_content)` -- asserts the returned wizard IS `cmd.get_wizard()` and carries registry/payload (SMOKE-08 return-value pattern). Then `cmd.set_wizard()` (Done) + `placement.cleanup_game_objects()`; assert the scene object list is exactly the pre-start baseline.
- PART 2 (negative control): the SAME synthetic row with `ligand_content=None` -- `engine.new_game(..., candidates=[row])` must RAISE EngineError (the atom_count check fires: 0 atoms loaded vs manifest count). Assert the error type and that the scene is unchanged after the refusal.
- PART 3 (regression): defaults flow untouched -- `gamestart.start_game()` (no args) still starts on the bundled manifest (seed 42 path); Done + cleanup restore baseline.
- Print `=== SMOKE-10 PASS ===`.
Never call cmd APIs that may throw inside an eager assert-detail string (03-04 smoke-authoring rule).
  </action>
  <verify>`bash smoke/run_smoke.sh smoke/smoke_10_upload_flow.py 240` prints `=== SMOKE-10 PASS ===` (T1a). Regression: `bash smoke/run_smoke.sh smoke/smoke_04_e2e.py 240` still PASS and `bash smoke/run_smoke.sh smoke/smoke_08_starter.py 240` still PASS (None defaults byte-identical). Full WSL suite green (T0).</verify>
  <done>The uploaded flow materializes from embedded text end-to-end in real PyMOL; the None default is regression-proven on SMOKE-04/08; the negative control fails closed.</done>
</task>

</tasks>

<verification>
- T0: py_compile all touched modules; full WSL suite green.
- T1a: SMOKE-10 PASS (synthetic upload -> new_game -> materialize -> start_game -> cleanup), SMOKE-04 + SMOKE-08 regression PASS.
- Grep: exactly four signatures gained `ligand_content=None`; no call site changed behavior when it is absent.
</verification>

<success_criteria>
The research's two GAPs (engine._ligand_data_for package-path hardcode; placement.materialize package-path hardcode) are closed additively: uploaded molecules flow by content, bundled molecules by package path, and the two never mix. Phase 7's import side can reuse the same parameter with zero new cmd-tier loading code.
</success_criteria>

<output>
After completion, create `.planning/phases/04-qt-setup-window/04-04-SUMMARY.md` (frontmatter: affects [04-08 upload pipeline, 04-12 export, 04-13 start, Phase 7], subsystem cmd-tier engine).
</output>
