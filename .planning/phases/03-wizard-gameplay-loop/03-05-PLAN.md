---
phase: 03-wizard-gameplay-loop
plan: 05
type: execute
wave: 5
depends_on: ["03-04"]
files_modified:
  - aamatch/gamestart.py
  - aamatch/__init__.py
  - smoke/smoke_08_starter.py
  - tests/test_wizard_source.py
autonomous: true

must_haves:
  truths:
    - "One call — gamestart.start_game() — takes a fresh scene to a playable state: engine.new_game → engine.materialize → GameWizard.activate, with any prior game's objects cleaned first and a prior top-of-stack wizard replaced cleanly (replace=1)"
    - "The Plugins → AA-match menu item launches the game: run_plugin_gui lazily imports gamestart (aamatch/__init__.py keeps ZERO module-level imports — Gate A2 still enforced)"
    - "SMOKE-08 proves the starter headlessly: wizard active, msm set to 0, scene grew by exactly 1+n*n per molecule, Done restores msm, restart is idempotent (old objects cleaned, exactly one game in the scene)"
  artifacts:
    - path: "aamatch/gamestart.py"
      provides: "start_game(setup=None, seed=42) — the cmd-tier starter Phases 4+ reuse (the Qt setup window calls the same function)"
      min_lines: 50
    - path: "aamatch/__init__.py"
      provides: "run_plugin_gui launching the Phase-3 game (lazy import; metadata block untouched)"
    - path: "smoke/smoke_08_starter.py"
      provides: "=== SMOKE-08 PASS === headless starter proof (menu-path function called directly; scene/msm/panel/restart asserts)"
      min_lines: 120
  key_links:
    - from: "aamatch/gamestart.py"
      to: "aamatch/engine.py"
      via: "engine.new_game(setup or DEFAULTS, seed, candidates) + engine.materialize(payload, 0)"
      pattern: "engine\\.(new_game|materialize)"
    - from: "aamatch/gamestart.py"
      to: "aamatch/wizard.py"
      via: "GameWizard(payload, registry, 0, 0).activate(replace=1)"
      pattern: "GameWizard\\(|activate\\("
    - from: "aamatch/__init__.py"
      to: "aamatch/gamestart.py"
      via: "lazy import INSIDE run_plugin_gui (zero module-level imports preserved)"
      pattern: "from \\. import gamestart|from aamatch import gamestart"
---

<objective>
Wire the start path: `aamatch/gamestart.py` (cmd-tier starter: clean scene → new_game → materialize → GameWizard.activate) and re-point `run_plugin_gui` at it, then prove the whole entry path headlessly with SMOKE-08.

Purpose: the human checkpoints (03-06/03-07) start the game from the Plugins menu on a fresh stock PyMOL — that path must exist, be idempotent on restart, and be proven headless first. This is the seam Phase 4's Qt setup window reuses (it will call start_game with the user's setup).
Output: aamatch/gamestart.py + updated aamatch/__init__.py + === SMOKE-08 PASS ===.
</objective>

<execution_context>
@~/.config/opencode/get-shit-done/workflows/execute-plan.md
@~/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@AGENTS.md                                        # standing gates — Gate A2 (zero module-level imports in __init__), module identity, plugin-path install method
@.planning/STATE.md                               # 01-09 verdict: plugin-path install (repo edits live); setup defaults FROZEN
@.planning/phases/03-wizard-gameplay-loop/03-RESEARCH-wizard-interaction.md   # §8 Start-Game wiring recommendation
@.planning/phases/03-wizard-gameplay-loop/03-RESEARCH-movement-spike.md       # §3.3 scheme law — NO start-time editor_scheme guard needed (contingency only)
@aamatch/__init__.py                              # the placeholder being replaced (metadata block MUST stay first)
@aamatch/engine.py                                # new_game/materialize
@aamatch/wizard.py                                # GameWizard + activate(replace=)
@aamatch/setup_state.py                           # DEFAULTS (frozen: molecules 2, difficulty 3, mode 'unset')
@aamatch/placement.py                             # cleanup_game_objects (prefix-only) — the restart primitive
@tests/test_purity.py                             # Gate A2 scans aamatch/__init__.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: aamatch/gamestart.py — the cmd-tier starter</name>
  <files>aamatch/gamestart.py, tests/test_wizard_source.py</files>
  <action>
Create aamatch/gamestart.py (CMD TIER; module-level imports legal here: `from . import engine, placement` + `from .setup_state import DEFAULTS` + `from .wizard import GameWizard`; NO Qt). Docstring: the single game entry point; Phase-4's setup window calls the same function with the user's validated setup; engine state (payload/registry/GameState) is module-side in engine — the wizard requires it live, which start_game guarantees by construction.

`def start_game(setup=None, seed=42, candidates=None):`
1. placement.cleanup_game_objects() FIRST — a restart must never leave two generations of _aam_* objects (prefix-only deletion; user objects untouched; returns {'deleted': n} — record it in the return/print).
2. payload, rows = engine.new_game(dict(setup_state.DEFAULTS if setup is None else setup), seed, candidates=candidates) — new_game validates the setup itself (fail-closed).
3. registry = engine.materialize(payload, 0) — ONE level (the game's current level; cross-level staging is Phase 4/6 flow).
4. wiz = GameWizard(payload, registry, 0, 0); wiz.activate(replace=1) — RECORDED DECISION: replace=1 so a prior top-of-stack wizard (an old game wizard, or the user's measurement wizard) is popped+cleaned properly instead of leaking beneath a push. On a fresh PyMOL (empty stack) replace=1 degenerates to a plain push. The editor_scheme contingency (RESEARCH-movement §5.6) is explicitly NOT implemented — no start-time scheme guard on 2.5.0 (record as contingency only).
5. Print a one-line status (version, molecule count, slot count, seed) and return wiz.

Extend tests/test_wizard_source.py's scanned set to include aamatch/gamestart.py (one-line edit — sequential plan, no conflict): the no-helper-visuals call ban now covers both cmd-tier UI modules. Keep everything else about that test unchanged.
Verify: py_compile + suite. Commit: `feat(03-05): gamestart starter — clean-scene new_game/materialize/activate`.
  </action>
  <verify>python3.6 -m py_compile aamatch/gamestart.py && python3.6 -m unittest tests.test_wizard_source -v && python3.6 -m unittest discover -s tests -v 2>&1 | tail -3</verify>
  <done>Starter module compiles; source gate extended and green; WSL suite green</done>
</task>

<task type="auto">
  <name>Task 2: run_plugin_gui launches the game (Gate A2 preserved)</name>
  <files>aamatch/__init__.py</files>
  <action>
Edit aamatch/__init__.py's run_plugin_gui body ONLY: replace the Phase-1 print placeholder with a lazy import + call — `from . import gamestart` then `gamestart.start_game()` (wrapped in nothing — let EngineError/WizardError propagate fail-closed; PyMOL's menu handler surfaces the traceback). Update the function docstring: Phase 3 launches the playable wizard loop; Phase 4 replaces this with the Qt setup window calling the same gamestart seam. CRITICAL CONSTRAINTS: the `# Version: 0.1.0` metadata block and the NOTE comment MUST remain the first lines of the file (pymol.plugins parses "# Key: value" lines at the very top and stops at the first non-# line — moving anything breaks reinstall version compares); ZERO module-level imports may be added (Gate A2 — lazy imports inside __init_plugin__/handlers are the design; the 01-09 checkpoint proved the menu wiring with addmenuitemqt FIRST — do not reorder __init_plugin__). __version__ stays 0.1.0 (no version bump: format/gate constants untouched).
Verify: py_compile + FULL suite (Gate A2 must stay green) + confirm the metadata block is byte-identical. Commit: `feat(03-05): Plugins menu launches the Phase-3 game loop`.
  </action>
  <verify>python3.6 -m py_compile aamatch/__init__.py && python3.6 -m unittest discover -s tests -v 2>&1 | tail -3 && head -12 aamatch/__init__.py</verify>
  <done>Menu entry point now starts a real game; metadata block intact; zero module-level imports; suite green</done>
</task>

<task type="auto">
  <name>Task 3: SMOKE-08 — headless starter proof</name>
  <files>smoke/smoke_08_starter.py</files>
  <action>
Write smoke/smoke_08_starter.py (house conventions; === SMOKE-08 PASS === marker; repo-root anchor; NO banned cmd CALLs — get_object_matrix legal; `import aamatch` directly, never the pmg_tk.startup path). Checks:
1. Call aamatch.run_plugin_gui() DIRECTLY (it is a plain function now — addmenuitemqt registration is irrelevant headless) → assert cmd.get_wizard() is a GameWizard instance; msm == 0; panel non-empty with all 3-element entries; scene grew by exactly 2×(1+9) = 20 objects (DEFAULTS: molecules_per_level 2, D=3 → n=3) — derive the expected count from the RETURNED wizard's registry (len(registry['molecules']) × (1 + n²)), don't hard-code 20; prompt mentions clicking.
2. Restore-table spot check: cmd.select('sele', <an AA object> + ' and name CA'); cmd.get_wizard().do_select('sele') → slot selected + recolored (spot-check one atom color). cmd.set_wizard() → msm restored (to the pre-smoke snapshot recorded at start), colors restored, game objects REMAIN.
3. Restart idempotence: aamatch.run_plugin_gui() again → old game objects GONE (cleanup_game_objects at start), exactly one fresh game in the scene (count matches the new registry again), wizard replaced (get_wizard() is the NEW instance). 
4. Determinism spot check: the two started games used the same seed → payload['seed'] equal and level-0 molecule ligand files identical (pull via the returned wizards' _payload).
5. Teardown: cmd.set_wizard(); placement.cleanup_game_objects(); assert the scene equals the smoke's pre-start snapshot. Print worst observed deviations. Run: `bash smoke/run_smoke.sh smoke/smoke_08_starter.py 240` until the marker prints; fix gamestart/__init__ bugs as Rule-1 deviations. Regression: SMOKE-07 + full WSL suite.
Commit: `feat(03-05): SMOKE-08 headless starter proof`.
  </action>
  <verify>bash smoke/run_smoke.sh smoke/smoke_08_starter.py 240 2>&1 | tail -5  (=== SMOKE-08 PASS ===) && bash smoke/run_smoke.sh smoke/smoke_07_wizard_loop.py 240 2>&1 | tail -3 && python3.6 -m unittest discover -s tests -v 2>&1 | tail -3</verify>
  <done>SMOKE-08 green (menu-path function, restart idempotence, restore table); SMOKE-07 regression green; suite green</done>
</task>

</tasks>

<verification>
bash smoke/run_smoke.sh smoke/smoke_08_starter.py 240 && python3.6 -m py_compile aamatch/*.py && python3.6 -m unittest discover -s tests -v 2>&1 | tail -3
</verification>

<success_criteria>
- start_game is the one-call entry (clean scene → new_game → materialize → activate(replace=1)) and run_plugin_gui reaches it through a lazy import with the metadata block and Gate A2 intact.
- SMOKE-08 proves the full entry path headlessly including restart idempotence and the msm/color restore on Done.
- No editor_scheme start-guard (contingency recorded, per RESEARCH-movement §5.6).
- Suite + SMOKE-07/04/03 regressions green.
</success_criteria>

<output>
After completion, create `.planning/phases/03-wizard-gameplay-loop/03-05-SUMMARY.md`
</output>
