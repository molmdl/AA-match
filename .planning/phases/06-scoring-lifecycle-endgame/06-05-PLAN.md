---
phase: 06-scoring-lifecycle-endgame
plan: 05
type: execute
wave: 3
depends_on: ["06-03", "06-04"]
files_modified:
  - aamatch/wizard.py
  - smoke/smoke_15_lifecycle.py
autonomous: true
must_haves:
  truths:
    - "Confirm records (guarded), shows score+total, then ADVANCES: next molecule in-level, next level (scene rebuilt), or game completion — the two position books move atomically in ONE op"
    - "The scored debrief (formed vs missed, numbers only) rides the last_event marker and is rendered by the pure status_text builders"
    - "Level advance rebinds the SAME wizard instance — the timer anchor is untouched (D6 intent) and the marker survives to the poll"
    - "After any advance the wizard's slot maps, ligand object, selection, result, and camera are consistent for the NEW molecule"
  artifacts:
    - path: "aamatch/wizard.py"
      provides: "_event_seq/_last_event/_game_over/_end_state + _set_event/_sync_end_state + reworked _confirm_molecule_impl + _rebind_molecule/_rebind_level/_advance_after_record/_compose_active_molecule"
      contains: "_advance_after_record"
    - path: "smoke/smoke_15_lifecycle.py"
      provides: "PART B: wizard-tier confirm/advance/rebind/marker E2E"
      contains: "PART B"
  key_links:
    - from: "wizard._confirm_molecule_impl"
      to: "engine.record_scored"
      via: "the guarded record (re-Confirm refusal inherited)"
      pattern: "record_scored"
    - from: "wizard._advance_after_record"
      to: "engine.advance_molecule/advance_level/complete_game"
      via: "ONE advancement decision site (molecule|level|complete)"
      pattern: "advance_level"
    - from: "wizard._state_dict"
      to: "status_text.status_events"
      via: "additive last_event/game_over/end_state keys (plain data, contract 2)"
      pattern: "last_event"
---

<objective>
Wizard lifecycle core (SCORE-01/02/03): rework Confirm into record -> event marker -> advance (molecule | level | complete), with atomic two-book advancement (GameState + wizard indexes), scene rebinds, camera re-framing via the 06-04 seam, and the event-marker/_state_dict infrastructure every later lifecycle op reuses.

Purpose: this is the progression spine — SCORE-01's "score + total, then advance" and SCORE-03's level escalation become real here; 06-06 (skip/give-up) and 06-07 (tab) consume the seams this plan builds.
Output: reworked wizard.py + SMOKE-15 PART B.
</objective>

<execution_context>
@~/.config/opencode/get-shit-done/workflows/execute-plan.md
@~/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-scoring-lifecycle-endgame/06-01-SUMMARY.md
@.planning/phases/06-scoring-lifecycle-endgame/06-03-SUMMARY.md
@.planning/phases/06-scoring-lifecycle-endgame/06-04-SUMMARY.md
@.planning/phases/06-scoring-lifecycle-endgame/06-RESEARCH.md
@aamatch/wizard.py
@aamatch/wizard_core.py
@aamatch/status_text.py
@aamatch/gamestart.py
@smoke/smoke_15_lifecycle.py

Anchors (verified):
- wizard.py:126-143 __init__ (position attrs); :301-325 _select_slot (restore-FIRST pattern :317-319); :333-356 _state_dict; :436-449 _guard (returns op result); :544-584 confirm_molecule/_confirm_molecule_impl (the REWORK target; the :550-553 re-Confirm caveat closes via engine.record_scored's guard); extras computation :567-580 (KEEP, reused).
- 06-03 ops available: engine.record_scored(level, molecule, required, records=None), engine.skip_molecule(...), engine.advance_molecule(), engine.advance_level() -> new registry, engine.complete_game(now=None) -> summary, engine.total_score(), engine.is_over().
- 06-04 seam: gamestart.compose_molecule_view(registry, molecule_index=0) = front-offset + above-grid roll + zoom-LAST.
- _state_dict consumers tolerate extra keys (wizard_text builders .get-based; 05-07 additive precedent).

Binding decisions (research resolutions, final):
- D3: Confirm advances IMMEDIATELY (spec.md:47 "show ... then move"); `_result` is CLEARED on advance (the debrief lives in the info box via the marker; the panel shows the NEXT molecule — recorded shift from the 04-15 clarification, rationale documented in the docstring).
- Level advance = SAME-INSTANCE REBIND (DELIBERATE DEVIATION from research Q1's new-wizard push, rationale recorded): (i) the last_event marker set on the polled instance survives to the tab's sync refresh — a new instance would LOSE the scored event for the level's final molecule; (ii) no msm ORDER-LAW re-run mid-game (msm stays 0); (iii) the dying level's color store is cleared WITH its objects instead of restoring colors on deleted objects. The D6 timer-continuation intent holds by construction: activate_game is never called mid-game; SMOKE-15 PART B asserts the anchor unchanged.
- Marker mechanism (Q7): `_event_seq` int + `_last_event` dict-or-None, plain data (contract 2 — picklable); every successful lifecycle op sets `_last_event = {'kind': ..., 'seq': _event_seq, ...payload}` and increments seq. On level advance the marker is set AFTER the rebind so the poll sees marker + new position in ONE diff.
- molecule_scored marker payload (consumed by 06-02's builders — shapes pinned): {'kind', 'seq', 'score': float, 'total': float, 'molecule_pos': int (1-based, the COMPLETED molecule — captured BEFORE the advance), 'molecule_total': int, 'formed': list, 'required': dict, 'extras': list}.
- PROSE_PIN: docstrings must not add/remove banned-token mentions.
- Module identity: lazy `from . import engine` / `from . import gamestart` INSIDE methods (wizard.py:22-29 law).
</context>

<tasks>

<task type="auto">
  <name>Task 1: marker mechanism + _state_dict extension + rebind helpers</name>
  <files>aamatch/wizard.py</files>
  <action>
    1. __init__ (:126-143) adds: `self._event_seq = 0`, `self._last_event = None`, `self._game_over = False`, `self._end_state = None` (plain data; contract 2). Extend the attribute-list docstring.
    2. `_set_event(self, kind, **payload)`: `self._event_seq += 1`; `self._last_event = dict(kind=kind, seq=self._event_seq); self._last_event.update(payload)`.
    3. `_sync_end_state(self)`: lazy `from . import engine`; `gs = engine._current_game()`; `self._game_over = bool(gs.game_over)`; `self._end_state = gs.end_state`. (Op-time mirroring — GameState.game_over only changes through wizard ops, the only callers; drift-free by construction.)
    4. `_state_dict` (:333-356) adds THREE keys: `'last_event': self._last_event, 'game_over': self._game_over, 'end_state': self._end_state` (additive; document).
    5. `_rebind_molecule(self, molecule_index)`: if `self._current_slot is not None`: restore that slot's colors FIRST (`self._restore_slot_colors(self._objects_by_slot[self._current_slot])` — the :317-319 pattern; objects still exist within a level); then rebuild `self._slot_by_object = wizard_core.build_slot_map(self._registry, molecule_index)` + the reverse map + `self._ligand_object = self._registry['molecules'][molecule_index]['ligand'][0]`; set `self._molecule_index = molecule_index`; `self._current_slot = None`; `self._result = None`.
    6. `_rebind_level(self, new_registry, level_index)`: `self._registry = new_registry`; `self._level_index = level_index`; `self._color_store.clear()` (the level's objects were DELETED by advance_level's cleanup — nothing to restore; document); `self._current_slot = None`; `self._result = None`; then molecule-0 maps + ligand exactly as _rebind_molecule does for index 0 (reuse: call the map/ligand portion — factor a tiny `_rebind_maps(molecule_index)` used by both).
    7. `_compose_active_molecule(self)`: lazy `from . import gamestart`; `gamestart.compose_molecule_view(self._registry, self._molecule_index)`.
  </action>
  <verify>py_compile + full WSL suite green (test_wizard_source AST gates, test_wizard_text pins untouched); grep `grep -n "_last_event\|_rebind_level" aamatch/wizard.py` shows the new members.</verify>
  <done>Marker + end-state mirroring + both rebind helpers + compose exist; _state_dict carries the three additive keys; all existing wizard tests still green.</done>
</task>

<task type="auto">
  <name>Task 2: confirm rework + shared _advance_after_record</name>
  <files>aamatch/wizard.py</files>
  <action>
    1. Extract the advancement decision into `_advance_after_record(self)` (called AFTER a successful record, BEFORE the event is set) returning `(advanced, summary)`:
       - capture `scored_level = self._level_index + 1`, `scored_pos = self._molecule_index + 1`, `m_total = len(self._registry['molecules'])`, `l_total = len(self._payload['levels'])` FIRST.
       - if `self._molecule_index + 1 < m_total`: `engine.advance_molecule()`; `self._rebind_molecule(self._molecule_index + 1)`; return ('molecule', None).
       - elif `self._level_index + 1 < l_total`: `new_registry = engine.advance_level()`; `self._rebind_level(new_registry, self._level_index + 1)`; `self._compose_active_molecule()`; return ('level', None).
       - else: `summary = engine.complete_game()`; return (None, summary). (No rebind — the panel keeps its state; the game is over.)
    2. Rework `_confirm_molecule_impl` (:556-584): lazy engine import; `if engine.is_over(): raise WizardError('The game is over.')` (gate; 06-06 hardens the rest); `required = self._required()`; `records = engine.detect_molecule(self._level_index, self._molecule_index)`; `score, formed = engine.record_scored(self._level_index, self._molecule_index, required, records=records)`; `total = engine.total_score()`; KEEP the extras computation verbatim (:567-580); `self._error = None`; then `advanced, summary = self._advance_after_record()`; `self._sync_end_state()`; THEN set the event with the CAPTURED completed-molecule position: `self._set_event('molecule_scored', score=float(score), total=float(total), molecule_pos=scored_pos, molecule_total=m_total, formed=list(formed), required=required, extras=extras)`; `cmd.refresh_wizard()`; return `{'score': float(score), 'total': float(total), 'advanced': advanced, 'game_over': self._game_over, 'summary': summary, 'level_pos': scored_level, 'molecule_pos': scored_pos}` (plain data through the _guard seam — the 06-07 tab consumes game_over/summary).
    3. Update confirm_molecule/_confirm_molecule_impl docstrings: the Phase-3 caveat (:550-553) is CLOSED by engine.record_scored's guard (a repeated Confirm now refuses with 'This molecule already has a recorded result (scored or skipped) -- use Restart to replay the game.' landing on the panel error line); `_result` clearing on advance is the D3 resolution (debrief lives in the info box).
    - Do NOT change confirm_molecule's public signature (no args, returns via _guard).
  </action>
  <verify>py_compile + WSL suite green; grep `grep -n "_advance_after_record\|record_scored" aamatch/wizard.py` shows both.</verify>
  <done>Confirm = guarded record -> total -> advance (molecule|level|complete) -> marker -> plain-data return; two books move in ONE op; the re-Confirm refusal is inherited from the engine.</done>
</task>

<task type="auto">
  <name>Task 3: SMOKE-15 PART B (wizard lifecycle E2E)</name>
  <files>smoke/smoke_15_lifecycle.py</files>
  <action>
    Append PART B (ALWAYS tier, drives wizard PUBLIC methods — no modals anywhere):
    1. `start_game(activate=True)` (defaults: 3 levels x 2 molecules); `wiz = cmd.get_wizard()`; capture `anchor0 = engine.game_status()['timer_anchor']` (float).
    2. Molecule advance: `wiz.confirm_molecule()` (zero-score; returns the plain dict) -> assert: returned 'advanced' == 'molecule', `wiz.get_status()['molecule_pos'] == 2`, `last_event['kind'] == 'molecule_scored'` with seq 1 and molecule_pos 1, `get_status()['result'] is None` (D3 cleared), selection cleared, and the required label data (`get_status()['required']`) reflects molecule 2.
    3. GUARD via the panel: `wiz.confirm_molecule()` again on the SAME molecule? NO — molecule advanced; instead prove the refusal on molecule 2 by... skip — the refusal proof lives in PART A (engine) + here: re-Confirm is unreachable after a real advance; assert instead the ADVANCE-path refusal surface: drive a SECOND confirm on molecule 2 (records it) then a third on molecule 2 is impossible (already advanced). Keep PART B focused: after confirm #2, 'advanced' == 'level' — assert: `get_status()['level_pos'] == 2`, molecule_pos 1, the SAME wiz instance (identity check `cmd.get_wizard() is wiz`), `engine.game_status()['timer_anchor'] == anchor0` (THE D6 INTENT ASSERT), fresh `_aam_*` object names (level-1 count), `_color_store` empty via get_status (no direct private reach — assert selection None + result None), marker seq incremented with molecule_pos 2 (the completed level-1 final molecule).
    4. Rebind proof: `wiz._select_slot` path — pick a slot of the NEW molecule via the SMOKE-07 scripted-pick recipe (`cmd.select('sele', '<slot object> and name CA')` + `do_select('sele')`) -> `get_status()['selected']['slot_id']` resolves (the rebuilt maps work).
    5. Completion: confirm through the remaining molecules/levels -> the LAST confirm returns 'advanced' None + 'game_over' True + a summary dict with the 06-01 keys; `get_status()['game_over'] is True`, end_state 'completed'; `engine.is_over()` True.
    6. Camera: after the level advance, `cmd.get_view()` differs from pre-advance (compose ran) — assert view-matrix change.
    7. Restore: done pop + prefix cleanup + `_last_start = None` (the PART A restore shape; timer discipline N/A — no tab in this smoke).
  </action>
  <verify>`bash smoke/run_smoke.sh smoke/smoke_15_lifecycle.py` greps `=== SMOKE-15 PASS ===` (PART A + B counts, zero FAIL).</verify>
  <done>The wizard lifecycle is proven headlessly: molecule advance, level advance (same instance, anchor untouched, scene rebuilt, maps work), completion, marker flow.</done>
</task>

</tasks>

<verification>
- `python3.6 -m py_compile aamatch/*.py` && `python3.6 -m unittest discover -s tests -v` ALL green (753+; purity + audit gates untouched).
- `bash smoke/run_smoke.sh smoke/smoke_15_lifecycle.py` -> PASS (A+B).
- Regression: SMOKE-07 (`bash smoke/run_smoke.sh smoke/smoke_07_wizard_loop.py`) PASS — the reworked confirm keeps the Phase-3 loop semantics for a FIRST confirm (result rendered... note: _result still set on a NON-advancing confirm? NO — every successful confirm advances now; smoke_07's confirm-driven asserts target the loop/movement invariants, verify it stays green and reconcile any stale _result expectation in the smoke if it asserts _result AFTER confirm — if smoke_07 asserts the result lines post-confirm, that expectation is now the ADVANCE behavior; adapt ONLY if it fails, documenting the deliberate evolution).
- PROSE_PIN untouched.
</verification>

<success_criteria>
- Confirm scores, shows, advances — molecule, level (anchor untouched, same instance), or complete — with the marker carrying the debrief data.
- The two position books (GameState + wizard indexes) can never drift: one op mutates both.
- SMOKE-15 A+B green; SMOKE-07 regression green.
</success_criteria>

<output>
After completion, create `.planning/phases/06-scoring-lifecycle-endgame/06-05-SUMMARY.md` (frontmatter: subsystem wizard, provides the marker payload shapes + confirm return contract + _advance_after_record/_rebind_* seams for 06-06/06-07/06-09; records the same-instance-rebind + D3 + D6-mechanism deviation decisions).
</output>
