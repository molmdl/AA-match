---
phase: 05-game-status-tab-start-sequence
plan: 07
type: execute
wave: 1
depends_on: []
files_modified: [aamatch/wizard.py, aamatch/engine.py, smoke/smoke_14_status_surface.py]
autonomous: true

must_haves:
  truths:
    - "GameWizard.get_status() is the PUBLIC plain-data snapshot the Game status tab polls: _state_dict() extended additively with level_pos (_level_index + 1) and level_total (len(_payload['levels'])) — pickle-safe (contract 2), no test pins the dict's exact key set (grep-verified zero _state_dict references in tests/), the pure builders consume .get and tolerate extra keys"
    - "engine.game_status() is the read-only plain-data snapshot of the live GameState (to_dict(): current_level_index, current_molecule_index, molecule_scores, skip_count, giveup_count, timer_anchor, formed_types_per_molecule) and raises EngineError before new_game — the READ path for running scores/counters that Phase 6 inherits"
    - "READ path only: no advance/skip/give-up writes anywhere in this plan (Phase 6 owns the scoring lifecycle); no new cmd-tier module"
  artifacts:
    - path: "aamatch/wizard.py"
      provides: "_state_dict() += level_pos/level_total; NEW public get_status() returning it (the tab never reaches _state_dict across modules)"
      contains: "def get_status"
    - path: "aamatch/engine.py"
      provides: "NEW game_status() -> _current_game().to_dict() (~4 lines, additive)"
      contains: "def game_status"
    - path: "smoke/smoke_14_status_surface.py"
      provides: "NEW headless smoke (=== SMOKE-14 PASS ===): T1a accessor parts (shape, plain-data, EngineError-before-game, no-mutation) — extended with T1b status-surface parts by plan 05-10"
  key_links:
    - from: "aamatch/game_window.py _refresh_status (plan 05-10)"
      to: "aamatch/wizard.py GameWizard.get_status"
      via: "the 1 Hz poll reads the live wizard's plain-data dict"
      pattern: "get_status"
    - from: "aamatch/wizard.py _state_dict"
      to: "aamatch/wizard_text builders"
      via: "the SAME dict the panel/prompt builders consume — level keys are additive so the existing consumers are unaffected (they read only their own keys)"
      pattern: "_state_dict"
---

<objective>
The read accessors of the status access path (researcher B's Q6): extend `_state_dict()` with the level keys, expose `GameWizard.get_status()` publicly, add `engine.game_status()`, and prove the shapes headlessly in a NEW SMOKE-14.

Purpose: the Game status tab must NEVER reach engine._game/_payload/wizard privates (05-RESEARCH-status-surface.md anti-patterns) and must NEVER push callbacks onto the wizard (PyMOL pickles the whole wizard stack on session save — SRC wizarding.py:175-180; contract 2). Everything the tab needs is already plain data on the wizard/GameState — it only lacks public accessors and the level keys. Landed in Wave 1 so the wiring plans (05-09/05-10) consume a green read path.
Output: two additive accessors + SMOKE-14 (T1a parts).
</objective>

<execution_context>
@~/.config/opencode/get-shit-done/workflows/execute-plan.md
@~/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/05-game-status-tab-start-sequence/05-RESEARCH-status-surface.md
@.planning/STATE.md
@aamatch/wizard.py
@aamatch/engine.py
@aamatch/game_state.py
@smoke/run_smoke.sh

Key facts (verified, transcribe exactly):
- _state_dict() (wizard.py:333-349) currently returns: molecule_id, molecule_pos, molecule_total, required, selected, result, error. Extend with 'level_pos': self._level_index + 1 and 'level_total': len(self._payload['levels']) — plain data; pickle-safe; the existing pure consumers (panel_entries/prompt_lines) read only their own keys and tolerate extras (wizard_text.py:209-265).
- NEW public method on GameWizard: `def get_status(self):` — docstring: "Plain-data status snapshot for the Game status tab's 1 Hz poll (READ path; contract 2 — plain data only, never Qt/callables). Returns _state_dict()." Body: `return self._state_dict()`.
- NEW engine function (research B Pattern 4, additive ~4 lines): `def game_status():` — docstring: "Read-only plain-data snapshot of the live GameState (no mutation). Returns GameState.to_dict(): current_level_index, current_molecule_index, molecule_scores, skip_count, giveup_count, timer_anchor, formed_types_per_molecule. Raises EngineError when no game is live." Body: `return _current_game().to_dict()`. Place beside the other public ops; lazy-sibling imports per the engine module law (game_state is a pure sibling — engine already imports it at module level; keep whatever import style the file uses).
- engine._current_game() raises EngineError before new_game (engine.py:98-103) — game_status inherits the guard for free.
- game_state.to_dict() keys (game_state.py:217-229): exactly the 7 listed above; lossless with from_dict.
- wizard.py contracts to respect: contract 2 (picklable plain data only, wizard.py:17-29); the msm ORDER LAW and every existing method untouched; PROSE_PIN zero-mention discipline (no banned-token docstring additions — test_wizard_source.py:134-146 pins wizard.py's zero-mention count).
- Smoke conventions: run_smoke.sh derives NN from the basename and greps '=== SMOKE-14 PASS ==='; smokes anchor the repo root via sys.argv first (01-07); SMOKE-14 is a NEW file — no existing smoke changes. SMOKE-13 is taken (timer probe); SMOKE-14 was reserved for the status surface (research B Q6/OQ-6: separate from SMOKE-11 so it is independent of A's PART G rework and C's hint PART).
</context>

<tasks>

<task type="auto">
  <name>Task 1: the two additive accessors</name>
  <files>aamatch/wizard.py, aamatch/engine.py</files>
  <action>
1. wizard.py _state_dict(): add the two level keys (level_pos = self._level_index + 1, level_total = len(self._payload['levels'])) to the returned dict with a comment citing the additive extension (05-07; the pure builders tolerate extras).
2. wizard.py: add the public get_status() method (docstring + body per the context facts) near _state_dict.
3. engine.py: add game_status() per the context facts.
4. Docstring discipline: zero banned-token mentions in both files (PROSE_PIN counts are exact).
Commit: `feat(05-07): additive status read accessors (get_status + game_status)`.
  </action>
  <verify>python3.6 -m py_compile aamatch/*.py && python3.6 -m unittest discover -s tests -v — full suite green (nothing in WSL imports these modules; this is the syntax + regression floor; test_code_audit's PROSE_PIN stays green)</verify>
  <done>Both accessors exist additively; no existing behavior changed</done>
</task>

<task type="auto">
  <name>Task 2: SMOKE-14 — T1a accessor proof</name>
  <files>smoke/smoke_14_status_surface.py</files>
  <action>Create smoke/smoke_14_status_surface.py with the established smoke skeleton (repo-root anchor via sys.argv first + cwd fallback; check(name, cond, detail) helper + REC dict; try/except per part; final verdict marker '=== SMOKE-14 PASS ===' as the SOLE verdict carrier; header docstring citing the status-surface research + the tier plan). Parts:
PART 1 (ALWAYS, T1a — accessors, no dialog):
1. BEFORE any game: `from aamatch import engine` + check that engine.game_status() raises engine.EngineError (try/except around the call — never let it throw into an eager check string).
2. `wiz = gamestart.start_game()` (defaults; the default path activates — fine for the accessor drive).
3. status = wiz.get_status(): check it is a dict carrying ALL of molecule_id, molecule_pos, molecule_total, required, selected, result, error, level_pos, level_total; check level_pos == 1 and level_total is an int >= 1; check it is plain data (json.dumps(status) succeeds — no callables/objects); check molecule_pos == 1 and molecule_total == len(wiz._registry['molecules']).
4. s2 = wiz.get_status(): check s2 == status (no mutation across calls).
5. gs = engine.game_status(): check the key set is EXACTLY the 7 to_dict keys; check timer_anchor is None (GO not yet run in this smoke — start_timer has not fired; if a future change makes it non-None, assert it is a float instead — write the check tolerance-aware); check molecule_scores == [].
6. Restore: cmd.set_wizard() + placement.cleanup_game_objects() + baseline-exact check.
Header notes that T1b status-surface parts are added by the status-wiring plan (05-10). Commit: `test(05-07): SMOKE-14 T1a accessor proof`.
  </action>
  <verify>bash smoke/run_smoke.sh smoke/smoke_14_status_surface.py 120 — === SMOKE-14 PASS ===</verify>
  <done>Both accessors proven headlessly: shapes, plain-data, no-mutation, EngineError guard</done>
</task>

</tasks>

<verification>
- `python3.6 -m py_compile aamatch/*.py` — floor.
- `python3.6 -m unittest discover -s tests -v` — full WSL suite.
- `bash smoke/run_smoke.sh smoke/smoke_14_status_surface.py 120` — === SMOKE-14 PASS ===.
- `bash smoke/run_smoke.sh smoke/smoke_08_starter.py 120` — regression (wizard/engine seams untouched in behavior).
</verification>

<success_criteria>
- get_status() returns the _state_dict shape + level_pos/level_total, publicly, pickle-safe.
- engine.game_status() returns the 7-key read-only snapshot and refuses before new_game.
- SMOKE-14 exists as the status-surface smoke with its T1a parts green (T1b extension slot documented).
</success_criteria>

<output>
After completion, create `.planning/phases/05-game-status-tab-start-sequence/05-07-SUMMARY.md`
</output>
