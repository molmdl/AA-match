---
phase: 02-headless-game-engine
plan: 09
type: execute
wave: 5
depends_on: ["02-06"]
files_modified:
  - aamatch/geometry.py
autonomous: true

must_haves:
  truths:
    - "The cmd bridge extracts detector-contract atom records (side/object/id/name/elem/resn/resi/alt/formal_charge/x/y/z) + ligand bond block + index->id map from game objects using ONLY iterate_state with explicit space dicts"
    - "World-frame poses are read back per-atom for pose assertions; cmd.get_model is never used"
    - "Bounding sphere + centroid helpers feed the generator's ligand_data"
  artifacts:
    - path: "aamatch/geometry.py"
      provides: "extract_game_atoms, ligand_bonds, bounding_sphere, centroid_of, coords_of"
      min_lines: 100
  key_links:
    - from: "aamatch/geometry.py"
      to: "detector record contract (02-06)"
      via: "records match the exact dict shape detector.detect consumes"
      pattern: "'side': 'aa'"
    - from: "aamatch/geometry.py"
      to: "aamatch/paths.py"
      via: "every file-path use routes through to_windows_path (loads live in placement; geometry only reads live objects)"
      pattern: "iterate_state"
---

<objective>
Build the cmd-tier geometry bridge: PyMOL objects -> plain data for the pure detector and generator (the extraction half of the detector contract, detection research §5.1).

Purpose: Geometry crosses the pure/cmd boundary at exactly two points (post-materialize bounds + detect-on-demand); this module IS that boundary. Must be usable identically by SMOKE-03/04/05 and the Phase-3 wizard.
Output: aamatch/geometry.py (cmd tier; pymol.cmd imported at module level — this is NOT a pure module).
</objective>

<execution_context>
@~/.config/opencode/get-shit-done/workflows/execute-plan.md
@~/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/02-headless-game-engine/02-RESEARCH-materialization.md  # §5 identity/read discipline, §6 pose assertion, §7.1 module split
@.planning/phases/02-headless-game-engine/02-RESEARCH-detection.md        # §5.1 atom-record + bond-block contract (EXACT shape)
@.planning/research/PITFALLS.md                                           # Pitfall 8 (ID uppercase, space=, b<0), Pitfall 15 (no get_model)
@aamatch/detector.py                                                      # the record shape this module must produce
</context>

<tasks>

<task type="auto">
  <name>Task 1: geometry.py — extraction + bounds + pose reads</name>
  <files>aamatch/geometry.py</files>
  <action>
Implement (cmd tier — `from pymol import cmd` at module level is LEGAL here; this module must NEVER be added to PURE_MODULES):

1. `game_object_names()` -> sorted list of cmd.get_names('objects') entries starting with '_aam_' (prefix filter only — never hetatm/polymer filters).
2. `extract_game_atoms()` -> list of atom-record dicts over ALL game objects (selection built as ' or '.join over game_object_names()): one cmd.iterate_state(1, sel, "...", space={'stored': out}) pass appending (model, ID, name, elem, resn, resi, alt, formal_charge) + coords via the SAME iterate_state expression (x,y,z are exposed there). Build records: {'side': 'aa' if object.startswith('_aam_aa') else 'lig', 'object': model, 'id': ID, 'name': name, 'elem': elem, 'resn': resn, 'resi': resi, 'alt': alt, 'formal_charge': formal_charge, 'x': x, 'y': y, 'z': z}. Uppercase ID; explicit space dict ALWAYS; NO round() inside expressions (probe-proven NameError). Sort records by (object, id) for deterministic input to the detector.
3. `ligand_bonds(lig_object)` -> (bonds, index_to_id): cmd.get_bonds(lig_object, 1) returns (atm1, atm2, order) with 0-based indices (querying.py caveat); build index_to_id via ONE iterate_state pass appending (index, ID); return ([(i, j, order)], {index: id}).
4. `bounding_sphere(records)` -> ((cx, cy, cz), radius): centroid of ALL ligand-side records, radius = max distance from centroid.
5. `centroid_of(object_name)` and `coords_of(object_name)` -> ([(id, x, y, z)]) via iterate_state — the pose-assertion helpers (assert coordinates, NEVER get_object_matrix).
6. Docstring: world-frame only; state 1 only; never cmd.get_model; identity = (object, id); this is the ONE extraction helper shared by smokes and (later) the wizard.

Guardrails: no numpy (plain tuples/lists); no file I/O; no thresholds/typing logic (bridge stays rule-free).
  </action>
  <verify>python3.6 -m py_compile aamatch/geometry.py</verify>
  <done>Module compiles under 3.6; record shape matches detector contract field-for-field</done>
</task>

<task type="auto">
  <name>Task 2: Headless probe — extraction proves out on real objects</name>
  <files>tmp/probe_geometry.py</files>
  <action>
Write tmp/probe_geometry.py (git-ignored tmp/, the established probe pattern): load aamatch/data/ligands/benzamide.sdf into a _aam_lig name via paths helpers; cmd.fragment('ser', '_aam_aa01') + alter segi='AAM', b=-999.0 + sort; call extract_game_atoms() and print PROBE lines (flush=True): record count, one full record dict, side split counts; ligand_bonds() -> bond count + orders + index_to_id size; bounding_sphere() values; centroid_of/coords_of on the AA; assert records exist for both sides and every record has all contract keys. Final line `=== PROBE-GEOM DONE ===`. Run: `python3.6 -m py_compile tmp/probe_geometry.py && timeout 150 cmd.exe /c "C:\\src\\run-conda-pymol.bat -cq tmp\\probe_geometry.py" 2>&1 | tail -30` and require the DONE marker in output.
  </action>
  <verify>Probe output contains '=== PROBE-GEOM DONE ===' with sane counts (benzamide 16 atoms, 11 bonds)</verify>
  <done>Extraction produces contract-shaped records + bond block + bounds from real PyMOL objects</done>
</task>

<task type="auto">
  <name>Task 3: Suite gate</name>
  <files>aamatch/geometry.py</files>
  <action>
Run the full WSL suite: geometry.py is cmd-tier and must NOT be in PURE_MODULES — confirm Gate A/A2/B/D still green (Gate D compiles it; Gates A/B correctly ignore it). Confirm no purity regression.
  </action>
  <verify>python3.6 -m unittest discover -s tests -v</verify>
  <done>Full suite green; geometry.py correctly outside the pure registry</done>
</task>

</tasks>

<verification>
python3.6 -m py_compile aamatch/*.py && python3.6 -m unittest discover -s tests -v && timeout 150 cmd.exe /c "C:\\src\\run-conda-pymol.bat -cq tmp\\probe_geometry.py" 2>&1 | grep -q "=== PROBE-GEOM DONE ==="
</verification>

<success_criteria>
The extraction boundary exists, is probe-proven on real objects, and matches the detector contract exactly.
</success_criteria>

<output>
After completion, create `.planning/phases/02-headless-game-engine/02-09-SUMMARY.md`
</output>
