---
phase: 08-demo-curation-citations
plan: 10
type: execute
wave: 6
depends_on:
  - "08-07"
files_modified:
  - tests/test_demo_supply.py
  - aamatch/generator.py
  - tests/test_generator.py
  - tests/test_generator_invariants.py
autonomous: true
must_haves:
  truths:
    - "The final curated manifest's size-class supply is MEASURED (bucket tally from manifest heavy-atom counts) and a default-config game (molecules_per_level=2, difficulty_levels=3) provably generates over ALL sets AND over each single curated set (WSL, pure generator, real manifest)"
    - "SIZE_S1/S2 are either unchanged (no-op decision RECORDED with the measured numbers) or re-tuned with the generator invariant tests updated and green — never silently edited"
    - "The generator's bucket rule stays single-homed in aamatch/generator.py (game_file.py's _candidate_class import untouched)"
  artifacts:
    - path: "tests/test_demo_supply.py"
      provides: "Permanent supply test: bucket tally + default-config generation over the real manifest (skips cleanly if curated sets are absent)"
      min_lines: 60
    - path: "aamatch/generator.py"
      provides: "(ONLY if re-tune triggered) SIZE_S1/S2 constants adjusted with source comments"
    - path: "tests/test_generator.py"
      provides: "(ONLY if re-tune triggered) bucket-boundary invariant updates"
  key_links:
    - from: "tests/test_demo_supply.py"
      to: "aamatch/data/MANIFEST.json"
      via: "enumerate_entries over the real bundled payload; ligand_data built per the 02-08 contract (profile from entry keys)"
      pattern: "enumerate_entries"
    - from: "tests/test_demo_supply.py"
      to: "aamatch.generator.new_game-level generation"
      via: "default config (m=2, D=3) generates without GenerationError over all-sets and per-set"
      pattern: "generate_payload|new_game|_select_pool"
---

<objective>
Measure the real size-class supply of the final curated manifest against the generator's bucket machine and decide — explicitly, with recorded numbers — whether SIZE_S1/S2 need re-tuning (generator.py:118-120 explicitly deferred this to Phase 8).

Purpose: closes open decision #5 (Q8) honestly. Predicted distribution is 12 small / 4 medium / 0 large; the NEAREST-BUCKET fallback (verified in `_select_pool`) is the designed carrier for large-tier games — this plan proves the default configs actually generate and records the no-op-or-retune verdict.
Output: permanent supply test + recorded verdict (+ conditional re-tune).
</objective>

<execution_context>
@~/.config/opencode/get-shit-done/workflows/execute-plan.md
@~/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/08-demo-curation-citations/08-RESEARCH-mechanics.md
@aamatch/generator.py
@tests/test_generator.py
@tests/test_generator_invariants.py
@aamatch/data/MANIFEST.json

Bucket machine (research Q8, VERIFIED): small < SIZE_S1(25) ≤ medium < SIZE_S2(60) ≤ large; `bucket_index = (L*3)//D`; D≥3 targets all three classes; fallback to the NEAREST non-empty bucket ONLY when the target pool is EMPTY; a non-empty-but-thin pool raises GenerationError naming the shortage; distinctness is per level so the binding constraint is `len(pool) >= molecules_per_level`. Minimum-viable floor: ≥ 3 entries per class (supports default m=2 + headroom at D=3+).
</context>

<tasks>

<task type="auto">
  <name>Task 1: tests/test_demo_supply.py — measure + prove default-config generation</name>
  <files>tests/test_demo_supply.py</files>
  <action>
Create the permanent supply test (python3.6 unittest, zero stubs, repo-root anchored via `__file__`):
1. Skip guard: if the manifest lacks the curated set ids (pre-Phase-8 tree), `self.skipTest('curated sets not bundled yet')` — the test never blocks a dev-only tree.
2. Bucket tally: read the real manifest via the pure chain, `enumerate_entries`, tally `_candidate_class` per entry; assert the tally matches a RECORDED expectation dict pinned as a module constant `EXPECTED_SUPPLY = {'small': 12, 'medium': 4, 'large': 0}` (update this constant ONLY in the same commit that changes bundled data — it is the measured contract).
3. Default-config generation: build ligand_data per the 02-08 contract shape ({'centroid', 'radius', 'profile'} keyed by (set_id, entry_id)) with HAND-BUILT support-capable profiles, following the tests/test_generator.py `profile()` helper pattern: `has_acceptor=True` base, plus `has_halogen_donor` / `has_metal` set from the entry's manifest flags (halogen_present / metal_present). Do NOT call capability.ligand_profile — it consumes atom/bond records that manifest counts + flags cannot supply in a pure test, and a fail-closed profile from reconstructed counts would refuse unset-mode generation for CAPABILITY reasons and falsely trigger Task 2's SIZE re-tune branch; do NOT mirror engine._ligand_data_for (temp-loading via cmd is the cmd tier's job, unavailable here). Why hand-built support-capable profiles isolate bucket supply: in unset mode the required-type draw pool is the ligand-SUPPORTED subset of the vocabulary minus hydrophobic (generator.py OQ-1/§4.3 mode rulings), so an unsupported type is never sampled and k clamps via min(n_required_types, len(pool)) — a support-capable profile can only shrink the draw pool, never refuse on capability. assert `generate` (aamatch/generator.py:703 — returns the level-spec payload dict; it does NOT return (payload, rows), that tuple is engine.new_game's) succeeds for: all-sets ('' filter) with DEFAULTS (m=2, D=3, source_mode 'demo', interaction_mode 'unset', allowed_interactions []), and EACH single curated set with m=2, D=3 — no GenerationError anywhere. Pin an EXPECTED refusal ONLY for genuinely thin single sets that cannot support m=2/D=3 by the verified pool rules (demo-easy-3: 1 entry — m=2 needs 2 distinct picks per level; binding constraint len(pool) >= molecules_per_level): pin the fail-closed refusal message naming the shortage instead of generation — refusals-are-correct is itself the assertion. Consult generator.py:809-818's refusal text and pin the exact wording per pinned set.
4. Print the tally in the test docstring/verbose output for the SUMMARY record.
Run the test green. If any all-sets default-config generation REFUSES (not the pinned per-set thin cases): that refusal is a BUCKET-SUPPLY shortage by construction (the profiles are support-capable, so capability cannot refuse) and triggers Task 2's re-tune branch. A capability-shaped refusal ('no amino acid can form ...' / 'supports none of the allowed interactions') means the test's hand-built profiles are wrong — fix the profiles; it is NEVER a SIZE re-tune trigger.
  </action>
  <verify>python3.6 -m unittest tests.test_demo_supply -v (green: tally matches EXPECTED_SUPPLY; default-config all-sets generation succeeds; per-set expectations match pinned behavior)</verify>
  <done>Supply measured + default-config generation proven over the real manifest; trigger decision is now evidence-based.</done>
</task>

<task type="auto">
  <name>Task 2: re-tune verdict — record no-op OR re-tune SIZE_S1/S2</name>
  <files>aamatch/generator.py, tests/test_generator.py, tests/test_generator_invariants.py</files>
  <action>
CONDITION (explicit): re-tune ONLY if Task 1's all-sets default-config generation (m=2, D=3) refuses, or the medium bucket < 3 (below the Q8 minimum-viable floor). Predicted outcome (12/4/0 with fallback to medium): NO re-tune.

IF NO RE-TUNE: record the no-op decision — SUMMARY states the measured tally, that generator.py:118-120's deferred re-tune question is answered NO with numbers, and that SIZE_S1/S2 stay 25/60. Add no code. Commit only the new test (`test(08-10): add curated-manifest supply test — SIZE_S1/S2 re-tune not triggered`).

IF RE-TUNE TRIGGERED: adjust SIZE_S1/S2 in aamatch/generator.py (update the source comments with the measured justification), update the bucket-boundary tests in tests/test_generator.py (lines ~92-140) and any test_generator_invariants.py bucket assumptions to the new constants (values are not pinned — monotonicity is), re-run the FULL generator batteries (test_generator + test_generator_invariants + the 100-seed suite), and update tests/test_demo_supply.py's expectations consistently. Commit as `feat(08-10): re-tune SIZE_S1/S2 to <values> (measured curated supply)` with the measured rationale in the message. NOTE: this is a GENERATOR-constant change, NOT a detector change — no DETECTOR_VERSION involvement (size_class in old saved specs remains their recorded truth).
  </action>
  <verify>python3.6 -m unittest tests.test_demo_supply tests.test_generator tests.test_generator_invariants -v (ALL green on whichever branch was taken) && grep -n "SIZE_S1\\|SIZE_S2" aamatch/generator.py (constants present with justification comments)</verify>
  <done>Explicit recorded verdict: either "no re-tune (measured 12/4/0, default configs generate)" or "re-tuned to <values> with all invariants green".</done>
</task>

<task type="auto">
  <name>Task 3: full WSL suite regression</name>
  <files>tests/test_demo_supply.py</files>
  <action>
Run the complete suite (`python3.6 -m unittest discover -s tests -v`) — purity gates, generator invariants (100-seed), and the new supply test together. This plan's files overlap the generator's test surface, so the full suite (not a subset) is the gate. Commit any remaining test-only adjustments as `test(08-10): ...`.
  </action>
  <verify>python3.6 -m unittest discover -s tests -v (ALL green) && python3.6 -m py_compile aamatch/*.py</verify>
  <done>Full suite green; supply verdict recorded; phase's conditional open decision closed.</done>
</task>

</tasks>

<verification>
- `python3.6 -m unittest tests.test_demo_supply -v` — green (or documented skip on a dev-only tree).
- `python3.6 -m unittest discover -s tests -v` — full suite green.
- The verdict (no-op vs re-tune, with measured numbers) appears in 08-10-SUMMARY.md.
</verification>

<success_criteria>
- The generator's Phase-8 deferral (generator.py:118-120) is answered with data, not assumption.
- Default-config games over the curated manifest provably generate (or their fail-closed refusals are pinned and correct).
- Any SIZE change is loud: committed with rationale + invariant updates, never silent.
</success_criteria>

<output>
After completion, create `.planning/phases/08-demo-curation-citations/08-10-SUMMARY.md`
</output>
