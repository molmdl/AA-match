---
phase: 06-scoring-lifecycle-endgame
plan: 09
type: execute
wave: 7
depends_on: ["06-08"]
files_modified:
  - aamatch/game_window.py
  - smoke/smoke_16_tab.py
autonomous: true
must_haves:
  truths:
    - "A finished game shows the endgame MODAL ~100 ms after the scene settles: parent self.window(), WindowStaysOnTopHint, headline + rich-text stats from the pure endgame_lines"
    - "The 1 Hz timer is explicitly stopped at game end and the label holds the EXACT final M:SS (the tick cannot stop the clock — engine._game stays live)"
    - "The endgame info-box block is the durable record with the SAME pinned lines as the modal (two surfaces, one wording home)"
    - "The full Phase-6 mechanical regression battery (all six smokes) is green before the human checkpoint"
  artifacts:
    - path: "aamatch/game_window.py"
      provides: "_show_endgame_modal (v1 _finish_win pattern) + wrapper modal-scheduling tails"
      contains: "_show_endgame_modal"
    - path: "smoke/smoke_16_tab.py"
      provides: "PART C: natural-end drive + final-timer asserts + full-suite regression record"
      contains: "PART C"
  key_links:
    - from: "_on_confirm/_on_skip/_on_giveup wrappers"
      to: "_show_endgame_modal(summary)"
      via: "cmd.refresh() + QTimer.singleShot(100, ...) AFTER the impl's pop burst (smoke-99: scheduling NEVER in impls)"
      pattern: "singleShot"
    - from: "_show_endgame_modal"
      to: "status_text.endgame_lines(summary)"
      via: "setText(lines[0]) + setInformativeText('<br>'.join(lines[1:]))"
      pattern: "endgame_lines"
---

<objective>
Endgame screen + final timer mechanics (SCORE-07) + the phase's full mechanical regression pass: the modal child QMessageBox scheduled after the wizard pop's color-restore burst, and the all-smokes green gate that hands a mechanically-proven phase to the human checkpoint.

Purpose: spec.md:57-58 — per-level scores + total, stopped timer, winning message with time taken, total molecules and levels, skip/give-up counts.
Output: the modal + scheduling tails + SMOKE-16 PART C + the six-smoke regression record.
</objective>

<execution_context>
@~/.config/opencode/get-shit-done/workflows/execute-plan.md
@~/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-scoring-lifecycle-endgame/06-01-SUMMARY.md
@.planning/phases/06-scoring-lifecycle-endgame/06-02-SUMMARY.md
@.planning/phases/06-scoring-lifecycle-endgame/06-08-SUMMARY.md
@.planning/phases/06-scoring-lifecycle-endgame/06-RESEARCH-endgame-ui.md
@aamatch/game_window.py
@aamatch/status_text.py
@smoke/smoke_16_tab.py

Anchors + binding mechanics (verified):
- 06-07 built: _confirm_now/_skip_now/_giveup_now impls (op -> sync _refresh_status -> _endgame_sequence = timer stop + exact label + wizard pop), the poll's game_over-transition block logging, _pop_game_wizard. The wrappers currently DISCARD _guard's return — this plan captures it and schedules the modal.
- The ~100 ms pattern (v1 gui_game.py:289-304, binding): stop-the-timer FIRST (done in 06-07's _endgame_sequence) -> `cmd.refresh()` -> `QtCore.QTimer.singleShot(100, show-modal)`. Rationale: a modal exec_() runs a nested loop that stops redraw servicing — the pop's color-restore burst must paint FIRST (v1 Bug A); the parent is `self.window()` + `WindowStaysOnTopHint` so the box appears ABOVE the OpenGL viewer (v1 Bug B, :313-314, :335-344).
- smoke-99 law extension: the singleShot lives in the WRAPPER only — a smoke's processEvents inside the 100 ms window would fire the modal and hang. Smokes NEVER call the wrappers' modal tails.
- Modal content: `msg = QtWidgets.QMessageBox(self.window())`; `msg.setIcon(QtWidgets.QMessageBox.Information)`; `msg.setWindowTitle('AA-match')`; `msg.setText(lines[0])` (the headline: 'You win! ...' or 'Game over -- gave up ...'); `msg.setInformativeText('<br>'.join(lines[1:]))` (rich text — the v1 stats shape :341-343); `msg.setWindowFlags(msg.windowFlags() | QtCore.Qt.WindowStaysOnTopHint)`; `msg.exec_()` (the sanctioned child-modal class, PITFALLS.md:105).
- The 100 ms constant is the v1-shipped value — keep it verbatim, no new constant.
- Wording: ALL strings come from status_text.endgame_lines (06-02) — the human confirms them at 06-10 (confirm-step, not a blocking gate).
- `QtCore` is already module-level in game_window.py:75; `cmd` stays lazy inside methods.
</context>

<tasks>

<task type="auto">
  <name>Task 1: _show_endgame_modal + wrapper scheduling tails</name>
  <files>aamatch/game_window.py</files>
  <action>
    1. `_show_endgame_modal(self, summary)`: build the QMessageBox exactly per the context mechanics (parent self.window(), Information icon, title 'AA-match', setText(endgame_lines(summary)[0]), setInformativeText('<br>'.join(lines[1:])), WindowStaysOnTopHint, exec_()). Docstring: the v1 _finish_win precedent + BOTH bug-fix rationales + "WRAPPER-ONLY — never call from an impl or the tick (smoke-99/P-6)".
    2. Wrapper tails — in `_on_confirm`, `_on_skip`, `_on_giveup`: capture the guarded result and, when it is truthy AND `result.get('game_over')`, schedule the modal:
       ```python
       result = self._guard(self._confirm_now)
       if result and result.get('game_over'):
           from pymol import cmd
           cmd.refresh()
           summary = result['summary']
           QtCore.QTimer.singleShot(100,
                                    lambda: self._show_endgame_modal(summary))
       ```
       (identical tail for skip/give-up after their question-box Yes branch). The impls stay untouched — the scheduling is wrapper-only.
    3. Update the module docstring's ROLE paragraph: the endgame sequence (impl: log/stop/label/pop -> wrapper: refresh + 100 ms + modal) and the two-surface discipline (modal = the moment, info box = the record; same pinned lines).
    - Guarded-refusal path: result None -> nothing extra (the _guard box already fired, or the panel surfaced it).
  </action>
  <verify>py_compile + full WSL suite green; grep `grep -n "singleShot\|WindowStaysOnTopHint" aamatch/game_window.py` hits BOTH the tail and the modal; grep law: zero engine._game/_payload hits.</verify>
  <done>The endgame modal exists with the v1-proven mechanics and is scheduled ONLY from the wrappers, after the pop burst.</done>
</task>

<task type="auto">
  <name>Task 2: SMOKE-16 PART C (natural end + final timer)</name>
  <files>smoke/smoke_16_tab.py</files>
  <action>
    Append PART C (T1b, impl-driven; the modal tail is NEVER driven):
    1. Fresh game via the countdown drive; confirm/skip through EVERY molecule of EVERY level via `dlg.game_tab._confirm_now()`/`_skip_now()` (zero-score records are sufficient — document) until the LAST confirm returns game_over True.
    2. Assert the natural-end state: info box contains the scored line for the final molecule + the endgame block with the 'You win! All 3 level(s) finished in' headline (defaults = 3 levels), per-level lines ('Level 1: 0.00' x3), 'Total score: 0.00.', a 'Time:' line, 'Molecules completed: 6 of 6. Levels: 3.', 'Skips: N. Give-ups: 0.' matching the driven counts; `_timer.isActive()` False; `_timer_label` == status_text.format_mss(final_time from the returned summary); wizard popped; `_last_status` None (post-pop poll cleared during the sync refresh).
    3. Post-endgame inertness: `_confirm_now()`/`_skip_now()`/`_giveup_now()` all return None (no wizard — isinstance gate); `_restart_now()` with a populated _last_start arms a fresh countdown (the endgame state composes with Restart — restart-reset Q5).
    4. Structural asserts (no modal drive): `hasattr(dlg.game_tab, '_show_endgame_modal')`; the wrapper source contains the singleShot tail — read via a plain file scan INSIDE the smoke is NOT house style; instead assert `QtCore.QTimer.singleShot` is referenced by checking the method's existence + document the [HUMAN] modal verification. Keep it to hasattr + a comment.
    5. Restore per PART A.
    6. At the file's end, print the phase regression record lines (each `SMOKE-0N: PASS/NOT-RUN` marker the orchestrator greps).
  </action>
  <verify>`bash smoke/run_smoke.sh smoke/smoke_16_tab.py` -> `=== SMOKE-16 PASS ===` (A+B+C).</verify>
  <done>Natural end proven through the tab impls: block lines, stopped timer, exact final label, popped wizard, Restart composes.</done>
</task>

<task type="auto">
  <name>Task 3: full Phase-6 mechanical regression battery</name>
  <files>smoke/smoke_16_tab.py</files>
  <action>
    Run and record the COMPLETE smoke battery (no code changes expected; fix-forward if any regression surfaces, documenting each as a Rule-1/3 deviation):
    `bash smoke/run_smoke.sh smoke/smoke_08_starter.py` (start seam + PART 5 compose)
    `bash smoke/run_smoke.sh smoke/smoke_11_window.py` (window/two-tab/countdown/cleanup/_last_start pins)
    `bash smoke/run_smoke.sh smoke/smoke_14_status_surface.py` (status surface + 11-key game_status pin)
    `bash smoke/run_smoke.sh smoke/smoke_15_lifecycle.py` (lifecycle A+B+C)
    `bash smoke/run_smoke.sh smoke/smoke_16_tab.py` (tab A+B+C)
    `bash smoke/run_smoke.sh smoke/smoke_04_e2e.py` (engine byte-identity guard)
    plus `python3.6 -m py_compile aamatch/*.py` and the FULL WSL suite.
    Record the pass counts in the SUMMARY (the 06-10 checkpoint cites them).
  </action>
  <verify>All six smokes grep their `=== SMOKE-0N PASS ===` verdicts; the WSL suite is fully green (target: 753+ tests, zero failures).</verify>
  <done>The phase's mechanical proof is complete and recorded — the human checkpoint verifies only what is not headlessly provable.</done>
</task>

</tasks>

<verification>
- `python3.6 -m py_compile aamatch/*.py` && `python3.6 -m unittest discover -s tests -v` ALL green.
- ALL six smokes PASS (08/11/14/15/16/04).
- PROSE_PIN untouched; purity gates green; no sys.modules stubs anywhere.
</verification>

<success_criteria>
- SCORE-07's screen mechanics are in place (modal + stopped timer + exact label + durable info-box record) with every string from status_text.
- The full regression battery is green — the phase is mechanically complete pending human GUI verification.
</success_criteria>

<output>
After completion, create `.planning/phases/06-scoring-lifecycle-endgame/06-09-SUMMARY.md` (frontmatter: subsystem ui, records the regression counts; provides the checkpoint input list for 06-10).
</output>
