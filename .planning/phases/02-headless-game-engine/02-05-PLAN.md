---
phase: 02-headless-game-engine
plan: 05
type: tdd
wave: 3
depends_on: ["02-01", "02-02", "02-03"]
files_modified:
  - aamatch/capability.py
  - tests/test_capability.py
  - tests/test_purity.py
autonomous: true

must_haves:
  truths:
    - "The approved capability table (docs/DETECTION_THRESHOLDS.md) is transcribed into pure code: aa_capable / residue_capabilities / ligand_support agree with it row for row"
    - "Ligand typing derives rings (bond-order first, 15-degree planarity fallback), donors (fail-closed: needs attached H), acceptors, charge groups, hydrophobes, halogen donors (Cl/Br/I only — C-F excluded), metals (approved 9-element list) from plain atom records + bond block"
    - "Salt-bridge and cation-pi capability is polarity/direction-aware (D3/D4); side-chain-only policy (D1) is encoded in the residue table"
    - "AA_TOKENS is the ONE vocabulary mapping generator aa-tokens to fragment names + resn"
  artifacts:
    - path: "aamatch/capability.py"
      provides: "AA_RESIDUES, AA_TOKENS, ligand_profile, ligand_support, aa_capable, residue_capabilities, ligand_has_metal"
      min_lines: 200
    - path: "tests/test_capability.py"
      provides: "Table-truthfulness + typing + polarity tests"
      min_lines: 120
  key_links:
    - from: "aamatch/capability.py"
      to: "docs/DETECTION_THRESHOLDS.md"
      via: "every table row traceable to the approved document"
      pattern: "source:"
    - from: "aamatch/capability.py"
      to: "aamatch/vec3.py"
      via: "dihedral/angle math for the planarity fallback"
      pattern: "from .vec3 import"
---

<objective>
Build aamatch/capability.py — the SINGLE atom/residue typing home (the detection research's proposed chem_types.py is merged here per the recorded gate decision) shared by generator (solvability, GEN-04), detector (typing agreement, DETECT-04), and later Hint (PLAY-05).

Purpose: DETECT-04 "capability checks and scoring agree" holds BY CONSTRUCTION only if both sides read the same tables. This module is that table.
Output: capability.py (pure) + tests + PURE_MODULES registration.
</objective>

<execution_context>
@~/.config/opencode/get-shit-done/workflows/execute-plan.md
@~/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@docs/DETECTION_THRESHOLDS.md                                            # THE approved table — transcribe, never invent
@.planning/phases/02-headless-game-engine/02-RESEARCH-generation.md      # §2 table + D1-D4 policies, §2.4 ligand support predicates
@.planning/phases/02-headless-game-engine/02-RESEARCH-detection.md       # §4 partner sides/roles, §7 chemistry policy (fail-closed, metal gating), §3 rows 8-9 (ring rules)
@.planning/phases/02-headless-game-engine/02-RESEARCH-materialization.md # §2 AA registry (AA_TOKENS: fragment/resn/charge), §1.4 verified fragment facts
@aamatch/vec3.py                                                          # dihedral/angle math for planarity fallback
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: AA residue table + capability matrix + AA_TOKENS (RED -> GREEN)</name>
  <files>aamatch/capability.py, tests/test_capability.py</files>
  <action>
RED: tests first. Cover:
1. Table truthfulness (the permanent invariant, generation research §2.3 counts): for the approved table assert per-type capable sets EXACTLY: h_bond donors {ARG, ASN, CYS, GLN, HIS, LYS, SER, THR, TRP, TYR}; h_bond acceptors {ASN, ASP, CYS, GLN, GLU, HIS, SER, THR, TYR} (Met acceptor: EXCLUDE — record the gate's resolution in a comment; the 02-01 doc rules); salt_bridge + {LYS, ARG} / - {ASP, GLU} (HIS NOT a cation under neutral-His policy); pi_stacking {PHE, TYR, HIS, TRP}; cation_pi cation-role {LYS, ARG}, ring-role {PHE, TYR, HIS, TRP} (either direction counts, D4); hydrophobic {ALA, VAL, LEU, ILE, PRO, PHE, MET, TRP}; halogen-acceptor role = the h_bond-acceptor set (O/N/S side chains); metal-chelator {ASP, GLU, HIS, CYS, SER, THR, TYR, ASN, GLN} per the approved resolutions (Asp/Gln INCLUDED).
2. aa_capable(resn, itype, ligand_profile): polarity tests — salt_bridge: LYS capable iff ligand_profile has an anionic group; ASP capable iff cationic; neither capable for an apolar ligand. cation_pi: LYS capable iff ligand has a ring (AA-cation direction); PHE capable iff ligand has a cation (ring direction); either counts (D4). metal: any chelator capable iff ligand_profile['has_metal'].
3. residue_capabilities returns the type set per residue; a ">= 2 capable AAs per type" invariant scan over all 20 residues x 7 types (generation research §2.3 guarantees — assert it holds so solvability never starves).
4. AA_TOKENS: 20 standard tokens, each {fragment: <same name lowercase>, resn: <UPPERCASE 3-letter>, charge_class: 'neutral'|'anion'|'cation'} — asp/glu anion, lys/arg cation, rest neutral; his neutral (HIS resn; the his fragment is the neutral HIE-like tautomer per the materialization probe). Protonation-variant tokens (hip, asph, ...) are NOT in v1 — a comment marks them additive-later.

GREEN: implement. Module docstring MUST record: D1 side-chain-only (backbone+cap atoms are NEVER typed as interacting for grid AAs), the single-typing-home decision, fail-closed donor typing, neutral-HIS policy, and a `source:` comment per table row group pointing at docs/DETECTION_THRESHOLDS.md sections. AA_RESIDUES[resn] = {'side_chain': frozenset(atom names incl. side-chain H), 'donors': [...], 'acceptors': [...], 'charge': ('+'|'-'|None, [representative atom names]), 'rings': [[atom names], ...], 'hydrophobic': bool}. Side-chain atom names: standard PDB naming (e.g. SER {'OG','HG'}, LYS {'CG','CD','CE','NZ','HZ1','HZ2','HZ3'}, ARG {'CG','CD','NE','CZ','NH1','NH2', Hs}, TRP two rings, PHE/TYR/HIS one ring). Ring atom-name lists per detection research §3 row 8 (Trp = two rings — BINANA assignment). Atom-name correctness is verified in the field by SMOKE-03 (plan 02-13 asserts zero unclassified atoms per materialized AA) — do not block this task on Windows probes.

Commit: `test(02-05): ...` then `feat(02-05): AA capability table (TDD)`.
  </action>
  <verify>python3.6 -m unittest tests.test_capability -v</verify>
  <done>Approved table transcribed exactly; polarity/direction logic tested; every type has >= 2 capable AAs</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Ligand-side typing + support predicates (RED -> GREEN)</name>
  <files>aamatch/capability.py, tests/test_capability.py</files>
  <action>
RED: tests first, using synthetic atom records (shape per detection research §5.1: dicts with side/object/id/name/elem/resn/resi/alt/x/y/z) + bond block [(i, j, order)]:
1. ligand_profile(atoms, bonds) returns a plain dict: {'has_donor', 'has_acceptor', 'charge_signs' (set of '+'/'-'), 'ring_count', 'aromatic_rings' (list of atom-index lists), 'has_hydrophobe', 'has_halogen_donor', 'has_metal', 'metal_elements'}. Cases: benzamide-like fixture (ring + amide N-H donor + carbonyl O acceptor) -> donor/acceptor/ring True, charges empty; acetate-like (C with two O, one formally -1) -> charge_signs {'-'}; halo-methane-like (C-Cl) -> has_halogen_donor True; C-F-only -> False (C-F EXCLUDED per gate); Zn atom -> has_metal True (element 'ZN'? — NO: match on element symbol case-insensitively AND on common ion names; keep it simple: elem in {'MG','ZN','FE','CA','MN','CU','NI','CO','CD'} uppercase).
2. Typing rules (unit-test each): aromatic rings from bond orders (5/6-member cycles whose bonds are alternating 1/2 or all aromatic-order markers; walk cycles via adjacency from the bond block — NO itertools, use an explicit stack/DFS); planarity fallback ONLY when bond orders are absent for the cycle: adjacent-dihedral deviation <= 15 deg computed via vec3 (dihedral of 4 consecutive ring atoms); donors FAIL-CLOSED (O/N/S with a bonded H atom — no H -> not a donor; test both branches); acceptors = O/N/S not bonded to H-only... (acceptor = O/N/S element, excluding atoms that are part of a donor-H pair? NO — an -OH oxygen is BOTH donor and acceptor-eligible; keep acceptor = any O/N/S; the geometric test decides); charge groups: carboxylate = C bonded to exactly 2 O where the Os carry formal charge < 0 (record carries formal_charge? atom records have no charge field — derive anionic carboxylate structurally: C with 2 O neighbors, one double-bonded — and mark sign '-' when the ligand's formal_charge_sum < 0 is not available... DECISION: accept an optional 'formal_charge' key on atom records; when present use it (SDF M CHG round-trips into formal_charge per materialization research §1.1 and the extraction in plan 02-09 carries it); when absent, carboxylate structure alone -> '-' (kekule C=O/O-C pattern), ammonium = N with formal_charge > 0 or N with 4 single bonds incl >= 1 H -> '+'; guanidino = C bonded to 3 N -> '+'. Test each.
3. ligand_support(itype, profile) per generation research §2.4: h_bond -> has_donor OR has_acceptor; salt_bridge -> charges non-empty; pi_stacking -> ring_count >= 1; cation_pi -> ring >= 1 OR '+' in charge_signs; hydrophobic -> has_hydrophobe; halogen -> has_halogen_donor; metal -> has_metal.
4. ligand_has_metal(atoms) -> bool (the detector/generator gating helper, detection research §7.6).

GREEN: implement in capability.py. Keep geometry OUT except the planarity dihedral fallback (vec3 import allowed — that is why this plan depends on 02-02). No thresholds here (typing only, detection research §5.1).

Commit: `feat(02-05): ligand typing + support predicates (TDD)`.
  </action>
  <verify>python3.6 -m unittest tests.test_capability -v</verify>
  <done>Typing rules each unit-tested; profile + support predicates agree with the approved §2.4 table</done>
</task>

<task type="auto">
  <name>Task 3: Register capability in PURE_MODULES + full suite</name>
  <files>tests/test_purity.py</files>
  <action>
Append 'capability' to PURE_MODULES. Imports must stay: math (via vec3), from .vec3 import ..., from .setup_state import INTERACTION_TYPES (ONE home for the enum — generation research §3.1). No itertools. No numpy.
  </action>
  <verify>python3.6 -m unittest discover -s tests -v</verify>
  <done>PURE_MODULES = 9; full suite green incl. purity gates over capability</done>
</task>

</tasks>

<verification>
python3.6 -m py_compile aamatch/*.py && python3.6 -m unittest discover -s tests -v
</verification>

<success_criteria>
capability.py is the single typing home; AA matrix + ligand predicates match the approved document; purity gates green; every interaction type has >= 2 capable AAs (solvability never starves).
</success_criteria>

<output>
After completion, create `.planning/phases/02-headless-game-engine/02-05-SUMMARY.md`
</output>
